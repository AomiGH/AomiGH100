# 嵌入式典型平台

## 面经

### 河畔学长

——[嵌入式开发]嵌入式（软件）开发的杂谈

## STM32 开发项目

单片机开发（STM32）

​	外设资源的使用和底层驱动的编写

​	GPIO（通用输入输出）：了解**GPIO的基础操作，包括工作模式（输入、输出、中断等）和寄存器设置**。**掌握如何点亮LED、读取按键状态等基础应用**。

​	中断机制：**熟悉中断系统，包括中断优先级、中断处理函数的编写以及外部中断的应用**。

​	定时器：**学习定时器基础知识**，例如计时、脉宽调制（PWM）信号产生和捕获功能。

​	存储器操作：**掌握Flash和SRAM基本原理及使用**，了解如何进行数据存储与读取。

​	通信接口：**学习串口（UART）、SPI、I2C**、CAN、485等**通信协议**，理解它们的特点和适用场景，并掌握这些**接口的基本配置和数据通信**。

​	学习方法：参考野火、正点原子等系统化学习资料，实现点灯、按键检测。深入了解每个外设的寄存器配置，尝试通过寄存器操作完成对硬件的控制。在实际项目中综合应用，**设计数据采集器，利用传感器读取环境数据并通过通信接口传输**。

### 01 IIC 控制器和传感器编程

IIC 通信介绍

**IIC 控制器编程**

IMX6ULL 有 4 个 IIC 控制器，每个 IIC 控制器有 5 个 16-bit 寄存器。

​	I2Cx_IADR（I2C 地址寄存器）：ADR [7:1] I2C 从设备地址

​	I2Cx_IFDR（I2C 分频寄存器）：IC [5:0] 设置 SCL 时钟，IC = 495/0x37，波特率为 100 k

​	I2Cx_I2CR（I2C 控制寄存器）：IEN [7] I2C 使能；IIEN [6] I2C 中断使能；MSTA [5] 主从模式选择；MTX [4] 发送/接收模式；TXAK [3] 发送响应（ACK）使能；RSTA [2] Repeat Start

​	I2Cx_I2SR（I2C 状态寄存器）：ICF [7] 数据正在传输标志；IAAS [6] I2C 控制器作为从设备时，通过此位判断是否有人来寻址它；IBB [5] I2C Bus 忙标志；IAL [4] 仲裁位，写入 0 清零；SRW [2] 作为从设备时，主机发送设备地址会带有读写位，读写位写入 SRW 中；IIF [1] I2C 中断状态；RXAK [0] 是否接收到回应信号；

​	I2Cx_I2DR（I2C 数据寄存器）：DATA [7:0] 接收时存有接收到的最新数据/发送时存有下一个即将发出的数据

I2C 控制器驱动程序

1. 初始化 IIC 控制器

```c++
i2c_init(I2C_REGISTERS *I2C_BASE)
    /* 1. 禁用 IIC 控制器：配置 I2Cx_I2CR */
    I2C_BASE->I2CR &= ~(1 << 7);
	/* 2. 配置 IIC SCL 时钟为 100 K */
	I2C_BASE->IFDR = 0x37;
	/* 3. 使能 IIC 控制器 */
	I2C_BASE->I2CR |= (1<<7);
```

2. 启动 IIC 传输

```c++
i2c_transfer(I2C_REGISTERS *I2C_BASE, I2C_TRANSFER *transfer)
	uint32_t ulRet = 0;
	uint32_t ulOpcode = transfer->ulOpcode;
	/* 1. 开始前准备工作，清除标志位和等待传输完成 */
	/* a. 清除标志位：bit-4 IAL 仲裁位，bit-1 IIF 中断标志位 */
	I2C_BASE->I2SR &= ~((1 << 1) | (1 << 4));
	/* b. 等待传输完成 */
	while(!((I2C_BASE->I2SR >> 7) & 0X1));
	
	/* 2. 发起 I2C 写操作，发送从设备地址帧：
	a. 在通信总线不忙的情况下，写入 I2CR 控制寄存器（作为主设备和发送模式）和 I2DR 数据寄存器（7 位从设备地址和读/写位）
	b. 等待传输完成（等待 I2SR 状态寄存器的中断标记位置为 1；
	c. 检查是否出错（检查 I2SR bit-4 总线仲裁位和 bit-0 是否接收到 ACK 信号位）） */
	if ((transfer->ulSubAddressLen > 0) && (transfer->ulOpcode == I2C_READ)) {
        ulOpcode = I2C_WRITE;
    }
    /* a. 发送从设备地址帧（等待传输完成不可以封装到函数里面吗） */
    i2c_start(I2C_BASE, transfer->ucSlaveAddress, ulOpcode);
    /* b. 等待传输完成 */
    while(!(I2C_BASE->I2SR & (1 << 1))){};
    /* c. 检查是否出错 */
    i2c_check(I2C_BASE, I2C_BASE->I2SR);
    
    /* 3. 如果 ulSubAddressLen 不为 0，则主机发送寄存器地址帧（如果地址为 32 位，则从高位向低位依次发送）和（可能的）重复启动信号 */
    /* 4. 主机发送 Repeat Start 重复启动信号：
	 在 I2C Bus 不忙和工作在主设备的情况下，写入 I2CR 重新开始标记位和发送位和 I2DR 的 7 位从设备地址和读/写位 */
    if (transfer->ulSubAddressLen) {
		do {
			/* a. 清除中断状态位 */
		    I2C_BASE->I2SR &= ~(1 << 1); 
			/* b. 发送寄存器地址：调整长度, 也许寄存器地址有多个字节, 本程序最多支持4字节 */
			transfer->ulSubAddressLen--;
			/* 从高字节到低字节依次发送寄存器地址 */
			I2C_BASE->I2DR = ((transfer->ulSubAddress) >> (8 * transfer->ulSubAddressLen)); 
			/* c. 等待传输完成: 中断状态位被置1 */
			while(!(I2C_BASE->I2SR & (1 << 1)));
            /* 检查是否出错 */
            i2c_check(I2C_BASE, I2C_BASE->I2SR);
		}
		while ((transfer->ulSubAddressLen > 0) && (ulRet == I2C_OK));

		if (I2C_READ == transfer->ulOpcode) {
			/* a. 清除中断状态位 */
            I2C_BASE->I2SR &= ~(1 << 1);		
            /* b. 发送重复开始信号和从机地址 */
            i2c_restart(I2C_BASE, transfer->ucSlaveAddress, I2C_READ); 
            /* c. 等待传输完成: 中断状态位被置1 */
    		while(!(I2C_BASE->I2SR & (1 << 1))){}; 
            /* 检查是否出错 */
			i2c_check(I2C_BASE, I2C_BASE->I2SR);
		}
	}
```

3. 封装 IIC 传输函数为发送/接收函数

```c++
i2c_write_one_byte(uint8_t addr,uint8_t reg, uint8_t data,I2C_REGISTERS *I2C_BASE);
i2c_read_one_byte(uint8_t addr, uint8_t reg,I2C_REGISTERS *I2C_BASE)
```



**AP3216C 传感器编程**

IMX6ULL 开发板资源包括 17. AP3216 三合一整合型光传感器，包括 ALS 光强传感器、PS 距离传感器和 IR LED 红外 LED。AP3216C 设备地址 0x1E。

​	写 AP3216C 寄存器的方法：向指定从设备和指定寄存器发起写操作，并且将指定数据写入寄存器。其中，Register Command 主机要写入指定寄存器地址的 8 位数据。

|  1   |       7       |  1   |   1   |        8         |   1   |        8         |   1   |  1   |
| :--: | :-----------: | :--: | :---: | :--------------: | :---: | :--------------: | :---: | :--: |
|  S   | Slave Address |  W   | **A** | Register Address | **A** | Register Command | **A** |  P   |

​	读 AP3216C 寄存器的方法：向指定从设备和指定寄存器发起写操作，并且主机在不释放总线的情况下进行另一个 I2C 读操作，等待 AP3216C（指定从设备） 将指定寄存器地址中的 8 位数据发送给主机。其中，Sr 表示主机发送重复信号，在不释放总线的情况下进行另一个 I2C 操作（读操作），然后主机再次发送从设备地址和读标记位。N 表示非应答信号 NACK，通知从设备主机已经完成数据接收，不需要再发送更多数据。

|  1   |       7       |  1   |   1   |        8         |   1   |  1   |       7       |  1   |   1   |          8           |  1   |  1   |
| :--: | :-----------: | :--: | :---: | :--------------: | :---: | :--: | :-----------: | :--: | :---: | :------------------: | :--: | :--: |
|  S   | Slave Address |  W   | **A** | Register Address | **A** |  Sr  | Slave Address |  R   | **A** | **Register Command** |  N   |  P   |

AP3216C 驱动程序

1. AP3216C 初始化 IO：I2C1 复用 UART4 为 SCL-TXD, SDA-RXD 并配置寄存器

```c++
IOMUXC_SetPinMux(IOMUXC_UART4_TX_DATA_I2C1_SCL, 1);
IOMUXC_SetPinMux(IOMUXC_UART4_RX_DATA_I2C1_SDA, 1);
IOMUXC_SetPinConfig(IOMUXC_UART4_TX_DATA_I2C1_SCL, 0x70B0);
IOMUXC_SetPinConfig(IOMUXC_UART4_RX_DATA_I2C1_SDA, 0X70B0);
```

2. 初始化 AP3216C：配置模式控制寄存器，完成 AP3216C 复位（清零）和开启全模式 ALS/PS/IR（配置为 0b011）。

```c++
ap3216c_init()
	/* 1. 初始化 IO，即为 AP3216C 初始化 IO */
	ap3216c_init_io();
	i2c_init(I2C1); 
	
	/* 2. 初始化/复位 AP3216C，将模式控制寄存器 System Config 的 4 bits 清零 */
	i2c_write_one_byte(AP3216C_ADDR, AP3216C_SYSTEMCONFIG, AP3216C_INIT, I2C1);
	/* 3. 开始 AP3216C 全模式 ALS/PS/IR */								        
	i2c_write_one_byte(AP3216C_ADDR, AP3216C_SYSTEMCONFIG, AP3216C_START_ALL, I2C1);
	/* 4. 读取 AP3216C 模式控制寄存器，判断 AP3216C 开启是否成功 */
	ap3216c_read_system_status();
```

3. AP3216C 红外值、光强和距离值的数据读取，每个值占据 2 个寄存器（IR DATA LOW, IR DATA HIGH），每个寄存器有 8 bits

```c++
/* 读取红外值 */
ap3216c_read_ir(uint16_t *ir)
    uint8_t ucIrLow;
    uint8_t ucIrHigh;
	/* 1. 分别读取 LOW 和 HIGH 寄存器值 */
	ucIrLow = i2c_read_one_byte(AP3216C_ADDR, AP3216C_IRDATALOW, I2C1);	
	ucIrHigh = i2c_read_one_byte(AP3216C_ADDR, AP3216C_IRDATAHIGH, I2C1);
	/* 判断读取的值是否有效 */
	if(ucIrLow & 0x80) {
		*ir = 0;
		return ;			
	}
	/* 2. 合并 LOW 和 HIGH 寄存器的值，得到 IR 值。其中，LOW 取 b[1:0]，HIGH 取 b[7:0] */
	else {
        *ir = ((uint16_t)ucIrHigh << 2) | (ucIrLow & 0X03);
    }
```



**用户空间程序（主程序）**

1. 初始化 IIC 控制器和 AP3216C 传感器

```c++
ap3216c_init();
```

2. 循环读取光强 ALS、距离 PS 和红外线强度 IR

```c++
uint16_t ir;
uint16_t ps;
uint16_t als;
ap3216c_read_data(&ir,&ps,&als);
```

3. 串口打印数据

```c++
printf("ir=%d ps=%d als=%d\n\r",ir,ps,als);
```



上机实验（编译、烧录、运行）

### 02 UART 串口

UART 串口介绍

​	UART 串口通信特点：通用异步收发通信，不依赖时钟线，通过约定波特率实现数据传输同步；逐位发送数据帧，包括起始位、数据位、校验位和停止位；全双工通信，TX 和 RX 独立，可同时运行；实现简单，通信效率较低。

​	UART 通信过程：首先，发端将 TX 线从高电平拉低，表示起始位，通知收端数据传输开始；发端按照约定的波特率逐位发送数据位（从低位到高位传输，长度为 1 字节），在数据位之后发送校验位；数据传输完成后，发端将 TX 线从低电平拉高，表示停止位，通知收端数据传输结束；收端按照约定的波特率对数据位、校验位和停止位进行采样和还原，得到传输数据。

​	UART 硬件连接：采用 TTL 电平转 USB 电平硬件连接，TTL 电平三线连接的抗干扰能力差，传输距离短，采用 USB 电平硬件连接的差分信号实现通信，提高了抗干扰能力和通信距离。

​	UART 寄存器：

​		UART1_URXD：串口接收寄存器，RX_DATA [7:0] 用于接收数据；

​		UART1_UTXD：串口发送寄存器，TX_DATA [7:0] 用于发送数据；

​		UART1_UCR1：串口控制寄存器 1，UARTEN [0] 串口使能；

​		UART1_UCR2：串口控制寄存器 2，IRTS [14] ；WS [5]；TXEN [2]；RXEN [1]；SRST [0]

​		UART1_UCR3：串口控制寄存器 3，RXDMUXSEL [2]

​		UART1_UFCR：串口 FIFO 控制寄存器，RFDIV [9:7] 设置时钟分配系数；

​		UART1_USR2：串口状态寄存器，ROR [0] 接收数据就绪位；TXDC [3] 发送完成位；

​		UART_UBIR/UART_UBMR：设置波特率

UART 编程

0. 代码：D:\Users\22499\Desktop\嵌入式学习\mine\裸机开发\02_GPIO_Interruption\001_exception_my

1. UART1_TX 和 UART1_RX 引脚功能复用，以及选择 UART1_RX_DATA_ALT0 作为 UART1_RX 引脚

```c++
*IOMUXC_SW_MUX_CTL_PAD_UART1_RX_DATA = 0;
*IOMUXC_UART1_RX_DATA_SELECT_INPUT	 = 3;
*IOMUXC_SW_MUX_CTL_PAD_UART1_TX_DATA = 0;
```

2. 配置并使能 UART 时钟：CCM_CSCDR1 和 CCM_CCGR5 采用默认值

3. UART1_TX 和 UART1_RX 硬件参数配置，包括驱动强度、速度和 Pull/Keep 使能

```c++
*IOMUXC_SW_PAD_CTL_PAD_UART1_TX_DATA = (DSE | SPEED | PKE);
*IOMUXC_SW_PAD_CTL_PAD_UART1_RX_DATA = (DSE | SPEED | PKE);
```

4. 设置 UART1 传输格式和波特率

```c++
UART1->UCR1 |= (1 << 0);
UART1->UCR2 |= (1 << 14) | (1 << 5) | (1 << 2) | (1 << 1);
UART1->UCR3 |= (1 << 2);
UART1->UFCR = 5 << 7;
UART1->UBIR = 71;
UART1->UBMR = 3124;
```

5. 使能 UART1

```c++
UART1->UCR1 |= (1 << 0);
```

6. 发送/接收单个字节

```c++
PutChar(int c)
	while (!((UART1->USR2) & (1 << 3)));	/*等待上个字节发送完毕*/
	UART1->UTXD = (unsigned char)c;
GetChar(void)
	while (!(UART1->USR2 & (1 << 0)));		/*等待接收数据*/
	return (u8)UART1->URXD;
```



### 03 LED 控制

LED 介绍：

​	LED 控制采用通用的 GPIO 引脚操作方法

LED 编程：

1. 使能对应的 GPIO 引脚

```c++
*CCM_CCGR1 |= (3 << 30);
```

2. GPIO 引脚复用功能选择和引脚属性配置

```c++
*IOMUXC_SNVS_SW_MUX_CTL_PAD_SNVS_TAMPER3 &= ~(0xf << 0);  // b3-b0清零
*IOMUXC_SNVS_SW_MUX_CTL_PAD_SNVS_TAMPER3 |= (5 << 0);
```

3. GPIO 引脚输入输出方向确定

```c++
*GPIO5_GDIR |= (1 << 3);
```

4. GPIO 引脚写/读数据

```C++
*GPIO5_DR &= ~(1 << 3);		/* 打开 LED */
*GPIO5_DR |= (1 << 3);		/* 关闭 LED */
```



### 04 KEY 控制

KEY 编程：

1. GPIO 引脚复用功能选择和引脚属性配置

```c++
*IOMUXC_SNVS_SW_MUX_CTL_PAD_SNVS_TAMPER1 = (0x5U << 0);
*IOMUXC_SNVS_SW_PAD_CTL_PAD_SNVS_TAMPER1 = (DSE | SPEED | PKE | HYS);
```

2. 配置按键功能: 边沿触发，中断使能，防止误触

```c++
*GPIO5_EDGE_SEL |= (0x1U << 1U);
*GPIO5_IMR |= (0x1U << 1U);
*GPIO5_ISR |= (0x1U << 1U);
```

3. GPIO5 使能：CCM_CCGR1
4. GPIO5 引脚读数据：*GPIO5_DR
5. 注册 KEY1 中断 ID (GPIO5_Combined_0_15_IRQn) 对应的中断处理函数

```c++
request_irq(nr_key1, (irq_handler_t)key1_irq_handler, NULL);
```

6. GIC 中断控制器使能指定中断 ID

```c++
gic->D_ISENABLER[nr_key1 >> 5] = (u32)(0x1U << (nr_key1 & 0x1FU));
```



### 05 GPIO 输入输出/中断

**GPIO 输入输出模式**：

​	GPIO 模块一般结构：GPIO 使能（时钟控制模块 CCM_CCGR）；GPIO 功能复用（IO 复用控制器 IOMUXC）；GPIO 方向（GPIO_GDIR）；GPIO 数值（GPIO_DR/GPIO_PSR）。

​	GPIO 编程：

1. GPIO 使能：CCM_CCGRx
2. GPIO 引脚功能复用和参数配置：IOMUXC 
3. GPIO 引脚方向：GPIO_GDIR
4. GPIO 写/读数据：GPIO_DR/GPIO_PSR

**GPIO 中断模式**：

GPIO 中断介绍：GPIO 中断指由 GPIO 模块产生的中断。GPIO 模块检测到引脚上电平的变化，并向中断控制器 (GIC) 发出中断信号，GIC 再向 CPU 发出中断信号。

GPIO 中断初始化：

1. 初始化 GPIO 模块中断源
2. 初始化 GIC 的分发器 Distributer
3. 初始化 GIC 的CPU 接口 Interface
4. 初始化 CPU 的 CPSR 寄存器

GIC 中断寄存器：GIC 寄存器分为 Distributor Register 和 CPU Interface Register。

​	GICC_IAR 寄存器：Interrupt ID [9:0] 正在处理的中断 ID

​	GICC_EOIR 寄存器：EOIINTED [9:0] 处理完成的中断 ID

CP 15 协处理器：

​	SCTLR 系统控制寄存器：

```
MRC p15, 0, <Rt>, c1, c0, 0 ;	把SCTLR寄存器的值读到ARM寄存器Rt中。
MCR p15, 0, <Rt>, c1, c0, 0 ;	把ARM寄存器Rt的值写入SCTLR寄存器。
```

​	VBAR 矢量基地址寄存器：[31:0] 异常向量表的映射地址

GPIO 中断寄存器：

​	GPIOx_ICR1 中断配置寄存器：ICRn [2n+1:2n] 设置 GPIO 中断 ID 为 n 的触发类型

​	GPIOx_ICR2 中断配置寄存器：ICRn [2n+1:2n] 设置 GPIO 中断 ID 为 n+16 的触发类型

​	GPIOx_IMR 中断标记寄存器：IMRn [n] 使能或者禁用 GPIO 中断

​	GPIOx_ISR 中断状态寄存器：ISRn [n] GPIO 中断状态

GPIO 中断编程：

GIC 初始化

1. 读取全局中断控制器 GIC 的基地址

```c++
GIC_Type* gic = get_gic_base();	/* a. 读取GIC基地址 */
```

2. 读取 GIC 支持的最大中断数：GICD_TYPER ITLinesNumber [4:0]

​	禁用所有中断：GICD_ICENABLER[i] = 0xFFFFFFFFUL

```c++
irq_num = (gic->D_TYPER & 0x1FU) + 1;	/* b. 读取GIC支持最大中断数 */
/* 禁用所有中断 */
for (i = 0; i < irq_num; i++) {
    gic->D_ICENABLER[i] = 0xFFFFFFFFU;
}
```

3. 设置优先级：GICC_BPR Priority [7:0]

​	设置优先级分组：GICC_BPR Binary Point [1:0]

```c++
/* c. 设置优先级 */
gic->C_PMR = 0xF8U;
/* 设置优先级分组 */
gic->C_BPR = 0x3;
```

4. 使能 Distributor: GICD_CTLR [0]

​	使能 CPU Interface: GICC_CLTR [0]

```c++
/* d. 使能distributor和CPU Interface */
gic->D_CTLR |= (0x1U << 0);
gic->C_CTLR |= (0x1U << 0);
```

IRQ_table 初始化：

1. 建立 IRQ 表格

```c++
static sys_irq_handler_t irq_table[160];
```

2. 用默认中断处理函数 (default_irq_handler) 初始化 IRQ 表格

```c++
for (int i = 0; i < (sizeof(irq_table) / sizeof(irq_table[0]) - 1); i++) {
    request_irq((IRQn_Type)i, (irq_handler_t)default_irq_handler, NULL);
}
```

3. 注册 (request_irq) 指定中断ID对应的中断处理函数

```c++
request_irq(nr_key1, (irq_handler_t)key1_irq_handler, NULL);
request_irq(nr_key2, (irq_handler_t)key2_irq_handler, NULL);
```

4. 使能指定中断 ID：

```c++
gic->D_ISENABLER[nr_key1 >> 5] = (u32)(0x1U << (nr_key1 & 0x1FU));
gic->D_ISENABLER[nr_key2 >> 5] = (u32)(0x1U << (nr_key2 & 0x1FU));
```

中断异常处理汇编部分：

1. 定义中断向量表 _vector_table

```
_vector_table:
	ldr pc, =Reset_Handle	/* Reset */
	b half					/* Undefined Instruction */
	b half					/* Supervisor Call */
	b half					/* Preferch Abort */
	b half					/* Data Abort */
	.word 0					/* Reserved */
	ldr pc, =IRQ_Handle		/* IRQ */
	b half					/* FIQ */
```

2. 单片机先执行复位中断处理 Reset_Handle

```
/* 设置中断向量表基地址 */
ldr r0, =_vector_table
mcr p15, 0, r0, c12, c0, 0
/* 清空 .BSS 段 */
bl clean_bss
/* 初始化系统（ C 程序） */
bl system_init
/* cps: Change Processor State, 开启IRQ中断 */
cpsie i 
/* 执行主程序（ C 程序） */
bl main
```

3. 自定义 IRQ 中断处理（汇编语言）

```
.align 2
IRQ_Handle:
	/* 保存现场 */
	/* 保存lr-4, 使程序从触发中断的指令继续执行 */
	sub lr, lr, #4
	stmdb sp!, {r0-r12, lr}
	/* IRQ处理 */
	bl irq_handler_c
	/* 恢复现场 */
	ldmia sp!, {r0-r12, pc}^
```

4. 自定义 IRQ 中断处理（ C 程序）

```c++
void irq_handler_c(void) {
	/* a. 读取 GIC 基地址 */
	GIC_Type* gic = get_gic_base();
	/* b. 读取中断 ID */
	u32 nr = gic->C_IAR;
	printf("IRQ %d is happened.\n\r", nr);
	/* c. 调用中断 ID 的中断处理函数 */
	irq_table[nr].irq_handle(nr, irq_table[nr].param);
	/* d. 清除中断 ID */
	gic->C_EOIR = nr;
}
```



### 06 中断与异常

中断控制器 (GIC) 包括分发器 (Distributor) 和CPU接口端 (CPU interface)。

分发器 (Distributor) 主要完成对整个中断控制器使能，设置中断优先级，设置中断触发方式，决定每个中断信号发送到哪一个具体的 CPU 上执行。

CPU接口端 (CPU interface) 主要完成使能和发送一个具体的中断信号到特定的 CPU 上，确认中断已被 CPU 接受、处理以及处理完成，设置 CPU 能接受中断的优先级以及基于级别的中断抢占。

中断信号先到达分发器，分发器根据该中断所设定的 CPU，把中断发送到 CPU 对应的 CPU interface 上；在 CPU interface 里判断该中断的优先级是否足够高，能否抢断或打断当前的中端处理，如果可以，CPU interface 就会发送一个物理的 signal 到 CPU 的 IRQ 线上；CPU 接收到中断信号，转到中断处理模式进行处理。



### 07 SPI 控制器和传感器编程

SPI 寄存器：

​	ECSPIx_RXDATA 用于从外部设备接收数据

​	ECSPIx_TXDATA 用于向外部设备发送数据

​	ECSPIx_CONFIG 用于配置 SPI 控制器的时钟频率、操作模式和数据传输长度

​	ECSPIx_CONFIGREG 用于配置 SPI 控制器的 4 个通道的操作模式

​	ECSPIx_STATREG 指示 SPI 控制器的执行状态

​	ECSPIx_TESTREG 测试 SPI 控制器是否正常

​	采用 SPI3 控制器，连接到 ICM-20608-G 的引脚包括

​		MISO -- UART2_RTS_B.alt8，将 IOMUXC_SW_MUX_CTL_PAD_UART2_RTS_B 设置为 ALT8 模式

​		MOSI -- UART2_CTS_B.alt8，将 IOMUXC_SW_MUX_CTL_PAD_UART2_CTS_B 设置为 ALT8 模式

​		SCLK -- UART2_RX_DATA.alt8，将 IOMUXC_SW_MUX_CTL_PAD_UART2_RX_DATA 设置为 ALT8 模式

​		SS0 -- UART2_TX_DATA.alt8，将 IOMUX_SW_MUX_CTL_PAD_UART2_TX_DATA 寄存器设置为 ALT5 模式，作为 GPIO 来拉高拉低进行片选操作。SPI3的SS0引脚用作GPIO时，对应GPIO1_20，需要设置它为输出引脚

SPI 控制器编程：

1. SPI 控制器引脚功能复用和属性配置

```c++
iomuxc_sw_set(UART2_RX_BASE,8);		/* SCLK */
iomuxc_sw_set(UART2_RTS_BASE,8);	/* MISO */
iomuxc_sw_set(UART2_CTS_BASE,8);	/* MOSI */
iomuxc_sw_set(UART2_TX_BASE,5);		/* CS/SS */
GPIO1_GDIR  = (volatile unsigned int *)(0x209C000 + 0x4);
GPIO1_DR  	= (volatile unsigned int *)(0x209C000);
*GPIO1_GDIR |= (1<<20);		// 设置为输出
*GPIO1_DR 	|= (1<<20);	
```

2. 时钟源选择、配置和使能

```c++
CCM_CSCDR2 时钟源选择，采用默认值
uc_num->CONREG |= (14<<12); //设置clk = 60/(14+1) = 4M
CCM_CCGR1  使能时钟，采用默认值
```

3. SPI 控制器配置和使能

```c++
uc_num->CONREG =  0;// clear all bits
/*
    bit0:使能SPI
    bit3:开始传输控制：写入TXDATA之后，立即发送
    bit4:设置通道0为master mode
    bit31:20 设置burst length ，7表示为8bits，一个字节
*/
uc_num->CONREG |= (7<<20)|(1<<4)|(1<<3)|(1<<0);
uc_num->CONFIGREG = 0;	// 采用默认值
```

4. SPI 从设备片选线 CS/SS 拉低/拉高（选中/结束）

```c++
spi_select(void)
    *GPIO1_DR &= ~(1<<20);	// spi片选拉低，GPIO来实现
spi_deselect(void)
    *GPIO1_DR |= (1<<20);	// spi片选拉高，GPIO来实现
```

5. SPI 输入/输出数据

```c++
spi_writeread(SPI_CTRL *spi_num,unsigned char uc_txdata)
	while(!(spi_num->STATREG & (1 << 0)));	//如果 FIFO 置 0 的话，则填充数据以开始下一次发送
	spi_num->TXDATA = uc_txdata;

	while(!(spi_num->STATREG & (1 << 3)));	//等待接收数据完成，当为1的时候表示有接收数据存在，可以进行读取
	return spi_num->RXDATA;
```

6. SPI 回环自测

```
a. 设置进入回环模式，进行测试
b. 进行数据的读写测试
c. 将读数据和写数据进行对比，输出 SPI 回环自测结果
```



ICM-20608-G 传感器：

​	温度、角速度、角速度传感器

ICM-20608-G 传感器编程：

1. 选择时钟源、配置采样率
2. 配置和使能温度传感器、角速度传感器、加速度传感器
3. 读取传感器数据

### 08 定时器

GPT 定时器配置：

​	GPT 通用目的定时器是带有时钟源选择的 32 位递增计数器。

​	GPT 计数模式：重新启动计数模式（计数值达到输出比较值时计数器复位重新计数）和自由运行模式（计数达到 0xFFFFFFFF 时再复位）

​	GPT 中断：翻转中断、输入捕获中断（2个输入捕获通道，捕获外部信号发生时计数器的值）和输出比较中断（3个输出比较通道）

​	**看门狗定时器**：用于监控系统的运行状态。如果在预设的时间内没有被“喂狗”，看门狗定时器会触发系统复位，防止系统因故障而停止响应。

​	**周期性任务：** 通过配置定时器周期性地触发中断，可以实现周期性执行的任务

GPT 定时器编程：

1. GPT 软件复位

```c++
base->CR |= (1 << 15);
while((base->CR >> 15) & 0x1) ;	/* 等待复位完成 */
```

2. 配置 GPT 时钟源、预分频值、计数模式、STOP Mode、WAIT Mode、ENMOD

```c++
base->CR = (1 << 6) | (1 << 5) | (1 << 3) | (1 << 1);
base->PR = 0;
```

3. 根据延迟时间设置输出比较寄存器 GPTx_OCRn

```c++
base->OCR[chan] =  USEC_TO_COUNT(us);
```

4. 清除 GPT 状态标记：GPTx_SR

```c++
base->SR |= (1 << chan);
```

5. 使能 GPT 和 GPT 中断

```c++
base->IR |= (1 << chan);
base->CR |= (1 << 0);
```

6. 计数开始，GPT 状态寄存器的 Compare Flag 被置位，表示延时时间已到

```c++
while(!((base->SR >> chan) & 0x1)) ;
```

7. 禁用 GPT 和 GPT 中断，清除 GPT 状态标记位

```c++
base->CR &= ~(1 << 0);
base->IR &= ~(1 << chan);
base->SR |= (1 << chan);
```

8. 用户主程序不断调用 GPT 定时器，实现定时操作。



EPIT 定时器配置

​	EPIT 定时器是增强的周期性中断定时器。

EPIT 定时器编程

### 09 时钟配置



### 10 外设资源使用/中断驱动编写



### 11 DMA 传输



## ARM 体系与架构

### 学习路线

——嵌入式软件工程师的秋招之路

​	关于Arm的体系架构这部分是一定要看的。推荐一本书，杜春雷老师写的《**ARM体系结构与编程**》，这本书其实就是ARMV7开发手册的中文版，很多内容都是手册里面的。书的话不一定都要看，学习过程中，哪里不了解再去查书，效果可能会更好。

​	书籍推荐：Armv7/Armv9数据手册，...

### 学习内容

第 2 章：ARM 编程模型的基本知识：ARM 处理器模式、ARM 体系中的寄存器及其使用方法、ARM 体系中异常中断处理的基本概念、ARM 体系中存储访问的基本知识

第 3 章：ARM 体系的指令系统以及寻址方法：指令的编码格式、语法格式、指令的操作以及应用方式

第 4 章：ARM 汇编语言设计的基本方法：伪操作 (Directives)、宏指令 (Pseudo-instruction)、汇编语言格式、armasm (ARM Assemble) 使用方法

第 5 章：ARM 体系的存储系统：用于存储管理的协处理器 CP15，存储管理单元 MMU、写缓冲以及 Cache、快速上下文切换技术、虚拟存储技术、缓冲技术以及 Cache 技术

第 6 章：ARM/Thumb 过程调用标准：使得单独编译的 C 语言程序和汇编程序之间相互调用

第 7 章：ARM 程序和 Thumb 程序混合使用的方法：如果程序遵守支持 ARM 程序和 Thumb 程序混合使用的 ATPCS，则程序中的 ARM 子程序和 Thumb 子程序可以相互调用。对于 C/C++ 源程序而言，只要在编译时指定 -apcs /interwork 选项，编译器生成的代码就遵守支持ARM程序和 Thumb 程序混合使用的 ATPCS。而对于汇编源程序而言，用户必须保证编写的代码遵守支持 ARM 程序和 Thumb 程序混合使用的ATPCS。

第 8 章：ARM 汇编程序以及 C/C++ 程序之间相互调用的技术

第 9 章：ARM 体系中的异常中断技术，包括异常中断处理的处理过程，各种异常中断处理的进入和返回机制，在应用程序中使用异常中断处理的方法以及各种异常中断的详细使用技术。

第 10 章：ARM 体系中 C/C++ 语言程序设计的基本知识，包括 ARM C/C++ 语言的一些特性、ARM C/C++ 编译器的使用方法以及 ARM C/C++ 运行时库的使用方法

第 11 章：如何由目标文件以及库文件得到可执行的映像文件，包括 ELF 格式的可执行映像文件的组成、ARM 连接器的使用方法和连接过程所执行的各种操作

第 12 章：介绍嵌入式应用程序设计的基本知识，介绍如何使用 ARM 开发工具编译、连接程序，生成映像文件。

### ARM 概述

##### 01 RISC 体系特点

具有大量的寄存器；

绝大多数操作都在寄存器中进行，通过 Load/Store 的体系结构在内存和寄存器之间传递数据；

寻址方式简单；

采用固定长度的指令格式。

##### 02 ARM 体系特点

常用于无线设备、蓝牙技术、存储设备、车载设备等领域。

具有 RISC 体系特点

采用了一些特别的技术，在保证高性能的同时，尽量减小芯片体积，减低芯片的功耗：

​	在同一条数据处理指令中包含算术逻辑处理单元处理和移位处理；

​	使用地址自动增加（减少）来优化程序中的循环处理；

​	Load/Store 指令可以批量传输数据，从而提高了数据传输的效率；

​	所有指令都可以根据前面指令执行的结果，来决定是否执行，以提高指令执行的效率。

##### 03 ARM 处理器运行模式

ARM 处理器有 7 种运行模式：

​	用户模式 (user, usr)：正常程序执行模式
​		应用程序不能够访问一些受操作系统保护的系统资源，也不能直接进行处理器模式的切换。通过产生异常处理，在异常处理过程中进行处理器模式的切换。
​		以下异常模式 (除了系统模式) 拥有自己的寄存器，供异常处理函数使用，保证进入异常处理后用户模式寄存器不被破坏，保护了程序运行状态。

​	快速中断模式 (FIQ)：用于高速数据传输和通道处理

​	外部中断模式 (IRQ)：用于通常的中断处理

​	特权模式 (Supervisor, sve)：供操作系统使用的一种保护模式

​	数据访问中止模式 (Abort, abt)：用于虚拟存储以及存储保护

​	未定义指令中止模式 (Undefined, und)：用于支持通过软件仿真硬件的协处理器

​	系统模式 (System, sys)：用于运行特权级的操作系统任务
​		系统模式并不是通过异常过程进入的，它和用户模式具有完全一样的寄存器。但是系统模式属于特权模式，可以访问所用的系统资源，也可以直接进行处理器模式切换。它主要供操作系统任务使用。

##### 05 ARM 寄存器

## RTOS

##### 01 FreeRTOS 程序：修改官方源码 -- 增加串口打印

用 Keil 自带的 simulator 来仿真实现 RTOS

FreeRTOS 核心代码：`tasks.c`, `list.c`, `queue.c`, `port.c`, `heap_2.c` 和 `STM32F10x.s` 汇编文件

##### 02 FreeRTOS 程序：task_create

```c
tasks.c
xTaskCreate(TaskFunction_t pxTaskCode,	// 任务函数
		const char* const pcName,		// 任务名称
        const configSTACK_DEPTH_TYPE usStackDepth,	// 栈的深度
        void* const pvParameters,		// 传递给任务函数的参数
        UBaseType_t uxPriority,			// 优先级
        TaskHandle_t* const pxCreatedTask	// 句柄
           );
```

* 动态内存的使用

任务结构体 TCB_t (Task Control Block, _t: struct)

```

```



##### 03 FreeRTOS 程序：源码结构

变量名称：前缀 (类型) + 含义；
函数名称：前缀 (类型) + 所处文件名 + 含义
前缀：prv -- private/static (私有的)
