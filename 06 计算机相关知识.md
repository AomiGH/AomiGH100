# 06 计算机相关知识

# 操作系统

### 概述

#### 01 计算机启动过程

首先，电源启动，基本输入/输出系统（BIOS）开始执行开机自检检查硬件，包括中央处理器（CPU）、内存（RAM）、硬盘驱动器和显卡，确保他们正常工作；

其次，引导启动程序。BIOS 在硬盘中寻找启动程序并加载到内存中，负责加载操作系统内核，将 CPU 控制器交给操作系统内核；

然后，操作系统内核初始化硬件设备，加载驱动程序，并设置中断向量表；初始化内存管理单元（MMU），建立虚拟内存机制，并为内核和其他进程分配内存；初始化进程管理，创建第一个 init/systemd 进程，负责管理进程的创建、状态转换和调度；初始化文件系统，挂载根文件系统，并加载文件系统驱动程序；初始化用户空间，启动用户空间程序，等待用户输入。

#### 02 Linux 中断与异常

首先，中断由键盘、网卡等硬件设备产生，是异步事件，与 CPU 正在执行的指令无关。在硬件阶段，中断信号由中断控制器捕获并送入 CPU 的中断接口，CPU 识别中断来源并通过中断向量表定位到相应的中断处理程序；在软件阶段，CPU 会保存当前执行状态并跳转到中断处理程序执行；中断处理完成后清除中断标志，恢复之前保存的状态，并继续执行被中断的程序。

其次，异常是 CPU 在执行指令时检测到的，是同步事件。Linux 内核通过中断描述符表来管理异常。CPU 暂停并保存当前执行状态，并根据检测到的异常类型，跳转到异常处理程序，尝试恢复程序运行，可能会直接终止程序。

#### 03 冯诺依曼结构

首先，冯诺依曼结构是现代计算机的理论基础，指出程序和数据应以二进制形式保存在存储器中，并按照指令顺序依次执行。

其次，冯诺依曼结构包括五个基本组成部分：运算器（算术逻辑单元），负责执行算术和逻辑运算；控制器，负责从内存中取出指令，解码指令，并控制其他部件运行指令；存储器，用于存储程序和数据；输入设备，用于与计算机进行交互；输出设备，用于输出计算机处理结果；

#### 04 谈谈操作系统

#### 05 操作系统实时性

首先，操作系统实时性表现在调度延迟、中断延迟、任务响应时间和抖动。

​	调度延迟是指任务触发到操作系统调度器将任务分配到 CPU 上的执行时间，取决于调度算法、任务优先级管理和系统任务复杂性，期望让高优先级任务可以快速获取 CPU 资源。

​	中断延迟是指从外部事件触发硬件中断到中断服务程序开始执行的时间延迟，取决于中断处理机制和中断优先级管理，期望低中断延迟，确保外部事件可以被迅速响应。

​	任务响应时间是指任务准备好执行到任务实际执行的时间，包括了调度延迟和中断延迟。

​	抖动是指任务或者中断在不同时间触发时响应时间的变化幅度，期望低抖动性确保任务稳定执行。

### 内存管理/调度

#### 01 程序栈空间

首先，Linux 程序栈空间大小由操作系统环境决定，默认值通常为 8 Mb，可以用 ulimit 命令来查看和设置栈空间大小。

#### 02 内存管理

首先，Linux 内存管理单元需要有效地管理物理内存资源，同时为多个进程提供隔离的虚拟地址空间。

其次，Linux 使用**虚拟内存技术**，为每个进程提供独立的虚拟地址空间。虚拟地址空间与物理内存之间通过页表进行映射，并且虚拟地址空间要大于实际物理内存。Linux 将虚拟地址空间和物理内存分成固定大小的**页面**（4 KB），并允许系统以页为单位进行内存分配和交换，提高内存利用率。内存分配方面采用**伙伴系统**将连续的物理页面分组为不同大小的块（2的幂次方），当需要分配内存时，从适当大小的块中分配；当释放内存时，将相邻的空闲内存块合并。内存分配采用 **Slab 分配器**管理小对象的内存分配。**内存映射**采用`mmap()`系统调用，允许进程将文件/设备映射到虚拟地址空间，提供了一个高效 I/O 方式，也可以实现共享内存。**页面置换**机制允许将不常用的物理内存交换到磁盘上，节省内存空间。

然后，内存管理单元的分页机制是虚拟内存管理的核心，将虚拟地址空间和物理内存划分为固定大小的块，成为"页"（4 KB），利用页表建立虚拟地址空间和物理内存的映射关系，提供了更灵活的内存分配方式，支持进程虚拟内存的按需加载。每个进程都有独立的虚拟地址空间和页表。

​	页表映射机制：MMU通过页表的页号查找虚拟地址对应的物理页号，并将物理页号和页内偏移结合得到实际物理地址

​	页表查找失败：1. 缺页中断，a. 操作系统查找该虚拟页在磁盘中的位置，将磁盘页面加载到物理内存中；b. 更新页表，将虚拟页映射到新加载的物理内存；c. 操作系统恢复程序的运行，并重新访问该虚拟页。2. 页面置换，如果物理内存已满，则将不常用的物理内存交换到磁盘上来腾出空闲内存空间，用于加载该虚拟页。

​	页表实现机制：多级页表将页表化为多个层次，最高层次用于索引顶层页表，中间层次用于索引中间层页表，最后的页内偏移用于确定实际物理地址。

#### 03 动态内存分配算法

首先，进程内存的堆区动态内存需要手动分配/释放。操作系统的内存管理单元将可用内存空间分成多个内存块，并用链表管理。分配时寻找合适大小的内存块，剩余内存作为空闲内存块放入链表，小的内存块会合并（伙伴系统），释放时标记为空闲状态。

其次，首次适应算法是指从低地址向高地址依次搜索，寻找满足内存空间大小的内存块并进行分配，剩余部分作为空闲内存块放入内存链表中；最佳适应算法是指在搜索时寻找满足内存大小的最小内存块来分配，减少产生的内存碎片，为大任务保留更多的连续内存空间。

#### 04 虚拟技术

首先，多进程并发

其次，虚拟地址空间分配

#### 05 堆区和栈区

#### 06 分页机制缺点

### 多进程/多线程并发

#### 01 进程/线程

首先，虚拟内存空间，上下文切换

#### 02 进程调度

首先，进程调度用于确保多进程共享 CPU 资源。进程创建并处于就绪态时，放入就绪/等待队列中等待调度器的调度。进程终止时，CPU 资源得到释放，调度器会根据预定的调度策略选择下一个就绪态进程。在进程切换上，调度器会将中断阻塞的进程放入阻塞队列，等待解除阻塞，并选择下一个就绪态进程。

#### 03 进程间通信

首先，管道、内存映射、共享内存、互斥锁/条件变量/信号量、IO 多路复用

### Linux 设备驱动

#### 01 Linux 设备树

首先，Linux 设备树是一种描述硬件配置信息的数据结构，由二进制格式（DTB）存储，在系统启动时由引导加载程序传递给操作系统内核，为内核提供硬件信息。

其次，Linux 设备树将硬件信息从内核代码中分离出来，以独立文件的形式提供。将内核移植到不同硬件平台，只需要提供对应的设备树文件，而无需修改内核代码，提高了内核通用性。修改硬件信息只需要更新设备树文件，简化了开发和维护。

然后，设备树以层次化的树形结构来表示系统硬件信息，由节点和属性构成。

​	根节点：表示整个系统

​	节点：表示系统中的一个硬件设备或者功能单元

​	属性：每个节点包含多个属性，用于描述该设备的具体配置信息。属性由键值对组成，键为属性的名称，位为具体的配置内容。

还有，配置设备树中的`pinctrl`节点

​	`pinctrl`节点是引脚控制子系统，用于配置 GPIO 引脚的复用功能和引脚状态。在设备树文件中每种功能模式的引脚配置被定义为一个`pinctrl`子节点；设备节点使用`pinctrl`属性将引脚配置与具体设备绑定；此外，`pinctrl`节点可以抽象为单独的 .dtsi 文件。

```
/* 定义 pinctrl 节点 */
&pinctrl {
	uart0_pins: uart0 {
		pinmux = <PINMUX_UART0_TX | MUX_FUNC1>,	// 配置UART0的TX引脚为功能1
				 <PINMUX_UART0_RX | MUX_FUNC1>;
		bias-disable;							// 禁止上下拉
	};
};
/* 为设备绑定引脚配置 */
&uart0 {
	pinctrl-names = "default";		// 使用的 pinctrl 配置模式名称
	pinctrl-0	  = <&uart0_pins>;	// 引用前面定义的 uart0_pins 配置
	status		  = "okey";			//	启用 UART0
};
```

​	`pinctrl`的函数集及其功能。

#### 02 内核态/用户态

首先，内核态和用户态是操作系统的两种运行模式，用于控制进程对系统资源的访问权限，避免用户程序对系统资源的非法操作，保证系统安全性和稳定性。

​	内核态：用于最高权限，可以访问所有的硬件资源和执行所有 CPU 指令，操作系统核心代码（内存管理、进程调度、设备驱动等）都运行在内核态。

​	用户态：只能执行非特权指令，不能直接操作硬件资源，对于系统资源的访问需要通过内核的系统调用。

其次，用户态进入内核态的方式有三种。

​	系统调用：用户态主动请求使用系统服务（文件操作、内存分配等），会通过系统调用进入内核态，由内核完成相应操作，例如，`open()`, `write()`等。

​	异常：程序运行时出现指令异常导致进入内核态，内核通过异常处理机制捕获错误，并进行异常处理，例如，内存已满时的页面切换。

​	设备中断：外部硬件设备向 CPU 发送中断信号，CPU 会暂停当前运行的进程，切换到内核态执行中断处理程序。

然后，用户态和内核态的切换过程包括：

 	1. CPU 暂停当前用户态程序的执行，并保存用户态程序的上下文信息到内核栈中；
 	2. CPU 切换到内核态，执行相应的系统调用函数/中断处理程序/异常处理程序；
 	3. CPU 将执行结果返回到用户态程序，如果异常无法恢复，可能直接终止进程并记录错误信息；
 	4. CPU 恢复用户态进程的上下文信息，切换回用户态。

#### 03 内核空间

首先，两个进程拥有独立的虚拟内存空间，但是共享同一份内核空间，供操作系统内核使用。

​	共享部分：内核代码和数据结构是所有进程共享的，内核代码包括操作系统的内存管理、进程调度、系统调用等，负责管理整个系统的资源；数据结构包括进程表、内存页表等，存储着系统中的所有进程和资源信息，供内核统一管理。

​	非共享部分：每个进程进入内核态时会使用自己的内核栈保存上下文信息，避免进程间相互干扰；每个进程拥有独立的进程控制块，用于记录该进程的状态信息。

#### 04 设备驱动开发

首先，Linux 设备驱动开发是为硬件设备提供与操作系统和应用程序交互的接口，负责控制硬件设备的运行和通信，包括字符设备驱动、块设备驱动和网络设备驱动。

​	字符设备驱动用于管理字符流传输的设备，例如摄像头、键盘等，主要内容包括：

1. 设备文件接口：字符设备通过 /dev 下的设备文件与用户空间交互；
2. 系统调用接口：实现内核和用户空间与字符设备的交互，包括 open() / read() / write() / ioctl() 等；
3. 缓冲管理：实现输入/输出数据的缓冲区管理，提高数据传输效率；
4. 中断处理：响应硬件中断；
5. 实现多进程并发和进程间通信。

​	网络设备驱动用于管理网络设备，例如 Wi-Fi 模块，通过网络协议栈与用户空间交互，支持数据包的发送和接收，主要内容包括：

1. 将复杂的网络设备操作抽象为一组设备配置和控制接口，供内核和用户空间使用；
2. 管理数据缓冲区，实现发送和接收数据包的功能；
3. 响应硬件中断；
4. 管理网络设备的硬件资源，例如 I/O 端口、DMA 通道等。

#### 05 字符设备驱动

首先，字符设备驱动用于管理字符流数据传输的设备，例如 LCD 屏、摄像头等。

1. 字符设备注册

​	使用 `register_chrdev()`或者`cdev_add()`函数将字符设备注册到内核中，并分配设备号，通过设备号来访问设备。

2. 文件操作/系统调用接口

​	字符设备的系统调用接口通过`file_operations`结构体实现，并在注册设备时传递给内核，定义了设备的读、写、打开、设置等操作。

3. 创建设备文件，用于供用户空间访问使用

​	创建设备文件的方法？？？

4. 实现中断处理程序

​	使用`request_irq()`注册中断处理程序，响应中断并返回中断处理结果。

5. 实现内存映射接口

​	实现内存映射`mmap()`接口，使用`remap_pfn_range()`函数将设备的物理内存映射到用户空间，使用户空间程序直接访问设备内存。

#### 06 内存分配 API

首先，内核中常用的内存分配 API 包括`kmalloc()`、`vmalloc()`、`get_free_pages()`和`alloc_pages()`等。

​	`kmalloc()`函数用于分配小块连续的物理内存，适用于 DMA （直接内存访问）等需要物理地址连续的情况。分配的内存来自内核空间，不能被用户空间程序直接访问。`kfree()`用于释放分配的内存。

​	`vmalloc()`函数用于分配较大的连续虚拟内存，在物理地址上不一定连续。内核通过页表机制将非连续的物理地址映射到连续虚拟内存中，实现大块内存的分配，适用于需要较大内存的数据缓冲区。`vfree()`用于释放分配的内存。

​	`alloc_pages()`函数用于分配多页物理连续内存，实现对内存分配行为的精细控制，适用于直接操作物理内存的场景。`__free_pages()`用于释放分配的内存。

#### 07 BootLoader 启动流程

首先，BootLoader 的启动包括两个阶段。

​	第一阶段：设置时钟，初始化内存空间，为加载 BootLoader 的第二阶段代码准备 RAM 空间，并加载 BootLoader 的第二阶段代码。

​	第二阶段：初始化需要使用的硬件设备，将内核映像和设备树文件加载到内存中，并设置内核启动参数，包括根文件系统路径、启动模式等。BootLoader 完成所有准备工作后，将 CPU 控制权交给 Linux 内核并启动 Linux 内核。Linux 内核完成初始化硬件设备、初始化内存管理单元、初始化进程管理、初始化文件系统、初始化用户空间，最后等到用户输入。

​	挂载根文件系统：BootLoader 通过设置环境变量`setenv bootargs`向内核提供根文件系统路径和挂载方式等启动参数；内核获取 CPU 控制权并启动后，寻找并挂载根文件系统，并执行文件系统初始化程序。

#### 08 V4L2 应用层

首先，V4L2 框架是 Linux 系统处理视频捕获和输出的标准化 API，用于操作摄像头模块。

1. 打开摄像头设备文件

```
fd = open("/dev/video0", O_RDWR);
```

2. 调用`ioctl`函数查询视频设备所支持的功能

```
struct v4l2_capability cap;
ioctl(fd, VIDIOC_QUERYCAP, &cap);
```

3. 枚举/设置/获取视频数据格式

```
struct v4l2_fmtdesc fmtdesc;
struct v4l2_format  fmtset;
ioctl(fd, VIDIOC_ENUM_FMT, &fmtdesc);
ioctl(fd, VIDIOC_S_FMT, &fmtset);
```

4. 申请数据缓冲区，查询缓冲区信息，并将缓冲区映射到用户空间的地址空间，让用户程序可以直接访问缓冲区数据。

```
struct v4l2_requestbuffers bufReq;
struct v4l2_buffer		   bufHas;
struct buffersHas;
ioctl(fd, VIDIOC_REQBUFS, &bufReq);
ioctl(fd, VIDIOC_QUERYBUF, &bufHas);
buffersHas[i].start = mmap(NULL, bufHas.length, PROT_READ|PROT_WRITE, MAP_SHARED, fd, bufHas.m.offser);
```

5. 将申请的数据缓冲区放入 incoming 队列，并启动视频数据流传输

```
enum v4l2_buf_type type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
ioctl(fd, VIDIOC_QBUF, &bufHas);
ioctl(fd, VIDIOC_STREAMON, &type);
```

6. 循环读取就绪队列 outgoing 中的缓冲区，并处理图像数据帧，处理完成后再将缓冲区放入 incoming 队列中

```
select(fd+1, &fds, NULL, NULL, &tv);
ioctl(fd, VIDIOC_DQBUF, &bufHas);
ioctl(fd, VIDIOC_QBUF, &bufHas);
```

7. 关闭视频数据流传输，释放内存映射的缓冲区，最后关闭视频设备。

```
ioctl(fd, VIDIOC_STREAMOFF, &type);
munmap(buffersHas[i], bufHas.length);
close(fd);
```

#### 09 驱动程序代码和数据存储

首先，驱动程序是操作系统内核的一部分，其代码运行在内核空间，驱动程序的全局变量和静态变量也都存储在内核空间（内核数据段），局部变量存储在内核栈，动态分配内存存储在内核堆。

#### 10 tasklet / workqueue

#### 11 阻塞和休眠

首先，阻塞是指进程在等待某个事件发生时被挂起，进入睡眠状态。阻塞期间进程不占用 CPU 资源，并进入阻塞队列，直到等待的事件发生或者超时，才会被唤醒并重新进入就绪队列。阻塞实现函数`wait_event()`，唤醒函数`wake_up()`，适用于需要等待特定事件发生的场景。

其次，休眠是指进程在没有外部事件的情况下主动让出 CPU 资源，进入睡眠状态，等待指定时间或者条件满足后重新被唤醒。休眠函数`msleep()`，适用于需要简单延迟的场景。



### I/O 原理

### 文件管理



# 计算机网络

## 网络模型

## TCP/IP 协议

## ping 原理



# MCU 原理

## MCU 基本组成

### STM32 基础

#### 01 STM32 架构

首先，STM32 系列微控制器采用 32 位 ARM Cortex-M 内核，采用精简指令集计算机（RISC）架构，支持 Thumb 指令集，支持硬件浮点运算单元（FPU）和数字信号处理（DSP）指令，适合复杂计算任务，支持 4 GB 的内存寻址空间，采用高级中断控制器（NVIC），中断响应速度快，外设丰富，包括 GPIO，UART，SPI，IIC 串行通信，USB 接口等，低功耗、高性能、实时性。

#### 02 启动流程

首先，STM32 系列微控制器的启动流程包括系统复位、启动代码的执行和用户程序加载等三个部分。

​	系统复位：STM32 在上电复位后，ARM Cortex-M 核心会清空寄存器状态，并设置程序计数器（PC）为系统启动向量表的初始值，其中包括堆栈指针和复位向量地址，分别存储在向量表的第 0 和 1 个地址。初始化堆栈指针后，处理器会跳转到复位向量地址指向的启动代码。启动模式包括从 Flash 存储器启动、从系统存储器启动和从 SRAM 存储器启动。

​	启动代码的执行：启动代码`Reset_Handler`初始化系统时钟、外设时钟树和所需的硬件资源，初始化堆栈指针和全局变量，跳转到应用程序的`main`函数。

​	用户程序加载：执行用户程序中的各种任务。

#### 03 底层接口实现

首先，HAL 库是 STM32 的硬件抽象层库，封装了大部分硬件控制逻辑，例如 GPIO、UART、IIC、SPI 等外设的初始化和操作，通过结构体和 API 调用简化了硬件控制，具有跨芯片的通用性和代码的可移植性。

#### 04 IO 口工作方式

首先，STM32 单片机的 IO 端口可以根据需求配置为不同的工作模式，用于数字信号的输入输出或者特定外设功能。

其次，IO 端口的基本模式包括输入模式、输出模式、复用功能模式和模拟模式。

​	输入模式：IO 端口配置为输入模式，外部信号可以通过引脚输入到开发板，包括浮空输入、上拉输入和下拉输入。上拉输入是指引脚内部连接到上拉电阻，默认高电平；下拉输入是指引脚内部连接到下拉电阻，默认低电平；浮空输入是指引脚没有内部上拉或者下拉电阻，外部需要提供指定的电平信号。

​	输出模式：IO 端口配置为输出模式，开发板可以通过引脚驱动外部电路，包括推挽（Push-pull）输出和开漏输出。推挽输出是指输出引脚通过两个 MOS 管连接到高电平和低电平，适合驱动数字信号；开漏输出是指引脚只有下拉能力，无上拉能力，需通过外部上拉电阻拉至高电平，适用于 IIC 等双向通信场景。

​	复用功能模式：IO 端口配置为复用功能模式，引脚可以与 UART、SPI、IIC等外设连接。外设通过复用功能访问引脚，而不直接通过 GPIO 寄存器操作。

​	模拟模式：IO 端口配置为模拟模式，引脚用于模拟输入或者输出信号通道，主要用于 ADC 和 DAC。

然后，IO 端口配置的参数包括输出速度、输入触发和引脚锁定。

​	输出速度影响着引脚的驱动能力和抗干扰能力，包括低速、中速、高速等。

​	输入触发包括上升沿、下降沿或者双边沿触发，用于外部中断触发。

还有，IO 端口工作模式的经典应用包括数字输入/输出、开漏输出、复用模式和模拟模式。

​	数字输入/输出：配置为输入模式或者推挽输出模式，用于驱动 LED、读取按键状态等；

​	开漏输出：用于 IIC 总线，多个设备共享同一根信号线，需要外部上拉电阻；

​	复用模式：外设信号通过复用功能与引脚连接，例如 UART 的 TX/RX，SPI 的 MOSI/MISO；

​	模拟模式：用于 ADC/DAC 输入输出信号。

#### 05 按键消抖

首先，按键消抖是为了过滤按键触发过程中因机械抖动而产生的误操作信号，包括硬件消抖和软件消抖。

​	硬件消抖：利用 RC 电路的充放电特性对电信号进行低通滤波，从而抑制按键抖动产生的高频噪声。

​	软件消抖：

​		时间延迟法：在按键触发后延迟一定时间再检查按键状态，确保信号稳定后才认为按键被有效按下，但是延迟时间过长会影响响应速度。

​		状态机法：设计一个有限状态机来跟踪按键的状态变化，确保每个状态之间的转换是稳定的，避免了抖动信号的误判，消抖效果好而且灵活，能够处理复杂的按键事件。

#### 06 栈工作原理

首先，嵌入式系统中的栈是一片连续的内存区域，由栈指针指向栈顶位置（从高地址向低地址生长），用于存储函数调用过程中的输入参数、局部变量和返回地址等，由硬件（处理器中的栈指针寄存器）和软件（操作系统）协同完成。

其次，栈的工作过程包括

​	当函数调用时，CPU 会自动将函数的输入形参、局部变量和返回地址等压入栈中，为函数调用创建独立的栈空间，并跳转到函数入口地址；当函数执行完毕时，分配的栈空间（栈帧）会被弹出，栈指针恢复到函数调用前的位置，同时将返回地址保存到程序计数器中，继续执行原函数的下一条指令。

​	当中断发生时，CPU 会保存当前运行状态的寄存器内容、程序计数器等到栈中，并根据中断向量表跳转到对应的中断处理程序地址；当中断处理完毕后，从栈中恢复原先运行状态，原程序继续执行。

然后，栈使用的注意事项包括

​	栈溢出是指如果函数递归过深、局部变量过多或者中断嵌套过多，导致栈空间耗尽，发生栈溢出。栈溢出会覆盖其他内存区域，导致程序崩溃。

​	栈大小的分配：栈的大小根据任务的复杂程度、局部变量的使用情况和中断嵌套深度进行合理配置，避免内存浪费和栈溢出。

​	多任务环境下的栈管理：

### STM32 中断

#### 01 STM32 中断控制器

首先，STM32 中断控制器是嵌套向量中断控制器（NVIC），负责管理中断的优先级、向量地址和中断嵌套，提供高效的中断处理能力。NVIC 的主要功能有中断向量表管理、中断优先级配置、中断使能和屏蔽、中断嵌套和系统异常管理。

​	NVIC 负责管理中断向量表，当中断发生时，NVIC 根据中断号找到对应的中断处理程序并跳转指向。

​	NVIC 支持配置中断优先级，包括主优先级和子优先级。主优先级决定中断是否能被抢断、子优先级决定同级中断的执行顺序，通过调用 HAL 函数`HAL_NVIC_SetPriority()`或者寄存器配置来设置中断优先级。

​	NVIC 允许对每个中断单独使能和屏蔽，从而控制中断的触发和响应，通过 NVIC 寄存器或者 HAL 函数`HAL_NVIC_EnableIRQ()`来设置中断使能和屏蔽。

​	NVIC 支持中断嵌套，通过配置中断优先级，允许中断处理过程中被更高优先级的中断抢占。

​	NVIC 可以处理 ARM Cortex-M 内核的系统异常。

其次，STM32 中断控制器 NVIC 的使用场景包括

​	在实时操作系统 FreeRTOS 中，NVIC 用于管理多任务的上下文切换和调度。

​	在高实时性应用中，NVIC 的中断嵌套特性使其能够快速响应高优先级的任务。

​	NVIC 还可用于处理系统运行中的异常。

#### 02 STM32 中断处理过程

首先，STM32 系统的中断处理过程包括中断触发、中断向量跳转、服务程序执行以及中断返回这四个阶段。

​	中断触发：

1. STM32 的外设（如 GPIO、UART、触控屏等）或者系统事件（如软中断）产生中断请求信号；
2. 中断请求信号被发送到中断控制器 NVIC，NVIC 根据中断优先级和使能状态决定是否响应中断。

​	中断向量跳转：

1. 当 NVIC 决定响应中断后，会查找中断向量表中相应的中断处理程序入口地址；
2. ARM Cortex-M 内核会自动保存当前程序执行状态，包括寄存器数据和程序状态寄存器（CPSR），并将程序计数器跳转到中断处理程序入口地址。

​	中断处理程序执行：

1. 执行中断处理程序逻辑，包括读取外设数据、触发其他外设、清除中断标记位等；
2. 中断处理程序逻辑应保持简洁，避免占用过多时间，影响其他任务的执行；

​	中断返回：

1. 中断处理程序执行完毕后，处理器会从栈中恢复保存的上下文信息；
2. 程序计数器恢复到中断发生前的地址，继续执行原任务或者指令。

​	关键寄存器包括中断使能寄存器`NVIC_ISERx`，中断清除使能寄存器`NVIC_ICERx`，中断优先级设置寄存器`NVIC_IPR`，程序状态寄存器`CPSR/SPSR`，中断控制状态寄存器`SCB->ICSR`等。

#### 03 中断嵌套

首先，STM32 中断控制器 NVIC 支持中断嵌套，通过配置中断的主优先级允许高优先级的中断可以打断正在执行的低优先级中断，保证系统对高优先级事件的快速响应，也允许低优先级事件在没有高优先级事情抢占的情况下的正常允许。

其次，中断嵌套适用于实时性要求较高的场景，保证对高优先级事件的快速响应；当某些中断的重要性远高于其他中断时，需要中断嵌套以确保高优先级事件不被低优先级事件阻塞，例如定时器中断的优先级高于 UART 中断。在多任务系统（RTOS）中，中断嵌套有助于实现更高效的任务切换和调度，同时要避免中断嵌套时多个中断共享资源时的资源竞争风险。对于中断处理时间短、系统复杂度较低的情况不需要使用中断嵌套。

然后，NVIC 通过配置中断优先级和中断的使能与屏蔽来控制中断嵌套，同时保持中断处理程序逻辑简洁，避免不必要的性能浪费。

#### 04 中断的使用

首先，嵌入式系统的中断用于处理异步事件，包括外设请求、定时器超时、信号输入等，可以快速响应外部或者内部的事件，而无需循环询问外设状态。

1. 在使用中断前，需要对中断相关的硬件资源（如外设、定时器或者 GPIO）进行初始化和配置。主要包括使能中断、时钟、设置中断触发模式（上升沿、下降沿或双沿触发）和优先级；
2. 中断请求会跳转到对应的中断服务程序，记录在中断向量表中；
3. 实现中断服务程序逻辑，包括数据读取、任务触发等操作，需要手动清除中断标志位，避免中断重复触发；
4. 通过中断控制器（如ARM Cortex-M的NVIC）可以使能或屏蔽特定的中断，动态管理系统中断。

其次，中断使用的注意事项

1. 中断服务程序应保持逻辑简洁，只做必要的操作，避免过长的中断占用时间，影响系统的实时性；
2. 合理设置中断优先级，避免中断嵌套过深增加系统的复杂度，导致栈空间的快速消耗，甚至出现栈溢出；
3. 合理配置中断优先级，保证高优先级事件得到快速响应；
4. 中断触发后及时清除中断标志位，避免中断重复触发；
5. 中断中避免使用复杂函数，例如`malloc()`、`sleep()`等，避免过长的中断处理时间；
6. 如果中断和主程序或其他任务共享数据，需通过同步机制(如互斥锁)保护共享资源，避免资源竞争；
7. 合理配置栈空间大小，避免内存浪费和栈溢出。

#### 05 中断非要延迟

#### 06 保护现场

首先，保护现场是指在进入中断处理程序之前，处理器会自动保存当前程序的执行状态，包括通用寄存器 R0~R12，堆栈指针 SP，链接寄存器 LR，程序计数器 PC，状态寄存器 CPSR/SPSR。

​	通用寄存器 R0~R3 用于存储临时变量、函数参数、返回值等和 R12 通用寄存器；

​	堆栈指针 SP 指向栈顶位置，用于管理栈操作；

​	链接寄存器 LR 存储函数调用的返回地址；

​	程序计数器 PC 指向当前执行的指令地址，保证中断处理完成后，继续指向原指令；

​	程序状态寄存器 CPSR/SPSR 保存了程序执行的状态信息。

#### 07 中断调用串口

#### 08 中断数据

首先，中断发生后，数据来源取决于触发中断的外设类型。

​	中断处理程序通过读取相关外设寄存器获取中断事件的信息，例如，UART 接收中断可以读取数据寄存器（DR)获取接收到的数据，定时器中断可以读取计数器值获取时间信息。

​	如果中断和主程序共享缓冲区或全局变量，中断处理过程中将数据写入这些区域， 主程序在中断后通过轮询、标志位或信号量获取数据。

​	在实时操作系统中，中断可以通过消息队列、事件标志或信号量通知任务，从而将数据传递到需要处理的任务。例如，中断将接收到的数据放入消息队列，等待任务读取处理。

#### 09 异常

首先，异常会终止 CPU 的正常执行流程，并且去执行异常处理程序。异常类型包括 FIQ (快速中断响应请求), IRQ (中断响应请求), ABT (中止), Reset (复位), SVC (系统调用请求)

​	FIQ, IRQ：异步异常

​	ABT (中止)：表示指令预取失败或者数据访问失败，例如 MMU 动态分配内存。

​	Reset (复位)：最高优先级异常，上电后立即执行

​	SVC (系统调用请求)：用户程序请求系统调用服务

## 指令和总线

### DMA 传输

#### 01 DMA 传输

首先，DMA（Direct Memory Access，直接内存访问）允许外设直接与内存交换数据，而无需通过 CPU 参与数据传输。外设能够在后台完成数据传输，从而减轻CPU的负担，提高系统的效率。在使用 DMA 的情况下，CPU 只需配置 DMA 控制器（如设置传输的地址、数据长度和模式)，传输的实际过程由 DMA 控制器独立完成，而CPU可以腾出时间处理其他任务。DMA 控制器负责管理数据传输，包括源地址、目标地址、传输大小和传输模式。每个DMA 控制器可以支持多个通道，允许同时管理多个外设的数据传输。此时，外设（如UART、SPI）或内存（如SRAM、Flash）之间的数据交互通过DMA控制器完成。

其次，DMA 的典型应用场景包括

​	外设数据传输，例如 UART 接收或发送大量数据，DMA 可将数据直接写入或读出内存

​	高速数据采集，例如在 ADC 或 DAC 应用中，DMA 可以快速传输大量数据，避免因 CPU 速度限制而丢失数据；

​	存储器之间的数据拷贝，例如将数据从 SRAM 复制到 Flash，DMA 可直接完成传输。

​	在需要保证实时性的系统中，DMA的非阻塞特性可以有效避免CPU的瓶颈。

然后，DMA 的操作流程包括

1. 初始化 DMA 控制器，配置 DMA 的源地址、目标地址、数据大小和传输模式；
2. CPU 启动 DMA 传输任务，DMA 控制器接管数据传输。
3. DMA 控制器完成任务后，会触发中断通知 CPU 传输已完成。

还有，DMA 的传输模式包括从内存到外设，从外设到内存，从内存到内存，从外设到外设。

​	内存到外设，例如将数据从内存发送到 UART；

​	外设到内存，例如从 ADC 将采集数据存储到内存；

​	内存到内存，例如数据在两个内存区域之间（内存和硬盘之间）的拷贝；

​	外设到外设：虽然较少见，但某些硬件支持外设间的直接数据交换。

最后，DMA 允许外设在后台完成数据传输，减轻 CPU 的负担，提高系统的效率；DMA 可以批量传输数据，减少中断次数；DMA 支持高速外设，例如 UART、SPI，DMA 能匹配其传输速率，避免数据丢失。

#### 02 DMA 配置

首先，DMA 的配置流程包括初始化 DMA 控制器，配置数据传输参数和启动传输。

​	启动 DMA 时钟（使用 STM32 HAL 库`__HAL_RCC_DMA1_CLK_ENABLE()`）；

​	配置 DMA 通道，包括源地址、目标地址、数据长度、传输方向、数据宽度和优先级；

​	设置 DMA 工作模式，例如单次传输模式和循环模式；

​	配置外设和内存地址，包括外设的数据寄存器地址和目标缓冲区地址；

​	使能 DMA 中断，在 DMA 传输完成或者发生错误时触发中断，执行中断处理函数；

​	启动 DMA 传输。

其次，具体配置代码示例，采用 STM32 HAL 库？？？？

#### 03 DMA 与缓存

首先，在带有缓冲的系统中，CPU 为了提高数据访问速度，会将主内存中部分数据复制到高速缓存（Cache）中。当 CPU 需要读取数据时，会首先查找 Cache，如果数据存在（Cache 命中），则直接从 Cache 中读取，速度远快于访问主内存。当 CPU 需要写入数据时，会将数据写入Cache，并根据缓存策略（如写直达、写回等）在稍后的某个时间更新到主内存。但是，由于 Cache 和主内存之间的内容可能不同步，会引入数据不一致的问题。

​	CPU 写 Cache 而主内存未更新：当 CPU 将数据写入缓存但未同步到主内存时，DMA 控制器从主内存中读取的可能是旧数据。这会导致 DMA 处理的内容与 CPU 操作的内容不一致。

​	DMA 写主内存而 Cache 未更新：当 DMA 将数据写入主内存后，CPU 可能从缓存中读取已过期的数据，而非主内存中的最新数据。这会导致 CPU 处理的内容落后于DMA操作的内容。 

其次，DMA 控制器与 Cache 协作的解决方法。

​	禁用缓冲，所有读写操作直接访问主内存，避免缓存和主内存的数据不同步问题，但是降低了系统性能。

​	在 CPU 写数据后且需要由 DMA 读取时，执行 Cache Flush（清理缓冲）操作，将缓存中的数据强制写回主内存。它通过调用硬件指令（如 ARM 的DC CLEAN）或通过操作系统 API（如` dma_sync_single_for_device`)。适用于 CPU 写内存，DMA 读取。

​	在 DMA 向主内存写数据后，CPU在读取前需要执行 Cache Invalidate（无效化缓冲）操作，确保缓存中的旧数据无效化，从主内存获取最新数据。它通过调用硬件指令（如ARM 的DC INVALIDATE）或操作系统 API。适用于 DMA 写内存，CPU 读取。

#### 04 DMA 与主内存

首先，DMA 在进行数据传输时，涉及多个主体（CPU、DMA 控制器）对主内存（Buffer）的访问， 可能出现数据竞争或一致性问题。

​	DMA 和 CPU 同时操作 Buffer ：DMA 控制器正在传输数据，而 CPU 尝试读取或修改 Buffer，可能导致数据冲突或一致性问题；

​	双 Buffer 问题：在某些应用中，CPU 需要处理一个 Buffer 中的数据，而 DMA 同时填充另一个 Buffer，如果管理不当，可能导致 Buffer 数据覆盖或丢失。

其次，解决 Buffer 互斥的常用方法。

​	使用双缓冲机制（Double Buffering）：**为 DMA 配置两个独立的 Buffer，一个用于 DMA 传输， 另一个供 CPU 使用**。当DMA完成传输后，交换 Buffer 的角色。首先初始化两个 Buffer，并将其中一个分配给 DMA。在配置 DMA 在传输完成时触发中断。最后在中断处理函数切换 Buffer 的角色(例如，CPU 处理已传输完成的 Buffer，而DMA 使用新的 Buffer)。优点是DMA和CPU可以并行工作，提高效率。但是要确保 Buffer 切换时的同步，避免切换前对未完成的数据进行操作。

​	使用缓冲标志位（Buffer Status Flag）：**在 Buffer 的数据结构中添加状态标志位，用于标识 Buffer 的当前状态（如“正在使用”或“可用”）**。CPU 或 DMA 操作前检查标志位，避免冲突。首先在 Buffer 的数据结构中添加状态标志。在 CPU 或 DMA 操作 Buffer 前，检查并修改标志位。最后确保操作完成后及时更新标志位。优点是简单易实现，适用于单 Buffer 场景。但是需要保护标志位的读写操作，防止竞态条件。

​	在实时操作系统（RTOS）环境下，**使用互斥锁或信号量控制 CPU 和 DMA 对 Buffer 的访问**。首先在任务中获取互斥锁后，访问 Buffer。在 DMA 完成传输时，通过信号量通知 CPU。 最后释放互斥锁或信号量，允许其他任务或 DMA 操作。优点是适用于多任务系统，便于扩展。但是需要避免互斥锁的长时间占用，影响系统的实时性。

然后，Buffer 互斥解决方法的实际应用包括

​	高实时性系统：优先使用双缓冲机制，因为它允许DMA和CPU并行工作，提高系统效率。 

​	简单系统或单 Buffer 场景：可以使用标志位或禁用 Cache 的方法，确保访问互斥。

​	多任务系统：使用 RTOS 提供的互斥锁或信号量，保证任务间访问的安全性。

#### 05 DMA 串口通信

首先，在串口通信中，DMA 的不同模式适用于不同的数据传输场景。根据传输需求， 可以选择普通模式、循环模式。

​	普通模式适用于一次性、固定长度的数据传输场景，例如，发送单次命令或短消息。在普通模式下，DMA 完成指定的数据传输后自动停止，需要由软件重新启动。普通模式逻辑简单，易于实现，适合较小数据量的任务。但是，它需要每次传输完成后重新启动 DMA，因而不适合持续性或大批量数据传输的场景。

​	循环模式适用于持续接收数据的场景，例如从传感器获取实时数据或 UART 接收固定频率的数据流。在循环模式下，DMA 在完成一次传输后会自动重启，无需软件干预，能实现连续数据传输。循环模式减少了对CPU的干预需求，特别适合实时性要求较高的场景。然而，由于数据始终覆盖写入，可能会出现数据竞争和一致性问题，需要通过标志位或缓冲区管理来确保数据完整性。

### 通信总线

#### 01 IIC

两线式通信总线（Inter-Integrated Circuit, IIC）是一种同步串行半双工通信总线，使用两根线（SCL 串行时钟线、SDA 串行数据线）实现多主多从通信。主设备通过时钟信号同步数据传输，并且发送从设备地址（7/10位）来选择与之通信的从设备，IIC 协议支持的设备数量取决于地址长度（7 位支持 128 个设备，10 位支持 1024 个设备）。I2C的特点是硬件简单，占用引脚少，支持低速短距离通信，数据传输速率一般为100kbps（标准模式）、400kbps （快速模式）、3.4Mbps（高速模式）。然而，l2C的通信距离有限，对时钟信号质量依赖较高，适合用于传感器（如温湿度传感器）、EEPROM、实时钟等设备的通信。

​	通信过程：

1. **起始信号 (START Condition)：** 主机在 SCL 线为高电平时，将 SDA 线从高电平拉低，表示通信开始。
2. **地址帧 (Address Frame)：** 主机发送要通信的从机设备的地址。地址可以是 7 位或 10 位，紧随其后的是一个读/写位 (R/W)。R/W 位为 0 表示主机要向从机写入数据，为 1 表示主机要从从机读取数据。
3. **应答信号 (ACK)：** 被寻址到的从机在接收到地址帧后，会发送一个应答位（将 SDA 线拉低）给主机。如果从机没有应答（SDA 线保持高电平），则主机可以采取相应的错误处理措施。
4. **数据传输 (Data Transfer)：** 数据以字节为单位进行传输。每个字节后都会跟随一个应答位，由接收方产生。读信号时，主设备释放 SDA，由从设备控制数据线发送数据。
5. **停止信号 (STOP Condition)：** 当数据传输完成后，主机在 SCL 线为高电平时，将 SDA 线从低电平拉高，表示通信结束。

​	接收方式：

1. 轮询方式：CPU 定期查询主机接收状态寄存器，检查是否有数据到达，并读取接收缓冲区的数据。实现简单，但是 CPU 利用效率较低，不适用于实时性要求高的场景。
2. 中断方式：主机接收到数据时触发中断，CPU 在中断处理程序中读取接收缓冲区。降低了 CPU 的空闲等待时间，适合实时性较高的应用。
3. DMA 方式：通过 DMA 控制器将数据直接从主机外设的接收寄存器传输到内存，CPU 无需干预。 这种方式效率高，适合大批量数据接收或实时性要求高的场景，但是实现复杂。

#### 02 SPI

SPI（串行外设接口）是一种高速全双工同步通信总线，通常由主设备和一个或多个从设备组成。SPl 需要至少4根线：SCLK（串行时钟线）、MOSI（主输出从输入）、MISO（主输入从输出）和CS（片选线）。

​	串行时钟线（SCLK）：由主机产生时钟信号，用于同步数据传输。

​	主机输出/从机输入线（MOSI）：用于主机向从机发送数据。MOSI 和 MISO 信号在 SCLK 上升沿变化，在下降沿锁存数据。

​	主机输入/从机输出线（MISO）：用于从机向主机发送数据。

​	从机选择线（CS）：由主机控制，用于选择与哪个从机进行通信。通常为低电平有效。每个从设备都需要一条独立的 CS 线连接到主设备上。

SPI 支持一主多从通信，通过从机选择线选择从设备，连接的设备数量取决于主设备的可用 GPIO 引脚数量，通信速率较高，可达几十Mbps或更高。SPl的优势在于通信速度快，协议简单，支持全双工通信，但随着从设备数量增加，片选线占用的 IO 口资源较多，常用于显示屏、存储器、ADC/DAC芯片的通信。

​	通信过程：

1. **主机选择从机：** 主机将目标从机的 CS 线拉低，选中该从机进行通信。对于未被选中的从机，其输出线 MISO 通常会处于高阻态。
2. **时钟信号产生：** 主机在 SCLK 线上产生连续的时钟信号。
3. **数据传输：** 在每个时钟信号的上升沿或下降沿，数据在 MOSI 线上由主机发送给从机，同时在 MISO 线上由从机发送给主机。数据通常以字节为单位进行传输，但也可以配置为其他数据宽度。
4. **取消从机选择：** 当数据传输完成后，主机将 SS/CS 线拉高，取消对该从机的选择，结束本次通信。

​	主从关系：

​		主设备控制时钟信号（SCLK），决定通信速率；通过 CS 信号选择具体的从设备。

​		从设备被动响应主设备的通信请求，根据时钟信号同步接收或发送数据。

​		特点：主设备主动发起通信，从设备只能响应，没有主动发起能力。

​	主动读写：

​		SPI 的主设备负责时钟生成，因此通信必须由主设备主动发起。即使主设备需要从设备的数据，也需发送“虚拟数据”来拉动时钟线（SCLK)。

​	接收方式：

1. 轮询方式：CPU 通过查询 SPI 状态寄存器，检查接收缓冲区是否有数据。
2. 中断方式：SPI 接收到数据后触发中断，CPU 在中断服务程序中读取数据。
3. DMA 方式：DMA 控制器直接将 SPI 接收的数据写入内存，CPU 仅在 DMA 完成后通过中断获知数据已接收。

#### 03 UART

UART（通用异步收发传输）是一种异步串行全双工通信方式，不需要单独的时钟信号线来同步数据传输，通过规定波特率（每秒传输的比特数）实现数据传输同步。数据以帧的形式传输，包括起始位、数据位、校验位和停止位。UART 采用全双工通信，TX 和 RX 线独立，允许数据在两个方向上同时传输。UART 采用点对点通信方式，用于两个设备之间的直接通信，波特率常见为9600~115200 bps。UART 简单易用，硬件实现非常广泛，但其通信效率较低，容易受到波特率误差的影响，常用于串口调试、单片机与PC机之间的通信。

​	通信过程：

1. **空闲状态：** 在没有数据传输时，TX 和 RX 线通常保持在高电平（逻辑 1）。
2. **起始信号：** 发送方将 TX 线从高电平拉低到低电平，作为起始位，通知接收方数据传输即将开始。
3. **数据传输：** 发送方按照约定的波特率，将数据位的每一位依次发送到 TX 线上。
4. **校验：** 如果启用了校验位，发送方会在数据位之后发送校验位。
5. **停止信号：** 发送方将 TX 线拉高到高电平，并保持停止位的时间长度，标志着一个数据帧的结束。
6. **接收：** 接收方在其 RX 线检测到起始位后，会按照与发送方相同的波特率对后续的数据位、校验位和停止位进行采样，从而恢复出原始数据。

​	主动读写：

​		UART 的发送和接收是相互独立的，双方都可以主动发送数据，而无需等待另一端请求。

​	接收方式：

1. 轮询方式：CPU 定期查询 UART 接收寄存器，检查是否有数据到达。
2. 中断方式：UART 接收到数据后触发中断，CPU 在中断服务程序中读取接收寄存器。
3. DMA 方式：DMA 控制器直接将 UART 接收到的数据传输到内存，CPU 仅处理 DMA 完成后的数据。

#### 04 CAN

CAN（控制器局域网络）是一种面向汽车和工业控制的多主通信总线，采用消息 ID 来标识数据包，支持多设备通信。CAN 总线具有高抗干扰能力，数据传输速率可达 1 Mbps，传输距离可达数百米。CAN 内置仲裁机制，允许多个设备同时发起通信且无冲突。它的优点是抗干扰能力强，支持多主通信，适合复杂网络，但实现复杂，调试困难，速率较低。CAN广泛应用于汽车电子（如ECU、传感器）和工业自动化领域。

#### 05 USB

USB（通用串行总线）是一种高速即插即用串行通信总线协议。USB 设备通过主从架构通信，主机（如PC）与从设备（如嵌入式设备）进行数据交换。USB 2.0 电缆包括 4 根线，VBUS（提供电源）、GND（地线）、D+（差分数据线 1）、D-（差分数据线 2）。USB 支持多种传输类型，包括批量传输、中断传输、等时传输等

​	批量传输：用于传输大量数据，如打印机和存储设备。它保证数据的完整性，但不保证传输的实时性；

​	中断传输：用于传输少量、对延迟敏感的数据，如键盘、鼠标和游戏手柄；

​	等时传输：用于传输对时间要求非常严格的流数据，如音频和视频。它保证传输的带宽，但不保证数据的可靠性。

USB 传输速率高，使用方便，但实现复杂，主机资源消耗较大，广泛用于 PC 与外设（如键盘、鼠标、摄像头)以及嵌入式设备的数据交互。在嵌入式 Linux 软件开发中，对 USB 的支持通常包括 USB 设备树配置、USB 设备驱动安装和用户空间 API 操作。



## 处理器架构和设计

## 存储系统

## I/O 系统



## xv6

### 简历

基于 RISC-V 架构的类 UNIX 操作系统内核开发

【项目背景】该项目采用 QEMU 模拟器运行 xv6-RISC 操作系统，涉及内核设计、系统调用、内存管理、进程调度等操作系统核心概念，我做了 ...

【技术细节】

写时复制特性 fork() ：在 fork() 系统调用中添加了写时复制特性，将页面复制推迟到写入访问时，减少内存浪费和加载进程创建速度。

多CPU并发分配内存：修改 xv6 操作系统的内存分配器，动态管理多 CPU 的内存池，在多核架构上实现内存分配的高并发性。

支持内存映射到文件：增添 xv6 操作系统的 mmap()/munmap() 系统调用，实现对内存映射文件的支持，扩展操作系统的内存管理模块

把 xv6 比较有分量的 lab 总结放上去，比如 mmap，cow，页表（虚拟内存机制、进程调度、中断管理，文件系统...）

从上电说到linux启动到一号进程并降级到用户态《嵌入式学习路线 -提高部分(中 内核部分)》

【项目基本信息】主要介绍了xv6整套中断处理流程，如何实现一个COW特性的fork()，如何实现用户级线程的上下文切换和创建，如何使用pthread中提供的互斥锁保护临界区以及使用条件变量实现一个barrier()，如何优化xv6内存分配使得锁的竞争减少提高内存分配时的效率，如何优化磁盘块缓冲区提高并行度，如何更改文件系统以增加文件的最大大小并实现符号链接。

【遇到的困难】代码难度都不是很大，主要功夫花费在阅读xv6手册和源码来理清执行流程，然后举了一个当时做Lab cow时遇到的一个bug，在通过了实验提供的测试数据的情况下，运行usertests检查xv6各个部分是否仍然正常运行时，在一个测试点处xv6表现的就像是死机了，然后gdb调试了也没找出问题所在，后来在重理代码的过程中才发现是因为假如进程写一个只读页面的时候中断处理函数返回值应该是非0值，但是在cow中断处理函数中因为逻辑没有理清，导致在进程尝试写只读页面的时候，中断处理程序会返回0，代表中断成功处理，进程不会被kill掉，于是进程会再次尝试执行之前产生中断的指令，然后还是会再次尝试写这个只读页面，从而再次产生中断，程序一直在尝试执行和中断处理之间来回切换，造成死机的表现。

### COW fork()

#### 原始 fork()

```
fork()
1. 为子进程分配 UNUSED 进程结构 proc
np = allocproc();
2. 为子进程复制父进程的页表，分配独立的地址空间
np->pgdir = copyuvm(curproc->pgdir, curproc->sz)
3. 设置子进程的大小、父进程和中断帧 trap frame，并且将子进程的返回值设为 0
np->sz = curproc->sz;
np->parent = curproc;
*np->tf = *curproc->tf; 
np->tf->eax = 0;
4. 为子进程复制父进程的文件描述符表和工作路径，并且增加文件引用次数
for(i = 0; i < NOFILE; i++) np->ofile[i] = filedup(curproc->ofile[i]);
np->cwd = idup(curproc->cwd);
5. 将子进程的状态设置为就绪态 RUNNABLE，表示可以调度执行
acquire(&ptable.lock);
np->state = RUNNABLE;
release(&ptable.lock);
6. 返回子进程的 PID 给父进程
```

#### copy-on-write fork()

```
原始 fork() 的 copyuvm() 为子进程分配新的物理页，并且将父进程的页表内容拷贝到新页表中
	mem = kalloc();
	memmove(mem, (char*)P2V(pa), PGSIZE);
	mappages(d, (void*)i, PGSIZE, V2P(mem), flags);
xv6操作系统中原来对于 fork() 的实现是将父进程的用户空间全部复制到子进程的用户空间。但如果父进程地址空间太大，那这个复制过程将非常耗时。另外，现实中经常出现 fork() + exec() 的调用组合，这种情况下 fork() 中进行的复制操作完全是浪费。基于此，我们可以利用页表实现写时复制 (copy-on-write) 机制。

在 uvmcopy() 中不再给子进程分配新的物理页，而是将父进程的物理页映射到子进程的页表中
1. 创建新的页表内存
d = setupkvm();
2. 遍历父进程的内存空间，获取父进程内存的物理页地址
for(i = 0; i < sz; i += PGSIZE)、
	pte = walk(old, i, 0)
	pa = PTE2PA(*pte);
3. 不为子进程分配新的物理页，而是将该物理页标记为写时复制 (COW) 和不可写入，并且将其引用次数 +1
reference_count[pa >> 12] += 1;
*pte &= ~PTE_W;	*pte |= PTE_COW;	// 只在子进程的页表中标记为 COW 页
4. 将父进程的物理页映射到子进程的页表中
mappages(new, i, PGSIZE, (uint64)pa, flags);

此时，父进程和子进程对 COW 页都没有写权限，如果某个进程试图对该页进行写操作，会触发 Page Fault，此时在 trap() 中处理这个异常
trap()
	if (r_scause() == 15) // write page fault
    {
        if (cowhandler(p->pagetable, r_stval()) < 0)
          p->killed = 1;
    } 
    
调用异常处理函数 cowhandler()
如果是 COW 页，则分配一个新的物理页，并将它映射到产生缺页异常的进程的页表中，同时清除 COW 位，设置写权限位。
pte = walk(pagetable, va, 0);
(*pte & PTE_COW) != 0
uint64 pa = PTE2PA(*pte);
uint64 ka = (uint64) kalloc();
memmove((char*)ka, (char*)pa, PGSIZE);
kfree((void*)pa);
uint flags = PTE_FLAGS(*pte);
*pte = PA2PTE(ka) | flags | PTE_W;
*pte &= ~PTE_COW;

修改物理页相关函数，添加计数器
kalloc(void) 分配物理页函数中将对应计数器置为1
进程在fork时会调用uvmcopy函数，我们要在其中将 COW 页对应的计数器加1。
kfree(void *pa) free掉某个物理页时，我们要将其计数器减1。
如果内核调用copyout函数试图修改一个进程的COW页，也需要进行cowhandler类似的操作来处理。
```

### mmap()

mmap 和 munmap 系统调用允许UNIX程序对其地址空间进行详细控制。它们可用于在进程之间共享内存， 将文件映射到进程地址空间，并作为用户级页面错误方案的一部分。在本实验中，重点关注内存映射文 件（memory-mapped files）。

添加`mmap`和`munmap`系统调用

```
// kernel/syscall.c
extern uint64 sys_mmap(void);
extern uint64 sys_munmap(void);
[SYS_mmap] sys_mmap,
[SYS_munmap] sys_munmap,
// kernel/syscall.h
#define SYS_mmap 22
#define SYS_munmap 23
// user/usys.pl
entry("mmap");
entry("munmap");
// user/user.h
void* mmap(void *, int, int, int, int, uint);
int munmap(void *, int);
```

在kernel/proc.h定义VMA（虚拟内存区域）对应的结构体，记录 mmap 创建的虚拟内存范围的地 址、长度、权限、文件。由于xv6内核中没有内存分配器，因此可以声明一个固定大小的VMA数组，并根据需要从该数组进行分配。大小为16应该就足够了，并将 struct vma vma[VMASIZE]; // 进程的vma结构体数组 添加到结构体 proc 中。

```
// mmap
#define VMASIZE 16
struct vma {
    int valid; // 有效位
    uint64 addr; // 内存起始地址，可假设始终为0
    int length; // 映射字节数
    struct file *f;
    int prot; // 内存是否应映射为可读、可写
    int flags; // MAP_SHARE 或者 MAP_PRIVATE
    int fd; // 文件的描述符
    int offset; // 偏移量，可假定为0
};
```

#### usertrap()

接下来修改 usertrap ，惰性地填写页表，以响应page fault。即 mmap 不应该分配物理内存或读取 文件，通常发生在延迟加载（lazy loading）或者内存映射文件（memory-mapped file）的上下文 中，逻辑如下： 

首先，检查引发页错误的虚拟地址（ va ）是否有效。如果虚拟地址超出了进程的地址空间， 或者与进程的堆栈重叠，那么进程将被标记为需要被杀死。

 然后，遍历进程的虚拟内存区域VMA，寻找包含这个虚拟地址的区域。如果找到了包含这个虚 拟地址的VMA，那么将为这个地址分配一块新的物理内存，并将这块内存清零。 

接下来，从VMA关联的文件中 readi 读取数据到新分配的物理内存中。 

然后，根据VMA的权限设置页表项的权限。例如，如果VMA标记为可写，那么页表项也会被 标记为可写。 

最后，更新页表，将虚拟地址 mappages 映射到新分配的物理内存。如果这个步骤失败（例 如，因为内存不足），那么新分配的物理内存将被释放，进程将被标记为需要被杀死。

```
usertrap()
1. 检查到缺页异常 page fault
if (r_scause() == 13 || r_scause() == 15)
2. 获取导致缺页异常的虚拟地址
va = r_stval();
3. 检查缺页地址是否有效：va 是否在进程内存的有效范围之内
if (va >= p->sz || va < p->trapframe->sp) { p->killed = 1; }
4. 遍历进程的虚拟内存区域 VMA，检查缺页地址是否在进程的虚拟内存区域 VMA 中
for (i = 0; i < NVMA; i++)
	if (p->vmas[i].valid)
	if (p->vmas[i].addr <= va && (p->vmas[i].addr + p->vmas[i].length) > va)
5. 如果找到了包含这个虚拟地址的VMA，那么将为这个地址分配一块新的物理内存，并将这块内存清零
ka = (uint64) kalloc();
memset((void *)ka, 0, PGSIZE);
6. 从VMA关联的文件中 readi 读取数据到新分配的物理内存中
ilock(p->vmas[i].mapfile->ip); 
readi(p->vmas[i].mapfile->ip, 0, ka, va - p->vmas[i].addr, PGSIZE); 
iunlock(p->vmas[i].mapfile->ip);
7. 根据VMA的权限设置页表项的权限。例如，如果VMA标记为可写，那么页表项也会被标记为可写。 
pm = PTE_U; 
if (p->vmas[i].prot & PROT_READ) pm |= PTE_R; 
if (p->vmas[i].prot & PROT_WRITE) pm |= PTE_W;
8. 最后，更新页表，将虚拟地址 mappages 映射到新分配的物理内存。
mappages(p->pagetable, va, PGSIZE, ka, pm);
```

#### sys_mmap()

在kernel/sysfile.c中实现函数 sys_mmap() ：在进程的地址空间中找到一个未使用的区域来映射文 件，并将VMA添加到进程的映射区域表中。VMA应该包含指向映射文件对应 struct file 的指 针； mmap 应该增加文件的引用计数，以便在文件关闭时结构体不会消失（参考 filedup ）： 

argaddr argint argfd 获取系统调用的参数，包括映射的起始地址、长度、权限、标志、 文件描述符和偏移量。 

检查文件的写权限是否与映射的权限匹配。如果文件不可写，但映射需要写权限，且映射类型 为共享映射，那么返回错误。 

检查映射的长度是否会导致进程的虚拟内存空间超过最大限制( p->sz > MAXVA - length )。 如果会，那么返回错误。 

遍历进程的虚拟内存区域（VMA）数组，找到一个未使用的区域。 

在找到的VMA中设置映射的信息，包括地址、长度、文件、权限、标志、文件描述符和偏移 量。

 调用 filedup() 函数增加文件的引用计数。这是因为现在有一个新的引用（即VMA）指向这 个文件，所以需要增加引用计数，防止文件在还有引用的情况下被关闭。 

更新进程的虚拟内存空间大小，并返回映射的起始地址

```
sys_mmap()
1. 使用 argaddr argint argfd 获取系统调用的参数，包括映射的起始地址、长度、权限、标志、 文件描述符和偏移量
if (argint(1, &length) < 0 || argint(2, &prot) < 0 ||
    argint(3, &flags) < 0 || argint(4, &fd) < 0)
2. 获取当前进程 proc 和文件描述符对应的文件结构 *file
p = myproc();
struct file *mapfile = p->ofile[fd];
3. 检查文件权限：文件可写，是否 PROT_WRITE 保护属性，是否 MAP_PRIVATE 标志
if ((!mapfile->writable)&&(prot&PROT_WRITE)&&(!(flags&MAP_PRIVATE)))
4. 在进程 proc 的虚拟内存区域 vmas 中找到未使用区域，并完成 VMA 设置，将文件引用次数 +1，防止文件在还有引用的情况下被关闭
p->vmas[i] ...
filedup(p->ofile[fd]);
5. 进程的大小 p->sz 增加了 length，为映射的区域留出空间。这里使用了“lazy mapping”，即并没有立即分配物理内存或建立页表映射，而是推迟到实际访问时再处理。
p->sz += length; // lazy mapping
6. 返回映射起始地址
return addr;
```

#### sys_munmap()

在kernel/sysfile.c中实现函数 sys_munmap() ：找到地址范围的VMA并取消映射指定页面（使用 uvmunmap ）。如果 munmap 删除了先前 mmap 的所有页面，它应该减少相应 struct file 的引用 计数。如果未映射的页面已被修改，并且文件已映射到 MAP_SHARED ，将页面写回该文件。查看 filewrite 以获得灵感。

 获取系统调用的参数，包括需要取消映射的起始地址和长度。

 将地址和长度分别向下和向上取整到页的边界，因为内存管理是以页为单位的。 

遍历进程的虚拟内存区域（VMA）数组，找到包含需要取消映射地址的区域。 

如果没有找到满足条件的VMA，那么直接返回。 

如果找到了满足条件的VMA，那么检查VMA的起始地址是否等于需要取消映射的地址。如果 等于，那么进行以下操作： 

​	更新VMA的起始地址和长度，将取消映射的部分从VMA中移除。

​	如果VMA的映射类型是共享映射，那么调用 filewrite() 函数将脏页的内容写回到文 件。脏页是指被修改过的页，需要写回到文件以保证数据的一致性。 

​	调用 uvmunmap() 函数取消页表的映射。这个函数的参数包括页表、起始地址、页数和是 否释放物理内存。这里设置为1，表示释放物理内存。 

​	如果VMA的长度变为0，那么关闭文件，将VMA标记为无效。 最后，函数返回0，表示操作成功。

```
sys_munmap()
1. 使用 argaddr 和 argint 获取系统调用的参数，包括需要取消映射的起始地址和长度。
if (argaddr(0, &addr) < 0 || argint(1, &length) < 0) return -1;
2. 获取当前进程
p = myproc();
3. 遍历进程的虚拟内存区域（VMA）数组，找到包含需要取消映射地址的区域。 
for (i = 0; i < NVMA; i++)
	if (p->vmas[i].valid == 1)
	if (p->vmas[i].addr <= addr && (p->vmas[i].addr + p->vmas[i].length) > addr)
4. 如果 VMA 是共享映射（MAP_SHARED 标志被设置），则将内存中的数据写回映射的文件。这是为了确保其他映射同一文件的进程能够看到当前的修改。filewrite 函数用于将数据写入文件。
if (vmap->flags & MAP_SHARED) { filewrite(vmap->mapfile, addr, length); }
5. 如果要解除映射的区域正好是整个 VMA，则调用 uvmunmap 解除整个 VMA 的映射，关闭映射的文件（fileclose），并将 VMA 标记为无效（vmap->valid = 0）。
if (vmap->addr == addr && vmap->length == length)
	uvmunmap(p->pagetable, addr, length/PGSIZE, 1);
    fileclose(vmap->mapfile);
    vmap->valid = 0;
6. 如果要解除映射的区域是从 VMA 的起始位置开始的一部分，则调用 uvmunmap 解除这部分区域的映射，并更新 VMA 的起始地址和长度。
else if (vmap->addr == addr)
	uvmunmap(p->pagetable, addr, length/PGSIZE, 1);
    vmap->addr += length;
    vmap->length -= length;
7. 如果要解除映射的区域是从 VMA 的结束位置开始的一部分，则调用 uvmunmap 解除这部分区域的映射，并更新 VMA 的长度。
else if (vmap->addr + vmap->length == addr + length)
	uvmunmap(p->pagetable, addr, length/PGSIZE, 1);
    vmap->length -= length;
```



修改kernel/vm.c中的uvmcopy和uvmunmap，避免因不合法而panic。



在kernel/proc.c中修改 exit 将进程的已映射区域取消映射，就像调用了 munmap 一样。



在kernel/proc.c中修改 fork 以确保子对象具有与父对象相同的映射区域。

### lock

multi-CPU 并发分配

在并发编程中我们经常用到锁来解决同步互斥问题，但是一个多核机器上对锁的使用不当会带来很多的所谓 “lock contention” 锁竞争问题。这个lab的目标就是对涉及到锁的数据结构进行修改已降低对锁的竞争。

通过为每个 CPU 提供独立的空闲链表，减少了多个 CPU 同时尝试分配或释放内存时对同一把锁的争夺，从而提高并发性能。正如前面提到的，`freerange` 的实现需要修改，以确保空闲内存均匀分配到每个 CPU 的空闲链表中。否则，某些 CPU 可能会“饥饿”。

#### 原始 lock

xv6将空闲的物理内存kmem组织成一个空闲链表kmem.freelist，同时用一个锁kmem.lock保护freelist，所有对kmem.freelist的访问都需要先取得锁，所以会产生很多竞争

```
kalloc.c
struct {
  struct spinlock lock;
  struct run *freelist;
} kmem;
```

#### 修改 lock

给每个CPU单独开一个freelist和对应的lock，这样只有同一个CPU上的进程同时获取对应锁才会产生竞争。

```
struct {
  struct spinlock lock;
  struct run *freelist;
} kmem[NCPU];
```

同时得修改对应的kinit和kfree的代码以适应数据结构的修改

```
kinit()
1. 为每个 CPU 初始化 kmem[i].lock 自旋锁，并生成相应的名称
snprintf(buf, 10, "kmem_CPU%d", i);
initlock(&kmem[i].lock, buf);
2. 为空闲页链表分配内存空间
freerange(end, (void*)PHYSTOP);
	kfree();

kfree()
1. 获取当前 CPU ID
push_off();
int cpu = cpuid();
pop_off();
2. 在当前 CPU 的空闲链表上执行内存释放操作
acquire(&kmem[cpu].lock);
r->next = kmem[cpu].freelist;
kmem[cpu].freelist = r;
release(&kmem[cpu].lock);
```

另外，一个相对麻烦的问题是当一个CPU的freelist为空时，需要向其他 CPU的freelist“借”空闲块。

```
kalloc()
1. 获取当前 CPU ID
push_off();
int cpu = cpuid();
pop_off();
2. 互斥访问当前 CPU 的空闲链表
非空则取出空闲页并分配
3. 在每个 CPU 的本地空闲链表为空时，允许 CPU 从其他 CPU 的空闲链表中“偷取”内存页，以提高内存分配的灵活性和效率
	循环遍历所有 CPU 的空闲页链表
	互斥访问其他 CPU 的空闲页链表 (申请锁)
	最多"偷取" 1024 个空闲页或者已经全部偷走时
	释放其他 CPU 的空闲页链表锁
4. 释放当前 CPU 的空闲页链表锁
5. 在空闲页中填充垃圾数据 1，并返回
```

Buffer cache 是xv6的文件系统中的数据结构，用来缓存部分磁盘的数据块，以减少耗时的磁盘读写操作。但这也意味着buffer cache的数据结构是所有进程共享的（不同CPU上的也是如此），如果只用一个锁 bcache.lock保证对其修改的原子性的话，势必会造成很多的竞争。

我的解决策略是根据数据块的 blocknumber 将其保存进一个哈希表，而哈希表的每个bucket都有一个相应的锁来保护，这样竞争只会发生在两个进程同时访问同一个bucket内的block。

bcache的数据结构如下：

```
struct {
  struct spinlock lock;
  struct buf head[NBUCKET];
  struct buf hash[NBUCKET][NBUF];
  struct spinlock hashlock[NBUCKET]; // lock per bucket
} bcache;
```

相应地，需要修改binit, bget和breles函数：

```
void
binit(void)
{
  struct buf *b;

  initlock(&bcache.lock, "bcache");
  for (int i = 0; i < NBUCKET; i++) {
    initlock(&bcache.hashlock[i], "bcache");

    // Create linked list of buffers
    bcache.head[i].prev = &bcache.head[i];
    bcache.head[i].next = &bcache.head[i];
    for(b = bcache.hash[i]; b < bcache.hash[i]+NBUF; b++){
      b->next = bcache.head[i].next;
      b->prev = &bcache.head[i];
      initsleeplock(&b->lock, "buffer");
      bcache.head[i].next->prev = b;
      bcache.head[i].next = b;
    }
  }
}
static struct buf*
bget(uint dev, uint blockno)
{
  struct buf *b;

  uint hashcode = blockno % NBUCKET;
  acquire(&bcache.hashlock[hashcode]);

  // Is the block already cached?
  for(b = bcache.head[hashcode].next; b != &bcache.head[hashcode]; b = b->next){
    if(b->dev == dev && b->blockno == blockno){
      b->refcnt++;
      release(&bcache.hashlock[hashcode]);
      acquiresleep(&b->lock);
      return b;
    }
  }

  // Not cached.
  // Recycle the least recently used (LRU) unused buffer.
  for(b = bcache.head[hashcode].prev; b != &bcache.head[hashcode]; b = b->prev){
    if(b->refcnt == 0) {
      b->dev = dev;
      b->blockno = blockno;
      b->valid = 0;
      b->refcnt = 1;
      release(&bcache.hashlock[hashcode]);
      acquiresleep(&b->lock);
      return b;
    }
  }
  panic("bget: no buffers");
}

void
brelse(struct buf *b)
{
  if(!holdingsleep(&b->lock))
    panic("brelse");

  releasesleep(&b->lock);

  uint hashcode = b->blockno % NBUCKET;
  acquire(&bcache.hashlock[hashcode]);
  b->refcnt--;
  if (b->refcnt == 0) {
    // no one is waiting for it.
    b->next->prev = b->prev;
    b->prev->next = b->next;
    b->next = bcache.head[hashcode].next;
    b->prev = &bcache.head[hashcode];
    bcache.head[hashcode].next->prev = b;
    bcache.head[hashcode].next = b;
  }
  
  release(&bcache.hashlock[hashcode]);
}
```



### 用户级线程



### 操作系统接口

应用程序使用操作系统接口

操作系统需求：

​	高效又易用 -- 抽象接口

​	功能强大     -- 简单接口

​	灵活性	 -- 安全性

​	大量特性和大量服务之间的相互交互（需要大量的思考）

Shell 命令的执行：子进程的创建和 cmd 命令解析；

文件描述符：终端的标准输入、标准输出、标准错误输出、I/O 重定向；

管道：I/O 重定向的实现；

文件系统：文件、目录和文件的描述

#### xv6 启动流程

```
kernel.ld 链接文件
	_entry 起始地址 0x80000000
	
entry.S 开始启动
	跳转到 main

bootloader: xv6 内核加载
/* bootloader 把 xv6 内核加载到物理地址 0x100000， */
	entry：xv6 内核入口 entry
		/* 1. 启动 Page Size Extension (PSE)，允许 4MB 大小的页 */
		cr4 |= (CR4_PSE);
		/* 2. 设置页目录 Page Directory，将页目录的物理地址写入 cr3 */
		cr3 = V2P_WO(entrypgdir);
			/* memlayout.h */
			#define V2P_WO(x) ((x) - KERNBASE)
			/* main.c
             *	PDXSHIFT = 22, PDX 的 offset，则该页大小为 4 MB
             */
			pde_t entrypgdir[NPDENTRIES] = {
              // Map VA's [0, 4MB) to PA's [0, 4MB)
              [0] = (0) | PTE_P | PTE_W | PTE_PS,
              // Map VA's [KERNBASE, KERNBASE+4MB) to PA's [0, 4MB)
              [KERNBASE>>PDXSHIFT] = (0) | PTE_P | PTE_W | PTE_PS,
            };
			
		/* 3. 启动分页和写保护 */
		cr0 |= (CR0_PG|CR0_WP);
		/* 4. 设置栈指针 Stack Pointer，内核栈的栈顶地址 = 内核栈的起始地址 stack + 内核栈的大小 KSTACKSIZE */
		esp = (stack + KSTACKSIZE);
		/* 5. 跳转到 main() 函数 */
		jmp main;
		
	main.c 页表定义
	/* 将虚拟地址 0:0x400000 映射到物理地址 0:0x400000，因为 entry 运行在内存低地址处 */
	/* 将虚拟地址 KERNBASE:KERNBASE+0x400000 映射到物理地址 0:0x400000，此时内核指令和内核数据出现在高虚拟地址处和实际加载的物理低地址 */
	/* 1. 内核初始化设备和子系统 */
		a. 初始化物理内存的分配器，维护空闲页链表，用于管理物理内存页的分配和释放
		kinit1(end, P2V(4*1024*1024));
		b. 设置内核的虚拟内存管理，创建内核的页表
		kvmalloc();
		c. 初始化多处理器支持
		d. 初始化本地 APIC（Advanced Programmable Interrupt Controller），用于处理中断
		lapicinit();
		e. 设置段描述符，用于内存分段
		f. 禁用 PIC（Programmable Interrupt Controller），因为 xv6 使用 APIC 来处理中断
		picinit();
		g. 初始化 I/O APIC，用于处理来自 I/O 设备的中断
		ioapicinit();
		h. 初始化控制台 (console)，允许内核与用户进行文本交互
		consoleinit();
		i. 初始化 UART 串口，用于串行通信
		uartinit();
		j. 初始化进程表，用于管理进程
		l. 设置陷阱向量表，用于处理中断、异常和系统调用
		tvinit();
		m. 初始化缓冲区缓存，用于缓存磁盘块，提高 I/O 性能
		n. 初始化文件表，用于管理打开的文件
		o. 初始化 IDE 磁盘驱动器，允许内核访问磁盘存储
		p. 启动其他的 CPU
		q. 再次初始化物理内存分配器：将更多空闲内存分配到空闲页链表
		kinit2(P2V(4*1024*1024), P2V(PHYSTOP));
		r. 创建第一个用户进程 init
		userinit()
		s. 完成当前 CPU 的设置
		mpmain();
			idtinit();
			scheduler();
```

#### 创建第一个进程

<img src="D:\Users\22499\Desktop\嵌入式学习\typora笔记\笔记图片\02 xv6 学习\03_进程创建时的内核栈.png" alt="03_进程创建时的内核栈" style="zoom:50%;" />

```
userinit()
	initcode 通过 exec 调用 init.c 程序
		init.c 运行 Shell
	allocproc()
		/* allocproc() 在 (互斥访问) proc table 进程表中找到一个标记为 UNUSED 的进程 proc，并将其状态设置为被使用的 EMBRYO，并且给该进程一个独有的 pid */
		acquire(&ptable.lock);
		for(p = ptable.proc; p < &ptable.proc[NPROC]; p++) if(p->state == UNUSED)
		release(&ptable.lock);
		/* 为该进程分配准备好的【内核栈(分配和初始化)】和一系列内存寄存器（p->kstack, p->tf, p->context）。如果分配失败，allocproc() 将该槽位的状态设置为 UNUSED，并返回 0 表示失败 */
		p->kstack = kalloc();
		/* a. 栈底地址：往低地址生长 */
  		sp = p->kstack + KSTACKSIZE;
  		/* b. 中断帧 (trap frame) 地址 (保存进程在发生系统调用或中断、异常时的 CPU 寄存器状态) */
  		sp -= sizeof *p->tf;
  		p->tf = (struct trapframe*)sp;
  		/* c. 放置中断返回地址 trapret, 进程开始执行于 forkret/initcode，返回到 trapret  */
      	sp -= 4;
      	*(uint*)sp = (uint)trapret;
      	/* d. context 地址 (用于保存进程的执行上下文) */
        sp -= sizeof *p->context;			// 在内核栈中分配 p->context 变量的地址
        p->context = (struct context*)sp;
        memset(p->context, 0, sizeof *p->context);
        p->context->eip = (uint)forkret;
    /* 2. 创建只映射内核区的页表 */
    p->pgdir = setupkvm();
    	/* 分配 page direction 的页面 */
    	pgdir = (pde_t*)kalloc();
    	/* 将 I/O 设备、内核代码、内核数据、空闲内存和其余设备【内核区】映射到页表中 */
    	mappages(pgdir, k->virt, k->phys_end - k->phys_start,
                (uint)k->phys_start, k->perm)
    /* 3. 将 initcode 放入到 pgdir 的地址 0 处 */
    inituvm(p->pgdir, _binary_initcode_start, (int)_binary_initcode_size);
    /* 设置进程 proc 的 trap frame，把 trap frame 设置为初始的用户模式状态，段选择器指向 SEG_UCODE 并处于特权级 DPL_USER（用户模式） */
    p->tf->eip = 0;  // beginning of initcode.S
	initcode.S 通过 exec 调用 init.c 程序
		init.c 运行 Shell
		initcode.S 汇编得到二进制代码嵌入内核中，并定义二进制码的位置和大小：_binary_initcode_start 和 _binary_initcode_size （由链接器生成）
	
	栈指针 %esp 被设为了进程的最大有效虚拟内存 p->sz。指令指针则指向初始化代码的入口点，地址 0
	将 p->name 设置为 initcode
	一旦进程初始化完毕，userinit 将 p->state 设置为 RUNNABLE，使进程能够被调度。
	
	/* 4. 进程设置完成，将该进程设置为就绪态 RUNNABLE */
	p->state = RUNNABLE;
```

#### 运行第一个进程

```
mpmain：位于 main.c 中
	scheduler
		寻找 p->state 为 RUNNABLE 的进程 initproc，将 per-cpu 的变量 proc 等于该进程
		switchuvm 通知硬件开始使用目标进程的页表（setupkvm 使得所有进程的页表都有相同的指向内核代码和数据的映射），同时还设置好任务状态段 SEG_TSS，让硬件在进程的内核栈中执行系统调用与中断
		接着把进程的 p->state 设置为 RUNNING，调用 swtch，切换上下文到目标进程的内核线程中
			swtch 会保存当前的寄存器，并把目标内核线程中保存的寄存器（proc->context）载入到 x86 的硬件寄存器中，其中也包括栈指针和指令指针。当前的上下文并非是进程的，而是一个特殊的 per-cpu 调度器的上下文。所以 scheduler 会让 swtch 把当前的硬件寄存器保存在 per-cpu 的存储（cpu->scheduler）中，而非进程的内核线程上下文中
		最后的 ret（2727）指令从栈中弹出目标进程的 %eip，从而结束上下文切换工作
		现在处理器就运行在进程 p 的内核栈上了。
```

#### 执行第一个系统调用

```
exec 系统调用：位于 initcode.S 中，exec 用一个新的程序来代替当前进程的内存和寄存器，但是其文件描述符、进程 id 和父进程都是不变的
	将 $argv，$init，$0 三个值推入栈中，接下来把 %eax 设置为 SYS_exec 然后执行 int T_SYSCALL：这样做是告诉内核运行 exec 这个系统调用。
	如果运行正常的话，exec 不会返回：它会运行名为 $init 的程序，init（7810行）会在需要的情况下创建一个新的控制台设备文件，然后把它作为描述符0，1，2打开。接下来它将不断循环，开启控制台 shell，处理没有父进程的僵尸进程，直到 shell 退出，然后再反复。系统就这样建立起来了。
```



### 页表

实现操作系统接口

isolation (隔离性)：不同的应用程序之间有强隔离性 (不同进程的代码和数据段互不干扰)；应用程序和操作系统之间有强隔离性 (操作系统完成不同进程间的切换)

​	抽象操作系统接口：(1) fork -- 进程，CPU 资源抽象；(2) exec -- 内存抽象；(3) file -- 磁盘抽象

Defensive (防御性)：操作系统需要能够应对恶意的应用程序。

​	硬件隔离 user/kernel(Supervisor) mode：普通权限指令和特殊权限指令；

​		`ECALL`指令：ECALL + 数字参数 (指定的 System Call)，将用户程序的控制权转移给内核；

​	虚拟内存 virtual memory/page table：不同进程的页表具有不重合的物理内存

操作系统内核设计

​	宏内核 (Monolithic Kernel Design)：所有操作系统服务都在 kernel mode 中；

​	微内核：内核代码数量较少；在 user/kernel mode 反复跳转带来的性能损耗

xv6 代码组成

​	kernel: 内核文件 (二进制文件)

​	user: 运行在 user mode 的程序

​	mkfs: 创建一个空的文件镜像，我们会将这个镜像存在磁盘上 (空的文件系统)

虚拟内存工作机制

​	地址空间

​	硬件支持

​	代码实现

#### 页表概述

操作系统通过页表机制实现了对内存空间的控制；

​	把不同地址空间的多段内存映射到同一段物理内存（内核部分）

​	在同一地址空间中多次映射同一段物理内存（用户部分的每一页都会映射到内核部分）

​	通过一个没有映射的页保护用户栈

x86 页表是一个包含 2^20（1,048,576）条页表条目（PTE）的数组，每条 PTE 包含了一个 20 位的物理页号（PPN）及一些标志位。分页硬件要找到一个虚拟地址对应的 PTE：(1) 虚拟地址的高 20 位寻找其在页表中的索引（高 10 位用来决定页目录条目，接下来的 10 位来从页表页中选择出对应的 PTE），并将高 20 位替换为对应 PTE 的 PPN；(2) 虚拟地址的低 12 位被分页硬件原样复制的。因此，页表可以为操作系统提供对一块块大小为 4096（2^12）字节的内存片（一页）。PTE 中的 12 位标志位说明分页硬件对应的虚拟地址的使用权限。

页表的物理内存地址存储在 SATP 寄存器，每个进程拥有不同的页表，在进程切换时，SATP 寄存器也会相应切换

页表为大小 4 KB 的页面创建表单条目 PTE，这样可以缩小页表大小；虚拟内存地址数量`2^39 = 512 GB`，剩余未使用的`25 bit`用于未来扩展；`27 bit`用作 index，`12 bit`用作 offset，对应了页面的 4 K 字节；

物理内存地址是`54 bit`，`44 bit`用作物理页遍号 PPN，`12 bit`用作页面的 offset，未使用的`10 bit`用于未来扩展；

页表的保存：

​	一级页表索引：需要创建一个包含`2^27`个 PTE 的页表

​	三级页表索引：前`9 bit`用来索引最高级的 page directory 中的 PPN，该 PPN 指向了中间级 page directory（将该`64`位页面条目翻译成中间级 page directory 的物理地址，即取出`44 bit` PPN ，并添上`12 bit`的 0），再到最低级 page directory，得到`44 bit`物理页编号 PPN，低`10 bit`存放标志位用于控制地址权限，高`10 bit`用于未来扩展。当只使用 1 个物理页时，只需要创建 3 个 4 KB 页表即可；

​		page directory: 索引其他 page directory 或者 page table，大小为 4 KB，因为包含 512 个条目，每个条目大小为`64 bit = 8 Bytes`。

​		PTE 低`10 bit`标志位：Valid 合法位，Readable 可读位，Writable 可写位，Executable 可执行位，User 可以被用户空间访问；

​	三级页表索引在 MMU 硬件中实现，而 xv6 的 walk 函数在软件中实现了 MMU 硬件相同的功能。

​		`walk`函数：设置了最初的 page table；内核进程有自己的 page table，用户进程也需要自己的 page table 来存放指向`struct sys_info`的指针。在 `copy_in, copy_out`中，内核需要通过用户进程的 page table 将用户进程的虚拟地址翻译成物理地址，实现内存读写。

#### 页表缓存

页表缓存 (Translation Lookside Buffer, TLB) 保存虚拟地址到物理地址的映射关系，当处理器再次访问该虚拟地址时，处理器查看 TLB，TLB 会直接返回物理地址，避免了三级页表索引的繁琐操作；

#### 内核页表

<img src="D:\Users\22499\Desktop\嵌入式学习\typora笔记\笔记图片\02 xv6 学习\04_虚拟和物理地址空间.png" alt="04_虚拟和物理地址空间" style="zoom: 33%;" />

boot ROM 物理地址 0x1000，DRAM 内存地址 0x80000000，PLIC 中断控制器，CLINT (Core Local Interrupter) 中断的一部分，UART0 负责与 console 和显示器交互，VIRTIO disk 与磁盘进行交互。

具体实现：`kvminit()`函数设置好 kernel 的地址空间

```
视频里的代码，不同于现在的 xv6 操作系统
kvminit():
	/* 为最高一级page directory分配物理page（注，调用kalloc就是分配物理page） */
	kernel_pagetable = (pagetable_t) kalloc();
	memset(kernel_pagetable, 0, PGSIZE);
	/* 通过kvmmap函数，将每一个I/O设备映射到内核，虚拟地址和物理地址相同
     *	UART0, UART0 物理地址和虚拟地址；PGSIZE 内存大小；PTE_R | PTE_W 标志位
     */
	kvmmap(UART0, UART0, PGSIZE, PTE_R | PTE_W);
kvminithart():
	/* 设置 SATP 寄存器，内核告诉MMU来使用刚刚设置好的page table */
	w_satp(MAKE_SATP(kernel_pagetable));
	sfence_vma();
```



#### 进程地址空间

entry 建立了页表，但是 main 还是调用了`kvmalloc`以转换到新的页表，因为内核建立的页表更精巧。

每个进程都有自己的页表【**struct proc 中的指向 page table 的指针**】，xv6 会在进程切换时通知分页硬件【**切换页表，即设置 SATP 寄存器**】；进程的用户内存从虚拟地址 0 开始，最多能够增长到 `KERNBASE`，这使得一个进程最多只能使用 2 GB (0x0 : 0x8000000) 的内存。当进程向 xv6 要求更多的内存时，xv6 首先要找到空闲的物理页，然后把这些页对应的 PTE 加入该进程的页表中，并让 PTE 指向对应的物理页。大多数进程是用不完整个内存空间的；xv6 会把没有被使用的 PTE 的 `PTE_P` 标志位设为0。不同进程的页表将其用户内存映射到不同的物理内存中，因此每个进程就拥有了私有的用户内存。

xv6 在每个进程的页表中都包含了内核运行所需要的所有映射 (KERNBASE : KERNBASE+PHYSTOP)：(1) 保证内核使用自己的指令和数据；(2) 固定的物理地址映射使得对物理页进行写操作更方便；(3) 用户通过中断或者系统调用转入内核时就不需要进行页表的转换

xv6 保证了每个进程只能使用其自己的内存，并且每个进程所看到的内存都是从虚拟地址 0 开始的一段连续内存。

​	对于一个进程，xv6 只把该进程所使用的内存对应的 PTE 的 `PTE_U` 设为 1，其他 PTE 则不然，这样就可以实现前者。

​	对于后者，则是让页表把连续的虚拟页映射到实际分配的物理页。

​	PTE_U标志位为 0 时，只有 supervisor mode 可以使用这个页表。

<img src="D:\Users\22499\Desktop\嵌入式学习\typora笔记\笔记图片\02 xv6 学习\01_虚拟地址空间和物理地址空间.png" alt="01_虚拟地址空间和物理地址空间" style="zoom:50%;" />

#### 创建只映射了内核区的页表

```
entry.S
    # Set page directory
    movl    $(V2P_WO(entrypgdir)), %eax
    movl    %eax, %cr3

kvmalloc：在 main 中调用，创建并切换到一个拥有内核运行所需的 KERNBASE 以上映射的页表
	setupkvm:
		/* a. 分配一页内存来放置页目录 */
		pgdir = (pde_t*)kalloc();
			/* 获取 kmem.lock，保证互斥访问空闲页链表 */
			if(kmem.use_lock) acquire(&kmem.lock);
			/* 取出空闲页链表中的第一个空闲页，并移除第一个空闲页 */
			r = kmem.freelist
			if(r) kmem.freelist = r->next;
			/* 释放锁 */
			if(kmem.use_lock) release(&kmem.lock);
			/* 返回物理内存页的虚拟地址 */
			return (char*)r;
		/* b. 将 I/O 设备、内核代码段、内核数据段和其他设备映射到虚拟内存空间
         *	参数依次为：页表，虚拟地址，内存大小，物理地址，标志位
         *	逐页映射大块内存
         */
		mappages(pgdir, k->virt, k->phys_end - k->phys_start,
                (uint)k->phys_start, k->perm)
        kmap[] = {
         { (void*)KERNBASE, 0,             EXTMEM,    PTE_W}, // I/O space
         { (void*)KERNLINK, V2P(KERNLINK), V2P(data), 0},     // kern text+rodata
         { (void*)data,     V2P(data),     PHYSTOP,   PTE_W}, // kern data+memory
         { (void*)DEVSPACE, DEVSPACE,      0,         PTE_W}, // more devices
        };
			/* 二级页表索引，返回物理页的 PTE 的地址 */
			pte = walkpgdir(pgdir, a, 1);
				/* 利用虚拟地址 va 的高 10 位索引值，在最高级 page direction 中找到下一级 page direction 的 PTE (下一级页表的物理地址) */
				pde = &pgdir[PDX(va)];
				/* if(*pde & PTE_P) 如果下一级页表存在，则得到下一级 page table 的虚拟地址 */
				pgtab = (pte_t*)P2V(PTE_ADDR(*pde));
				否则
				/* 创建下一级页表（用新的物理页），并设置标记位 */
				pgtab = (pte_t*)kalloc();
				memset(pgtab, 0, PGSIZE);
				*pde = V2P(pgtab) | PTE_P | PTE_W | PTE_U;
				/* 返回物理页的 PPN 和标志位（PTE）的地址 */
				return &pgtab[PTX(va)];
			walkpgdir 模仿 x86 的分页硬件为一个虚拟地址寻找 PTE 的过程。walkpgdir 通过虚拟地址的前 10 位来找到在页目录中的对应条目，如果该条目不存在，说明要找的页表页尚未分配；如果 alloc 参数被设置了，walkpgdir 会分配页表页并将其物理地址放到页目录中。最后用虚拟地址的接下来 10 位来找到其在页表中的 PTE 地址
	switchkvm();
	切换到内核页表
```

#### 物理内存的分配

在运行时，内核需要为页表、进程的用户内存、内核栈及管道缓冲区分配空闲的物理内存

xv6 **使用从内核结尾 end 到 `PHYSTOP` 之间的物理内存为运行时分配提供内存资源**。每次分配，它会**将整块 4096 字节大小的页分配出去**。xv6 还会通过**维护一个物理页组成的链表来寻找空闲页**。所以，分配内存需要将页移出该链表，而释放内存需要将页加入该链表

为了让分配器能够初始化该空闲链表，所有的物理内存都必须要建立起映射，但是建立包含这些映射的页表又必须要分配存放页表的页。xv6 通过在 `entry` 中使用一个【**特别的页分配器 (分配存放第一个页表的页)**】来解决这个问题。该分配器会在内核数据部分的后面（即低虚拟内存地址）分配内存。该分配器不支持释放内存，并受限于 `entrypgdir` 中规定的 4MB 分配大小。即便如此，该分配器还是足够为内核的第一个页表分配出内存

#### 代码：物理内存分配器

分配器中的数据结构是一个【**由可分配物理内存页（空闲页）构成的空闲链表**】，链表的元素是结构体 `struct run`。分配器将每个空闲页的 `run` 结构体保存在该空闲页本身中，因为空闲页中没有其他数据。分配器还用一个 spin lock（2764-2766）来保护空闲链表。链表和这个锁都封装在一个结构体中，这样逻辑就比较明晰：锁保护了该结构体中的域。忽略这个锁，以及对 `acquire` 和 `release` 的调用

分配器有时将地址看作是整型，这是为了对其进行运算（譬如在 `kinit` 中遍历所有页）；而有时将地址看作读写内存用的指针（譬如操作每个页中的 `run` 结构体）；【**对地址的双重使用导致分配器代码中充满了类型转换**】。另外一个原因是，释放和分配内存隐性地改变了内存的类型。

```
main.c
	kinit1: 在前 4 MB 进行了不需要锁的空闲页分配，内存区间 [end(内核末尾), P2V(2*1024*1024 = 4 MB)]，维护由可分配空闲页 (struct run) 构成的单向链表
	kinit2: 允许了锁的使用，并使得更多的内存可用于分配，内存区间 [P2V(4 MB), P2V(224 MB)]
		/* kinit1 和 kinit2 调用 freerange 将内存加入空闲链表中 */
		freerange(vstart, vend);
			/* 虚拟地址对齐：将低 12 位清 0 */
			p = (char*)PGROUNDUP((uint)vstart);
			/* 逐页处理空闲页，将空闲页填充垃圾数据后放入空闲链表 struct run* r 中 */
			kfree(p);
```

#### 地址空间中的用户部分

【栈】

Stack Frame 的寄存器

​	SP (Stack Pointer): 指向Stack的底部并代表了当前Stack Frame的位置

​	FP (Frame Pointer): 它指向当前Stack Frame的顶部

在用户进程内存空间结构中，堆在栈之上，所以它可以增长（通过 `sbrk`）。栈占用了单独的一页内存，其中存有 `exec` 创建的初始数据。【**栈 (数据布局) 的最上方放着字符串形式的命令行参数以及指向这些参数的指针数组，其下方放的值 (返回地址)**】使得一个程序可以从 `main` 开始，仿佛刚刚调用了函数 `main(argc, argv)`。为了防止栈使用了它不应该使用的页，【**栈的下方有一个保护页**】。保护页没有映射，因此当栈的增长超出其所在页时就会产生异常，因为无法翻译这个错误的地址

<img src="D:\Users\22499\Desktop\嵌入式学习\typora笔记\笔记图片\02 xv6 学习\02_用户进程内存结构.png" alt="02_用户进程内存结构" style="zoom:50%;" />

#### 代码：exec 系统调用

```
userinit()
	struct proc* p = allocproc();
		/* a. 互斥访问锁 ptable.lock，在 proc table 中找到 UNUSED proc，将该进程的状态设置为 EMBRYO 并且分配 PID； */
		acquire(&ptable.lock);
		for(p = ptable.proc; p < &ptable.proc[NPROC]; p++) if(p->state == UNUSED)
		p->state = EMBRYO;
        p->pid = nextpid++;
        release(&ptable.lock);
        /* b. 分配内核栈页面，初始化内核栈。该进程开始于 forkret/initcode，结束于 trapret */
        p->kstack = kalloc();
        
initcode.S
	/* 系统调用 exec 将 init 程序加载到用户进程中（将 ELF 文件读取并写入用户进程的虚拟内存中） */
	exec(init, argv)
	char *argv[] = { init, 0 };
	/* init 创建一个新的控制台设备文件，然后把它作为描述符0，1，2打开。接下来它将不断循环，开启控制台 shell，将命令行输入 exec 为子进程来运行，处理没有父进程的僵尸进程，直到 shell 退出，然后再反复。系统就这样建立起来了。 */

exec(char *path, char **argv)
系统调用 exec 创建地址空间中用户部分，它根据文件系统中保存的某个可执行文件 ELF 文件来初始化用户部分
	1. 通过 namei 打开二进制文件，这一点将在第 6 章进行解释。
	ip = namei(path);
	获取文件的 inode 锁
	ilock(ip);
	2. 读取 ELF 二进制文件并且检查是否包含“魔法数字” magic
	readi(ip, (char*)&elf, 0, sizeof(elf));
	if(elf.magic != ELF_MAGIC)
	3. 建立只映射了内核区的页表
	pgdir = setupkvm();
	4. 分配足够的物理页并映射到页表中，将 ELF 文件中的程序段 struct proghdr 加载到内存中 
	for(i=0, off=elf.phoff; i<elf.phnum; i++, off+=sizeof(ph))
		readi(ip, (char*)&ph, off, sizeof(ph));
		/* 分配足够的物理页并映射到页表中 */
		sz = allocuvm(pgdir, sz, ph.vaddr + ph.memsz);
		/* 将程序段 struct proghdr 加载到内存中 */
		loaduvm(pgdir, (char*)ph.vaddr, ip, ph.off, ph.filesz) 
	5. 接着再分配两个物理页，并且将第一个物理页标记为不可访问 inaccessible (PTE_U位清0)，选择第二个物理页作为用户栈区
	sz = allocuvm(pgdir, sz, sz + 2*PGSIZE);
	clearpteu(pgdir, (char*)(sz - 2*PGSIZE));
	解锁
	iunlockput(ip);
	6. 依次压入输入参数，并将指向它们的指针保存在 ustack 中，并且在最后放入空指针
	for(argc = 0; argv[argc]; argc++)
		/* 为 argv[argc] 在栈上分配空间，向低地址方向生长 */
		sp = (sp - (strlen(argv[argc]) + 1)) & ~3;
		/* 将输入参数保存到物理内存中 */
		copyout(pgdir, sp, argv[argc], strlen(argv[argc]) + 1)
			/* 只读用户区页表的 walkpgdir() */
			uva2ka()
			/* 将指定长度的数据拷贝到物理地址 */
			memmove(pa0 + (va - va0), buf, n);
		ustack[3+argc] = sp;
	ustack[3+argc] = 0;				// 空指针
    ustack[0] = 0xffffffff;  		// fake return PC
    ustack[1] = argc;
    ustack[2] = sp - (argc+1)*4;  	// argv pointer 指向 argv[argc]
    /* 为 ustack 在栈中分配空间 */
    sp -= (3+argc+1) * 4;
    copyout(pgdir, sp, ustack, (3+argc+1)*4);
    /* 更新当前(子)进程的页表，并释放从(父)进程复制的页表 */
    curproc->pgdir = pgdir;
    freevm(oldpgdir);
    /* 设置当前进程的入口函数地址，设置栈指针，切换到当前页表 */
    curproc->tf->eip = elf.entry;
    curproc->tf->esp = sp;
    switchuvm(curproc);	切换到指定进程的用户页表
    	/* 禁止 CPU 中断，防止在切换页表时发生中断 */
    	pushcli();
    	/* 将当前使用的页表物理地址保存到 cr3 寄存器中 */
		lcr3(V2P(p->pgdir));
    	/* 恢复之前的中断状态 */
    	popcli();
```

`exec` 创建的第一个用户程序 `/init` 程序段的头是这样的：

```
#objdump -p _init

_init:    file format elf32-i386
/* struct proghdr 成员 */
Program Header:
    LOAD off    0x00000054 vaddr 0x00000000 paddr 0x00000000 align 2**2
         filesz 0x000008c0 memsz 0x000008cc flags
```

#### 汇编

RISC-V 处理器具有 ISA (Instruction Sets Architecture)，每一条指令都有一个对应的二进制编码或者一个Opcode

### 陷入、中断和驱动程序

#### Trap 机制

用户空间和内核空间的切换 【trap】：

​	可能的 trap 使用场景：

​		程序执行系统调用

​		exception: 程序出现了类似page fault、运算时除以0的错误

​		interrupt: 一个设备触发了中断使得当前程序运行需要响应内核设备驱动

​	关心硬件的状态：用户/内核模式

​		程序计数器 (Program Counter Register)

​		SATP (Supervisor Address Translation and Protection) 寄存器：指向page table的物理内存地址

​		STVEC (Supervisor Trap Vector Base Address Register) 寄存器：指向了内核中处理trap的指令的起始地址

​		SEPC (Supervisor Exception Program Counter) 寄存器：在trap的过程中保存程序计数器的值

​	主要工作：

​		需要保存32个用户寄存器，用于恢复用户应用程序的执行

​		需要保存程序计数器，用于在用户程序运行中断的位置继续执行用户程序

​		将mode改成supervisor mode，以便使用内核中的各种各样的特权指令

​		将指向 user page table 的 SATP 寄存器指向 kernel page table

​		需要设置堆栈寄存器，来调用 C 函数

​		最后，跳入 C 程序

​	安全和隔离：trap 中涉及到的硬件和内核机制不能依赖任何来自用户空间东西

1）内核必须使处理器能够从用户态转换到内核态（并且再转换回用户态）2）内核和设备必须协调好他们并行的活动。3）内核必须知道硬件接口的细节。

术语`exception`指产生中断的非法程序操作，例如除以0，尝试访问 PTE 不存在的内存等等。术语`interrupt`指硬件产生的希望引起操作系统注意的信号，例如时钟芯片可能每100毫秒产生一个中断，以此来实现分时。

陷入是由在 cpu 上运行的当前进程导致的，而中断是由设备导致的

所有的中断都由内核管理，而不是进程。因为在大多数情况下只有内核拥有处理中断所需的特权和状态。

​	系统必须保存寄存器以备将来的状态恢复。

​	系统必须准备好在内核中执行，必须选择一个内核开始执行的地方。

​	内核必须能够获得关于这个事件的信息，例如系统调用的参数。

​	同时还必须保证安全性；系统必须保持用户进程和系统进程的隔离。

#### 中断管理初始化

xv6 操作系统启动时，执行非易失存储器中的 BIOS 程序，将引导加载器加载到内存中并执行，然后从磁盘中把 xv6 内核加载到内存中，并将 CPU 控制权交给内核。

中断管理初始化

```
初始化中断控制器
lapicinit()
	初始化本地 APIC 中断控制器
	/* 启用本地 APIC */
	lapicw(SVR, ENABLE | (T_IRQ0 + IRQ_SPURIOUS));
	/* 配置了 APIC 的本地定时器。APIC 定时器可以用于产生周期性的中断，用于实现时间片轮转、调度等 */
	APIC Timer Configuration
	/* 初始化 APIC 错误中断处理 */
	lapicw(ERROR, T_IRQ0 + IRQ_ERROR);
	/* 初始化处理器间中断 (IPI, Inter-Processor Interrupt) */
	【时钟芯片是在 LAPIC 中的，所以每一个处理器可以独立地接收时钟中断 `lapicinit()`】。xv6 在 `lapicinit`（6651）中设置它。关键的一行代码是 `timer`（6664）中的代码，这行代码告诉 LAPIC 周期性地在 IRQ_TIMER（也就是 IRQ 0) 产生中断。第 6693 行打开 CPU 的 LAPIC 的中断，这使得 LAPIC 能够将中断传递给本地处理器。
picinit()
	禁用 PIC，使用 APIC 来处理中断
ioapicinit()
	禁用所有外部中断，并设置它们的属性（边沿触发、高电平有效）
	不同的设备自己开启自己的中断，并且同时指定哪一个处理器接受这个中断
ioapicenable()
	启用特定的中断，并将它们路由到特定的 CPU
	
设置 I/O、设备端口的硬件中断
consoleinit()
	初始化控制台的锁，用于同步控制台的访问
	设置控制台设备的读写函数，将其与文件系统的接口关联起来
	ioapicenable() 启用键盘中断，使得系统能够响应用户的键盘输入
uartinit()
	设置 UART 串口的波特率、数据帧格式等
	使能接收中断，清除可能存在的未处理中断状态
	ioapicenable() 启用 UART 串口中断，并将它路由到特定 CPU

初始化中断描述符表 IDT (struct gatedesc)，关联 vector.S 中的中断 IDT 表项，存储了中断处理程序的入口地址
中断 0-31 被定义为软件异常，比如除 0 错误和访问非法的内存页。xv6 将中断号 32-63 映射给硬件中断，并且用 64 作为系统调用的中断号
tvinit()
	/* 初始化中断描述符表 IDT，并关联 vector.S 中的中断 IDT 表项 */
	/* 设置特权级为内核模式，表示只有内核可以调用这些中断处理函数 */
	/* 设置为中断门，表明运行时屏蔽中断 */
	for(i = 0; i < 256; i++)
    	SETGATE(idt[i], 0, SEG_KCODE<<3, vectors[i], 0)
    /* 设置系统调用处理函数的入口地址 */
    /* 设置特权级为用户模式；设置为陷阱门 (trap gate) */
    SETGATE(idt[T_SYSCALL], 1, SEG_KCODE<<3, vectors[T_SYSCALL], DPL_USER);
    /* 初始化时钟锁 tickslock，用于保护 ticks 变量 */
    initlock(&tickslock, "time");
    
mpmain()
	/* 将 IDT 地址加载到当前 CPU 的 IDT 寄存器中 */
	idtinit();
	/* 启动进程调度器 */
	scheduler();
		/* 将 CPU 正在运行的进程初始化为 0 */
		mycpu()->proc = 0;
		/* 开启中断：调度器在选择进程的过程中是关闭中断的，以避免竞争条件。但在选择完成后，必须开启中断，以允许硬件中断被处理 */
		sti();
		/* 互斥 (锁) 访问进程表，查找就绪态进程 */
		acquire(&ptable.lock);
		for(p = ptable.proc; p < &ptable.proc[NPROC]; p++) ...
		release(&ptable.lock);
		/* ... 切换到选定进程 */
		/* a. 将 CPU 正在运行的进程设置为选定进程 */
		c->proc = p;
		/* b. 切换到选定进程的页表 */
		switchuvm(p);
		/* c. 将该进程状态设置为运行态 */
		p->state = RUNNING;
		/* d. swtch() 保存当前调度器的上下文，并恢复选定进程的上下文。当进程让出 CPU 时，会再次切换回调度器 */
		swtch(&(c->scheduler), p->context);
		/* e. 切换到内核页表 */
		switchkvm();
		/* f. 表示当前 CPU 没有运行任何进程 */
		c->proc = 0;
```



内核态转为用户态

​	userinit()

​		proc -> trap frame (tf) -> cs, ds, es, ss 处于 DPL_USER

​		schedule() 初始化该进程的页表、切换上下文信息

​		trapret (eip): init.c，将 proc -> tf 中的寄存器转移到 CPU，设置 %cs 低位 CPL = 3

#### trap() 系统调用、中断

trap() 系统调用

首先，中断管理会初始化中断描述符表 IDT，关联所有中断和系统调用的处理程序入口地址，其中，

​	系统调用允许处理时接收其他中断，而硬件中断则不允许；

​	系统调用的特权级为用户模式，硬件中断的特权级为内核模式，如果用户通过`int`指令执行硬件中断，CPU 会检测到特权级错误，产生通用保护异常。

其次，当用户程序执行系统调用 `int T_SYSCALL`，CPU 触发中断，CPU 自动将使用的栈从用户态切换到内核态 (通过任务段描述符 TSS) 并且切换进程的页表，跳转到`alltrap` (xv6 会将所有 traps 都跳转到 alltrap)，`alltrap`将中断发生时处理器的寄存器状态保存到中断帧，`trap()`根据中断号`tf->trapno`来执行具体的处理程序。`trap()`返回后，执行`trapret`处代码，弹出中断帧会恢复用户模式下的寄存器，然后执行` iret `会跳回到用户空间。其中涉及到特权级切换、栈切换、中断处理逻辑

​	特权级切换是指用户态和内核态之间的切换，他们使用不同的栈；

​	首先，栈切换：`switchuvm()`函数将用户进程的内核栈顶写入任务段描述符 (TSS) 中，并切换进程的页表 (切换时禁用中断)

​	用户态到内核态：从 TSS 中加载内核栈的 `SS` 和 `ESP`，完成栈切换；CPU 自动将用户栈的 `SS` 和 `ESP` (用户态任务状态) 压入内核栈，以便稍后返回

​	内核态到内核态：不会进行栈切换

​	其次，CPU 会把 %eflags (标志寄存器)，%cs (代码段寄存器)，%eip (指令指针寄存器) 压栈；

​	然后，调用 `alltraps` 将中断发生时处理器的寄存器状态保存到中断帧 (trap frame)，并压入栈中；

​	最后，`alltraps`跳转到`trap()`根据中断向量号，从 IDT 中加载新的代码段和指令入口地址 `CS` 和 `EIP`，指向相应的中断处理程序；`trap()`返回后，执行`trapret`处代码，弹出中断帧会恢复用户模式下的寄存器，然后执行 iret 会跳回到用户空间

然后，`trap()`函数处理系统调用、中断和异常

​	

#### x86 的保护机制

x86 有 2 个常用特权级，为内核模式和用户模式。当前执行指令的特权级存在于 %cs 寄存器中的 CPL 域中。

在 x86 中，中断处理程序的入口在中断描述符表（IDT）中被定义。这个表有256个表项，每一个都提供了相应的 %cs 和 %eip。

一个程序要在 x86 上进行一个系统调用，它需要调用 int n 指令，这里 n 就是 IDT 的索引。

```
系统调用 int n 指令
    从 IDT 中获得第 n 个描述符，n 就是 int 的参数。
    检查 %cs 的域 CPL <= DPL，DPL 是描述符中记录的特权级。
    如果目标段选择符的 PL < CPL，就在 CPU 内部的寄存器中保存 %esp 和 %ss 的值。
    从一个任务段描述符中加载 %ss 和 %esp。
    将 %ss 压栈。
    将 %esp 压栈。
    将 %eflags 压栈。
    将 %cs 压栈。
    将 %eip 压栈。
    清除 %eflags 的一些位。
    设置 %cs 和 %eip 为描述符中的值。
    	在任何一种情况下，%eip 都指向中断描述符表中指定的地址，这个地址的第一条指令就是将要执行的下一条指令，也是 int n 的中断处理程序的第一条指令
    	
操作系统可以使用 iret 指令来从一个 int 指令中返回。它从栈中弹出 int 指令保存的值，然后通过恢复保存的 %eip 的值来继续用户程序的执行。
```

#### 代码：第一个系统调用

```
initcode.S
	exec(init, argv);
```

#### 代码：汇编陷入处理程序

x86 允许 256 个不同的中断。。

#### 代码：C 中断处理程序

每一个处理程序会建立一个中断帧然后调用 C 函数 `trap`。`trap`（3101）查看硬件中断号 `tf->trapno` 来判断自己为什么被调用以及应该做些什么。如果中断是 `T_SYSCALL`，`trap` 调用系统调用处理程序 `syscall`。我们会在第五章再来讨论这里的两个 `cp->killed` 检查。

当检查完是否是【系统调用】，`trap` 会继续检查是否是【硬件中断】（我们会在下面讨论）。中断可能来自硬件设备的正常中断，也可能来自异常的、未预料到的硬件中断。

如果中断不是一个系统调用也不是一个硬件主动引发的中断，`trap` 就认为它是一个发生中断前的一段代码中的错误行为导致的中断（如除零错误）。

如果是内核程序正在执行，那就出现了一个内核错误：`trap` 打印错误细节并且调用 `panic`。

#### 代码：系统调用

```
对于系统调用，trap 调用 syscall（3375）。
	syscall 从中断帧中读出系统调用号，中断帧也包括被保存的 %eax，以及到系统调用函数表的索引。对第一个系统调用而言，%eax 保存的是 SYS_exec（3207），并且 syscall 会调用第 SYS_exec 个系统调用函数表的表项，相应地也就调用了 sys_exec。
	syscall 在 %eax 保存系统调用函数的返回值。当 trap 返回用户空间时，它会从 cp->tf 中加载其值到寄存器中。因此，当 exec 返回时，它会返回系统调用处理函数返回的返回值（3381）。系统调用按照惯例会在发生错误的时候返回一个小于 0 的数，成功执行时返回正数。如果系统调用号是非法的，syscall 会打印错误并且返回 -1。
	如何获得系统调用的参数。工具函数 argint、argptr 和 argstr 获得第 n 个系统调用参数，他们分别用于获取整数，指针和字符串起始地址。argint 利用用户空间的 %esp 寄存器定位第 n 个参数：%esp 指向系统调用结束后的返回地址。参数就恰好在 %esp 之上（%esp+4）。因此第 n 个参数就在 %esp+4+4*n。
		argint 调用 fetchint 从用户内存地址读取值到 *ip。fetchint 可以简单地将这个地址直接转换成一个指针，因为用户和内核共享同一个页表，但是内核必须检验这个指针的确指向的是用户内存空间的一部分。内核已经设置好了页表来保证本进程无法访问它的私有地址以外的内存：如果一个用户尝试读或者写高于（包含）p->sz的地址，处理器会产生一个段中断，这个中断会杀死此进程，正如我们之前所见。但是现在，我们在内核态中执行，用户提供的任何地址都是有权访问的，因此必须要检查这个地址是在 p->sz 之下的。
		argptr 和 argint 的目标是相似的：它解析第 n 个系统调用参数。argptr 调用 argint 来把第 n 个参数当做是整数来获取，然后把这个整数看做指针，检查它的确指向的是用户地址空间。注意 argptr 的源码中有两次检查。首先，用户的栈指针在获取参数的时候被检查。然后这个获取到得参数作为用户指针又经过了一次检查。
		argstr 是最后一个用于获取系统调用参数的函数。它将第 n 个系统调用参数解析为指针。它确保这个指针是一个 NUL 结尾的字符串并且整个完整的字符串都在用户地址空间中。
		系统调用的实现（例如，sysproc.c 和 sysfile.c）仅仅是封装而已：他们用 argint，argptr 和 argstr 来解析参数，然后调用真正的实现。
```

#### 代码：中断

主板上的设备可以产生中断，xv6 必须配置硬件来处理这些中断。中断可以使用与系统调用和异常处理相同的代码。

【分时硬件和时钟中断】。我们希望分时硬件大约以每秒 100 次的速度产生一个中断，这样内核就可以对进程进行时钟分片。100 次每秒的速度足以提供良好的交互性能并且同时不会使处理器进入不断的中断处理中。

主板有一个简单的可编程中断控制器（被称作 PIC），你可以在 picirq.c 中找到管理它的代码。

随着多核处理器主板的出现，需要一种新的处理中断的方式，因为每一颗 CPU 都需要一个中断控制器来处理发送给它的中断，而且也得有一个方法来分发中断。这一方式包括两个部分：【第一个部分是在 I/O 系统中的（IO APIC，ioapic.c），另一部分是关联在每一个处理器上的（局部 APIC，lapic.c）。xv6 是为搭载多核处理器的主板设计的，每一个处理器都需要编程接受中断】。

为了在单核处理器上也能够正常运行，xv6 也为 PIC 编程（6932）。每一个 PIC 可以处理最多 8 个中断（设备）并且将他们接到处理器的中断引脚上。为了支持多于八个硬件，PIC 可以进行级联，典型的主板至少有两集级联。使用 `inb` 和 `outb` 指令，xv6 配置主 PIC 产生 IRQ 0 到 7，从 PIC 产生 IRQ 8 到 16。【最初 xv6 配置 PIC 屏蔽所有中断】。timer.c 中的代码设置时钟 1 并且使能 PIC 上相应的中断（7574）。这样的说法忽略了编写 PIC 的一些细节。这些 PIC（也包括 IOAPIC 和 LAPIC）的细节对本书来说并不重要，但是感兴趣的读者可以参考 xv6 源码引用的各设备的手册。

在多核处理器上，xv6 必须编写 IOAPIC 和每一个处理器的 LAPIC。IO APIC 维护了一张表，处理器可以通过内存映射 I/O 写这个表的表项，而非使用 `inb` 和 `outb` 指令。【在初始化的过程中 `ioapicinit()`，xv6 将第 0 号中断映射到 IRQ 0，以此类推，然后把它们都屏蔽掉。不同的设备自己开启自己的中断，并且同时指定哪一个处理器接受这个中断】。

【时钟芯片是在 LAPIC 中的，所以每一个处理器可以独立地接收时钟中断 `lapicinit()`】。xv6 在 `lapicinit`（6651）中设置它。关键的一行代码是 `timer`（6664）中的代码，这行代码告诉 LAPIC 周期性地在 IRQ_TIMER（也就是 IRQ 0) 产生中断。第 6693 行打开 CPU 的 LAPIC 的中断，这使得 LAPIC 能够将中断传递给本地处理器。

【处理器可以通过设置 `eflags` 寄存器中的 `IF` 位来控制自己是否想要收到中断】。指令 `cli` 通过清除 `IF` 位来屏蔽中断，而 `sti` 又打开一个中断。【xv6 在启动主 cpu（8412）和其他 cpu（1126）时屏蔽中断。每个处理器的调度器打开中断（2464）。为了控制一些特殊的代码片段不被中断，xv6 在进入这些代码片段之前关中断（例如 `switchuvm`（1773））】。

xv6 在 `idtinit`（1265）中设置时钟中断触发中断向量 32（xv6 使用它来处理 IRQ 0）。中断向量 32 和中断向量 64（用于实现系统调用）的唯一区别就是 32 是一个中断门，而 64 是一个陷阱门。中断门会清除 IF，所以被中断的处理器在处理当前中断的时候不会接受其他中断。从这儿开始直到 `trap` 为止，中断执行和系统调用或异常处理相同的代码——建立中断帧。

#### 驱动程序

【驱动程序是操作系统中用于管理某个设备的代码：它提供设备相关的中断处理程序，操纵设备完成操作，操纵设备产生中断，等等】。驱动程序可能会非常难写，因为它和它管理的设备同时在并发地运行着。另外，驱动程序必须要理解设备的接口（例如，哪一个 I/O 端口是做什么的），而设备的接口又有可能非常复杂并且文档稀缺。

xv6 的硬盘驱动程序给我们提供了一个良好的例子。【磁盘驱动程序从磁盘上拷出和拷入数据。磁盘硬件一般将磁盘上的数据表示为一系列的 512 字节的块（亦称扇区）】：扇区 0 是最初的 512 字节，扇区 1 是下一个，以此类推。为了表示磁盘扇区，操作系统也有一个数据结构与之对应。这个结构中存储的数据往往和磁盘上的不同步：可能还没有从磁盘中读出（磁盘正在读数据但是还没有完全读出），或者它可能已经被更新但还没有写出到磁盘。【磁盘驱动程序必须保证 xv6 的其他部分不会因为 (磁盘数据与操作系统的数据结构) 不同步的问题而产生错误】。

#### 代码：磁盘驱动程序

通过 IDE 设备可以访问连接到 PC 标准 IDE 控制器上的磁盘。IDE 现在不如 SCSI 和 SATA 流行，但是它的接口比较简单使得我们可以专注于驱动程序的整体结构而不是硬件的某个特别部分的细节。

```
磁盘硬件驱动程序
磁盘扇区结构体
struct buf *idequeue;
	idequeue 指向现在正在读/写磁盘的 buf
	idequeue->qnext 指向下一个要处理的 buf
	操作队列时必须持有idelock
	每一个缓冲区 `buf` 表示磁盘设备上的一个扇区。域 `dev` 和 `sector` 给出了设备号和扇区号，域 `data` 是该磁盘扇区数据的内存中的拷贝。域 `flags` 记录了内存和磁盘的联系：B_VALID 位代表数据已经被读入，B_DIRTY 位代表数据需要被写出。B_BUSY 位是一个锁；它代表某个进程正在使用这个缓冲区，其他进程必须等待。当一个缓冲区的 B_BUSY 位被设置，我们称这个缓冲区被锁住。

ideinit 初始化磁盘驱动程序
	初始化 IDE 控制器：初始化 IDE 锁，开启 IOAPIC 中的 IDE 设备中断
	initlock(&idelock, "ide");
	ioapicenable(IRQ_IDE, ncpu - 1);
	等待 IDE 控制器准备好接受命令，尝试获取状态位，直到 busy 位 (IDE_BSY) 被清除，以及 ready 位 (IDE_RDY) 被设置
	idewait(0);
	检查第二个 IDE 磁盘是否存在：已知第一个 IDE 磁盘存在，因为启动加载器和内核都是从磁盘 0 加载的
	Check for Disk 1 Presence

iderw 将缓冲区 buf 与 IDE 磁盘同步，即执行读或写磁盘的操作
	检查调用 iderw 的进程是否已经持有了缓冲区 b 的睡眠锁，因为 IDE 操作是异步的，需要使用睡眠锁来确保缓冲区在 I/O 操作完成前不会被其他进程修改
	if(!holdingsleep(&b->lock))
	申请自旋锁 idelock，用于保护对 IDE 队列的操作
	acquire(&idelock);
	轮询/阻塞睡眠 : iderw 维护一个等待中的磁盘请求队列，然后用中断来指明哪一个请求已经完成。将队首的缓冲区送至磁盘硬件；其他的只是在等待他们被处理。
	iderw 将缓冲区 buf 送到队列的末尾。如果这个缓冲区在队首，iderw 通过 idestart 将它送到磁盘上；在其他情况下，一个缓冲区被开始处理当且仅当它前面的缓冲区被处理完毕。
	将缓冲区 b 添加到 idequeue 队列的末尾
	b->qnext = 0;
	for(pp=&idequeue; *pp; pp=&(*pp)->qnext) ;	// 找到 idequeue 队列末尾
	*pp = b;
	如果 b 是队列中的第一个元素（即队列之前为空），则调用 idestart(b) 启动磁盘 I/O 操作。
	否则，只是将 buf 请求放入队列后就返回，让调用者继续执行其他任务。
	if(idequeue == b) idestart(b);
		检查传入的缓冲区指针 b 是否为空；检查要访问的块号 b->blockno 是否超出了文件系统的大小 FSSIZE
		if(b == 0)
		if(b->blockno >= FSSIZE) 
		计算磁盘扇区号，并选择正确的 IDE 命令 (读/写)
		int sector_per_block = BSIZE/SECTOR_SIZE; int sector = b->blockno * sector_per_block;
		int read_cmd = (sector_per_block == 1) ? IDE_CMD_READ : IDE_CMD_RDMUL; 
		int write_cmd = (sector_per_block == 1) ? IDE_CMD_WRITE : IDE_CMD_WRMUL;
		等待 IDE 控制器准备好接受命令
		idewait(0);
		通知 IDE 控制器产生中断
		outb(0x3f6, 0); // generate interrupt
		向 IDE 控制器的端口写入参数，设置 I/O 操作的扇区数、起始扇区号等
		根据缓冲区的 B_DIRTY 标志位，选择执行读操作还是写操作
			B_DIRTY 被设置，iderw 将缓冲区内容写入磁盘 (写操作)，iderw 必须提供数据而在写出到磁盘完成后会发出一个中断
			B_VALID 被设置，iderw 从磁盘中读取数据到缓冲区 (读操作)，iderw 发出一个代表数据就绪的中断，然后中断处理程序会读出数据
		if(b->flags & B_DIRTY){ // 写操作
            outb(0x1f7, write_cmd); 
            outsl(0x1f0, b->data, BSIZE/4); // 将缓冲区中的数据写入 IDE 控制器的数据端口 0x1f0
        } 
		else { 
            outb(0x1f7, read_cmd); }
	等待 I/O 操作完成 (等待 IDE 控制器完成 I/O 操作)
	while((b->flags & (B_VALID|B_DIRTY)) != B_VALID) sleep(b, &idelock); 
		sleep(b, &idelock) 使当前进程进入睡眠状态，直到缓冲区 b 的 B_VALID 标志被设置。sleep 函数会释放 idelock，并在进程被唤醒时重新获取它
	释放自旋锁 idelock
	release(&idelock);

当磁盘硬件完成 I/O 操作后，会产生一个中断。xv6 的中断处理程序 (ideintr) 会处理这个中断，并通知相应的请求已经完成
ideintr
	申请 idelock 自旋锁，用于保护对 IDE 队列的操作、
	acquire(&idelock);
	获取当前处理的缓冲区
	if((b = idequeue) == 0){	// 如果 IDE 队列为空，则释放自旋锁后返回
        release(&idelock);
        return;
    }
    idequeue = b->qnext;		// 将 idequeue 指向下一个待处理的缓冲区，从而将当前缓冲区从队列中移除
    读取数据：如果该缓冲区正在被读入并且磁盘控制器有数据在等待
    if(!(b->flags & B_DIRTY) && idewait(1) >= 0)
        insl(0x1f0, b->data, BSIZE/4);	// 将数据读入缓冲区
    设置 B_VALID，清除 B_DIRTY，表示数据已经读取或者写入完成；唤醒任何等待这个缓冲区完成 I/O 操作的进程
    b->flags |= B_VALID;
    b->flags &= ~B_DIRTY;
    wakeup(b);
    启动下一步请求：如果队列中还有其他待处理的请求（idequeue != 0），则调用 idestart(idequeue) 启动下一个磁盘操作
    if(idequeue != 0)
        idestart(idequeue);
    释放自旋锁
    release(&idelock);
	
```



【磁盘驱动程序用结构体 buf（称为缓冲区）（3500）来表示一个磁盘扇区】。

`ideinit` 之后，就只能通过块高速缓冲（buffer cache）调用 `iderw`，`iderw` 根据标志位更新一个锁住的缓冲区。如果 B_DIRTY 被设置，`iderw`将缓冲区的内容写到磁盘；如果 B_VALID 没有被设置，`iderw`从磁盘中读出数据到缓冲区。

磁盘访问耗时在毫秒级，对于处理器来说是很漫长的。引导加载器发出磁盘读命令并反复读磁盘状态位直到数据就绪。这种轮询或者忙等待的方法对于引导加载器来说是可以接受的，因为没有更好的事儿可做。但是在操作系统中，更有效的方法是让其他进程占有 CPU 并且在磁盘操作完成时接受一个中断。`iderw` 采用的就是后一种方法，维护一个等待中的磁盘请求队列，然后用中断来指明哪一个请求已经完成。虽然 `iderw` 维护了一个请求的队列，简单的 IDE 磁盘控制器每次只能处理一个操作。磁盘驱动程序的原则是：它已将队首的缓冲区送至磁盘硬件；其他的只是在等待他们被处理。

`iderw`（3954）将缓冲区 `buf` 送到队列的末尾（3967-3971）。如果这个缓冲区在队首，`iderw` 通过 `idestart` 将它送到磁盘上（3924-3926）；在其他情况下，一个缓冲区被开始处理当且仅当它前面的缓冲区被处理完毕。

`idestart` 发出关于缓冲区所在设备和扇区的读或者写操作，根据标志位的情况不同。如果操作是一个写操作，`idestart` 必须提供数据（3889）而在写出到磁盘完成后会发出一个中断。如果操作是一个读操作，则发出一个代表数据就绪的中断，然后中断处理程序会读出数据。注意 `iderw` 有一些关于 IDE 设备的细节，并且在几个特殊的端口进行读写。如果任何一个 `outb` 语句错误了，IDE 就会做一些我们意料之外的事。保证这些细节正确也是写设备驱动程序的一大挑战。

`iderw` 已经将请求添加到了队列中，并且会在必要的时候开始处理，`iderw` 还必须等待结果。就像我们之前讨论的，轮询并不是有效的利用 CPU 的办法。相反，`iderw` 睡眠，等待中断处理程序在操作完成时更新缓冲区的标志位（3978-3979）。当这个进程睡眠时，xv6 会调度其他进程来保持 CPU 处于工作状态。

最终，磁盘会完成自己的操作并且触发一个中断。`trap` 会调用 `ideintr` 来处理它（3124）。`ideintr`（3902）查询队列中的第一个缓冲区，看正在发生什么操作。如果该缓冲区正在被读入并且磁盘控制器有数据在等待，`ideintr` 就会调用 `insl` 将数据读入缓冲区（3915-3917）。现在缓冲区已经就绪了：`ideintr` 设置 B_VALID，清除 B_DIRTY，唤醒任何一个睡眠在这个缓冲区上的进程（3919-3922）。最终，`ideintr` 将下一个等待中的缓冲区传递给磁盘（3924-3926）。



### 锁

#### 概念

xv6 运行在多处理器上，即计算机上有多个单独执行代码的 CPU。这些 CPU 操作同一片地址空间并分享其中的数据结构；xv6 必须【建立一种合作机制防止它们互相干扰】。即使是在单个处理器上，xv6 也必须使用某些机制来防止中断处理程序与非中断代码之间互相干扰。xv6 为这两种情况使用了相同的低层概念：*锁*。【锁提供了互斥功能，保证某个时间点只有一个 CPU 能持有锁】。

本章的其余部分将解释为何 xv6 需要锁，以及 xv6 是如何实现、使用锁的。我们需要重点注意的是在读代码时，你一定要问自己另一个处理器的存在是否会让这行代码无法达到期望的运行结果（因为另一个处理器也可能正在运行该行代码，或者另一行修改这个共享变量的代码），还要考虑如果这里执行一个中断处理程序，又会发生什么情况。与此同时，一定要记住一行 C 代码可能由多条机器指令组成，而另一个处理器或者中断可能在这些指令中间影响之。你不能假设这些代码是顺序执行的，也不能假设一个 C 指令是以原子操作执行的。【并发使得考虑代码的正确性变得困难】。

#### 为什么需要锁：竞争条件

竞争问题在于它们的结果由 CPU 执行时间以及其内存操作的先后决定的，并且这个问题难以重现。恰当地使用锁就能保证一时间只有一个 CPU 操作数据结构，这样在不变量不正确时就不可能有其他 CPU 对数据结构进行操作了。

#### 代码：锁

xv6 用结构体 `struct spinlock`（1401）。结构体中的临界区用 `locked` 表示。这是一个字，在锁可以被获得时值为0，而当锁已经被获得时值为非零。

在获取锁时为了避免不同 CPU 的竞争条件，需要采用原子操作

 xv6 采用了386硬件上的一条特殊指令 `xchg`（0569）。在这个原子操作中，`xchg` 交换了内存中的一个字和一个寄存器的值。函数 `acquire`（1474）在循环中反复使用 `xchg`；每一次都读取 `lk->locked` 然后设置为1（1483）。如果锁已经被持有了，`lk->locked` 就已经为1了，故 `xchg` 会返回1然后继续循环。如果 `xchg` 返回0，但是 `acquire` 已经成功获得了锁，即 `locked` 已经从0变为了1，这时循环可以停止了。

函数 `release`（1502）则做了相反的事：清除调试信息并释放锁。

#### 代码：使用锁

 IDE 驱动中`iderw`（3954）有一个磁盘请求的队列，处理器可能会并发地向队列中加入新请求（3969）。为了保护链表以及驱动中的其他不变量，`iderw` 会请求获得锁 `idelock`（3965）并在函数末尾释放锁。

锁的原则：

使用锁的一个难点在于要决定使用多少个锁，以及每个锁保护哪些数据、不变量。不过有几个基本原则。首先，当一个 CPU 正在写一个变量，而同时另一个 CPU 可能读/写该变量时，需要用锁防止两个操作重叠。第二，当用锁保护不变量时，如果不变量涉及到多个数据结构，通常每个数据结构都需要用一个单独的锁保护起来，这样才能维持不变量。

什么时候不需要锁呢？由于锁会降低并发度，所以我们一定要避免过度使用锁。【当效率不是很重要的时候，完全可以使用单处理器计算机，这样就完全不用考虑锁了】。当我们要保护内核的数据结构时，使用一个内核锁还是值得的，当进入内核时必须持有该锁，而退出内核时就释放该锁。许多单处理器操作系统就用这种方法运行在了多处理器上，有时这种方法被称为“内核巨锁（giant kernel lock）”，但使用这种方法就牺牲了并发性：即一时间只有一个 CPU 可以运行在内核上。如果我们想要依靠内核做大量的计算，那么使用一组更为精细的锁来让内核可以在多个 CPU 上轮流运行会更有效率。

xv6 中使用了一个单独的锁来保护进程表及其不变量，我们将在第5章讨论这个问题。

锁的顺序：

如果一段代码要使用多个锁，那么必须要注意代码每次运行都要以相同的顺序获得锁，否则就有死锁的危险。假设某段代码的两条执行路径都需要锁 A 和 B，但路径1获得锁的顺序是 A、B，而路径2获得锁的顺序是 B、A。这样就有能路径1获得了锁 A，而在它继续获得锁 B 之前，路径2获得了锁 B，这样就死锁了。这时两个路径都无法继续执行下去了，因为这时路径1需要锁 B，但锁 B已经在路径2手中了，反之路径2也得不到锁 A。【为了避免这种死锁，所有的代码路径获得锁的顺序必须相同】。避免死锁也是我们把锁作为函数使用规范的一部分的原因：调用者必须以固定顺序调用函数，这样函数才能以相同顺序获得锁。

xv6 最长的锁链也就只有两个锁。例如，`ideintr` 在调用 `wakeup` 时持有 ide 锁，而 `wakeup` 又需要获得 `ptable.lock`。还有很多使用 `sleep`/`wakeup` 的例子，它们要考虑锁的顺序是因为 `sleep` 和 `wakeup` 中有比较复杂的不变量，我们会在第5章讨论。文件系统中有很多两个锁的例子，例如文件系统在删除一个文件时必须持有该文件及其所在文件夹的锁。xv6 总是首先获得文件夹的锁，然后再获得文件的锁。

#### 锁：中断处理程序

【xv6 用锁来防止中断处理程序与另一个 CPU 上运行非中断代码使用同一个数据】。例如，时钟中断（3114）会增加 `ticks` 但可能有另一个 CPU 正在运行 `sys_sleep`，其中也要使用该变量（3473）。锁 `tickslock` 就能够为该变量实现同步。

即使在单个处理器上，中断也可能导致并发：在允许中断时，内核代码可能在任何时候停下来，然后执行中断处理程序。假设 `iderw` 持有 `idelock`，然后中断发生，开始运行 `ideintr`。`ideintr` 会试图获得 `idelock`，但却发现 `idelock` 已经被获得了，于是就等着它被释放。这样，`idelock` 就永远不会被释放了，只有 `iderw` 能释放它，但又只有让 `ideintr` 返回 `iderw` 才能继续运行，这样处理器、整个系统都会死锁。

【为了避免这种情况，当中断处理程序会使用某个锁时，处理器就不能在允许中断发生时持有锁】。xv6 做得更决绝：允许中断时不能持有任何锁。它使用 `pushcli`（1555）和 `popcli`（1566）来屏蔽中断（`cli` 是 x86 屏蔽中断的指令）。`acquire` 在尝试获得锁之前调用了 `pushcli`（1476），`release` 则在释放锁后调用了 `popcli`（1521）。`pushcli`（1555）和 `popcli`（1566）不仅包装了 `cli` 和 `sti`，它们还做了计数工作，这样就需要调用两次 `popcli` 来抵消两次 `pushcli`；这样，如果代码中获得了两个锁，那么只有当两个锁都被释放后中断才会被允许。

`acquire` 一定要在可能获得锁的 `xchg` 之前调用 `pushcli`（1483）。如果两者颠倒了，就可能在几个时钟周期里，中断仍被允许，而锁也被获得了，如果此时不幸地发生了中断，系统就会死锁。类似的，`release` 也一定要在释放锁的 `xchg` 之后调用 `popcli`（1483）。

#### 指令乱序

在本章中，我们都假设了处理器会按照代码中的顺序执行指令。但是【许多处理器会通过指令乱序来提高性能。如果一个指令需要多个周期完成，处理器会希望这条指令尽早开始执行，这样就能与其他指令交叠，避免延误太久】。例如，处理器可能会发现一系列 A、B 指令序列彼此并没有关系，在 A 之前执行 B 可以让处理器执行完 A 时也执行完 B。但是并发可能会让这种乱序行为暴露到软件中，导致不正确的结果。

我们最好以锁为基础来构建高级的同步队列，虽然 xv6 并没有这么做。如果你使用锁进行编程，那么你最好用一些工具来确定竞争条件，否则很容易遗漏掉某些需要锁保护的不变量。

用户级程序也需要锁，但 xv6 的程序只有一个运行线程，进程间也不会共享内存，所以就不需要锁了。

### 同步机制

### 进程调度

#### 概念

【任何操作系统都可能碰到进程数多于处理器数的情况，这样就需要考虑如何分享处理器资源】。理想的做法是让分享机制对进程透明。【通常我们对进程造成一个自己独占处理器的假象，然后让操作系统的`多路复用机制（multiplex）`将单独的一个物理处理器模拟为多个虚拟处理器】。本章将讲述 xv6 是如何为多个进程模拟出多处理器的。

#### 多路复用

xv6 中`多路复用`的实现如下：【**阻塞等待/ CPU 时间片**】当一个进程等待磁盘请求时，xv6 使之进入睡眠状态，然后调度执行另一个进程。另外，当一个进程耗尽了它在处理器上运行的时间片（100毫秒）后，xv6 使用时钟中断强制它停止运行，这样调度器才能调度运行其他进程。这样的多路复用机制为进程提供了独占处理器的假象，类似于 xv6 使用内存分配器和页表硬件为进程提供了独占内存的假象。

实现多路复用有几个难点。首先，应该如何从运行中的一个进程切换到另一个进程？xv6 采用了普通的【**上下文切换机制**】；虽然这里的思想是非常简洁明了的，但是其代码实现是操作系统中最晦涩难懂的一部分。第二，如何让上下文切换透明化？xv6 只是简单地【**使用时钟中断处理程序来驱动上下文切换**】。第三，可能出现【**多个 CPU 同时切换进程的情况，那么我们必须使用一个带锁的方案来避免竞争**】。第四，【**进程退出时必须释放其占用内存与资源，但由于它本身在使用自己的资源（譬如其内核栈），所以不能由该进程本身释放其占有的所有资源**】。xv6 希望能够简洁明了地处理这些难点，不过最后其代码实现还是比较“巧妙”。

进程阻塞进入睡眠状态来等待其他进程发出事件来唤醒它们。但我们必须要小心设计以防【**睡眠进程遗漏事件通知**】。

#### 代码：上下文切换

上下文切换的具体细节：

1. 两种上下文切换：

​	从进程的内核线程切换到当前 CPU 的调度器线程

​	从调度器线程到进程的内核线程

​	这种切换是通过用户态-内核态切换（系统调用或中断）、切换到调度器、切换到新进程的内核线程、最后这个陷入返回实现的【进程调度发生在 CPU 的调度器线程中】

​	进程拥有自己的内核栈以及寄存器集合，线程的切换涉及到了保存旧线程的 CPU 寄存器，恢复新线程之前保存的寄存器；其中 `%esp` 和 `%eip` 的变换意味着 CPU 会切换运行栈与运行代码

​	当进程让出 CPU 时，进程的内核线程调用 `swtch` 来保存自己的上下文然后返回到调度器的上下文中。上下文是指进程的内核栈和寄存器集合，以结构体 `struct context*` 表示，将当前 CPU 的寄存器压入栈中并将栈指针保存在 `*old` 中。然后 `swtch` 将 `new` 拷贝到 `%esp` 中，弹出之前保存的寄存器，然后返回。

2. `swtch` 底层实现：

接下来我们先不考察调度器调用 `swtch` 的过程，我们先回到用户进程中看看。在第3章中我们知道，有可能在中断的最后，`trap` 会调用 `yield`。`yield` 又调用 `sched`，其中 `sched` 会调用 `swtch` 来保存当前上下文到 `proc->context` 中然后切换到之前保存的调度器上下文 `cpu->scheduler`（2516）。

【**`swtch`底层实现**】`swtch`（2702）一开始从栈中弹出参数，放入寄存器 `%eax` 和 `%edx`（2709-2710）中【`swtch` 函数通过旧的`%esp` (栈指针寄存器) 接收两个参数：指向当前进程上下文结构的指针 (`old`) 和指向下一个进程上下文结构的指针 (`new`)；之后修改`%esp`指向新的上下文结构】。然后 `swtch` 将 `%ebp %ebx %esi %edi %esp %eip`压入栈 (上下文信息) 中。保存了旧寄存器后，`swtch` 就准备要恢复新的寄存器了。它将指向新上下文的指针放入栈指针中（2720）。所以 `swtch` 就能颠倒一下保存旧上下文的顺序来恢复新上下文。它弹出 `%edi %esi %ebx %ebp` 然后返回（2723-2727）。由于 `swtch` 改变了栈指针，所以这时恢复的寄存器就是新上下文中的寄存器值。当 `swtch` 完成上下文加载后，CPU 会开始执行 `scheduler` 函数的代码，并且此时 CPU 使用的是 `cpu->scheduler` 的栈。

​	`%ebp` 基址指针寄存器：函数调用栈帧，用于访问函数参数、局部变量和返回地址等；

​	`%ebx` 通用寄存器：保存临时数据

​	`%esi` 源索引寄存器：

​	`%edi` 目标索引寄存器：

​	`%esp` 栈指针寄存器：保存上下文信息的栈指针

​	`%eip` 指令指针寄存器 (程序计数器)：存储着下一条即将被 CPU 执行的指令的内存地址

在我们的例子中，`sched` 调用 `swtch` 切换到 `cpu->scheduler`，即 per-cpu 的调度器上下文。这个上下文是在之前 `scheduler` 调用 `swtch`（2478）时保存的。当 `swtch` 返回时，它不会返回到 `sched` 中，而是返回到 `scheduler`，其栈指针指向了当前 CPU 的调度器的栈，而非 `initproc` 的内核栈。

#### 代码：调度

上一节中我们查看了 `swtch` 的底层细节；现在让我们将 `swtch` 看做一个既有的功能，来研究【从进程到调度器然后再回到进程的切换过程】中的一些约定。进程想要让出 CPU 必须要获得进程表的锁 `ptable.lock`，并释放其拥有的其他锁，修改自己的状态（`proc->state`），然后调用 `sched`。`yield`（2522）和 `sleep exit` 都遵循了这个约定，我们稍后将会详细研究。`sched` 检查了两次状态（2507-2512），这里的状态表明由于进程此时持有锁，所以 CPU 应该是在中断关闭的情况下运行的。最后，`sched` 调用 `swtch` 把当前上下文保存在 `proc->context` 中然后切换到调度器上下文即 `cpu->scheduler` 中。`swtch` 返回到调度器栈中，就像是调度器调用的 `swtch` 返回了一样（2478）。调度器继续其 `for` 循环，找到一个进程来运行，切换到该进程，然后继续轮转。

我们看到，在对 `swtch` 的调用的整个过程中，xv6 都持有锁 `ptable.lock`：【`swtch` 的调用者必须持有该锁，并将锁的控制权转移给切换代码】。锁的这种使用方式很少见，通常来说，持有锁的线程应该负责释放该锁，这样更容易让我们理解其正确性。但对于上下文切换来说，我们必须使用这种方式，【因为 `ptable.lock` 会保证进程的 `state` 和 `context` 在运行 `swtch` 时保持不变】。如果在 `swtch` 中没有持有 `ptable.lock`，可能引发这样的问题：在 `yield` 将某个进程状态设置为 `RUNNABLE` 之后，但又是在 `swtch` 让它停止在其内核栈上运行之前，有另一个 CPU 要运行该进程。其结果将是两个 CPU 都运行在同一个栈上，这显然是不该发生的。

【(共行程序) 内核线程只可能在 `sched` 中让出处理器，在 `scheduler` 中切换回对应的地方，当然这里 `scheduler` 也是通过 `sched` 切换到进程中的】。所以，如果要写出 xv6 中切换线程的代码行号，我们会发现其执行规律是（2478），（2516），（2478），（2516），不断循环。以这种形式在两个线程之间切换的过程有时被称作*共行程序（coroutines）*；在此例中，`sched` 和 `scheduler` 就是彼此的共行程序。

但在一种特殊情况下，调度器调用的切换到新进程的 `swtch` 不会在 `sched` 中结束。我们在第2章学到了这个例子：【当一个新进程第一次被调度时，它从 `forkret`（2533）开始运行。之所以要运行 `forkret`，只是为了按照惯例释放 `ptable.lock`；否则，这个新进程是可以就从 `trapret` 开始运行的】。

【`scheduler`（2458）运行了一个普通的循环】：找到一个进程来运行，运行直到其停止，然后继续循环。`scheduler` 大部分时间里都持有 `ptable.lock`，但在每次外层循环中都要释放该锁（并显式地允许中断）。当 CPU 闲置（找不到 `RUNNABLE` 的进程）时这样做十分有必要。如果一个闲置的调度器一直持有锁，那么其他 CPU 就不可能执行上下文切换或任何和进程相关的系统调用了，也就更不可能将某个进程标记为 `RUNNABLE` 然后让闲置的调度器能够跳出循环了。而之所以周期性地允许中断，则是因为可能进程都在等待 I/O，从而找不到一个 `RUNNABLE` 的进程（例如 shell）；如果调度器一直不允许中断，I/O 就永远无法到达了。

`scheduler` 不断循环寻找可运行，即 `p->state == RUNNABLE` 的进程。一旦它找到了这样的进程，就将 per-cpu 的当前进程变量 `proc` 设为该进程，用 `switchuvm` 切换到该进程的页表，标记该进程为 `RUNNING`，然后调用 `swtch` 切换到该进程中运行（2472-2478）。

下面我们来从另一个层面研究这段调度代码。【对于每个进程，调度维护了进程的一系列固定状态，并且保证当状态变化时必须持有锁 `ptable.lock`】。【第一个固定状态是，如果进程为 `RUNNING` 的，那么必须确保使用时钟中断的 `yield` 时，能够无误地切换到其他进程】；这就意味着 CPU 寄存器必须保存着进程的寄存器值（这些寄存器值并非在 `context` 中），`%cr3` 必须指向进程的页表，`%esp` 则要指向进程的内核栈，这样 `swtch` 才能正确地向栈中压入寄存器值，另外 `proc` 必须指向进程的 `proc[]` 槽中。【另一个固定状态是，如果进程是 `RUNNABLE`，必须保证调度器能够无误地调度执行该进程】；这意味着 `p->context` 必须保存着进程的内核线程变量，并且没有任何 CPU 此时正在其内核栈上运行，没有任何 CPU 的 `%cr3` 寄存器指向进程的页表，也没有任何 CPU 的 `proc` 指向该进程。

正是由于要坚持以上两个原则，所以 xv6 必须在一个线程中获得 `ptable.lock`（通常是在 `yield` 中），然后在另一个线程中释放这个锁（在调度器线程或者其他内核线程中）。如果一段代码想要将运行中进程的状态修改为 `RUNNABLE`，那么在恢复到固定状态中之前持有锁；最早的可以释放锁的时机是在 `scheduler` 停止使用该进程页表并清空 `proc` 时。类似地，如果 `scheduler` 想把一个可运行进程的状态修改为 `RUNNING`，在该进程的内核线程完全运行起来（`swtch` 之后，例如在 `yield` 中）之前必须持有锁。

除此之外，`ptable.lock` 也保护了一些其他的状态：进程 ID 的分配，进程表槽的释放，`exit` 和 `wait` 之间的互动，保证对进程的唤醒不会被丢失等等。我们应该思考一下 `ptable.lock` 有哪些不同的功能可以分离，使之更为简洁高效。

#### 睡眠与唤醒

锁的机制使得 CPU 之间，进程之间不会互相打扰；调度使得进程可以共享 CPU。但是现在我们还不知道进程之间是如何交换信息的。【睡眠和唤醒实际上就提供了进程间通信的机制】，它们可以让一个进程暂时休眠，等待某个特定事件的发生，然后当特定事件发生时，另一个进程会唤醒该进程。睡眠与唤醒通常被称为*顺序合作（sequence coordination）*或者*有条件同步（conditional synchronization）*机制，在操作系统的哲学中，还有很多类似的机制。

考虑一对调用 `sleep` 和 `wakeup`，其工作方式如下。`sleep(chan)` 让进程在任意的 `chan` 上休眠，称之为*等待队列（wait channel）*。`sleep` 让调用进程休眠，释放所占 CPU。`wakeup(chan)` 则唤醒在 `chan` 上休眠的所有进程，让他们的 `sleep` 调用返回。如果没有进程在 `chan` 上等待唤醒，`wakeup` 就什么也不做。让我们用 `sleep` 和 `wakeup` 来重新实现上面的代码：

```c++
/* 代码：使用睡眠与唤醒 */
struct q {
    struct spinlock lock;
    void *ptr;
};

void *
send(struct q *q, void *p)
{
    acquire(&q->lock);
    while(q->ptr != 0)
        ;
    q->ptr = p;
    wakeup(q);
    release(&q->lock);
}

void*
recv(struct q *q)
{
    void *p;
    acquire(&q->lock);
    while((p = q->ptr) == 0)
        sleep(q, &q->lock);
    q->ptr = 0;
    release(&q->lock;
    return p;
}
```

【避免死锁】`sleep` 必须将锁作为一个参数，然后用原子操作释放`q->lock`并让接收进程进入休眠状态；【避免遗失唤醒】`recv` 持有 `q->lock` 就能防止 `send` 在 `recv` 检查 `q->ptr` 与调用 `sleep` 之间调用 `wakeup` 了。

【用信号量实现】完整的发送者/接收者的实现还应该让发送者在等待接收者拿出前一个 `send` 放入的值时处于休眠状态。

#### 代码：实现睡眠与唤醒

接下来让我们看看 xv6 中 `sleep` 和 `wakeup` 的实现。总体思路是希望 `sleep` 将当前进程转化为 `SLEEPING` 状态并调用 `sched` 以释放 CPU，而 `wakeup` 则寻找一个睡眠状态的进程并把它标记为 `RUNNABLE`。

`sleep` 首先会进行几个必要的检查：必须存在当前进程（2555）并且 `sleep` 必须持有锁 `lk`（2558-2559）。接着 `sleep` 要求持有 `ptable.lock`（2568）。于是该进程就会同时持有锁 `ptable.lock` 和 `lk` 了。调用者（例如 `recv`）是必须持有 `lk` 的，这样可以保证其他进程（例如一个正在运行的 `send`）无法调用 `wakeup(chan)`。而如今 `sleep` 已经持有了 `ptable.lock`，那么它现在就能安全地释放 `lk` 了：这样即使别的进程调用了 `wakeup(chan)`，`wakeup` 也不可能在没有持有 `ptable.lock` 的情况下运行，所以 `wakeup` 必须等待 `sleep` 让进程睡眠后才能运行。这样一来，`wakeup` 就不会错过 `sleep` 了。

这里有一个复杂一点的情况：即 `lk` 就是 `ptable.lock` 的时候，这样 `sleep` 在要求持有 `ptable.lock` 然后又把它作为 `lk` 释放的时候会出现死锁。这种情况下，`sleep` 就会直接跳过这两个步骤（2567）。例如，当 `wait`（2403）持有 `&ptable.lock` 时调用 `sleep`。

现在仅有该进程的 `sleep` 持有 `ptable.lock`，于是它通过记录睡眠队列，改变进程状态，调用 `sched`（2573-2575）让进程进入睡眠。

稍后，进程会调用 `wakeup(chan)`。`wakeup`（2603）要求获得 `ptable.lock` 并调用 `wakeup1`，其中，实际工作是由 `wakeup1` 完成的。对于 `wakeup` 来说持有 `ptable.lock` 也是很重要的，因为它也要修改进程的状态并且要保证 `sleep` 和 `wakeup` 不会错过彼此。而之所以要单独实现一个 `wakeup1`，是因为有时调度器会在持有 `ptable.lock` 的情况下唤醒进程，稍后我们会看到这样的例子。当 `wakeup` 找到了对应 `chan` 中处于 `SLEEPING` 的进程时，它将进程状态修改为 `RUNNABLE`。于是下一次调度器在运行时，就可以调度该进程了。

`wackup` 必须在有一个监视唤醒条件的锁的时候才能被调用: 在上面的例子中这个锁就是 `q->lock`。至于为什么睡眠中的进程不会错过唤醒，则是因为从 `sleep` 检查进程状态之前，到进程进入睡眠之后，`sleep` 都持有进程状态的锁或者 `ptable.lock` 或者是两者兼有。由于 `wakeup` 必须在持有这两个锁的时候运行，所以它必须在 `sleep` 检查状态之前和一个进程已经完全进入睡眠后才能执行。

有些情况下可能有多个进程在同一队列中睡眠；例如，有多个进程想要从管道中读取数据时。那么单独一个 `wakeup` 的调用就能将它们全部唤醒。他们的其中一个会首先运行并要求获得 `sleep` 被调用时所持的锁，然后读取管道中的任何数据。而对于其他进程来说，即使被唤醒了，它们也读不到任何数据，所以唤醒它们其实是徒劳的，它们还得接着睡。正是由于这个原因，我们在一个检查状态的循环中不断调用 `sleep`。

`sleep` 和 `wakeup` 的调用者可以使用任何方便使用的数字作为队列号码；而实际上，xv6 通常使用内核中和等待相关的数据结构的地址，譬如磁盘缓冲区。即使两组 `sleep`/`wakeup` 使用了相同的队列号码，也是无妨的：对于那些无用的唤醒，它们会通过不断检查状态忽略之。`sleep`/`wakeup` 的优点主要是其轻量级（不需另定义一个结构来作为睡眠队列），并且提供了一层抽象（调用者不需要了解与之交互的是哪一个进程）。

#### 代码：管道

上面我们提到的队列只是一个简单的模型，实际上在 xv6 中有两个使用 `sleep`/`wakeup` 来同步读者写者的队列。一个在 IDE 驱动中：进程将未完成的磁盘请求加入队列，然后调用 `sleep`。中断处理程序会使用 `wakeup` 告诉进程其磁盘请求已经完成了。

更为复杂的一个例子是管道。我们在第0章已经介绍了管道的接口：我们从管道的一端写入数据字节，然后数据被拷贝到内核缓冲区中，接着就能从管道的另一端读取数据了。后面的章节中还会讲到文件系统是怎样帮助我们实现管道的。不过现在先让我们来看看 `pipewrite` 和 `piperead` 的实现。

每个管道由一个结构体 `struct pipe` 表示，其中有一个锁 `lock`和内存缓冲区。其中的域 `nread` 和 `nwrite` 表示从缓冲区读出和写入的字节数。`pipe` 对缓冲区做了包装，使得虽然计数器继续增长，但实际上在 `buf[PIPESIZE - 1]` 之后写入的字节存放在 `buf[0]`。这样就让我们可以区分一个满的缓冲区（`nwrite == nread + PIPESIZE`）和一个空的缓冲区（`nwrite == nread`），但这也意味着我们必须使用 `buf[nread % PIPESIZE]` 而不是 `buf[nread]` 来读出/写入数据。现在假设 `piperead` 和 `pipewrite` 分别在两个 CPU 上连续执行。

`pipewrite`（6080）首先请求获得管道的锁，以保护计数器、数据以及相关不变量。`piperead`（6101）接着也请求获得锁，结果当然是无法获得。于是它停在了 `acquire`（1474）上等待锁的释放。与此同时，`pipewrite` 在循环中依次写入 `addr[0], addr[1], ..., addr[n-1]` 并添加到管道中（6904）。在此循环中，缓冲区可能被写满（6086），这时 `pipewrite` 会调用 `wakeup` 通知睡眠中的读者缓冲区中有数据可读，然后使得在 `&p->nwrite` 队列中睡眠的读者从缓冲区中读出数据。注意，`sleep` 在让 `pipewrite` 的进程进入睡眠时还会释放 `p->lock`。

现在 `p->lock` 被释放了，`piperead` 尝试获得该锁然后开始执行：此时它会检查到 `p->nread != p->nwrite`（6106）（正是在 `nwrite == nread + PIPESIZE` （6086）的时候 `pipewrite` 进入了睡眠），于是 `piperead` 跳出 `for` 循环，将数据从管道中拷贝出来（6113-6117），然后将 `nread` 增加读取字节数。现在缓冲区又多出了很多可写的字节，所以 `piperead` 调用 `wakeup`（6118）唤醒睡眠中的写者，然后返回到调用者中。`wakeup` 会找到在 `&p->nwrite` 队列上睡眠的进程，正是该进程之前在运行 `pipewrite` 时由于缓冲区满而停止了。然后 `wakeup` 将该进程标记为 `RUNNABLE`。

#### 代码：wait, exit, kill

`sleep` 和 `wakeup` 可以在很多需要等待一个可检查状态的情况中使用。如我们在第0章中所见，父进程可以调用 `wait` 来等待一个子进程退出。在 xv6 中，当一个子进程要退出时它并不是直接死掉，而是将状态转变为 `ZOMBIE`，然后当父进程调用 `wait` 时才能发现子进程可以退出了。所以父进程要负责释放退出的子进程相关的内存空间，并修改对应的 `struct proc` 以便重用。每个进程结构体都会在 `p->parent` 中保存指向其父进程的指针。如果父进程在子进程之前退出了，初始进程 `init` 会接收其子进程并等待它们退出。我们必须这样做以保证可以为退出的进程做好子进程的清理工作。另外，所有的进程结构都是被 `ptable.lock` 保护的。

`wait` 首先要求获得 `ptable.lock`，然后查看进程表中是否有子进程，如果找到了子进程，并且没有一个子进程已经退出，那么就调用 `sleep` 等待其中一个子进程退出（2439），然后不断循环。注意，这里 `sleep` 中释放的锁是 `ptable.lock`，也就是我们之前提到过的特殊情况。

`exit` 首先要求获得 `ptable.lock` 然后唤醒当前进程的父进程（2376）。这一步似乎为时过早，但由于 `exit` 这时还没有把当前进程标记为 `ZOMBIE`，所以这样并不会出错：即使父进程已经是 `RUNNABLE` 的了，但在 `exit` 调用 `sched` 以释放 `ptable.lock` 之前，`wait` 是无法运行其中的循环的。所以说只有在子进程被标记为 `ZOMBIE`(2388)之后， `wait` 才可能找到要退出的子进程。在退出并重新调度之前，`exit` 会把所有子进程交给 `initproc`（2378-2385）。最后，`exit` 调用 `sched` 来让出 CPU。

退出进程的父进程本来是通过调用 `wait`（2439）处于睡眠状态中，不过现在它就可以被调度器调度了。对 `sleep` 的调用返回时仍持有 `ptable.lock` ；`wait` 接着会重新查看进程表并找到 `state == ZOMBIE`（2382）的已退出子进程。它会记录该子进程的 `pid` 然后清理其 `struct proc`，释放相关的内存空间（2418-2426）。

子进程在 `exit` 时已经做好了大部分的清理工作，但父进程一定要为其释放 `p->kstack` 和 `p->pgdir`；当子进程运行 `exit` 时，它正在利用 `p->kstack` 分配到的栈以及 `p->pgdir` 对应的页表。所以这两者只能在子进程结束运行后，通过调用 `sched` 中的 `swtch` 被清理。这就是为什么调度器要运行在自己单独的栈上，而不能运行在调用 `sched` 的线程的栈上。

`exit` 让一个应用程序可以自我终结；`kill`（2625）则让一个应用程序可以终结其他进程。在实现 `kill` 时有两个困难：1）被终结的进程可能正在另一个 CPU 上运行，所以它必须在被终结之前把 CPU 让给调度器；2）被终结的进程可能正在 `sleep` 中，并持有内核资源。`kill` 很轻松地解决了这两个难题：它在进程表中设置被终结的进程的 `p->killed`，如果这个进程在睡眠中则唤醒之。如果被终结的进程正在另一个处理器上运行，它总会通过系统调用或者中断（例如时钟中断）进入内核。当它离开内核时，`trap` 会检查它的 `p->killed`，如果被设置了，该进程就会调用 `exit`，终结自己。

如果被终结的进程在睡眠中，调用 `wakeup` 会让被终结的进程开始运行并从 `sleep` 中返回。此举有一些隐患，因为进程可能是在它等待的状态尚为假的时候就从 `sleep` 中返回了。所以 xv6 谨慎地在调用 `sleep` 时使用了 `while` 循环，检查 `p->killed` 是否被设置了，若是，则返回到调用者。调用者也必须再次检查 `p->killed` 是否被设置，若是，返回到再上一级调用者，依此下去。最后进程的栈展开（unwind）到了 `trap`，`trap` 若检查到 `p->killed` 被设置了，则调用 `exit` 终结自己。我们已经在管道的实现中（6087）看到了在 `sleep` 外的 `while` 循环中检查 `p->killed`。

有一处的 `while` 没有检查 `p->killed`。ide 驱动（3979）直接重新调用了 `sleep`。之所以可以确保能被唤醒，是因为它在等待一个磁盘中断。如果它不是在等待磁盘中断的话，xv6 就搞不清楚它在做什么了。如果有第二个进程在中断之前调用了 `iderw`，`ideintr` 会唤醒该进程（第二个），而非原来等待中断的那一个（第一个）进程。第二个进程会认为它收到了它正在等待的数据，但实际上它收到的是第一个进程想要读的数据。

#### 补充

xv6 所实现的调度算法非常朴素，仅仅是让每个进程轮流执行。这种算法被称作*轮转法（round robin）*。真正的操作系统使用了更为复杂的算法，例如，让每个进程都有优先级。主要思想是优先处理高优先级的可运行进程。但是由于要权衡多项指标，例如要保证公平性和高的吞吐量，调度算法往往很快变得复杂起来。另外，复杂的调度算法还会无意中导致像*优先级倒转（priority inversion）*和*护航（convoy）*这样的现象。优先级倒转是指当高优先级进程和低优先级进程共享一个锁时，如果锁已经被低优先级进程获得了，高优先级进程就无法运行了。护航则是指当很多高优先级进程等待一个持有锁的低优先级进程的情况，护航一旦发生，则可能持续很久。如果要避免这些问题，就必须在复杂的调度器中设计更多的机制。

在 `wakeup` 中遍历整个进程表来寻找对应 `chan` 的进程是非常低效的。更好的办法是用另一个结构体代替 `sleep` 和 `wakeup` 中的 `chan`，该结构体中维护了一个睡眠进程的链表。许多线程库都把相同的一个结构体作为一个状态变量；如果是这样的话，`sleep` 和 `wakeup` 操作则被称为 `wait` 和 `signal`。所有此类机制都有同一个思想：使得睡眠状态可以被某种执行原子操作的锁保护。

在 `wakeup` 的实现中，它唤醒了特定队列中的所有进程，而有可能很多进程都在同一个队列中等待被唤醒。操作系统会调度这里的所有进程，它们会互相竞争以检查其睡眠状态。这样的一堆进程被称作*惊群（thundering herd）*，而我们最好是避免这种情况的发生。大多数的状态变量都有两种不同的 `wakeup`，一种唤醒一个进程，即 `signal`；另一种唤醒所有进程，即 `broadcast`。

【信号量是另一种合作机制】。一个信号量是一个提供两种操作，即加和减的整数。我们可以不断对一个信号量进行加操作，但是我们只能在信号量为正时进行减操作，否则当我们对为零的信号量减时，进程会进入睡眠，直到另一个进程对其进行加操作，这对加减操作就会抵消掉。这个整数实际上反映了一个真正的计数值，例如管道缓冲区中可写的字节数，或者进程拥有的僵尸子进程数。在这个抽象中显式地计数可以避免“丢失的唤醒”问题：通过显式地记录唤醒次数。这样计数同时还能避免无谓的唤醒和惊群问题。

【终结、清理进程是 xv6 中较为复杂的内容】。而大多数操作系统则更为复杂。例如，被终结的进程可能在内核深处睡眠，我们需要谨慎地编写代码以展开其栈。许多操作系统使用显式的处理异常的机制来展开栈，例如使用 `longjmp`。另外，有些事件可能让睡眠的进程被唤醒，即使这些事件尚未发生。例如，当进程在睡眠时，另一个进程向它发送一个*信号*。进程就会从被打断的系统调用中返回值-1并在 `EINTR` 中存放错误代码。应用程序可以查看这些值以决定下一步怎么做。xv6 并不支持信号，所以没有这么复杂。





# 嵌入式软件 笔试

## 操作系统

### 进程和线程

**什么是进程？什么是线程？**

进程：正在运行的一个**独立的应用程序**。每个进程都有自己**独立的内存空间**；进程是操作系统进行**资源分配和调度的基本单位**，拥有以下资源包括：独立的地址空间，系统资源，进程控制块；

线程：**轻量级进程 (Lightweight Process, LWP)**，它是进程内部的一个**执行单元**。一个进程可以包含多个线程，这些线程**共享同一个进程的内存空间和系统资源**；线程是操作系统进行**CPU调度的基本单位**。这意味着CPU可以直接分配时间片给线程来执行

**进程和线程有什么区别？**

资源分配，内存空间，独立性，切换开销，并发性，创建和销毁开销

**何时使用多进程，何时使用多线程？**

多进程：

​	对于需要高可靠性的系统，将不同的功能模块放在不同的进程中可以提高系统的鲁棒性；

​	需要利用多核处理器进行并行计算，且任务之间数据共享较少；

​	使用多进程可以更好地进行应用程序的权限管理和隔离；

多线程：

​	应用程序的不同任务需要频繁地访问和修改共享数据，那么使用多线程可以避免进程间通信的复杂性和性能开销；

​	需要更高的响应速度和更小的上下文切换开销；

​	IO密集型任务：使用多线程可以在一个线程等待IO的过程中，让其他线程继续执行，从而提高CPU的利用率；

​	使用多线程可以在同一个进程内实现并发执行，简化程序设计和资源管理；

**进程有几种状态？画一下进程状态转换图？**

创建态--就绪态--运行态--阻塞态--终止态

**创建进程有哪几种方式?**

fork() 系统调用：会创建一个与调用进程几乎完全相同的子进程，先拷贝页表，写时拷贝内存

vfork() 系统调用：子进程会**共享父进程的内存空间**，并且**父进程会阻塞，直到子进程调用 `execve()` 或 `_exit()` 之后才会继续执行**；在 `execve()` 之前共享内存可以减少不必要的复制开销 (写时拷贝内存)

clone() 系统调用：是一个更底层的、功能更强大的进程创建方式。它允许调用者更精细地控制子进程共享哪些资源

**进程间通信方式？有什么优缺点？**

管道 (匿名/有名)：一种半双工的通信方式，数据只能单向流动。通常用于父子进程或者兄弟进程之间（需要通过它们的共同祖先创建）。

​	简单易用；半双工通信，只能单向传输数据；

消息队列：一个存在于内核中的消息链表。一个进程可以向队列中添加消息，另一个或多个进程可以从队列中读取消息。

​	可以实现异步通信，发送带有类型的数据，方便接收进程选择性接收，允许不同优先级的消息传递；

​	需要在内核中维护消息队列，有一定的系统开销，消息的复制需要额外的开销，队列的容量可能有限制

信号量：一种计数器，用于控制多个进程对共享资源的访问。它可以用来实现进程间的同步和互斥。

​	可以有效地控制对共享资源的并发访问

共享内存：多个进程可以访问同一块物理内存区域。这是最快的IPC方式之一，因为进程可以直接读写共享的内存，而不需要内核的介入。通常需要与其他同步机制（如信号量或互斥锁）配合使用，以避免并发访问导致的数据不一致问题。

​	速度快，效率高，因为数据不需要在进程和内核之间复制；

​	需要显式地管理共享内存的同步，容易出错（例如，产生竞争条件）

套接字：为网络通信设计的，但也可以用于同一台机器上的不同进程之间的通信（Unix Domain Socket）。它提供了面向连接（TCP）和无连接（UDP）的通信方式。

信号：一种异步通信机制，用于通知进程发生了某个事件

​	简单，开销小；可以异步通知进程事件的发生；

​	能够传递的信息有限，通常只用于通知事件类型，不能传递大量数据

**线程间同步方法有哪些？**

互斥锁：一种基本的同步机制，用于保护临界区；

​	简单易用，保证了对临界资源的独占访问；

​	如果一个线程长时间持有锁而不释放，可能会导致其他等待该锁的线程饥饿（starvation）。还可能引发死锁（deadlock）问题，例如当多个线程试图以不同的顺序获取多个互斥锁时

条件变量：提供了一种线程等待特定条件发生的方式。它通常与互斥锁一起使用。线程首先获取互斥锁，然后检查某个条件是否满足。如果条件不满足，线程可以调用条件变量的等待函数释放互斥锁并进入等待状态。当其他线程改变了条件并发出信号（signal）或广播（broadcast）时，等待的线程会被唤醒（重新获取互斥锁）并重新检查条件。

​	允许线程在特定条件满足时才继续执行，避免了忙等待（busy-waiting），提高了CPU利用率；

​	使用起来比互斥锁稍微复杂，需要正确地与互斥锁配合使用，以防止竞态条件

读写锁：允许多个线程同时读取共享资源，但只允许一个线程写入共享资源。当有线程持有写锁时，其他任何线程（无论是读还是写）都将被阻塞。当没有线程持有写锁时，多个线程可以同时持有读锁。

​	在读多写少的场景下，可以显著提高并发性能，因为多个读线程可以并行访问共享资源；

​	实现比互斥锁复杂。可能存在写饥饿问题，即如果一直有读线程在访问，写线程可能长时间无法获得锁。

自旋锁：当一个线程尝试获取一个已经被其他线程持有的自旋锁时，它不会进入睡眠状态，而是会不断地循环（自旋）检查锁是否被释放。只有当锁被释放时，该线程才能获取到锁。

​	在锁的持有时间非常短的情况下，自旋锁可以避免线程切换的开销，从而提高性能；

​	如果锁的持有时间较长，自旋等待会消耗大量的CPU资源；

信号量：一种更通用的同步机制，它可以用来控制对一组资源的并发访问。信号量维护一个计数值。线程可以执行 `wait()`（或 `P()`）操作来尝试获取资源（使计数值减一），如果计数值为负，则线程进入等待状态。线程可以执行 `signal()`（或 `V()`）操作来释放资源（使计数值加一），这可能会唤醒一个等待的线程。二值信号量（计数器只能为0或1）可以实现互斥锁的功能

​	比互斥锁更通用，可以控制多个资源的并发访问；

​	使用不当也可能导致死锁。

**什么是内核线程和用户线程？**

内核线程：直接由操作系统内核支持和管理的线程

​	内核线程的上下文切换发生在内核态，需要操作系统进行完整的上下文切换，包括保存和恢复寄存器、程序计数器、堆栈指针等

​	每个内核线程通常会关联一个内核栈，因此会占用一定的内核资源

​	如果一个内核线程因为进行阻塞式系统调用（例如 I/O 操作）而进入等待状态，不会影响到同一个进程中的其他内核线程的执行

​	内核可以进行统一的调度管理，能够更好地进行资源分配和优先级控制；

​	创建、销毁和切换内核线程的开销相对较大，因为需要陷入内核态执行

用户线程：在用户空间实现的线程

​	用户线程的创建、调度、同步和销毁等操作完全由用户级的线程库来管理，无需内核的干预

​	由用户级线程库实现的调度器在进程内部进行调度。由于内核只将 CPU 时间分配给进程，因此进程内的用户线程只能在一个 CPU 时间片内轮流执行，无法实现真正的并行执行

​	用户线程的上下文切换发生在用户态，通常只需要保存和恢复线程的局部状态（例如寄存器、堆栈指针），开销较小，速度快；

​	如果一个进程中的某个用户线程因为阻塞式系统调用而进入等待状态，由于内核只知道进程的存在而不知道进程内的用户线程，因此整个进程（包括该进程中的所有其他用户线程）都会被阻塞

​	可以在不支持内核级线程的操作系统上实现多线程；

​	一个线程的阻塞会导致整个进程的阻塞；

内核线程和用户线程的区别？



内核线程和用户线程有什么优缺点？



**什么是僵尸进程，孤儿进程，守护进程？**

僵尸进程：处于这种已终止但等待父进程回收其残留信息的子进程

​	子进程已经执行完毕，但其父进程尚未调用 `wait()` 或 `waitpid()` 来获取子进程的退出状态。

​	僵尸进程本身不占用很多资源（主要是进程表中的一个条目），但如果数量过多，可能会耗尽系统的进程ID资源，导致新的进程无法创建

​	父进程应该在子进程结束后及时调用 `wait()` 或 `waitpid()` 来清理子进程。如果父进程不关心子进程的退出状态，可以使用 `wait(NULL)` 来等待任意一个子进程结束，或者使用 `waitpid(-1, NULL, WNOHANG)` 进行非阻塞等待。另外，也可以通过信号处理机制，例如捕获 `SIGCHLD` 信号并在信号处理函数中调用 `wait()` 或 `waitpid()` 来清理退出的子进程。或者直接杀死父进程，使这些僵尸进程变为孤儿进程。

孤儿进程：一个父进程在子进程运行结束之前终止或退出

​	当一个进程变成孤儿进程时，操作系统会将它的父进程重新设置为 `init` 进程，负责领养所有的孤儿进程，并在它们终止后调用 `wait()` 回收其资源，防止它们变成僵尸进程

​	通常情况下，孤儿进程不会对系统造成危害；

守护进程：一种在后台运行且不与任何终端（控制台）关联的特殊进程。它们通常在系统启动时启动，并在后台持续运行，提供各种系统服务，例如网络服务（如 SSH、HTTP）、系统日志服务、定时任务服务等。通常在系统启动时创建，直到系统关闭才终止

​	创建守护进程：

僵尸进程有什么危害？



如何清理僵尸进程？



**如何唤醒被阻塞的socket线程？**

关闭 Socket 文件描述符：从另一个线程或进程中关闭被阻塞线程正在等待的 socket 文件描述符，这会导致被阻塞的 socket 操作立即返回

设置 Socket 超时：在创建 socket 后，可以使用 `setsockopt()` 函数设置 socket 的接收或发送超时时间；超时后阻塞的 socket 操作会返回；允许线程在等待一定时间后自动唤醒，避免永久阻塞

使用 `select()` 或 `poll()` 设置超时：在调用阻塞的 socket 操作之前，使用 `select()` 或 `poll()` 等多路复用函数等待 socket 变为可读、可写或发生错误。可以为 `select()` 或 `poll()` 设置超时时间；可以同时监控多个文件描述符，并且可以设置精确的超时时间

**如何确定当前线程是繁忙还是阻塞？**

使用系统工具进程观察：

​	**`top` 或 `htop`: **可以显示系统中运行的进程和线程的实时信息，通常一个繁忙的线程会导致其所属进程的CPU使用率升高

​	`ps` 命令：可以列出当前系统中的进程信息。线程的状态也会在 `STAT` 列中显示。常见的阻塞状态包括 `D` (不可中断睡眠，通常是I/O等待) 和 `S` (可中断睡眠)。运行或可运行的线程通常显示为 `R`。

**空闲的进程和阻塞的进程状态会不会在唤醒的时候误判？**



请问就绪状态的进程在等待什么？



如何实现线程池？



**请你回答一下fork和vfork的区别**

资源复制/共享：

父子进程执行顺序：

**server端监听端口，但还没有客户端连接进来，此时进程处于什么状态？**

阻塞在等待连接的系统调用`accept()/select()/epoll()`

### 堆和栈

什么是代码段，数据段，bss段，堆，栈？
为什么堆的空间是不连续的？

**什么是用户栈和内核栈？**

用户栈：用户栈是分配在用户空间内存中的一块区域，主要用于存储用户态程序执行时的函数调用信息、局部变量、函数参数以及函数返回地址等。每个线程在用户态执行时，都会拥有自己的用户栈；

​	用户栈随着线程的创建而分配，随着线程的终止而释放

​	**函数调用管理：** 记录函数之间的调用关系，保证函数执行完毕后能正确返回。

​	**存储局部变量：** 函数内部定义的局部变量存储在栈上，随着函数的进入和退出而分配和释放。

​	**传递函数参数：** 函数调用时，参数通常通过栈传递给被调用函数。

​	**保存上下文：** 在函数调用时，需要保存一些寄存器的值，以便函数返回后能够恢复执行环境。

内核栈：内核栈是分配在内核空间内存中的一块区域，每个线程都有一个对应的内核栈。当线程因为系统调用、异常或中断等原因陷入内核态执行时，内核代码会使用该线程的内核栈；

​	内核栈在线程创建时分配，在线程终止时释放

​	保存内核态上下文：当线程进入内核态时，需要保存用户态的寄存器、程序计数器、栈指针等上下文信息到内核栈中，以便稍后能够返回用户态继续执行

​	**存储内核态局部变量：** 内核函数执行时使用的局部变量可以存储在内核栈上。

​	**传递内核函数参数：** 内核函数之间的调用也可能通过内核栈传递参数。

​	**支持中断处理：** 当发生硬件中断时，中断处理程序会在当前正在运行线程的内核栈上执行，并保存被中断的上下文。

​	**系统调用执行：** 用户态程序发起系统调用后，会切换到内核态，系统调用的处理函数会在发起调用的线程的内核栈上执行。

**用户栈和内核栈，为什么不能共用一个栈？**

安全性和隔离性：用户态程序的错误或恶意行为（例如栈溢出）可能会覆盖内核栈中的数据，导致内核崩溃甚至被恶意利用，危及整个系统的安全

可靠性和稳定性：内核是操作系统的核心，任何在内核态发生的错误都可能导致整个系统崩溃。因此，内核需要一个独立且受保护的栈空间，以确保其运行的稳定性和可靠性

并发执行：在多线程环境下，每个线程都有自己的用户栈和内核栈；如果所有线程共用一个内核栈，那么在多个线程同时进行系统调用时，内核栈的使用会变得非常复杂和难以管理，容易发生数据竞争和混乱

**线程是否具有相同的堆栈?**

同一个进程内的多个线程**共享相同的堆 (Heap)** 内存区域，但每个线程都拥有**自己独立的栈**

并发和互斥
驱动里面为什么要有并发、互斥的控制？如何实现？讲个例子？
自旋锁是什么？信号量是什么？二者有何异同？
自旋锁和信号量可以睡眠吗？为什么？
自旋锁和信号量可以用于中断中吗？
读写锁是什么？
产生死锁的原因是什么？
**死锁的4个必要条件是什么？**

互斥条件

持有并等待条件

非剥夺条件

环路等待条件

**死锁的处理方式有哪些？**



如何避免死锁？
请问单核机器上写多线程程序，是否需要考虑加锁，为什么？

### 内存

**在1G内存的计算机中能否malloc(1.2G)？为什么？**

物理内存限制；连续物理内存分配的困难；

**malloc能申请多大的空间？**



**内存管理有哪几种方式？**

静态内存分配：

栈内存分配：

堆内存分配：

什么是虚拟内存？
解释下内存碎片，内碎片，外碎片？
解释下虚拟地址、逻辑地址、线性地址、物理地址？
请问虚拟内存和物理内存怎么对应？
虚拟内存置换方式是怎么样的？
给你一个类，里面有static，virtual之类的，来说一说这个类的内存分布？
假设临界区资源释放，如何保证只让一个线程获得临界区资源而不是都获得？

**操作系统中的缺页中断是什么？**

缺页中断：指当程序试图访问一个**已经映射到进程的虚拟地址空间中，但是对应的物理页帧尚未加载到内存中**时，CPU 所产生的一种异常或中断信号

fork(), mmap() 发生缺页中断后，再拷贝内容

OS缺页置换算法如何实现的？
**系统调用是什么，你用过哪些系统调用，和库函数有什么区别？**

kmalloc()

vmalloc()

为什么要有page cache，操作系统怎么设计的page cache？



### 上下文

上下文有哪些？怎么理解？
为什么会有上下文这种概念？
什么情况下进行用户态到内核态的切换？
中断上下文代码中有哪些注意事项？
请问线程需要保存哪些上下文，SP、PC、EAX这些寄存器是干嘛用的
