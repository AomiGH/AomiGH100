# 09 嵌入式软件面经整理

## xv6 操作系统

### 项目细节

##### 自我介绍

面试官您好！我是贾铭杰，来自电子科技大学。在工作方向上，我对嵌入式软件开发岗位有着浓厚的兴趣。我学习并开发了基于 RISC 架构的类 UNIX xv6 操作系统内核，阅读了内核各个子功能模块实现的源码，并改进了内存管理模块的系统调用；我学习并开发了基于 IMX6ULL 开发板的相机驱动开发项目，实现了主页面、相册页面、相机页面等的多页面运行、线程间同步和基本的字符设备驱动模型。

我希望通过这次面试来获得参加嵌入式软件开发实习生岗位的宝贵机会，来进一步深入学习软件开发技术。

##### 项目技术栈/技术细节

##### 代码量/工作量体现

##### debug/调优

### Linux 驱动

#### 指令

##### 01 Linux Shell 指令

##### 02 GCC 指令

##### 03 GDB 调试指令 / 使用？

##### 04 驱动开发指令

#### Uboot

##### 01 bootloader 启动流程

##### 02 uboot 和 内核传递参数

##### 03 关闭 caches

#### 文件系统

##### 01 根文件系统

##### 02 可执行映像文件

#### 中断

##### 01 硬中断/软中断

##### 02 tasklet/workqueue

##### 03 Linux 中断处理流程

中断处理流程：外部硬件 (鼠标、硬盘) 触发中断，会产生中断请求信号 (指定中断号) 给 CPU 的中断控制器 (NVIC)；中断控制器接收到中断请求后，根据优先级原则管理中断请求，并且选择一个中断请求发送给 CPU；CPU 会保存上下文信息 (进程/中断上下文？) 切换到内核态并根据中断向量表跳转到中断处理程序入口地址；中断处理程序识别中断源，执行具体的中断事件相关操作，处理完成后，CPU 会向中断控制器发送一个中断应答信号，告知该中断已被处理；在比较紧急的中断上半部处理完成后，进行中断登记，将下半部处理程序放入到相应的 (指定设备/内核子系统的) 下半部执行队列中。

​	下半部处理程序包括软中断、tasklet 和 workqueue。软中断包括软中断类型、软中断触发和软中断处理函数，运行在软中断上下文中，不允许导致睡眠的操作。软中断的优先级低于硬中断，允许将中断处理中比较耗时复杂的部分延迟运行，来保证硬中断的快速响应。此外，软中断允许不同类型的软中断在多 CPU 上并发运行，同一类型的软中断串行执行。

​	tasklet 是构建在软中断之上的更简单、更方便的延迟执行机制，方便开发者使用。

​	workqueue 运行在进程上下文中，通过内核线程来调度执行各项任务，允许会导致睡眠/阻塞的操作。

##### 04 中断上下文

中断上下文不允许导致睡眠的操作：

​	中断处理程序是原子性的，是连续的、不可分割的操作序列，并且中断上下文中没有 "进程" 概念，并且禁止进程调度；而睡眠操作会导致进程让出 CPU，

进程上下文可以进行进程调度，同样也支持导致睡眠/阻塞的操作。

#### Linux 驱动模型

##### 01 字符设备驱动模型

a. 字符设备驱动模型分为platform_device和platform_driver两部分
   platform_device : 指定硬件资源, 来自设备树文件 .dts
	dts 文件被编译为 dtb 文件。在 bootloader 程序启动阶段，会设置 CPU 寄存器 R0, R1, R2，其中 R1 表示机器 ID (Machine Desc)，用于判断内核是否支持该机器，R2 保存了内核程序入口地址，内核大小，根文件系统挂载方式和设备树文件。操作系统内核启动后会解析 dtb 文件，构造一系列的 device_node 结构体，并将根节点下具有 compatible 属性的子节点 (或者 compatible 属性中包含特殊值 "arm", "simple-bus" 等的节点) 转换为 platform_device 结构体，将 i2c 和 spi 等总线节点下的子节点转换为 i2c_client 和 spi_device 结构体，并交由对应的总线驱动程序来处理。platform_device 结构体中的 resource (存储了设备所使用的具体资源) 和 dev.of_node (指向描述该设备的设备树节点指针)
   platform_driver : 驱动程序抽象为了 platform_driver 结构体
	platform_driver 结构体包含了操作函数指针，包括 .probe, .remove 成员函数和 .driver 结构体，driver 结构体中包含了驱动程序名称和 .of_match_table (其中的 compatible 属性描述了该驱动程序能够处理的 platform_device 类型的 of_device_id 结构体)。

b. 匹配过程：compatible 属性的对比
	在内核启动时或者动态加载，操作系统内核会将 platform_device 结构体中的 dev.of_node 指向的设备树节点中的 compatible 属性与当前的 platform_driver 结构体中的 of_match_table 中的每一项 compatible 属性进行对比。

c. 设备初始化/注册
	匹配成功后，内核调用 probe 函数，通过匹配到的 platform_device 结构体来获取设备的资源信息 (包括地址、中断号等)，此外，依次分配设备描述结构体 cdev，设置操作函数指针集结构体 file_operations (包括 .open, .read, .write, .ioctl, .release)，通过 cdev_init() 设置 cdev，动态分配主设备号和次设备号 alloc_chrdev_region()，注册 cdev_add()，并且创建设备类 class_create() (用于统一管理设备类的共同属性，例如统一的设备文件权限管理、统一的热插拔策略等)，和创建设备节点 device_create() (用于在指定的设备类下创建一个设备，进程通过设备文件来与硬件进行交互)。在 remove() 函数中销毁创建的设备文件 device_destory()，注销分配的设备号 unregister_chrdev_region()。
特点：
    易于扩展，没有冗余代码
    硬件有变动时不需要重新编译内核或驱动程序，只需要提供不一样的dtb文件

##### 02 LCD 驱动开发

##### 03 platform 总线匹配规则

##### 04 创建设备文件和需要的参数

##### 05 字符设备和块设备

##### 06 insmod/rmmod

##### 07 Linux 驱动开发调试方法

#### Linux 内核

##### 01 内核映像文件格式/内容

##### 02 内核申请内存的函数

`kmalloc(), vmalloc(), malloc(), mmap(), __alloc_pages()`

##### 03 内核空间/用户空间

##### 04 内核态/用户态

##### 05 内核态和用户态通信

##### 06 通用链表

##### 07 应用程序的 open() 执行过程

##### 08 多 CPU 并发分配内存

vmalloc 动态内存区，normal 映射区，DMA 映射区，固定映射区，永久映射区，临时映射区

##### 09 伙伴系统/slab 分配器

伙伴系统：相邻内存块合并和内存块分割分配，内存碎片 (分配的内存和所需内存大小不一致导致)

slab 分配器：管理维护内核常用对象的内存分配，用于分配和释放特定对象大小的内存空间；没有内存碎片，额外的管理开销

### 操作系统

#### 进程/线程

##### 01 进程/线程

##### 02 多进程/多线程

##### 03 进程间通信

共享内存：通过让多个进程访问同一块物理内存区域，由于数据不需要在进程之间复制，是一个高效的进程间通信方式。

​	具体来说，进程使用`shmget()`系统调用来创建一个新的共享内存区域或者获取一个已经存在的共享内存区域的 ID；然后使用`shmat()`系统调用将共享内存映射到自己的虚拟内存空间中，使得进程可以通过虚拟地址访问共享内存；当多个进程都将同一个共享内存区域映射到自己的虚拟地址空间后，可以像访问普通内存一样读写这块共享内存中的数据。由于它们操作的是同一块物理内存，一个进程对共享内存所做的修改，会立即被其他映射了这块内存的进程看到。结束进程间通信时，进程会使用`shmdt()`系统调用解除与共享内存的映射；最后一个解除映射的进程会通过`shmctl()`系统调用删除共享内存区域。

管道：

消息队列：

信号：

##### 04 线程间同步

锁：

条件变量：

信号量：

##### 05 创建进程/线程

##### 06 用户线程/内核线程

##### 07 僵尸进程/孤儿进程/守护进程

##### 08 实现线程池

##### 09 调度策略/算法

调度策略：

​	进程响应速度尽可能快；后台进程尽可能多的快速的完成任务；调和低优先级和高优先级进程的运行时间；避免进程饥饿现象

​	调度策略根据进程的优先级来适当地分配 CPU，包括静态优先级和动态优先级；静态优先级决定了系统分配给进程的时间片长度 (值越小优先级越高)；动态优先级通过奖励/惩罚机制决定了调度程序是否优先选择该进程 (值越大优先级越高)，例如，睡眠时间越长的进程动态优先级相对会更高；

​	避免低优先级进程的饥饿现象：调度程序维护活动进程和过期进程两个集合，活动进程是指还没有使用完时间片的进程；过期进程是指已经用完了时间片，会被禁止运行，直到所有活动进程都过期；保证一个进程运行完时间片后，会被还没有使用完时间片的低优先级进程替代。此外，不同于普通进程，实时进程总是当作活动进程，因此会禁止低优先级进程的运行。实时进程的优先级为 1~99，值越大优先级越高。

调度算法：

​	SCHED_FIFO (先进先出的实时进程服务策略)：具有相同优先级的进程会按照它们变为就绪态的顺序获得 CPU。一个 SCHED_FIFO 进程会一直运行，直到它主动放弃 CPU（例如，通过系统调用进行休眠或等待 I/O）、被更高优先级的实时进程抢占或者运行结束。

​	SCHED_RR (基于时间片轮转的实时进程调度策略)：具有相同优先级的 SCHED_RR 进程会按照 FIFO 的方式获得 CPU，但在运行一段时间（时间片）后，即使进程没有主动放弃 CPU，也会被强制暂停，轮到下一个相同优先级的进程运行。这样可以保证相同优先级的实时进程都能得到执行机会，避免某个进程长时间占用 CPU。当所有相同优先级的进程都轮转过一次后，会重新开始下一轮。

​	SCHED_NORMAL (普通进程的调度策略)：为每个进程维护一个虚拟运行时间 vruntime 和优先级权重，优先级越高的进程，vruntime 增长速度越慢，在所有进程运行完尽可能接近的 vruntime 时，优先级越高的进程实际占用的 CPU 时间片会更长。目标延迟和最小粒度：保证所有进程在一定时间内至少运行一次，并且避免频繁的上下文切换 (即使时间片小于最小粒度，也会执行最小粒度的时间长度)。CFS 通过红黑树数据结构不断选择 vruntime 最小的进程来运行，努力实现 CPU 时间的公平分配。即使是低优先级的进程，最终也会获得运行的机会，从而避免了绝对的饥饿现象。只是高优先级的进程会更快地“赶上”它们的应得运行时间。

#### 堆/栈

##### 01 代码段/数据段/BSS段/堆/栈

##### 02 堆内存分配

##### 03 用户栈/内核栈

#### 并发与互斥

##### 01 并发与互斥的实现

##### 02 自旋锁/睡眠锁/信号量/读写锁

##### 03 死锁的产生/处理

##### 04 单核多线程是否需要锁

#### 上下文

##### 01 上下文信息

进程上下文 (用户态/内核态)，寄存器上下文，中断上下文

##### 02 上下文使用场景

##### 03 中断上下文注意事项

### 通用素质面试

思维能力，思考深度，逻辑表达能力

#### 个人

##### 01 排解压力的方式

骑车，看书，与亲人、朋友聊天

##### 02 对自己的评价，面试前的准备

总结自己，总结别人的面经，再反思自己

##### 03 最有成就感的事情

自己的能力展示

学习能力：中考、高考、保研

团队合作/沟通/协调：竞赛、论文

工作能力：项目的学习和完成

生活/运动能力：骑行绿道 100 km

最有成就感的事情：1 个月完成论文的 idea，仿真和攥写

##### 04 优点/缺点

对于自己的判断是否中肯，自信、自卑和自傲的倾向如何

抗压能力：

学习能力：

适应能力：团队沟通与协作，融入教研室并有所收获，工作上的努力学习和尝试。认识到在社会/国家/全球洪流中，个人的有限性；适应社会。薪资随项目的落实程度而变动

————————

长时间执着于一个问题：认识到自己的不足，并向别人学习请教；换一个角度思考问题，或许会得到新的角度 (刷题、项目问题)

运动不足：长时间待在教研室里，缺少运动；应该保证身体活力，精力充沛，高效率工作

工作地点的看法

##### 05 失败经历

项目难题：阅读基于 RISC 架构的类 UNIX xv6 操作系统各个子功能模块源码，较为复杂；具有写时复制特性的 fork() 系统调用，缺少了对于只有不可写入异常的处理导致进程卡死，gdb 调试包括 gcc -g, run + args, start, next, step, breakpoint, delete breakpoint, display, watch, continue, finish, quit。基于 IMX6ULL 开发板的相机驱动开发项目，select(), 摄像头模块坏了，printk(), USB 2.0 等等小的细节问题。

抗压能力：SCI 1 区论文被审稿人挑剔，过年期间修改论文和添加仿真，最后仍然以奇怪的理由拒绝了。在耗时一年后，最终在 SCI 2 区成功投递。总结教训：技术和心态

善于总结教训

##### 06 喜欢的工作？如何选择工作？如何看待工作待遇和条件？

技术过硬是一件很帅的事情，对于硬件设备抽象、驱动程序开发和计算机底层原理感兴趣，也希望在这个领域做出自己的贡献；

嵌入式智能产品的行业发展潜力：安防领域龙头企业，视频监控，同时也延申产品线到生产线、质检线、物流线等领域的智能嵌入式产品；具有视频监控等嵌入式产品的优势和核心能力，探索新的业务方向；坚持在具体的场景解决具体的难题；坚持高度重视和投入技术创新，坚持稳健经营；

学习能力和学习意愿：

自己的分析能力和自知力

##### 07 为什么选择公司？有哪些了解？为什么选择这个岗位？更喜欢什么样的公司

不能盲目求职，不能不了解公司

##### 08 喜欢什么样的领导和同事？

技术过硬，认真负责

为人和善，团结合作

不自傲/不自卑，谦逊，对新技术/新领域有着学习热情

喜欢什么样的人，自己也将成为什么样的人

##### 09 理想和抱负，如何实现？

分析能力和自知力，不能夸夸其谈

##### 10 面试通过的理由是什么？

自信而不狂妄

##### 11 成功的决定性因素？具备哪些？

崇拜的人：刘谦，宁诺夫斯基

##### 06 最近的工作/职责/在团队中的位置

对于自己工作的关注情况，是否了解工作重点

##### 07 工作成就

目前：在一个产品研发过程中做出自己的贡献，认识到自己的不足和自己的主观能动性

之后：作为产品研发的负责人，给公司交出满意的答卷，获得市场的认可。同时，考虑到为之后的学习者提供帮助。

编写出高质量、易于理解和维护的代码

自己的能力和对于工作成就的看法

##### 08 日常工作中处理的问题

归纳工作，对业务的熟练程度和关注度

##### 09 工作中的良好的建议和计划

对工作的改善能力

#### 公司

##### 01 对外界xx不好的传言的看法

否认，

对海康的了解

##### 02 对海康的了解

安防领域龙头企业，视频监控，同时也延申产品线到生产线、质检线、物流线等领域的智能嵌入式产品，

具有视频监控等嵌入式产品的优势和核心能力，探索新的业务方向；坚持在具体的场景解决具体的难题；坚持高度重视和投入技术创新，坚持稳健经营；

价值观：成就客户、价值为本 (公司)、诚信务实、追求卓越 (个人)

#### 可能的技术面

##### 01 项目中最大的难题，如何解决的

##### 02 接到一个项目，你会如何展开

认识到自己的不足和主观能动性，坚持不懈，面对困难，解决困难



### 反问

还有面试吗？培养流程？转正率？薪资？

# 公司面经



自我介绍：

姓名，学校，自我评价 (后介绍)，项目 (又不详细又不精简)，希望进入公司

xxx工程师深入了解，包括个人技能 (C, 操作系统，数据结构与算法)，插入实践项目，负责内容 (一句话)，丰富了自己的理解

希望能通过面试，进一步学习