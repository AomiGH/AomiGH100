# 03 C/C++编程语言

## C语言

### 基础原理

#### 01 如何学习 C++ 编程

首先，《C Primer Plus》学习基础语法、函数用法、基本数据类型、复杂数据类型等基础知识

其次，通过网上课程，学习 C++ 编程的特性及其原理

然后，结合数据结构与算法，在实际项目应用中熟练使用 C++ 编程语言

最后，C++ 编程是有用的工具，能够实现很多意想不到的效果，这也激励我继续学习。

#### 02 获取可执行文件

首先，预编译阶段，执行以 '#' 开头的预编译指令，包括宏定义、条件编译指令、头文件插入、删除注释等

其次，汇编阶段，将文件转化为汇编文件，进行词义、语义、语法纠正和代码优化

然后，编译阶段，将文件转化为机器语言

最后，进行静态库链接或者动态库链接。静态库链接将整个库文件复制到可执行文件中，不依赖外部库文件，具有更好的可移植性，运行速度快，但是可执行文件过大；动态库链接在文件执行时将库文件加载到内存中，并与程序建立连接，此时，多个程序可以共享同一个动态库，节省内存，并且可执行文件也更小；但是依赖于外部库文件，运行速度较慢。

#### 04 解释 main 函数

首先，main 函数是程序的入口地址，负责处理命令行参数

其次，main 函数的输入参数包括输入参数数量 argc 和字符串数组指针 argv，其中第一个参数是程序名称；

最后，main 函数的返回值向操作系统表示了程序的执行结果，'0' 表示正常允许，其余返回值表示程序异常，通常返回 errno

#### 05 解释函数调用

首先，将实参传递给形参，包括值传递、引用传递和指针传递，其中值传递中形参是实参的 copy，两者拥有不同的内存；引用传递中形参是实参的别名，两者执行同一块内存；指针传递中形参是实参的内存地址，可以解引用访问和修改实参数据。

其次，程序跳转到函数的入口地址，并分配栈区内存空间保存输入形参、局部变量和返回值；

然后，程序运行函数体，并返回值，同时释放已分配的栈区内存空间；

最后，程序跳转到函数的返回地址，继续执行剩余程序。

函数调用实现了功能的模块化，可以复用代码块，提高了程序的可读性。函数调用会进行输入参数类型检查，提高了程序的安全性。

#### 06 宏定义

首先，宏定义是预编译指令，用于将程序中的宏替换为相应文本，可用于简化代码、提高代码可读性；

其次，宏没有类型检查和作用域的概念。

#### 07 C 和 C++

首先，相对于 C，C++ 引入了类、对象、继承、多态等面向对象特性，强调了数据的封装和抽象；泛型编程支持用于多种数据类型的通用代码，无需指定具体类型。

其次，C 采用 malloc() 和 free() 来动态分配和释放内存，得到的 void * 类型内存需要转换为对应变量类型；而 C++ 引入了 new 和 delete 来直接分配和释放对应变量类型的内存，内存管理更加安全；

然后，C++ 的类型检查更加严格，并提供了显式类型转换运算符（static_cast\<float\> 可将其他基本数据类型转换为 float 类型，const_cast 可以将 const 对象转换为非 const 对象），减少了隐式类型转换的风险。

此外，C++ 还提供了大量数据类型，包括 vector、list、map、string等，并提供了封装的函数，以及 iostream 的流式输入输出，格式控制更加灵活，实用性和灵活性更强。

此外，C++ 支持函数重载，允许同一函数名根据输入参数类型的不同来区分；C++ 支持变量的引用，简化了代码

#### 08 内联函数

首先，inline 关键字用于声明和定义内联函数，不同于常规函数调用，内联函数在编译期间被展开，避免了函数调用时的参数传递、内存分配和销毁的开销，适用于函数体较小的函数。

#### 09 类型安全

首先，类型安全包括动态分配内存时的类型转换（malloc/new）、类型检查（函数调用、const 关键字、volatile 关键字）、宏定义不具有类型检查、显式数据类型转换（static_cast<>、const_cast）、引用类型避免空引用等。

#### 10 Debug/Release

首先，调试版本（Debug）包含调试信息，生成的程序文件比较大，包括可执行文件和调试信息文件；调试版本不进行代码优化，支持断点调试和逐步运行，便于我们发现和修改程序中存在的问题；

其次，发布版本（Release）不包含调试信息，并且会进行代码优化（速度优化和大小优化），生成的程序文件较小，运行速度更快，便于程序的交付和使用。

#### 11 面向对象特性

首先，封装是指类或者结构体会将成员变量和成员函数组合成一个独立单元，并用访问修饰符（public, protected, private）控制成员的访问权限，仅对外提供访问接口，而不允许直接修改私有成员。封装隐藏了数据的实现细节，并限制了数据的访问权限，提高了安全性；

其次，继承是指允许创建一个派生类来继承现有类（基类）的成员，并且允许派生类扩展或者修改基类的功能；

然后，多态是指允许使用同一接口来操作不同类型的对象（函数重载，同一函数名根据输入参数类型的不同来区分；基类的虚函数重写/覆盖，用同一个变量名来定义和存储不同类型的对象）；提高了代码的灵活性。

#### 12 继承

首先，继承是指允许创建一个派生类来继承现有类（基类）的成员，并且允许派生类扩展或者修改基类的功能；

其次，派生类对象和基类对象之间的类型转换包括向上转换和向下转换；

​	向上转换是派生类对象的指针或者引用转换为基类对象的指针或者引用，注意转换后基类对象只会复制属于基类的部分，而忽视派生类特有的部分，发生 "切片" 现象；

​	向下转换是基类对象的指针或者引用转换为派生类对象的指针或者引用，要求基类对象的指针或者引用指向的是派生类对象，并且使用 dynamic_cast() 进行显示转换，否则会报错；

#### 13 多态

首先，多态是指允许使用同一接口来操作不同类型的对象（函数重载，同一函数名根据输入参数类型的不同来区分；基类的虚函数重写/覆盖，用同一个变量名来定义和存储不同类型的对象）；提高了代码的灵活性。

其次，virtual 关键字声明的虚函数是指在运行时根据对象的实际类型来调用相应的函数，用于实现多态特性；例如，当基类的指针或者引用指向派生类对象时，调用虚函数会通过虚函数表来查找要调用的函数地址，实际执行的是派生类中函数重写后的版本；此外，派生类重写虚函数时，必须保证函数名、参数列表和返回值相互一致。

然后，虚函数不包括构造函数、静态成员函数、全局函数、内联函数等。

​	虚函数依赖于实际对象类型，而构造函数是负责函数的创建；

​	静态成员函数属于整个类，不依赖于任何对象的存在，并且没有 this 指针；在编译期间就确定了具体的函数调用；

​	虚函数必须是类的成员函数，而全局函数不属于任何类；

​	内联函数在编译期间被确认和展开，而虚函数在运行时根据实际对象类型来确认具体的函数调用。

静多态/动多态：函数重载和虚函数

#### 14 静态/动态绑定

首先，静态绑定是指在编译期间，编译器就确定了具体的函数调用，例如函数调用、函数重载，执行效率较高；

其次，动态绑定是指在运行时才能根据对象类型来决定具体的函数调用，例如基类的虚函数和派生类的函数重写，具有较好的灵活性；

#### 15 虚函数表的构建

当类中存在虚函数时，编译器会为该类生成虚函数表和虚函数表指针；

虚函数表指针在构造函数中赋值 (？？？)

父类中必须含有虚函数，子类中必须重写父类中的虚函数；父类指针指向子类对象，或者父类引用绑定子类对象；通过父类指针或者引用调用子类中重写的虚函数。

基类和派生类拥有不同的虚函数表和虚函数表指针。虚函数重写/覆盖是修改虚函数表中的函数指针。

### 数据类型

#### 01 const 关键字

首先，const 关键字用于定义常数，限制程序修改常量，提高了代码的安全性和可读性。

其次，const 关键字可用于修饰基本数据类型、指针、函数形参、类成员等。int const* p / const int* p 表示常数指针，指不能修改指针指向的内存空间；int *const p 表示指针常数，指不能修改指针的指向；const int *const p 表示既不能修改指针的指向，也不能修改指针指向的内存空间。const 修饰类的成员函数 int func() const {} 表示该函数内不会修改任何成员变量；const 修饰类的成员变量必须在函数构造初始化中初始化，在对象生命周期内不可修改。

然后，const 关键字在编译阶段进行类型检查，保证其不会被修改；并且 const 常量修饰的全局变量或者静态局部变量可能存储在只读内存区域（.rodata 段），而修饰的局部变量仍然存储在栈区中。

最后，const 关键字可以通过直接内存操作，例如 memcpy 方式强制修改。

#### 02 指针

首先，指针指向一个地址，可以指向空地址，其大小为地址所占字节大小，在传递参数时，节省内存空间

其次，指针还包括指针数组、数组指针、函数指针和多级指针。指针数组 int *p[10] / vector<int *> p = vector<int *>(10) 表示 p 是 int * 类型指针的数组，要为数组中的每个指针元素 p[i] 分配内存空间，并通过解引用访问数组元素 *p[i]；数组指针 int (\*p)[10] 表示 p 是指向 int 类型数组地址的指针，其中数组地址等同于数组首个元素的地址，但是不同于数组地址变量，数组指针 p 的指向是可以修改的；函数指针 int (\*p) (int) 表示 p 指向输入参数为 (int)、返回值为 (int) 的函数地址；多级指针 int **p 表示 p 指向指针 int * 类型变量的地址，常用于动态分配二维数组和作为函数参数传递，用于在函数内部修改指针指向，例如分配内存空间。

然后，野指针是指未初始化的指针；悬挂指针指向的内存空间被释放，也指向了无效内存地址；对于野指针和悬挂指针的访问是不安全的，会造成程序崩溃，应该将它们赋值为空指针；

然后，函数调用时使用指针传递参数的用法包括，需要修改指针变量的指向时，例如动态分配内存空间；允许传递空指针；传递数组地址；更好地兼容 C 语言代码，因为 C 语言中不包括引用。

#### 03 引用

首先，引用是原变量的别名，必须初始化，不可以为空，不可以改变引用的目标，sizeof() 返回的是原变量的大小

其次，函数调用时使用引用传递参数的用法包括，不需要解引用即可访问和修改原变量，可以简化代码；可以避免空引用；可用于重载运算符；避免了复杂数据类型的 copy，可以结合 const 关键避免对原变量的修改。

#### 04 typedef 关键字

首先，typedef 关键字为数据类型创建别名，可以安全地声明多个变量，提高代码的可读性和更容易维护。

其次，typedef 关键字在编译阶段处理。

#### 05 变量声明与定义

首先，变量声明告知编译器该变量的类型、名称和作用域，但并不分配内存空间，可以多次声明同一个变量，具有 extern 关键字用于声明全局变量；通常放于头文件；

其次，变量定义为变量分配内存空间，并可能初始化变量值；每个变量只可以定义一次，通常放于源文件。

然后，初始化和赋值都会赋予对象一个新值；针对基本数据类型，初始化和赋值作用相同；对于复杂数据类型，例如类、结构体，初始化仅在对象创建时调用构造函数中完成，包括了创建和赋值对象，而赋值仅将新值赋予对象，而不创建新对象。

#### 06 结构体

首先，struct 关键字用于声明结构体变量，其中包含不同数据类型的成员变量、成员函数，默认访问权限为 public；访问权限包括 public, protected, private，其中 public 成员可以被类内成员函数和类外代码访问，protected 可供类内和派生类的成员函数访问，private 仅限类内成员函数访问，对类外代码隐藏；

其次，结构体变量内存分配按照成员变量的顺序依次分配，结构体变量内存地址等于其第一个成员变量的地址；默认采用的内存对齐为按照最大成员变量类型的所占字节数来对齐各个成员变量的地址，例如 int/long long 类型是 4/8 字节；同时会采用内存填充，保证所有成员变量的起始地址符合对齐要求；

#### 07 类

首先，class 关键字用于定义类，包括访问修饰符、成员变量、成员函数、构造函数和析构函数，默认访问权限为 private，继承权限为 private

其次，class 类允许封装，即数据和操作数据的函数组合形成一个独立的单元，并通过访问修饰符控制成员变量的访问权限，隐藏数据实现细节；允许创建一个派生类，继承现有类的成员，并扩展、修改基类的功能；允许使用相同的接口来操作不同类型的对象；实现了模块化编程，提高了代码的可维护性和可扩展性。

然后，类的常用构造函数包括默认构造函数、拷贝构造函数、移动构造函数和初始化列表构造函数。

​	默认构造函数是没有参数或者所有参数都有默认值的构造函数；如果类中没有显式定义构造函数，编译器会自动采用默认构造函数；

​	拷贝构造函数使用现有对象拷贝创建一个新对象；如果类中没有显示定义拷贝构造函数，编译器会自动采用浅拷贝构造函数；

​	移动构造函数用于创建一个新对象时，它从另一个对象“移动”资源，而不是复制资源，用于临时对象或者即将被销毁的对象；Myclass obj2 = move(obj1); 其中 move() 不执行任何移动操作，只是将对象标记为可以被 "移动"

​	成员初始化列表是一种特殊语法，位于构造函数的参数列表之后，函数体之前，以 : 开头，后面跟着成员变量名和初始值，可以直接初始化成员变量，避免了类类型的成员变量先调用默认构造函数再赋值的额外开销；成员初始化列表是 const 和 引用类型成员变量初始化的唯一方法。

还有，类的成员变量和成员函数的内存分布。

​	成员变量按照声明顺序依次存储到内存空间，类对象地址等于首个成员变量的地址。为了提高内存访问效率，编译器会进行内存对齐（`#pragma pack(n)`）。而静态成员变量属于整个类，而不属于单个类对象，存储在 .bss 段。

​	成员函数存储在代码段，所有类对象共享同一份成员函数的代码；

​	空类占据一字节内存空间，因为 C++ 标准要求所有独立对象都要有唯一的地址；

​	有虚函数的类包含一个虚函数表指针，占据 4/8 字节；

​	静态成员变量存储在 .bss 段，不会影响类对象的大小；

#### 08 volatile 关键字

首先，volatile 关键字是类型修饰符，用于告知编译器不对该变量进行优化，每次访问该变量时都必须从内存中读取；mutable 关键字用于修饰类的非静态成员变量，即使在 const 修饰的成员函数中，也可以修改 mutable 变量；explicit 关键字用于修饰类的构造函数，禁止编译器进行隐式类型转换。

#### 09 static 关键字

首先，static 关键字修饰的变量的作用域仅限于当前文件中，避免了多文件之间的变量命名冲突；该变量存储在静态数据区（.bss 段），只在第一次初始化时分配内存，其生命周期贯穿整个程序运行期间；静态函数的作用域也限制在声明他们的文件内；

其次，在类中声明的 static 成员变量/成员函数在类外初始化，并且属于整个类，而不是类的某个特定对象（该类声明/定义的所有对象共享这个静态成员变量）；

然后，静态对象在编译期间进行内存分配，在第一次使用时调用构造函数初始化；

#### 10 memcpy

首先，memcpy() 函数用于任何类型的内存拷贝，需要指定数据长度，安全性较高；strcpy() 函数用于字符串拷贝，如果目标字符串的内存空间不足，会发生内存溢出，相比之下，需要指定数据长度的 strncpy() 函数更不容易出现内存溢出，但是需要手动加上停止符 '\0'；sprintf() 函数将格式化数据存入字符串中，源数据可以为任意数据类型；

#### 11 深拷贝/浅拷贝

首先，浅拷贝是指在对象复制时，只复制对象的数据成员的值，而不复制对象所指向的内存空间；即原对象和拷贝对象会共享同一块动态分配的内存；当原对象/拷贝对象被销毁时，可能会导致悬挂指针；赋值运算符默认执行浅拷贝；

其次，深拷贝在复制对象时，不仅复制对象的数据成员的值，还会为拷贝对象分配新的内存空间；需要自定义深拷贝构造函数和重载赋值运算符。

#### 12 string 类

首先，string 类是用于处理字符串的标准类，提供了很多适用封装函数，包括字符串的创建和初始化、字符串连接、字符串比较等，自动分配和释放内存空间，简化了字符串操作；

其次，char * 类型是指针变量，需要手动管理内存，也拥有标准库中的字符串操作函数，但是功能相对有限，并且内存操作和结束符 '\0' 使用比较麻烦和不安全。

#### 13 this 指针

首先，`this`指针是隐含的指针，指向调用成员函数的对象，在类的非静态成员函数中用于访问对象的成员变量和成员函数；`this`指针在调用成员函数时被创建，函数执行结束后被销毁；`this`作为隐含的指向调用成员函数的对象的指针参数来传入成员函数。

#### 14 一元操作符

`*ip += 1`是将`ip`指向的变量加1，相当于`++*ip`或`(*ip)++`。注意：一元操作符从右向左结合，所以`*ip++`相当于`*(ip++)`

#### 15 extern 关键字

首先，extern 关键字声明外部变量，可以跨文件访问同一个全局变量；extern 关键字声明外部函数；extern "C": C++ 调用 C 语言编写的函数

### 内存管理

#### 01 程序的内存起始地址

首先，Linux 系统中的可执行文件/程序可以没有后缀，或者用 .bin 表示二进制程序

其次，程序的内存起始地址取决于操作系统的内核管理单元。

最后，程序的内存空间范围包括，代码段、数据段、未初始化数据段、堆区（用 new/malloc 动态分配内存）、栈区（函数调用时的输入形参、局部变量和返回地址）、共享库映射区和内核空间。

​	代码段存储程序的指令代码，是只读的；

​	数据段存储已初始化的全局变量和静态变量，其生命周期贯穿程序的运行期间；

​	.bss 段存储未初始化的全局变量和静态变量，并且被自动初始化为 0；

​	堆区用于动态分配内存，包括 new, malloc() 等；堆区会维护一个记录空闲内存空间的链表。当申请内存时，系统遍历链表找到满足要求大小的空闲内存块并分配给对象。堆区空间较大且不连续，向高地址方向增长，适合分配大块内存空间。

​	栈区用于存储函数调用时的参数传递、局部变量和返回地址等；栈区使用一块固定大小的连续内存空间。当申请内存时，如果栈区内存空间足够大，就会直接分配内存空间。栈区空间较小并且连续，向低地址方向增长，常用于函数调用、中断处理等。此外，栈区的内存分配与释放是自动进行的。 

​	动态库映射区用于将动态链接库加载到程序内存空间中，从而调用库提供的功能；

#### 02 静态/动态内存分配

首先，静态分配的内存在编译时期进行，由编译器自动管理，通常位于数据段或者 .bss 段，其生命周期贯穿整个程序运行期间。例如，全局变量和静态局部变量的内存分配。效率较高但灵活性较差；

其次，动态分配的内存在运行时进行，由我们手动管理，通常位于堆区，其生命周期也由我们自行决定。例如，new 和 malloc() 函数，灵活性高，但容易出现内存泄漏和悬挂指针等问题。

然后，栈内存分配是由程序的函数调用控制的。函数开始执行时，会保存输入形参、局部变量和返回地址到栈空间；当函数返回时，会自动释放内存。

然后，new/delete 是 C++ 运算符，会返回指定类型的指针，类型检查安全，并且会调用构造函数和析构函数；拥有数组操作符 new[]/delete[]，可以动态管理数组内存，并逐个调用对象的构造/析构函数。而 malloc() / free() 是 C 标准库函数，返回 void * 类型指针，需要强制转换类型和手动指定分配内存大小，处理失败会返回空指针。

还有，在实现原理上，new 运算符首先调用底层的`operator new`函数来分配内存空间，再调用对象的构造函数来初始化对象；`delete`运算符先调用对象的析构函数来清理对象，然后调用`operator delete`函数来释放内存空间；`malloc()`函数的实现依赖于操作系统提供的内存管理单元。操作系统会将堆空间分成若干块来管理，`malloc()`函数让内存管理单元在堆区中找到符合要求大小的内存空间并分配给指针变量；当程序调用`free()`函数时，操作系统将已分配的内存重新标记为空闲，用于下次分配。

还有，`mmap()/munmap()`函数将文件或者设备内存空间映射到进程的内存空间，使得程序可以访问文件或者设备的内存数据。不同进程映射到同一块内存区域，可以实现进程间通信的数据共享。

还有，`mmap()`的 API 包括

​	`mmap()`原型函数，需要指定映射的起始地址（NULL表示由内核自动分配），映射的内存大小（以字节为单位），内存的访问权限，内存类型，指定要映射的文件描述符，文件偏移量。

```c++
void *mmap(void *addr, size_t length, int prot, int flags, int fd, off_t offset)；
```

​	`munmap()`函数需要指定映射的起始地址和内存大小。

#### 03 内存泄漏/越界

首先，内存泄漏是指程序中已动态分配的内存无法释放，造成系统资源浪费；例如 `new/malloc()`分配内存后没有使用`delete/free()`来释放；或者指针丢失，无法访问分配的内存空间。对于长期运行的程序，内存泄漏可能导致系统崩溃。

其次，内存越界是指程序访问了不属于其的内存空间，导致了非法访问；例如数组下标越界、指针操作错误等；非法访问内存空间，可能覆盖其他变量的数据导致程序逻辑错误，也存在安全问题；

然后，僵尸进程的产生导致进程资源没有被释放，如果存在过多的僵尸进程，则系统无法生成新进程导致系统崩溃，也是一种内存泄漏。

还有，如何检查内存泄漏：

​	通过 ps 命令查看进程内存状态，包括进程运行前的内存状态和运行时每隔一段时间查看内存状态，观察进程内存占用情况。

​	使用`mtrace()`和`muntrace()`将需要检查内存泄漏的代码块包含起来，观察日志文件，其中 "+" 表示程序在申请内存空间，"-" 表示程序在释放内存空间。

#### 04 栈的应用

首先，函数调用时，函数的传递参数、局部变量和返回地址存入栈区空间；当函数返回时，会清空相应的栈区空间；系统发生中断处理时，处理器会将程序上下文信息，包括寄存器数据和返回地址保存到栈中，再跳转到中断处理程序处执行；多任务操作系统中，会将当前任务的执行状态存入栈中，再切换到下一个任务继续执行；

#### 05 Flash/Ram

首先，Flash 存储器是非易失型存储器，断电后数据不会丢失；程序代码存储在 Flash 中，在程序运行时，代码从 Flash 中加载到 RAM 中执行。在嵌入式系统中，需要长期保存的数据也可以存储在 Flash 中，例如配置文件和校准数据；

其次，RAM 存储器是易失型存储器，断电后数据丢失；栈区通常存储于 RAM 中，可以实现快速的读写操作，用于存储临时数据。

内存分配策略？？？

### 结构体/枚举/位操作

### 函数/指针/内存管理



## C++语言

### C++基础

### 面向对象特性

### 模板/泛型/STL/新特性





# 题目练习

## 刷题方法和目标

代码随想录：涵盖大量的经典算法和解题思路，2遍（大概我需要什么速度呢，秋招6，7月前刷至少2遍，刷熟，）

labuladong的算法小抄：提供了清晰的算法思维导图和技巧

LeetCode精选100题：集中联系高频面试题，在3月实习前和6月提前批之前，刷3~4遍。

灵茶山艾府：LeetCode题解

10种排序算法（自己整理、实现代码）

up主做了400道，提交了1000次。

刷题建议：

理解每道题的核心算法和优化方法

尝试不同的解题思路，比较它们的时间和空间复杂度。（面试官提问：大家的解题思路都一样）

整理题解，分类记忆，定期复习，深化记忆。

烤鹅up主在后期的机考和手撕过程种发现，要是机考比较难，可能需要刷更多的题才能应对；机考简单，代码随想录的内容掌握也足够了；面试手撕在嵌入式相关岗位中不算常见，手撕题目一般是中等及以下题目。

在面试前整理对应公司的面经，练习其中的手撕题目。刷题不在多而在精！

### ACM

#### 01 a+b

```c++
#include <bits/stdc++.h>

using namespace std;

int main(void) {
    int a, b;
    /* 1. 读取任意行，直到 EOF */
    while (cin >> a >> b) {
        cout << a+b << endl;
    }
    /* 2. 读取 N 行 */
    while (cin >> N)
        for (int i = 0; i < N; i++) {
            cin >> a >> b;
            cout << a+b << endl;
        }
    /* 3. 结束标识 {0, 0} */
    while (cin >> a >> b) {
        if (a == 0 && b == 0) {
            break;
        }
        cout << a+b << endl;
    }
    /* 4. 读取 N 个数，N = 0 时结束 */
    while (cin >> N) {
        ans = 0;
        if (N == 0) {
            break;
        }
        while (N--) {
            cin >> a;
            ans += a;
        }
        cout << ans << endl;
    }
    /* 5. 输出 /n 分行 */
    while (cin >> a >> b) {
        cout << a+b << endl << endl;
    }
    /* 6. 综合 */
    while (cin >> N) {
        while (N--) {
            cin >> M;
            ans = 0;
            while (M--) {
                cin >> a;
                ans += a;
            }
            cout << ans << endl;
            if (N != 0) {
                cout << endl;
            }
        }
    }
    return 0;
}
```

#### 02 平均绩点

```c++
#include <bits/stdc++.h> 

using namespace std;

int main(void) {
    unordered_map<char, int> records;
    string str;
    bool isRight;
    float ans;
    int num;
    /* 哈希法记录键值对 */
    records.clear();
    records.insert({'A', 4});
    records.insert({'B', 3});
    records.insert({'C', 2});
    records.insert({'D', 1});
    records.insert({'F', 0});
    /* 读取 1 行字符串 */
    while (getline(cin, str)) {
        ans = 0;
        num = 0;
        isRight = true;
        /* 逐个处理字符串元素 */
        for (char c : str) {
            if (c == ' ' || c == '\n') {
                continue;
            }
            if (records.count(c) != 0) {
                ans += records[c];
                num++;
            }
            else {
                isRight = false;
            }
        }
        /* 输出 %.2f 精度 */
        if (isRight) {
            printf("%.2f\n", ans/num);
            continue;
        }
        cout << "Unknown" << endl;
    }
}
```

#### 03 摆平积木

```c++
#include <bits/stdc++.h> 

using namespace std;

int main(void) {
    int N;
    int sum;
    int meanNum;
    while (cin >> N) {
        if (N == 0) {
            break;
        }
        /* 读取 int 数组 */
        vector<int> nums = vector<int>(N, 0);
        sum = 0;
        for (int i = 0; i < N; i++) {
            cin >> nums[i];
            sum += nums[i];
        }
        meanNum = sum/N;
        sum = 0;
        /* 计算数组元素与均值的差 */
        for (int num : nums) {
            sum += abs(num - meanNum);
        }
        cout << sum/2 << endl << endl;
    }
    return 0;
}
```

#### 04 奇怪的信

```c++
#include <bits/stdc++.h>

using namespace std;

int main(void) {
    string str;
    int tmp;
    int ans;
    /* 1. 字符串法：取数字的各个位数 */
    while (getline(cin, str)) {
        ans = 0;
        for (char c : str) {
            tmp = (c - '0') % 2;
            if (tmp == 0) {
                ans += (c - '0');
            }
        }
        cout << ans << endl << endl;
    }
    /* 2. 取数字的各个位数 */
    while (cin >> n) {
        int result = 0;
        while (n != 0) {
            a = (n % 10);
            n = n / 10;
            if (a % 2 == 0) result += a;
        }
        cout << result << endl << endl;
    }
}
```

#### 05 句子缩写

```c++
#include <bits/stdc++.h>

using namespace std;

int main(void) {
    string str;
    string word;
    string ans;
    int N;
    cin >> N;
    getchar();  /* 忽略换行符 */
    while (N--) {
        getline(cin, str);
        /* stringstream 可以像处理输入输出流一样处理字符串，进行格式化输入输出 */
        stringstream ss = stringstream(str);
        ans = "";
        /* 1. 提取被多个空格分割的单词 */
        while (ss >> word) {
            if (!word.empty()) {
                /* 字母大小写转换 */
                if ('a' <= word[0] && word[0] <= 'z') {
                    ans += (word[0] - 32);
                }
                else {
                    ans += word[0];
                }
            }
        }
        /* 2. 提取被多个空格分割的单词 */
        for (int i = 1; i < s.size() - 1; i++) { // 单词之间可能有多个空格
            if (s[i] == ' ' && s[i + 1] != ' ') result += changeChar(s[i + 1]);
        }
        cout << ans << endl;
    }
    return 0;
}
```

#### 06 神秘字符

```c++
#include <bits/stdc++.h>

using namespace std;

int main(void) {
    string str1;
    string str2;
    string ans;
    int N;
    cin >> N;
    getchar();
    while (N--) {
        getline(cin, str1);
        getline(cin, str2);
        ans = "";
        /* 将 str1 分割为 2 个子串，字符串长度：str1.length() / str1.size() */
        ans += str1.substr(0, str1.length()/2);
        ans += str2;
        ans += str1.substr(str1.length()/2, str1.length()/2);
        cout << ans << endl;
    }
    return 0;
}
```

#### 07 链表的基本操作

```c++
#include <bits/stdc++.h>

using namespace std;

class DList {
private:
    struct DListNode {
        int val;
        DListNode* prev;
        DListNode* next;
        DListNode() : val(0), prev(nullptr), next(nullptr) {};
        DListNode(int _val) : val(_val), prev(nullptr), next(nullptr) {};
    };
    DListNode* head;
    DListNode* tail;
    int size;

public:
    DList() : size(0) {
        head = new DListNode();
        tail = new DListNode();
        head->next = tail;
        tail->prev = head;
    }

    int getVal(int idx) {
        DListNode* curNode = getNode(idx);
        if (curNode == nullptr) {
            return INT_MAX;
        }
        return curNode->val;
    }

    DListNode* getNode(int idx) {
        if (idx > size) {
            return nullptr;
        }
        DListNode* curNode = head;
        while (idx --) {
            curNode = curNode->next;
        }
        return curNode;
    }

    bool deleteNode(int idx) {
        if (idx > size) {
            return false;
        }
        DListNode* curNode = getNode(idx);
        curNode->prev->next = curNode->next;
        curNode->next->prev = curNode->prev;
        size--;
        delete(curNode);
        return true;
    }

    bool insertNode(int idx, int val) {
        if (idx > size + 1) {
            return false;
        }
        DListNode* newNode = new DListNode(val);
        /* 在头部插入节点 */
        if (idx == 1) {
            newNode->prev = head;
            newNode->next = head->next;
            head->next->prev = newNode;
            head->next = newNode;
            size++;
            return true;
        }
        /* 在尾部插入节点 */
        if (idx == size + 1) {
            newNode->prev = tail->prev;
            newNode->next = tail;
            tail->prev->next = newNode;
            tail->prev = newNode;
            size++;
            return true;
        }
        DListNode* curNode = getNode(idx);
        curNode->prev->next = newNode;
        newNode->prev = curNode->prev;
        newNode->next = curNode;
        curNode->prev = newNode;
        size++;
        return true;
    }

    void showList(void) {
        if (size == 0) {
            cout << "Link list is empty" << endl;
            return ;
        }
        DListNode* curNode = head->next;
        while (curNode != tail) {
            cout << curNode->val << " ";
            curNode = curNode->next;
        }
        cout << endl;
        return ;
    }
};

int main(void) {
    string str;
    string word;
    int N, M;
    int num;
    int idx;
    int val;
    DList* theList = new DList();
    cin >> N;
    while (N --) {
        /* 在链表头添加节点 */
        cin >> num;
        theList->insertNode(1, num);
    }
    cin >> M;
    getchar();  // 删除换行符
    while (M --) {
        getline(cin, str);
        stringstream ss = stringstream(str);
        ss >> word;
        if (word == "show") {
            theList->showList();
        }
        else if (word == "get") {
            ss >> idx;
            val = theList->getVal(idx);
            if (val == INT_MAX) {
                cout << "get fail" << endl;
            }
            else {
                cout << val << endl;
            }
        }
        else if (word == "insert") {
            ss >> idx >> val;
            if (theList->insertNode(idx, val)) {
                cout << "insert OK" << endl;
            }
            else {
                cout << "insert fail" << endl;
            }
        }
        else if (word == "delete") {
            ss >> idx;
            if (theList->deleteNode(idx)) {
                cout << "delete OK" << endl;
            }
            else {
                cout << "delete fail" << endl;
            }
        }
    }
    return 0;
}
```

#### 08 单链表翻转

```c++
#include <bits/stdc++.h>

using namespace std;

typedef struct ListNode {
    int val;
    ListNode* next;
    ListNode() : val(0), next(nullptr) {};
    ListNode(int _val) : val(_val), next(nullptr) {};
} ListNode;

/* 链表翻转：返回链表头 */
ListNode* reverseList(ListNode* head) {
    ListNode* preNode = head;
    ListNode* curNode;
    ListNode* tmpNode = head->next;
    /* tmpNode = curNode->next; 保存下一个待访问节点
     * curNode->next = preNode; 实现反转
     */
    while (tmpNode != nullptr) {
        curNode = tmpNode;
        tmpNode = curNode->next;
        curNode->next = preNode;
        preNode = curNode;
    }
    head->next->next = nullptr;
    head->next = curNode;
    return head;
}

void showList(ListNode* head) {
    ListNode* curNode = head->next;
    while (curNode != nullptr) {
        cout << curNode->val << " ";
        curNode = curNode->next;
    }
    cout << endl;

    return ;
}

void deleteList(ListNode* head) {
    ListNode* curNode = head->next;
    ListNode* tmpNode;
    while (curNode != nullptr) {
        tmpNode = curNode->next;
        delete(curNode);
        curNode = tmpNode;
    }

    return ;
}

int main(void) {
    int size, num;
    ListNode* fakeHead = new ListNode();
    while (cin >> size) {
        if (size == 0) {
            cout << "list is empty" << endl;
            continue;
        }
        /* 构造链表 */
        ListNode* curNode = fakeHead;
        while (size --) {
            cin >> num;
            ListNode* newNode = new ListNode(num);
            curNode->next = newNode;
            curNode = curNode->next;
        }
        showList(fakeHead);
        /* 翻转链表 */
        fakeHead = reverseList(fakeHead);
        /* 展示链表 */
        showList(fakeHead);
        /* 销毁链表 */
        deleteList(fakeHead);
    }
    return 0;
}
```



#### 09 二叉树遍历

```c++
/**
 * 1. 构建二叉树：节点数组 vector<TreeNode*> nodeArr;
 * 2. 二叉树遍历：空指针标记法
 */

#include <bits/stdc++.h>

using namespace std;

typedef struct TreeNode {
    char val;
    TreeNode* left;
    TreeNode* right;
    TreeNode() : val(0), left(nullptr), right(nullptr) {};
    TreeNode(char _val) : val(_val), left(nullptr), right(nullptr) {}
} TreeNode;

/* 节点数组 */
vector<TreeNode*> nodeArr;
vector<char> valArr;
vector<int> leftArr;
vector<int> rightArr;

/* 构建二叉树 */
TreeNode* buildTree(int size) {
    for (int i = 1; i <= size; i++) {
        nodeArr[i]->left = nodeArr[leftArr[i]];
        nodeArr[i]->right = nodeArr[rightArr[i]];
    }
    return nodeArr[1];
}

/* 前序遍历：中左右 */
vector<char> preOrderTravel(TreeNode* root) {
    stack<TreeNode*> stkTree;
    vector<char> ans;
    TreeNode* curNode;
    if (root == nullptr) {
        return ans;
    }
    stkTree.push(root);
    while (!stkTree.empty()) {
        curNode = stkTree.top();
        if (curNode != nullptr) {
            /* 取出中节点 */
            stkTree.pop();
            /* 压入右 */
            if (curNode->right != nullptr) {
                stkTree.push(curNode->right);
            }
            /* 压入左 */
            if (curNode->left != nullptr) {
                stkTree.push(curNode->left);
            }
            /* 再次压入中 */
            stkTree.push(curNode);
            stkTree.push(nullptr);
        }
        else {
            /* 弹出空指针 */
            stkTree.pop();
            /* 取出待访问节点 */
            curNode = stkTree.top();
            stkTree.pop();
            ans.push_back(curNode->val);
        }
    }
    return ans;
}

/* 中序遍历：左中右 */
vector<char> inOrderTravel(TreeNode* root) {
    stack<TreeNode*> stkTree;
    vector<char> ans;
    TreeNode* curNode;
    if (root == nullptr) {
        return ans;
    }
    stkTree.push(root);
    while (!stkTree.empty()) {
        curNode = stkTree.top();
        if (curNode != nullptr) {
            /* 取出中节点 */
            stkTree.pop();
            /* 压入右 */
            if (curNode->right != nullptr) {
                stkTree.push(curNode->right);
            }
            /* 再次压入中 */
            stkTree.push(curNode);
            stkTree.push(nullptr);
            /* 压入左 */
            if (curNode->left != nullptr) {
                stkTree.push(curNode->left);
            }
        }
        else {
            /* 弹出空指针 */
            stkTree.pop();
            /* 取出待访问节点 */
            curNode = stkTree.top();
            stkTree.pop();
            ans.push_back(curNode->val);
        }
    }
    return ans;
}

/* 后序遍历：左右中 */
vector<char> postOrderTravel(TreeNode* root) {
    stack<TreeNode*> stkTree;
    vector<char> ans;
    TreeNode* curNode;
    if (root == nullptr) {
        return ans;
    }
    stkTree.push(root);
    while (!stkTree.empty()) {
        curNode = stkTree.top();
        if (curNode != nullptr) {
            /* 取出中节点 */
            stkTree.pop();
            /* 再次压入中 */
            stkTree.push(curNode);
            stkTree.push(nullptr);
            /* 压入右 */
            if (curNode->right != nullptr) {
                stkTree.push(curNode->right);
            }
            /* 压入左 */
            if (curNode->left != nullptr) {
                stkTree.push(curNode->left);
            }
        }
        else {
            /* 弹出空指针 */
            stkTree.pop();
            /* 取出待访问节点 */
            curNode = stkTree.top();
            stkTree.pop();
            ans.push_back(curNode->val);
        }
    }
    return ans;
}

/* 打印 char 数组 */
void showAns(vector<char> ans) {
    for (int i = 0; i < ans.size(); ++i) {
        cout << ans[i];
    }
    cout << endl;
    return ;
}

int main(void) {
    vector<char> ans;
    char val;
    int size;
    int leftIdx;
    int rightIdx;
    cin >> size;
    nodeArr  = vector<TreeNode*>(size+1, nullptr);
    leftArr  = vector<int>(size+1, 0);
    rightArr = vector<int>(size+1, 0);
    valArr   = vector<char>(size+1, '0');
    for (int i = 1; i <= size; i++) {
        cin >> valArr[i] >> leftArr[i] >> rightArr[i];
        nodeArr[i] = new TreeNode(valArr[i]);
    }
    /* 构建二叉树 */
    TreeNode* root = buildTree(size);
    /* 前序遍历 */
    ans.clear();
    ans = preOrderTravel(root);
    showAns(ans);
    /* 中序遍历 */
    ans.clear();
    ans = inOrderTravel(root);
    showAns(ans);
    /* 后序遍历 */
    ans.clear();
    ans = postOrderTravel(root);
    showAns(ans);

    return 0;
}
```





## 排序算法

### 01 选择排序

```c++
/* 选择排序：
 *	0. 时间复杂度 O(N*N), 空间复杂度 O(1)
 *	1. 在未排序序列中找到最小（最大）元素，与未排序序列的起始位置交换
 *	2. 第 i = 0~N-1 次遍历，未排序序列从 i 开始
 *	3. 直到所有元素排序完成。
 */
void selectionSort(vector<int> &nums) {
    int minIdx;
    for (int i = 0; i < nums.size() - 1; i++) {
        minIdx = i;
        for (int j = i+1; j < nums.size(); j++) {
            /* 更新最小值下标 */
            if (nums[minIdx] > nums[j]) {
                minIdx = j;
            }
        }
        swap(nums[i], nums[minIdx]);
    }
}
```



### 02 冒泡排序

```c++
/* 冒泡排序：
 *	0. 时间复杂度: O(N*N), 空间复杂度: O(1)
 *	1. 遍历数组，比较两个相邻元素，并根据大小交换位置
 *	2. 第 i 次遍历数组，遍历长度为 nums.size() - (i+1)，即每次会确定最后一位元素的位置
 *	3. 直到不再交换元素
 */
void bubbleSort(vector<int> &nums) {
    bool isSwap;
    int temp;
    for (int i = 1; i < nums.size(); i++) {
        isSwap = false;
        for (int j = 0; j < (nums.size() - (i+1)); j++) {
            if (nums[j] > nums[j+1]) {
                /* swap(nums[j], nums[j+1]); */
                temp 	  = nums[j];
                nums[j]   = nums[j+1];
                nums[j+1] = temp;
                isSwap = true;
            }
        }
        if (isSwap) break;
    }
}
```



### 03 插入排序

```c++
/* 插入排序：
 *	0. 时间复杂度：O(N*N), 空间复杂度：O(1)
 *	1. 第一个元素默认已排序
 *	2. 选择下一个元素，从后往前遍历"已排序数组"，将该元素插入到合适位置
 		"已排序数组"的元素大于该元素，则"已排序数组"的元素后移
 		否则，将该元素放置到当前位置；
 *	3. 遍历数组所有元素，直到全部插入完成。
 */
void insertionSort(vector<int> &nums) {
    for (int i = 0, j, tmp; i < nums.size(); i++) {
        tmp = nums[i+1];
        j = i;
        while (tmp < nums[j] && j >= 0) {
            nums[j+1] = nums[j];	/* "已排序数组"后移一位 */
            j--;
        }
        nums[j+1] = tmp;
    }
}
```



### 04 快速排序

``` c++
/* 快速排序：
 *	0. 时间复杂度：O(N*logN)/O(N*N)，空间复杂度：O(logN) 栈空间
 *	1. 分割：选择基准 pivot = nums[right]，将所有小于 pivot 的移到 pivot 左边，大于 pivot 的元素交换到 pivot 右边，最后将 pivot 放置到最后的 smallIdx 位置，即从 pivot 处分割成两个子数组
 *	2. 分治：再继续分割并排序两个子数组，直到分割后的子数组大小为 0/1。
 */
int partition(vector<int> &nums, int left, int right) {
    int pivot    = nums[right];
    int smallIdx = left;
    int bigIdx;
    for (bigIdx = left; bigIdx < right; bigIdx++) {
        if (nums[bigIdx] < pivot) {
            swap(nums[bigIdx], nums[smallIdx]);
            smallIdx++;
        }
    }
    swap(nums[smallIdx], nums[right]);
    return smallIdx;
}

void quickSort(vector<int> &nums, int left, int right) {
    if (left < right) {
        int mid = partition(nums, left, right);
        quickSort(nums, left, mid-1);
        quickSort(nums, mid+1, right);
    }
}

void qSort(vector<int> &nums) {
    quickSort(nums, 0, nums.size()-1);
}
```



### 05 归并排序

```c++
/* 归并排序：
 *	0. 时间复杂度：O(N*logN), 空间复杂度：O(N)
 *	1. 用新申请的数组空间 T 来存放合并后的数组，然后将 T 更新到原数组 nums 上
 *	2. 将数组分割为左右区间 [left, mid) 和 [mid, right)，并且设定两个指针 startIdx1 = left, startIdx2 = mid，最初位置分别为两个"已排序数组"（已排序的左右区间）的起始位置；
 		将数组分割为左右区间，直到两个子数组都只包含 1 个元素时，两个子数组一定是"已排序数组"，开始合并。
 *	3. 比较两个指针所指向的元素，选择相对较小的元素放入到合并空间，并移动指针指向下一位置；重复操作，直到某一指针到达子数组尾部 endIdx1/endIdx2；
 *	4. 将另一个子数组的所有元素直接复制到合并数组 T 的尾部；然后将 T 更新到原数组 nums 上。
 */
void mergeSort(vector<int> &nums, vector<int> &T, int left, int right) {
    if (right - left == 1) { /* 该子数组都只有 1 个元素，一定是"已排序数组" */
        return ;
    }
    int mid = right + left >> 1;
    mergeSort(nums, T, left, mid);	/* 子数组：左区间[left, mid) */
    mergeSort(nums, T, mid, right);	/* 子数组：右区间[mid, right) */
    /* 合并两个子数组，即合并左区间 [left, mid) 和右区间 [mid, right) */
    int startIdx1 = left, endIdx1 = mid;
    int startIdx2 = mid, endIdx2 = right;
    int TIdx = left;
    while (startIdx1 < endIdx1 || startIdx2 < endIdx2) {
        /* 选择子数组 1 放入到合并空间 T 的必要条件 */
        if (startIdx2 >= endIdx2 || (startIdx1 < endIdx1 && nums[startIdx1] < nums[startIdx2])) {
            T[TIdx++] = nums[startIdx1++];
        }
        else {
            T[TIdx++] = nums[startIdx2++];
        }
    }
    /* 将合并数组 T 更新到原数组 nums 中 */
    for (int i = left; i < right; i++) {
        nums[i] = T[i];
    }
    return ;
}
void mSort(vector<int> &nums) {
    vector<int> T = vector<int>(nums.size());
    mergeSort(nums, T, 0, nums.size());
}
```

### 06 堆排序

### 07 桶排序

### 08 计数排序

### 09 基数排序





## 数组

### 01 移除元素

```c++
/* 移除元素：
 *	0. 类似于快排中的 partition，将小于 pivol 的元素放到左区间，大于 pivol 的元素放到右区间，将 pivol 放到中间
 *	1. 将不等于 val 的元素保留在前 k 位，k 为元素数量
 */
class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        /* 移除元素：将不等于 val 的元素保留在前 k 位，k 为元素数量 */
        int slowIdx;
        int fastIdx;

        slowIdx = 0;
        fastIdx = 0;
        for ( ; fastIdx < nums.size(); ++fastIdx) {
            if (nums[fastIdx] != val) {
                swap(nums[fastIdx], nums[slowIdx]);
                ++slowIdx;
            }
        }

        return slowIdx;
    }
};
```

### 02 合并两个有序数组

```c++
/* 合并两个有序数组：
 *	0. 类似于归并排序中的 merge
 */
class Solution {
public:
    void merge(vector<int>& nums1, int m, vector<int>& nums2, int n) {
        vector<int> ans = vector<int>((m+n), 0);
        int startIdx1 = 0;
        int endIdx1 = m;
        int startIdx2 = 0;
        int endIdx2 = n;
        int ansIdx = 0;
        while (startIdx1 < endIdx1 || startIdx2 < endIdx2) {
            if ((startIdx2 >= endIdx2) || (startIdx1 < endIdx1 && nums1[startIdx1] < nums2[startIdx2])) {
                ans[ansIdx++] = nums1[startIdx1++];
            }
            else {
                ans[ansIdx++] = nums2[startIdx2++];
            }
        }
        for (int i = 0; i < (m+n); i++) {
            nums1[i] = ans[i];
        }
        return ;
    }
};
```

### 03 查找共用字符

```c++
/* 查找共用字符：
 *	1. 二维数组
 *	2. 将 i+'a' 转换为 string 添加到 vector<string> 中
 */
class Solution {
public:
    vector<string> commonChars(vector<string>& words) {
        /* 查找共用字符：二维数组 */
        vector<vector<int>> records = vector<vector<int>>(words.size(), vector<int>(26, 0));
        vector<int> minRecords = vector<int>(26, INT_MAX);
        vector<string> ans;
        string word;

        /* 记录字符出现次数 */
        for (int i = 0; i < words.size(); ++i) {
            word = words[i];
            for (char ch : word) {
                ++records[i][ch - 'a'];
            }
        }

        /* 记录每个字符出现的最小次数 */
        for (int i = 0; i < words.size(); ++i) {
            for (int j = 0; j < 26; ++j) {
                minRecords[j] = min(minRecords[j], records[i][j]);
            }
        }

        /* 输出答案 */
        for (int j = 0; j < 26; ++j) {
            word = "";
            word.push_back('a'+j);
            for (int i = 0; i < minRecords[j]; ++i) {
                ans.push_back(word);
            }
        }

        return ans;
    }
};
```

### 04 最长连续递增序列

```c++
/* 最长连续递增子序列：
 *	0. 记录最大值的地点：要在遍历结束后再次更新最大值，防止遗漏
 *	1. 贪心，如果非递增则重新开始计数，中途保存最大值
 */
class Solution {
public:
    int findLengthOfLCIS(vector<int>& nums) {
        /* 最长连续递增子序列：贪心，如果非递增则重新开始计数，中途保存最大值 */
        int ans = 1;
        int curLen = 1;
        for (int i = 1; i < nums.size(); ++i) {
            if (nums[i-1] < nums[i]) {  // 最长连续递增子序列 +1
                ++curLen;
            }
            else {  // 记录最大值，重新开始计数
                ans = max(ans, curLen);
                curLen = 1;
            }
        }
        ans = max(ans, curLen);
        return ans;
    }
};
```



### 二分查找

```c
#include <stdio.h>
/* 1. 数组排序: 快排, compare函数 */
qsort(arr, arrSize, sizeof(int), compare);
int compare(const void* a, const void* b) {
	return (*(int*)a - *(int*)b); // 升序排序
}
/* 2. 二分查找: left <= right */
for (int left = 0, right = arrSize - 1, middle = 0; right >= left;) {
	middle = (left + right) / 2;
	if (arr[middle] == target) {
		return middle;
	}
	else if (arr[middle] > target) {
		right = middle - 1;
	}
	else {
		left = left + 1;
	}
}
return -1;
```

### 二分查找最小绝对值（有序数组的平方）

```c
/**
 * 注意：二分查找取闭环[left, middle]或者[middle, right]，可能出现最后right - left = 1，而不是两者相等。注意二分查找循环判断条件
 * 注意：for()循环条件是进入条件，而不是跳出条件。
 * 注意：此题简单方法：双向（左右）指针法，先填写最大值。我寻找最小绝对值，是从最小值开始填写的，麻烦很多。
 * 注意：时间复杂度：暴力O(n*n)，双向指针法O(n)
 */
int* sortedSquares(int* nums, int numsSize, int* returnSize) {
    *returnSize = numsSize;
    /* 1. 二分查找绝对值最小的数的下标： */
    /* 双向指针法 */
    int left = 0, right = numsSize-1, middle = 0;
    int min_idx;
    for ( ; !(right - left <= 1); ) {
        middle = (right + left) / 2;
        if (nums[middle] < 0) {  // 则取区间[middle, right]
            left = middle;
        }
        else {  // 则取区间[left, middle]
            right = middle;
        }
    }
    min_idx = left;
    if (right - left == 1) {
        if (nums[left] * nums[left] < nums[right] * nums[right]) {
            min_idx = left;
        }
        else {
            min_idx = right;
        }
    }

    /* 2. 从上述下标开始遍历数组，按照平方值排序： */
    /* 双向指针法 */
    int *ans = (int *)malloc(numsSize * sizeof(int));
    int new_idx = 0;
    int left_num = 0;
    int right_num = 0;
    ans[new_idx++] = nums[min_idx] * nums[min_idx];
    for (left = min_idx - 1, right = min_idx + 1; !(left < 0 && right > numsSize - 1); ) {
        if (left >= 0 && right <= numsSize - 1) {
            left_num = nums[left] * nums[left];
            right_num = nums[right] * nums[right];
            if (left_num < right_num) {
                ans[new_idx++] = left_num;
                left--;  // left继续左移，直到起始
            }
            else {
                ans[new_idx++] = right_num;
                right++;  // right继续右移，直到末尾
            }
        }
        if (left < 0) {  // left已读取完毕
            right_num = nums[right] * nums[right];
            ans[new_idx++] = right_num;
            right++;  // right继续右移，直到末尾
        }
        else if (right > numsSize - 1) {  // right已读取完毕
            left_num = nums[left] * nums[left];
            ans[new_idx++] = left_num;
            left--;  // left继续左移，直到起始
        }
    }
    return ans;
}
```

### 滑动窗口子数组：满足条件的长度最小的子数组

```c
/**
 * 注意：滑动窗口根据当前子数组是否满足条件来决定起始、终止位置的移动，达到遍历可能满足条件的子数组的目的。
 * 注意：时间复杂度：暴力O(n*n)，滑动窗口O(n)
 */
int minSubArrayLen(int target, int* nums, int numsSize) {
    /* 总和大于等于target的长度最小子数组 */
    /* 快慢指针法 */
    int arrMinLen = numsSize + 1;
    for (int slow_idx = 0, fast_idx = 0, subArrSum = 0, tmplen = 0; !(fast_idx >= numsSize); ) {
        /* fast_idx右移，直到子数组满足条件 */
        subArrSum += nums[fast_idx++];
        while (subArrSum >= target) {
            tmplen = fast_idx - slow_idx;
            arrMinLen = (arrMinLen > tmplen)? tmplen: arrMinLen;
            
            /* slow_idx右移，继续寻找子数组 */
            subArrSum -= nums[slow_idx++];
        }
    }
    if (arrMinLen > numsSize) {
        return 0;
    }
    return arrMinLen;
}
```

### 螺旋矩阵

```c
/**
 * 注意：*returnSize和**returnColumnSizes
 * 注意：螺旋：移动顺序为右下左上，并且每螺旋一圈，移动边界要缩小
 * 注意：n = 1很特殊（进不去循环）
 * 注意：看不懂题解。。
 * 注意：模拟类题目，题目的解决算法都是简洁、有原则性的。
 */
int** generateMatrix(int n, int* returnSize, int** returnColumnSizes) {
    /* 螺旋数组 */
    /* 螺旋：移动顺序为右下左上 */
    *returnSize = n;
    *returnColumnSizes = (int *)malloc(n * sizeof(int));
    int **ans = (int **)malloc(n * sizeof(int *));
    for (int i = 0; i < n; i++) {
        ans[i] = (int *)malloc(n * sizeof(int));
        (*returnColumnSizes)[i] = n;  // 每一列的元素数
    }
    /* 定义方向： */
    int movX[4] = {1, 0, -1, 0};
    int movY[4] = {0, 1, 0, -1};

    for (int idx = 1, mov = 0, idx_x = 0, idx_y = 0, new_idx_x = 0, new_idx_y = 0, circle = 0; (idx < n*n); mov++) {
        if (mov == 4) {  // 新的一圈，边界要缩小
            mov = mov % 4;  // mov: 0~3
            circle++;
        }
        while (1) {
            ans[idx_y][idx_x] = idx;
            /* 移动 */
            new_idx_x = idx_x + movX[mov];
            new_idx_y = idx_y + movY[mov];
            /* 四个方向的边界条件 */
            /* 右 */
            if (mov == 0 && new_idx_x >= n - circle) {
                break;
            }
            /* 下 */
            if (mov == 1 && new_idx_y >= n - circle) {
                break;
            }
            /* 左 */
            if (mov == 2 && new_idx_x < 0 + circle) {
                break;
            }
            /* 上 */
            if (mov == 3 && new_idx_y < 1 + circle) {
                break;
            }
            idx_x = new_idx_x;
            idx_y = new_idx_y;
            idx++;
        }
    }
    if (n == 1) {
        ans[0][0] = 1;
    }
    return ans;
}
```

### 区间和

```c
/**
 * 注意：练习ACM输入输出模式（笔试面试必备），scanf()读取直至文件末尾
 * 注意：时间复杂度：暴力O(n*m)，前缀和O(m)
 */
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int numsSize;
    scanf("%d", &numsSize);
    int *nums = (int *)malloc(sizeof(int) * (numsSize+1));
    nums[0] = 0;  // 第一个存零，数据从第二个位置开始保存，这样第一个数据就不特殊了
    for (int i = 1; i <= numsSize; i++) {
        scanf("%d", &nums[i]);
        /* 前缀和：数组序列连续，某区间等于大区间减去小区间 */
        /* sums[i+1] = nums[i+1] + sums[i] */
        nums[i] = nums[i] + nums[i-1];
    }
    /* 读取询问直到文件结束 */
    int left, right;
    /* scanf()返回成功匹配和赋值的个数 */
    while (scanf("%d %d", &left, &right) == 2) {
        printf("%d\n", nums[right+1] - nums[left]);
    }
    
    free(nums);
    
    return 0;
}
```

### 开发商购买土地

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>

int main(void) {
    int arrCol, arrRow;
    scanf("%d %d", &arrRow, &arrCol);
    /* 分配二维数组空间 */
    int ** nums = (int **)malloc(sizeof(int *) * (arrRow + 1));
    for (int i = 0; i <= arrRow; i++) {
        nums[i] = (int *)malloc(sizeof(int) * (arrCol + 1));
        /* 设置数组清零 */
        memset(nums[i], 0, sizeof(int) * (arrCol + 1));
    }
    /* 读取矩阵权重：ix是列坐标，iy是横坐标 */
    for (int iy = 1; iy <= arrRow; iy++) {
        for (int ix = 1; ix <= arrCol; ix++) {
            scanf("%d", &nums[iy][ix]);
            /* 计算前缀和 */
            nums[iy][ix] = nums[iy][ix-1] + (nums[iy-1][ix] - nums[iy-1][ix-1]) + nums[iy][ix];
        }
    }
    /* 遍历所有横向分割方法 */
    int minDiff = INT_MAX;
    for (int iy = 1, diff = 0, sumA = 0, sumB = 0; iy < arrRow; iy++) {
        sumA = nums[iy][arrCol];
        sumB = nums[arrRow][arrCol] - sumA;
        diff = abs(sumB - sumA);
        minDiff = (minDiff < diff)? minDiff: diff;
    }
    /* 遍历纵向 */
    for (int ix = 1, diff = 0, sumA = 0, sumB = 0; ix < arrCol; ix++) {
        sumA = nums[arrRow][ix];
        sumB = nums[arrRow][arrCol] - sumA;
        diff = abs(sumB - sumA);
        minDiff = (minDiff < diff)? minDiff: diff;
    }
    printf("%d", minDiff);
    
    for (int i = 0; i <= arrRow; i++) {
        free(nums[i]);
    }
    free(nums);
}
```

## 链表

### 移除重复节点

```c++
/* 移除重复节点：
 *	1. 空链表
 *	2. unordered_set<int> 记录节点是否重复出现
 *	3. 删除节点/遍历下一个节点，使用 curNode, preNode;
 		delete curNode;
 */
class Solution {
public:
    ListNode* removeDuplicateNodes(ListNode* head) {
        /* 移除重复节点 */
        unordered_set<int> records;
        ListNode* curNode = nullptr;
        ListNode* preNode = nullptr;
        
        /* 空链表 */
        if (head == NULL) {
            return head;
        }

        records.clear();
        records.insert(head->val);
        curNode = head->next;
        preNode = head;
        while (curNode != nullptr) {
            /* 查找该节点是否重复出现 */
            if (records.find(curNode->val) != records.end()) {
                /* 删除节点 */
                preNode->next = curNode->next;
                delete curNode;
                curNode = preNode->next;
            }
            else {
                /* 记录该节点 */
                records.insert(curNode->val);
                /* 遍历下一个节点 */
                preNode = curNode;
                curNode = curNode->next;
            }
        }
        records.clear();
        return head;
    }
};
```

### 链表倒数第 N 个节点

```c++
/* 链表的倒数第 N 个节点：快慢指针法
 *	1. 快指针先移动 N 个节点
 *	2. 快慢指针同时逐步移动，直到快指针遇到 NULL，慢指针即为倒数第 N 个节点
 */
class Solution {
public:
    ListNode* trainingPlan(ListNode* head, int cnt) {
        /* 链表的倒数第 N 个节点：快慢指针法 */
        ListNode* fastNode = nullptr;
        ListNode* slowNode = nullptr;
        
        /* 快指针先移动 N 个节点 */
        fastNode = head;
        slowNode = head;
        for (int i = 0; i < cnt; i++) {
            if (fastNode == NULL) {
                return NULL;
            }
            fastNode = fastNode->next;
        }
        /* 快慢指针同时逐步移动，直到快指针遇到 NULL，慢指针即为倒数第 N 个节点 */
        while (fastNode != NULL) {
            slowNode = slowNode->next;
            fastNode = fastNode->next;
        }
        return slowNode;
    }
};
```

### 链表的中间节点

```c++
/* 链表的中间节点：快慢指针法
 *	1. 慢指针移动一次，快指针移动两次，直到快指针遇到 NULL
 *	2. 结束条件：偶数节点时，有两个中间节点
 		取第一个中间节点：fast != NULL && fast->next != NULL && fast->next->next != NULL
 		取第二个中间节点：fast != NULL && fast->next != NULL
 */
class Solution {
public:
    ListNode* middleNode(ListNode* head) {
        /* 链表的中间节点：快慢指针法 */
        ListNode* slowNode = head;
        ListNode* fastNode = head;
        /* 慢指针移动一次，快指针移动两次，直到快指针遇到 NULL */
        while (fastNode != NULL && fastNode->next != NULL) {
            slowNode = slowNode->next;
            fastNode = fastNode->next->next;
        }
        return slowNode;
    }
};
```



### 删除节点

```c
/**
 * 注意：释放链表节点内存
 * 注意：指针指向的内存地址和指针本身所处地址的区别与联系
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
struct ListNode* removeElements(struct ListNode* head, int val) {
    /* 删除链表节点 */
    typedef struct ListNode ListNode;
    /* 生成虚拟链表头 */
    ListNode *newHead = (ListNode *)malloc(sizeof(ListNode));
    newHead->next = head;
    /* 此时，curNode和newHead指向同一个内存地址，但是改变curNode指向的内存地址，不会影响newHead中存储的内存地址 */
    ListNode *curNode = newHead;
    ListNode *nextNode;
    while (curNode->next != NULL) {
        nextNode = curNode->next;
        if (nextNode->val == val) {
            curNode->next = nextNode->next;
            free(nextNode);
        }
        else {
            curNode = curNode->next;
        }
    }
    head = newHead->next;
    free(newHead);
    return head;
}
```

### 创建链表

```c
/**
 * 注意：C语言有"类"概念？？这版代码的运行超乎我的想象了。
 */
typedef struct Node {
    int val;
    struct Node *next;
} Node;


typedef struct {
    int size;
    Node *data;
} MyLinkedList;


MyLinkedList* myLinkedListCreate() {
    MyLinkedList *obj = (MyLinkedList *)malloc(sizeof(MyLinkedList));
    /* obj->data = head是虚拟链表头 */
    Node *head = (Node *)malloc(sizeof(Node));
    head->next = NULL;
    obj->size = 0;
    obj->data = head;
    return obj;
}

int myLinkedListGet(MyLinkedList* obj, int index) {
    /* 寻找第i个节点 */
    if (index < 0 || index >= obj->size) {
        return -1;
    }
    /* 真实链表头 */
    Node *curNode = obj->data->next;
    for (int i = 0; i < index; i++) {
        if (curNode->next == NULL) {
            return -1;
        }
        curNode = curNode->next;
    }
    return curNode->val;
}

void myLinkedListAddAtHead(MyLinkedList* obj, int val) {
    /* 插入新的真实链表头 */
    Node *newNode = (Node *)malloc(sizeof(Node));
    newNode->val = val;
    newNode->next = obj->data->next;
    /* 虚拟链表头指向新的真实链表头 */
    obj->data->next = newNode;
    obj->size++;
}

void myLinkedListAddAtTail(MyLinkedList* obj, int val) {
    /* 插入最后一个节点 */
    /* 1. 创建新节点 */
    Node *newNode = (Node *)malloc(sizeof(Node));
    newNode->val  = val;
    newNode->next = NULL;
    /* 2. 遍历链表 */
    Node *headNode = obj->data;
    while(headNode->next != NULL) {
        headNode = headNode->next;
    }
    headNode->next = newNode;
    obj->size++;
}

void myLinkedListAddAtIndex(MyLinkedList* obj, int index, int val) {
    /* 在第i个节点前添加节点 */
    if (index > obj->size) return;
    /* 1. 创建新节点 */
    Node *newNode = (Node *)malloc(sizeof(Node));
    newNode->val = val;
    /* 2. 遍历链表 */
    Node *headNode = obj->data;
    while (index-- > 0) {
        headNode = headNode->next;
    }
    newNode->next = headNode->next;
    headNode->next = newNode;
    obj->size++;
}

void myLinkedListDeleteAtIndex(MyLinkedList* obj, int index) {
    /* 删除第i个节点 */
    if (index < 0 || index >= obj->size) return;
    /* 1. 遍历链表:找到第index-1个节点 */
    Node *headNode = obj->data;
    Node *tmpNode;
    while (index-- > 0) {
        headNode = headNode->next;
    }
    tmpNode = headNode->next;
    headNode->next = tmpNode->next;
    free(tmpNode);
    obj->size--;
}

void myLinkedListFree(MyLinkedList* obj) {
    /* 释放整个链表 */
    Node *headNode = obj->data;
    Node *tmpNode;
    while (headNode != NULL) {
        tmpNode = headNode;
        headNode = headNode->next;
        free(tmpNode);
    }
    free(obj);
}

/**
 * Your MyLinkedList struct will be instantiated and called as such:
 * MyLinkedList* obj = myLinkedListCreate();
 * int param_1 = myLinkedListGet(obj, index);
 
 * myLinkedListAddAtHead(obj, val);
 
 * myLinkedListAddAtTail(obj, val);
 
 * myLinkedListAddAtIndex(obj, index, val);
 
 * myLinkedListDeleteAtIndex(obj, index);
 
 * myLinkedListFree(obj);
*/
```

### 反转链表

```c
/* 反转链表
 *	1. 空链表 || 链表只有一个节点
 *	2. 逐个翻转节点指向 next，涉及 preNode, curNode, tmpNode;
 *	3. 处理原始 head 指向
 */
class Solution {
public:
    ListNode* trainningPlan(ListNode* head) {
        /* 反转链表 */
        /* 空链表 || 链表只有一个节点 */
        if (head == NULL || head->next == NULL) {
            return head;
        }

        ListNode* fakeHead = new ListNode(0, head);
        ListNode* preNode = head;
        ListNode* curNode = head->next;
        ListNode* tmpNode = curNode->next;
        /* 逐个翻转节点指向 next */
        while (curNode != NULL) {
            tmpNode = curNode->next;
            curNode->next = preNode;
            preNode = curNode;
            curNode = tmpNode;
        }
        /* 处理原始 head 指向 */
        fakeHead->next->next = NULL;
        delete fakeHead;
        head = preNode;
        return head;
    }
};
```

### 两两交换链表节点

```c
/**
 * 注意：虚拟链表头curNode, nextNode, next2Node来完成两两交换节点
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
struct ListNode* swapPairs(struct ListNode* head) {
    /* 两两交换链表节点 */
    /* 偷懒：交换节点的val */
    typedef struct ListNode ListNode;
    /* 虚拟链表头 */
    ListNode *fakeHead = (ListNode *)malloc(sizeof(ListNode));
    fakeHead->next = head;
    
    ListNode *curNode = fakeHead;
    ListNode *nextNode;
    ListNode *next2Node;
    while (1) {
        nextNode = curNode->next;
        if (nextNode == NULL) {
            break;
        }
        next2Node = nextNode->next;
        if (next2Node == NULL) {
            break;
        }
        /* 两两交换 */
        curNode->next = next2Node;
        nextNode->next = next2Node->next;
        next2Node->next = nextNode;
        /* 下一对节点 */
        curNode = nextNode;
    }
    head = fakeHead->next;
    return head;
}
```

### 删除链表的倒数第n个节点

```c
/**
 * 注意：快慢指针法，指定倒数第（n+1）个节点，以便删除第n个节点
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
struct ListNode* removeNthFromEnd(struct ListNode* head, int n) {
    /* 删除链表的倒数第N个节点 */
    /* 快慢指针法：fast比slow快N步，直到fast指向NULL，删除slow */
    typedef struct ListNode ListNode;
    ListNode *fakeHead = (ListNode *)malloc(sizeof(ListNode));
    fakeHead->next = head;
    ListNode *slowNode = fakeHead;
    ListNode *fastNode = fakeHead;
    ListNode *tmpNode;
    for (int diff = 0; ; ) {
        if (fastNode == NULL && diff != (n + 1)) {
            break;
        }
        if (diff == (n + 1)) {
            if (fastNode == NULL) {
                /* 删除slowNode->next */
                tmpNode = slowNode->next;
                slowNode->next = tmpNode->next;
                free(tmpNode);
                break;
            }
            slowNode = slowNode->next;
            diff--;
        }
        else {
            fastNode = fastNode->next;
            diff++;
        }
    }
    
    head = fakeHead->next;
    free(fakeHead);
    return head;
}
```

### 链表相交

```c
/**
 * 注意：向后对齐链表，再比较
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
struct ListNode *getIntersectionNode(struct ListNode *headA, struct ListNode *headB) {
    /* 链表相交 */
    /* (×) 遍历链表A，再遍历链表B，相交节点会被遍历两次（无法标记被遍历节点） */
    /* 向后对齐链表A和B，再逐个比较 */
    typedef struct ListNode ListNode;
    ListNode *curNodeA = headA;
    ListNode *curNodeB = headB;
    int lenA = 0;
    int lenB = 0;

    /* 1. 计算链表长度(没有虚拟链表头) */
    while (curNodeA != NULL) {
        lenA++;
        curNodeA = curNodeA->next;
    }
    while (curNodeB != NULL) {
        lenB++;
        curNodeB = curNodeB->next;
    }

    /* 2. 向后对齐链表A和B */
    ListNode *curNodeX = (lenA > lenB)? headA : headB;
    ListNode *curNodeY = (lenA <= lenB)? headA : headB;
    int diff = abs(lenA-lenB);
    for (int i = 0; i < diff; i++) {
        curNodeX = curNodeX->next;
    }

    /* 3. 寻找相交节点（注意：相交节点及其后续节点完全相同） */
    while (curNodeX != NULL) {
        // (curNodeX->val == curNodeY->val) && (curNodeX->next == curNodeY->next)
        if (curNodeX == curNodeY) {
            return curNodeX;
        }
        curNodeX = curNodeX->next;
        curNodeY = curNodeY->next;
    }
    return NULL;
}
```

### 环形链表

```c
/**
 * 注意：数学问题：快慢指针必定在环内相遇；从头节点和相遇节点同时出发，两者再次相遇即为环形链表头
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
struct ListNode *detectCycle(struct ListNode *head) {
    /* 环形链表 */
    if (head == NULL) {
        return NULL;
    }
    /* 1. 快慢指针法：快慢指针必定在环内相遇 */
    typedef struct ListNode ListNode;
    ListNode *slowNode = head;
    ListNode *fastNode = head;
    ListNode *meetNode = head;
    while (1) {
        /* 慢指针走一步 */
        slowNode = slowNode->next;
        /* 快指针走两步 */
        fastNode = fastNode->next;
        if (fastNode == NULL) {
            return NULL;
        }
        fastNode = fastNode->next;
        if (fastNode == NULL) {
            return NULL;
        }
        /* 如果相遇 */
        if (fastNode == slowNode) {
            meetNode = fastNode;
            break;
        }
    }
    /* 2. 从头节点和相遇节点同时出发，两者再次相遇即为环形链表头 */
    ListNode *curNodeX = head;
    ListNode *curNodeY = meetNode;
    while (curNodeX != NULL) {
        if (curNodeX == curNodeY) {
            return curNodeX;
        }
        curNodeX = curNodeX->next;
        curNodeY = curNodeY->next;
    }
    return NULL;
}
```

## 哈希表

哈希碰撞：两个不同的元素存储到了相同的位置；

拉链法：将发生冲突的元素存储到链表中；

线性探测法：发生冲突后，新的元素向下继续寻找空闲位置；要确保哈希表尺寸大于元素数量；

哈希表的实现：

​	数组：

​	集合：set -- 红黑树；multiset -- 红黑树；unordered_set -- 哈希表

​	映射：map -- 红黑树；multimap -- 红黑树；unordered_map -- 哈希表

### 01 有效的字母异位词

```c++
/* 有效的字母异位词：
 *	1. 哈希数组：记录单词中每个字母的出现次数。
 *	2. 比较两个单词的哈希数组是否相等。
 */
```

### 02 两个数组的交集

```c++
/* 两个数组的交集：
 *	1. 哈希法存储和查找元素
 		unordered_set<int> records = unordered_set<int>(nums1.begin(), nums2.end());
 		records.find(num) == records.end()
 *	2. 保证答案不重复
 		unordered_set<int> ans;
 		ans.insert(num);
 		return vector<int>(ans.begin(), ans.end());
 */
```

### 03 快乐数

```c++
/* 快乐数：
 *	1. 判断一个数是否重复出现，则用哈希法（unordered_set）存储和查找元素
 */
```

### 04 两数之和

```c++
/* 两数之和：
 *	0. map : <key, value> 存储结构
 *	1. 遍历当前元素，并在 map 中寻找是否有匹配的 key；
 		unordered_map<int, int> records;
 		auto iter = records.find(target - nums[i]);
 		if (iter != map.end()) {
 			return {iter->second, i};
 		}
 *	2. 如何没找到匹配对，就把访问过的元素和下标放入 map 中。
 		map.insert(pair<int, int>(nums[i], i));
 */
```

### 05 四数相加 II

```c++
/* 四数相加 II：
 *	1. 遍历 A, B 数组，在 map 中记录两两之和 key 的出现次数 value
 		unordered_map<int> records;
 		for (int a : A) 
 			for (int b : B)
 				records[a+b]++;
 *	2. 再遍历 C, D 数组，在 map 中查找 0-c-d 的出现次数
 		if (records.find(0-c-d) != records.end()) 
 			ans += records[0-c-d];
 */
```

### 06 三数之和

```c++
/* 三数之和：
 *	1. 哈希法求解
 *		a. 排序 sort(nums.begin(), nums.end());
 *		b. 取 a = nums[i]
 			去重：if (i > 0 && nums[i] == nums[i-1]) continue;
 *		c. 取 c = nums[k], k = i+1, ...
 *		d. 取 b，如果 b = 0-a-c 在 map 中没有找到，则将 c 放入 set，作为候选 b 
 			if (records.find(b) != records.end())
 				ans.push_back({a, b, c});
 				records.erase(b);
 			else
 				records.insert(b);
 *	2. 双指针法求解
 *		a. 排序 sort(nums.begin(), nums.end());
 *		b. 取 a = nums[i], i = 0~nums.size()-3
 			去重：if (i > 0 && nums[i] == nums[i-1]) continue;
 *		c. 双指针法：left/b = i+1, right/c = nums.size()-1;
 			if (a + b + c > 0) right --;
 			else if (a + b + c < 0) left ++;
 			else 
 				ans.push_back({a, b, c});
 				去重 b 和 c
				while (right > left && nums[right] == nums[right-1]) right--;
				while (right > left && nums[left] == nums[left+1]) left++;
				找到答案后，双指针同时收缩
				right --;
				left ++;
 */
```

### 07 四数之和

```c++
class Solution {
public:
    vector<vector<int>> fourSum(vector<int>& nums, int target) {
        /* 四数之和（双指针法）
         *	1. 排序 sort(nums.begin(), nums.end())
         *  2. 遍历 a = nums[i], i = 0~nums.size()
                去重 if (i > 0 && nums[i] == nums[i-1]) continue;
                剪枝 if (a > target && a >= 0) continue;
         *  3. 遍历 b = nums[j], j = i+1~nums.size()
                去重，剪枝
         *  4. 双指针法寻找 c 和 d, left/c = j+1, right/d = nums.size()-1
                if (a+b+c+d > target) right --;
                else if (a+b+c+d < target) left ++;
                else
                    ans.push_back({a,b,c,d});
                    while (right > left && nums[right] == nums[right-1]) right--;
                    while (right > left && nums[left] == nums[left+1]) left++;
                    找到答案后，双指针收缩
                    right --;
                    left ++;
         */
        vector<vector<int>> ans;
        int left;
        int right;
        sort(nums.begin(), nums.end());

        for (int i = 0; i < nums.size(); i++) {
            if (nums[i] > target && nums[i] >= 0) continue;
            if (i > 0 && nums[i] == nums[i-1]) continue;
            for (int j = i+1; j < nums.size(); j++) {
                if ((nums[i] + nums[j] > target) && (nums[i] + nums[j] >= 0)) continue;
                if (j > i+1 && nums[j] == nums[j-1]) continue;
                left = j+1;
                right = nums.size()-1;
                while (right > left) {
                    if ((long) nums[i]+nums[j]+nums[left]+nums[right] > target) right--;
                    else if((long) nums[i]+nums[j]+nums[left]+nums[right] < target) left++;
                    else {
                        ans.push_back({nums[i], nums[j], nums[left], nums[right]});
                        while (right > left && nums[right] == nums[right-1]) right--;
                        while (right > left && nums[left] == nums[left+1]) left++;
                        right--;
                        left++;
                    }
                }
            }
        }
        return ans;
    }
};
```







## 字符串

### 01 字符串相加

```c++
/* 字符串相加：
 *	1. 从个位数开始依次相加，保存相加结果 carry%10，保留进位 carry/10
 *	2. 二进制相加：carry%2, carry/2
 */
class Solution {
public:
    string addStrings(string num1, string num2) {
        /* 字符串相加：从个位数开始依次相加，保存相加结果，保留进位 */
        string ans;
        int carry = 0;  // 当前位的相加结果 carry%10，并且保留进位 carry/10
        for (int idx1 = num1.length()-1, idx2 = num2.length()-1; idx1 >= 0 || idx2 >= 0 || carry > 0; --idx1, --idx2) {
            /* 当前位相加 */
            carry += (idx1 >= 0 ? (num1[idx1] - '0') : 0) + (idx2 >= 0 ? (num2[idx2] - '0') : 0);
            /* 保存相加结果 */
            ans.push_back(carry % 10 + '0');
            /* 保留进位 */
            carry = carry/10;
        }
        /* 翻转字符串 */
        reverse(ans.begin(), ans.end());
        return ans;
    }
};
```

### 02 反转字符串中的单词 III

```c++
class Solution {
public:
    void reverseOneWord(string& s, int startIdx, int endIdx) {
        for( ; startIdx < endIdx; ++startIdx, --endIdx) {
            swap(s[startIdx], s[endIdx]);
        }
    }
    string reverseWords(string s) {
        /* 反转字符串中的单词 */
        string word;
        int startIdx;
        int endIdx;

        startIdx = 0;
        endIdx = 0;
        for (char ch : s) {
            /* 遇到空格，反转已保存的单词，并重新寻找单词的起始/结束位置 */
            if (ch == ' ') {
                reverseOneWord(s, startIdx, endIdx-1);
                startIdx = endIdx+1;
            }
            ++endIdx;
        }
        reverseOneWord(s, startIdx, endIdx-1);
        return s;
    }
};
```

### 03 反转字符串中的元音字母

```c++
class Solution {
public:
    string reverseVowels(string s) {
        /* 反转字符串中的元音字母 */
        unordered_set<char> records = {'a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U'};
        int left;
        int right;

        /* 双指针法：记录左/右的元音字母的位置，并交换 */
        left = 0;
        right = s.size() - 1;
        while (left < right) {
            if (records.count(s[left]) && records.count(s[right])) {
                swap(s[left], s[right]);
                ++left;
                --right;
            }
            else if (records.count(s[left])) {
                --right;
            }
            else {
                ++left;
            }
        }

        return s;
    }
};
```

### 04 验证回文串

```c++
class Solution {
public:
    /* 移除所有非字母数字字符，并且大写字母转为小写字母 */
    int removeSomeChar(string& s) {
        char ch;
        int start1 = 0;
        int start2 = 0;
        while (s[start1] != '\0') {
            ch = s[start1++];
            if (('0' <= ch && ch <= '9')
                || ('a' <= ch && ch <= 'z')
                || ('A' <= ch && ch <= 'Z')) {
                s[start2++] = ('A' <= ch && ch <= 'Z') ? (ch + 'a' - 'A') : ch;
            }
        }
        s[start2] = '\0';
        return start2;
    }
    bool isPalindrome(string s) {
        /* 验证回文串 */
        int left;
        int right;

        /* 1. 移除所有非字母数字字符 */
        /* 2. 判断回文串 */
        left  = 0;
        right = removeSomeChar(s) - 1;
        for ( ; left < right; ++left, --right) {
            if (s[left] != s[right]) {
                return false;
            }
        }
        return true;
    }
};
```

### 05 验证回文串 II

```c++
class Solution {
public:
    bool isPalindrome(string s, int startIdx, int endIdx) {
        for ( ; startIdx < endIdx; ++startIdx, --endIdx) {
            if (s[startIdx] != s[endIdx]) {
                return false;
            }
        }
        return true;
    }
    bool validPalindrome(string s) {
        /* 验证回文串：最多可以删除一个字符，即判断 [i+1, j] or [i, j-1] 是否是回文串 */
        int startIdx;
        int endIdx;

        startIdx = 0;
        endIdx   = s.size()-1;
        for ( ; startIdx < endIdx; ++startIdx, --endIdx) {
            if (s[startIdx] != s[endIdx]) {
                return (isPalindrome(s, startIdx+1, endIdx) || isPalindrome(s, startIdx, endIdx-1));
            }
        }
        return true;
    }
};
```

### 06 字符频率排序

```c++
class Solution {
public:
    string frequencySort(string s) {
        unordered_map<char, int> records;
        priority_queue<pair<int, char>> pq;
        pair<int, char> curPair;
        string ans;

        /* 1. 统计字符出现频率 */
        for (char ch : s) {
            records[ch]++;
        }

        /* 2. 优先队列排序 */
        for (auto& pair : records) {
            pq.push({pair.second, pair.first});
        }
        while (!pq.empty()) {
            curPair = pq.top();
            for (int i = 0; i < curPair.first; ++i) {
                ans.push_back(curPair.second);
            }
            pq.pop();
        }

        return ans;
    }
};
```

### 07 字符串中的单词数

```c++
class Solution {
public:
    int countSegments(string s) {
        /* 字符串中的单词数：
         *  1. 没有前缀空格、没有中间冗余空格、没有后缀空格
         *  2. 判断是否为一个单词：(i == 0 || s[i-1] == ' ') && s[i] != ' '
         */
        int ans;

        ans = 0;
        for (int i = 0; i < s.size(); ++i) {
            if ((i == 0 || s[i-1] == ' ') && s[i] != ' ') {
                ++ans;
            }
        }

        return ans;
    }
};
```

### 08 最长公共前缀

```c++
/* 最长公共前缀：模拟，两两比较，比较后得到结果作为新的字符串，继续与下一个比较 */
class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        if (strs.size() == 1) {
            return strs[0];
        }

        /* 最长公共前缀：模拟，两两比较，比较后得到结果作为新的字符串，继续与下一个比较 */
        string preWord;
        string curWord;
        int i, j;

        preWord = strs[0];
        for (i = 1; i < strs.size(); ++i) {
            curWord = strs[i];
            for (j = 0; j < preWord.size() && j < curWord.size(); ++j) {
                if (preWord[j] != curWord[j]) {
                    break;
                }
            }
            preWord = curWord.substr(0, j);
        }

        return preWord;
    }
};
```



### 反转字符串

```c
/**
 * 注意：字符串库函数以及库函数的实现原理
 */
void reverseString(char* s, int sSize) {
    /* 反转字符串 */
    /* 双向指针法 */
    for (int left = 0, right = sSize-1, tmp = 0; left < right; ) {
        tmp = s[left];
        s[left] = s[right];
        s[right] = tmp;
        left++;
        right--;
    }
    return;
}
```

### 花式反转字符串

```c
/**
 * 注意：模拟规则
 */
void reverseString(char *s, int left, int right) {
    char tmp = 0;
    while (left < right) {
        tmp = s[left];
        s[left] = s[right];
        s[right] = tmp;
        left++;
        right--;
    }
    return;
}

char* reverseStr(char* s, int k) {
    /* 花式反转字符串 */
    /* 模拟 */
    int count = 0;
    int idx_start, idx_end, idx;
    for (idx = 0; s[idx] != '\0'; idx++) {
        count++;
        if (count >= 2*k) {
            idx_start = idx - 2*k + 1;
            idx_end   = idx - k;
            /* 反转字符串 */
            reverseString(s, idx_start, idx_end);
            count = 0;
        }
    }
    idx = idx-1;  // 删去'\0'
    if (count < k) {
        idx_start = idx - count + 1;
        idx_end   = idx;
        reverseString(s, idx_start, idx_end);
    } else {
        idx_start = idx - count + 1;
        idx_end   = idx - (count-k);
        reverseString(s, idx_start, idx_end);
    }
    return s;
}

```

### 替换数字

```c
/**
 * 注意：读取字符串
 * 注意：快慢指针法，从前往后放入新数组（申请了新数组）
 * 注意：快慢指针法，将数组扩展为指定大小，从后往前放入扩展数组（节省空间）
 */
#include <stdio.h>
#define StrLenMax 10001

int main(void) {
    /* 替换数字 */
    /* 1. 读取字符串 */
    char str[StrLenMax];
    scanf("%s", str);
    
    /* 2. 遍历字符串 */
    int count = 0;
    int len = 0;
    for (len = 0; str[len] != '\0'; len++) {
        if (str[len] >= '0' && str[len] <= '9') {
            count++;
        }
    }
    /* 分配结果字符串的内存 */
    char *newStr = (char *)malloc((len + count*6 + 1)*sizeof(char));
    
    /* 3. 快慢指针法 */
    int slowIdx = 0, fastIdx = 0;
    for (slowIdx = 0, fastIdx = 0; str[slowIdx] != '\0'; slowIdx++) {
        if (str[slowIdx] >= '0' && str[slowIdx] <= '9') {
            newStr[fastIdx++] = 'n';
            newStr[fastIdx++] = 'u';
            newStr[fastIdx++] = 'm';
            newStr[fastIdx++] = 'b';
            newStr[fastIdx++] = 'e';
            newStr[fastIdx++] = 'r';
        } else {
            newStr[fastIdx++] = str[slowIdx];
        }
    }
    newStr[fastIdx] = '\0';
    printf("%s\n",newStr);
    
    return 0;
}
```

### 反转字符串中的单词 II

```c
/**
 * 注意：删除多余空格的模拟操作
 * 注意：大反转+小反转
 */
/* 删除多余空格 */
int deleteSpace(char *s) {
    int slowIdx = 0, fastIdx = 0;
    while (s[fastIdx] == ' ') {
        fastIdx++;
    }
    /* 去除中间冗余空格 */
    int mark = 1;
    while (s[fastIdx] != '\0') {
        if (s[fastIdx] == ' ') {
            if (mark) {
                s[slowIdx++] = s[fastIdx++];
                mark = 0;
            } else { // 说明是多余空格
                fastIdx++;
            }
        } else {
            s[slowIdx++] = s[fastIdx++];
            mark = 1;
        }
    }
    /* 去除尾部空格 */
    if (s[slowIdx-1] == ' ') {  // 最多有一个尾部空格
        slowIdx--;
    }
    return slowIdx;
}

/* 反转字符串 */
void reverseStr(char* s, int strStart, int strEnd) {
    for (int left = strStart, right = strEnd - 1, tmp = 0; left < right; left++, right--) {
        tmp = s[left];
        s[left] = s[right];
        s[right] = tmp;
    }
}

char* reverseWords(char* s) {
    /* 反转字符串中的单词 */
    /* 1. 删除多余空格 */
    int strLen = deleteSpace(s);
    /* 2. 反转整个字符串 */
    /* 此时，字符串从idx = 0起始，自slowIdx - 1终止 */
    reverseStr(s, 0, strLen);
    /* 3. 反转每个单词 */
    int slowIdx = 0;
    int fastIdx = 0;
    s[strLen] = '\0';
    while (fastIdx <= strLen) {
        if (s[fastIdx] == ' ' || s[fastIdx] == '\0') {  // 这个单词从slowIdx起始，从fastIdx结束
            reverseStr(s, slowIdx, fastIdx);
            slowIdx = fastIdx + 1;
        }
        fastIdx++;
    }
    return s;
}
```

### 右旋字符串

```c
/**
 * 注意：字符串向后偏移tar，需要构造新的数组
 * 注意：大反转+小反转，不需要新数组
 */
#include <stdio.h>
#include <string.h>
#define strLenMax 10001

void reverseStr(char *str, int start, int end) {
    for (int left = start, right = end - 1, tmp = 0; left < right; left++, right--) {
        tmp = str[left];
        str[left] = str[right];
        str[right] = tmp;
    }
}

int main(void) {
    /* 右旋字符串 */
    /* 1. 读取字符串 */
    int tar;
    char str[strLenMax];
    scanf("%d", &tar);
    scanf("%s", str);
    /* 2. 反转字符串 */
    int strLen = strlen(str);
    reverseStr(str, 0, strLen);
    /* 3. 反转部分字符串 */
    reverseStr(str, 0, tar);
    reverseStr(str, tar, strLen);
    
    printf("%s\n",str);
    return 0;
}
```

### 寻找字符串中的特定子串

```c
/**
 * 注意：KMP算法：向前回退看不懂欸，向前回退：找到最后一个不相等的下标（next[j]保存的是最长相同前后缀，最长有用前缀的长度和end）
 */
int strStr(char* haystack, char* needle) {
    /* 在字符串中寻找子串 */
    int tarLen = 0;
    while (needle[tarLen] != '\0') {
        tarLen++;
    }
    int slowIdx = 0;
    int fastIdx = 0;
    int tmpIdx = 0;
    while (haystack[slowIdx] != '\0') {
        if (haystack[slowIdx] == needle[0]) {
            fastIdx = slowIdx;
            tmpIdx = 0;
            while (haystack[fastIdx] == needle[tmpIdx]) {
                fastIdx++;
                tmpIdx++;
                if (tmpIdx == tarLen) {
                    return slowIdx;
                }
                if (haystack[fastIdx] == '\0') {
                    return -1;
                }
            }
        }
        slowIdx++;
    }
    return -1;
}
```

### 重复的子字符串

```c
return (s + s).substring(1, 2 * s.length() - 1).contains(s);
```

## 栈与队列

### 01 用栈实现队列

```c++
/** 
 * 注意：定义栈std::stack<int>
 *      基础的栈操作：stk.empty(), stk.top(), stk.push(x), stk.pop(), stk.size()
 */
class MyQueue {
public:
    /* stk.pop()不返回值，防止异常：
     *   元素被移除，但是没有成功返回给调用者
     */
    std::stack<int> stkIn;      /* 输入栈 */
    std::stack<int> stkOut;     /* 输出栈 */
    MyQueue() {
        
    }
    
    void push(int x) {
        stkIn.push(x);

        return;
    }
    
    int pop() {
        /* 先进先出 */
        int queuePop;
        /* 1. 如果输出栈为空 */
        if (stkOut.empty()) {
            /* 将输入栈全部压入输出栈 */
            while (!stkIn.empty()) {
                stkOut.push(stkIn.top());
                stkIn.pop();
            }
        }
        /* 2. 输出栈压出的值即为第一个进入队列的元素 */
        queuePop = stkOut.top();
        stkOut.pop();

        return queuePop;
    }
    
    int peek() {
        /* peek(): 返回队头 */
        int queueTop;
        /* 1. 如果输出栈为空 */
        if (stkOut.empty()) {
            /* 将输入栈全部压入输出栈 */
            while (!stkIn.empty()) {
                stkOut.push(stkIn.top());
                stkIn.pop();
            }
        }
        /* 2. stdOut.top()即为队头 */
        return stkOut.top();
    }
    
    bool empty() {
        return (stkIn.empty() && stkOut.empty()) ? true : false;
    }
};

```

### 02 用队列实现栈

```c++
/**
 * 注意：用队列实现栈，出栈时推出队列的尾部，入栈时则正常压入队列
 *      时间复杂度？？
 */
class MyStack {
public:
    std::queue<int> que;
    MyStack() {
        
    }
    
    void push(int x) {
        /* stk.push(x): 正常压入队列 */
        que.push(x);
    }
    
    int pop() {
        /* stk.pop(): 推出队列尾部
         *   将除了尾部的其余元素逐个压入队列，再推出队列尾部（此时已为队列头部）
         */
         int val;
         /* 1. 将除了尾部的其余元素逐个推出队列，再压入队列 */
         for (int i = 0; i < (que.size() - 1); i++) {
            val = que.front();
            que.pop();
            que.push(val);
         }
         /* 2. 推出队列头部（即初始队列尾部） */
         val = que.front();
         que.pop();
         return val;
    }
    
    int top() {
        /* stk.top(): 类似于stk.pop(); */
         int val;
         /* 1. 将除了尾部的其余元素逐个推出队列，再压入队列 */
         for (int i = 0; i < que.size(); i++) {
            val = que.front();
            que.pop();
            que.push(val);
         }
         return val;
    }
    
    bool empty() {
        return (que.empty()) ? true : false;
    }
};

```

### 03 有效的括号

```c++
class Solution {
public:
    bool isValid(string s) {
        /* 有效的括号：
         *  1. 左括号入栈，右括号与栈顶匹配，并出栈
         *  2. 特殊情况：只有左括号和只有右括号
         *	3. 更简洁：左括号入栈相应的右括号；右括号与栈顶相比较 (栈不为空)，并出栈
         */
        stack<char> stk;
        for (char ch : s) {
            if (ch == '(' || ch == '[' || ch == '{') {
                stk.push(ch);
                continue;
            }
            else if (stk.empty()
                    || (ch == ')' && stk.top() != '(')
                    || (ch == ']' && stk.top() != '[')
                    || (ch == '}' && stk.top() != '{')
                    || (ch != ')' && ch != ']' && ch != '}')) {
                return false;
            }
            stk.pop();
        }
        if (!stk.empty()) {
            return false;
        }
        return true;
    }
};

/*******************************************/
class Solution {
public:
    bool isValid(string s) {
        if (s.size() % 2 != 0) return false; // 如果s的长度为奇数，一定不符合要求
        stack<char> st;
        for (int i = 0; i < s.size(); i++) {
            if (s[i] == '(') st.push(')');
            else if (s[i] == '{') st.push('}');
            else if (s[i] == '[') st.push(']');
            // 第三种情况：遍历字符串匹配的过程中，栈已经为空了，没有匹配的字符了，说明右括号没有找到对应的左括号 return false
            // 第二种情况：遍历字符串匹配的过程中，发现栈里没有我们要匹配的字符。所以return false
            else if (st.empty() || st.top() != s[i]) return false;
            else st.pop(); // st.top() 与 s[i]相等，栈弹出元素
        }
        // 第一种情况：此时我们已经遍历完了字符串，但是栈不为空，说明有相应的左括号没有右括号来匹配，所以return false，否则就return true
        return st.empty();
    }
};
```

### 04 删除字符串中的所有相邻重复项

```c++
/**
 * 注意：栈的应用
 */
class Solution {
public:
    string removeDuplicates(string s) {
        /* 思路：字符入栈前，如果与队列尾部相同，则推出队列 */
        std::string strNew = "";
        std::stack<int> stk;
        /* 1. 删除所有相邻重复项 */
        for (char curChar : s) {
            /* 如果 栈为空 或者 当前字符与栈顶不匹配，则入栈 */
            if (stk.empty() || stk.top() != curChar) {
                stk.push(curChar);
            }  /* 否则相互匹配，则推出栈顶 */
            else {
                stk.pop();
            }
        }
        /* 2. 构建新的string类型对象 */
        while (!stk.empty()) {
            strNew += stk.top();  /* 将栈顶元素添加到字符串末尾 */
            stk.pop();
        }
        std::reverse(strNew.begin(), strNew.end());
        return strNew;
    }
};
```

### 05 逆波兰表达式求值

```c++
/**
 * 注意：
 *   1. 模拟：逆波兰表达式
 *	 2. vector<string>& tokens: 遍历const string& str : tokens
 * 								遍历tokens[i]
 *	 3. stoll(string): 将字符串转为long long整数
 */
class Solution {
public:
    int evalRPN(vector<string>& tokens) {
        /**
         * 逆波兰表达式：从左到右逐个处理字符（错误理解会很懵逼）
         *   操作数：压入栈中
         *   运算符：从栈中弹出2个操作数，计算后再压入栈中
         */
        std::stack<long long> stk;
        long long ops1 = 0;
        long long ops2 = 0;
        char curChar = '\0';
        /* tokens: 字符串数组 */
        for (const std::string& str : tokens) {
            /* a. 运算符：字符串比较 */
            if (str == "+" || str == "-" || \
                str == "*" || str == "/") 
            {
                ops1 = stk.top();  /* 栈空不空的，不想考虑那么复杂 */
                stk.pop();
                ops2 = stk.top();
                stk.pop();
                curChar = str[0];
                switch (curChar) {  /* 需要char类型输入 */
                    case '+':
                    stk.push(ops2 + ops1);
                    break;
                    case '-':
                    stk.push(ops2 - ops1);
                    break;
                    case '/':
                    stk.push(ops2 / ops1);
                    break;
                    case '*':
                    stk.push(ops2 * ops1);
                    break;
                }
            }  /* b. 操作数：将字符串转换为long long int整数 */
            else {
                stk.push(stoll(str));
            }
        }
        return stk.top();
    }
};
```

### 06 滑动窗口最大值

#### 111

```c++
class Solution {
public:
    /* 维护单调队列：只维护当前最大值和后续可能的最大值
     *              当前最大值：如果当前值大于队列头部，则弹出队列所有元素，然后压入当前值
     *              后续最大值：如果当前值小于队列头部（并且一定是后加入的），则压入当前值
     */
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> ans;
        std::deque<int> queMono;
        int count = 0;
        int curNum = 0;
        for (int i = 0; i < nums.size(); i++) {
            curNum = nums[i];
            count++;  /* 计数加1 */
            /* 0. 如果count > 3，则弹出nums[i-3] */
            if (count > k) {
                /* 得到该窗口的最大值 */
                ans.push_back(queMono.front());
                /* 弹出窗口之外的最大值 */
                if (queMono.front() == nums[i-k]) {
                    queMono.pop_front();
                }
                count--;
            }
            /* a. 队列为空，则压入当前值 */
            if (queMono.empty()) {
                queMono.push_back(curNum);
                continue;
            }
            /* b. 比较当前值与队列头部 */
            if (curNum > queMono.front()) {
                /* 弹出队列所有元素 */
                while (!queMono.empty()) {
                    queMono.pop_front();
                }
                /* 压入当前值 */
                queMono.push_back(curNum);
            }  /* 当前值小于等于队列头部 */
            else {
                /* 如果当前值比队列尾部大，则弹出队尾 */
                while (curNum > queMono.back()) {
                    queMono.pop_back();
                }
                queMono.push_back(curNum);
            }
        }
        /* 加入最后一个窗口的结果 */
        ans.push_back(queMono.front());
        return ans;
    }
};
```

#### 简洁

```c++
class Solution {
public:
    /* 维护单调队列：只维护当前最大值和后续可能的最大值
     *              当前最大值：如果当前值大于队列头部，则弹出队列所有元素，然后压入当前值
     *              后续最大值：如果当前值小于队列头部（并且一定是后加入的），则压入当前值
     */
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> ans;
        std::deque<int> queMono;
        int count = 0;
        int curNum = 0;
        for (int i = 0; i < nums.size(); i++) {
            curNum = nums[i];
            count++;  /* 计数加1 */
            /* 0. 如果count > 3，则弹出nums[i-3] */
            if (count > k) {
                /* 得到该窗口的最大值 */
                ans.push_back(queMono.front());
                /* 弹出窗口之外的最大值 */
                if (queMono.front() == nums[i-k]) {
                    queMono.pop_front();
                }
                count--;
            }
            /* a. 压入当前值：比较当前值与队列尾部 */
            while (!queMono.empty() && curNum > queMono.back()) {
                queMono.pop_back();
            }
            queMono.push_back(curNum);
        }
        /* 加入最后一个窗口的结果 */
        ans.push_back(queMono.front());
        return ans;
    }
};
```

### 07 前k个高频元素

优先级队列：容器适配器。缺省（默认）情况下优先队列（priority_queue）利用大顶堆（max_heap）完成对元素的排序，是以vector为表现形式的完全二叉树（complete binary tree）。

堆：是一棵完全二叉树，树中每个节点的值都不小于（不大于）其左右孩子的值。

无序映射（unordered_map）：使用哈希表实现键值对的存储和访问，插入、删除和查找的时间复杂度为O(1)。

```c++
unordered_map<int, int> map;
/* 无序映射（unordered_map）标准操作 */
/* 1. 插入元素 */
map[key] = value;			/* 对于int类型，默认值为0 */
map.insert({key, value});
/* 2. 访问元素 */
map[key];		/* 如果键不存在，则插入一个新的键值对，并返回默认值 */
map.at(key);	/* 如果键不存在，抛出std::out_of_range异常 */
/* 3. 查找元素 */
map.find(key);	/* 返回指向键的迭代器。如果键不存在，返回map.end() */
map.count(key);	/* 返回键的出现次数。由于在unordered_map中键是唯一的，因此返回0/1 */
/* 4. 删除元素 */
map.erase(key);	/* 删除键对应的元素 */
map.clear();	/* 删除所有元素 */
/* 5. 其他操作 */
map.empty();	/* 检查map是否为空 */
map.size();		/* 返回map中元素的个数 */
```

#### 小堆顶 + 无序映射

```c++
/**
 * 注意：
 *     Line-15: 重载operator()以实现自定义比较, const pair<int, int>& lhs, 引用符号&: 引用是变量的别名，它与原始变量共享同一块内存。
 *	   Line-18: class类定义末尾的分号;
 *     Line-29: 优先队列（小顶堆）定义priority_queue<pair<int, int>, vector<pair<int, int>>, mycomparison> queMin; 元素类型pair<int, int>, 底层容器vector<pair<int, int>>
 *     Line-30: 迭代器for (unordered_map<int, int>:: iterator it = map.begin(); it != map.end(); it++); *运算符用于访问迭代器指向的元素的值
 *     Line-38: vector<int> ans(k); 指定vector容器的大小
 *     Line-40: 优先队列的标准操作empty(), size(), top(), push(x), pop();
 */
class Solution {
public:
    /* 小顶堆：自定义比较函数 */
    class mycomparison {
    public:
        bool operator()(const pair<int, int>& lhs, const pair<int, int>& rhs) {
            return lhs.second > rhs.second;
        }
    };
    vector<int> topKFrequent(vector<int>& nums, int k) {
        /* 前k个高频元素 */
        /* 1. 无序映射: 统计每个元素的出现频率 */
        unordered_map<int, int> map;
        /* 插入/查找键对应的值，并统计该键的出现次数 */
        for (int curNum : nums) {
            map[curNum]++;
        }

        /* 2. 优先级队列（小顶堆）：寻找前K大的频率 */
        priority_queue<pair<int, int>, vector<pair<int, int>>, mycomparison> queMin;
        for (unordered_map<int, int>::iterator it = map.begin(); it != map.end(); it++) {
            queMin.push(*it);
            if (queMin.size() > k) {
                queMin.pop();
            }
        }

        /* 3. 输出前K大的频率：小顶堆倒序输出前K大的元素 */
        vector<int> ans(k);
        for (int i = k - 1; i >= 0; i--) {
            ans[i] = queMin.top().first;
            queMin.pop();
        }
        return ans;
    }
};
```

## 二叉树

C++中map、set、multimap、multiset的底层实现都是平衡二叉搜索树，所以map、set的增删操作时间复杂度是logn。unordered_map、unordered_set底层实现是哈希表。增删操作为O(1)

### 01 二叉树的遍历

#### 前序遍历

```c++
/**
 * 注意：
 *     栈的应用：栈先进后出，对应于前序遍历的中左右，则先压入右，先弹出左
 *     二叉树栈：std::stack<TreeNode*> stkTree;
 *     二叉树节点的定义struct TreeNode { ... };
 *     Lines-33: ans.push_back(curNode->val);
 */
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    vector<int> preorderTraversal(TreeNode* root) {
        /* 二叉树的前序遍历: 栈的应用 */
        std::stack<TreeNode*> stkTree;
        vector<int> ans;
        /* 0. 如果树为空，则返回空 */
        if (root == NULL) {
            return ans;
        }
        TreeNode* curNode;
        stkTree.push(root);
        while (!stkTree.empty()) {
            curNode = stkTree.top();
            ans.push_back(curNode->val);
            stkTree.pop();
            /* 栈：先进后出；对应于前序遍历的中左右，则先压入右，先弹出左 */
            if (curNode->right != NULL) {
                stkTree.push(curNode->right);
            }
            if (curNode->left != NULL) {
                stkTree.push(curNode->left);
            }
        }
        return ans;
    }
};
```

#### 中序遍历

```c++
/**
 * 注意：
 *     栈的应用：保存当前节点（中），访问其左节点（左）；栈保留了左在前的访问路径
 *			   在回顾访问路径时，再访问其右节点（右）
 */
class Solution {
public:
    vector<int> inorderTraversal(TreeNode* root) {
        /* 二叉树的中序遍历：栈的应用 */
        std::stack<TreeNode*> stkTree;
        TreeNode* curNode = root;
        vector<int> ans;
        while ((curNode != NULL) || (!stkTree.empty())) {
            /* 保存当前节点（中），访问其左节点（左） */
            /* 栈保留了左在前的访问路径 */
            if (curNode != NULL) {
                stkTree.push(curNode);          /* 左 */
                curNode = curNode->left;
            }  /* 到达叶子节点后，打印，并访问其右节点（右） */
            else {
                curNode = stkTree.top();
                stkTree.pop();
                ans.push_back(curNode->val);    /* 中 */
                curNode = curNode->right;       /* 右 */
            }
        }
        return ans;
    }
};
```

#### 后序遍历

```c++
/**
 * 注意：
 *     后序遍历：左右中是中右左的反序，中右左是前序遍历的变形
 *     Line-32: std::reverse(ans.begin(), ans.end());
 */
class Solution {
public:
    vector<int> postorderTraversal(TreeNode* root) {
        /* 二叉树的后序遍历：栈的应用 */
        /* 左右中是中右左的反序，中右左是前序遍历的变形 */
        std::stack<TreeNode*> stkTree;
        TreeNode* curNode = NULL;
        vector<int> ans;

        if (root == NULL) {
            return ans;
        }

        stkTree.push(root);
        while (!stkTree.empty()) {
            curNode = stkTree.top();        /* 中 */
            stkTree.pop();
            ans.push_back(curNode->val);
            /* 左：先入栈，后访问 */
            if (curNode->left != NULL) {    /* 左 */
                stkTree.push(curNode->left);
            }
            if (curNode->right != NULL) {   /* 右 */
                stkTree.push(curNode->right);
            }
        }
        std::reverse(ans.begin(), ans.end());
        return ans;
    }
};
```

#### 空指针标记

##### 前序遍历

```c++
/**
 * 注意：
 *     保留中左右的访问路径（先右、再左、后中），对于访问但未处理的（中）节点，其跟随压入一个空指针标记
 *     前/中/后序遍历的空指针标记：代码统一
 */
```



##### 中序遍历

```c++
/**
 * 
 */
class Solution {
public:
    vector<int> inorderTraversal(TreeNode* root) {
        /* 二叉树的中序遍历：栈的应用 */
        /* 空指针标记法：保留左中右的访问路径，对访问但未处理的节点，压入一个空指针标记
         *   由于（中）节点先访问后处理，其跟随一个空指针标记，可以在回顾访问路径时及时处理
         *   处理完（左）（中）后，再处理（右）：先压入（中）的右节点，再遍历（中）的全部左孩子节点
         */
        std::stack<TreeNode*> stkTree;
        TreeNode* curNode = NULL;
        vector<int> ans;

        if (root != NULL) {
            stkTree.push(root);
        }
        while (!stkTree.empty()) {
            curNode = stkTree.top();
            /* 1. 访问节点：保留左中右的访问路径 */
            if (curNode != NULL) {
                /* 先弹出当前节点（中） */
                stkTree.pop();
                /* a. 压入右节点（先压入，后访问） */
                if (curNode->right != NULL) {
                    stkTree.push(curNode->right);
                }
                /* b. 压入中节点（访问但未处理），并且随之空指针标记 */
                stkTree.push(curNode);
                stkTree.push(NULL);
                /* c. 压入左节点（后压入，先访问） */
                if (curNode->left != NULL) {
                    stkTree.push(curNode->left);
                }
            }  /* 2. 处理节点：抵达叶子节点/访问待处理节点 */
            else {
                /* a. 弹出空指针 */
                stkTree.pop();
                /* b. 弹出访问待处理节点 */
                curNode = stkTree.top();
                stkTree.pop();
                ans.push_back(curNode->val);
            }
        }
        return ans;
    }
};
```

#### 层序遍历

##### a_二叉树的层序遍历

```c++
/**
 * 注意：
 */
class Solution {
public:
    vector<vector<int>> levelOrder(TreeNode* root) {
        /* 二叉树的层序遍历：队列的应用 */
        /* 队列先进先出，逐层访问时，保留这一层的访问路径，并自左向右添加下一层的访问路径
         * 访问节点和处理节点同步进行
         */
        std::queue<TreeNode*> que;
        TreeNode* curNode = root;
        vector<vector<int>> ans;
        vector<int> ansFloor;
        int sizeFloor;

        if (curNode != NULL) {
            que.push(curNode);
        }
        while (!que.empty()) {
            sizeFloor = que.size();     /* 该层的节点数 */
            ansFloor.clear();
            while (sizeFloor--) {
                /* a. 访问节点和处理节点同步进行 */
                curNode = que.front();
                que.pop();
                ansFloor.push_back(curNode->val);
                /* b. 自左向右添加下一层的子节点（访问路径） */
                if (curNode->left != NULL) {
                    que.push(curNode->left);
                }
                if (curNode->right != NULL) {
                    que.push(curNode->right);
                }
            }
            ans.push_back(ansFloor);
        }
        return ans;
    }
};
```

##### b_二叉树的层序遍历II

```c++
/**
 * 注意：
 *     自底向上的层序遍历是标准层序遍历的reverse
 */
```

##### c_二叉树的右视图

```c++
/**
 * 注意：
 *     输出标准层序遍历的每层最后一个节点：if (i == (size - 1))
 */
```

##### d_二叉树的层平均

```c++
/**
 * 注意：
 *     输出标准层序遍历的每层的平均值
 */
```

##### e_二叉树的最大深度

```c++
/**
 * 注意：
 *     在标准层序遍历中记录层数
 */
```

##### f_二叉树的最小深度

```c++
/**
 * 注意：
 *     在标准层序遍历中，返回第一个没有子节点的层数
 */
```

### 02 翻转二叉树

```c++
/**
 * 注意：
 *     访问所有的节点（前/后序遍历和层序遍历，中序遍历会访问两次（中）节点），并交换其左右孩子
 *     std::swap(curNode->left, curNode->right);
 */
```

### 03 对称二叉树

```c++
/**
 * 注意：
 *     通过手动模拟来理解每个节点是否都被访问
 */
class Solution {
public:
    bool isSymmetric(TreeNode* root) {
        /* a. 队列：从根节点的左右孩子开始，比较左左和右右孙子，左右和右左孙子（遍历了所有可能） */
        /* b. 中序遍历的应用：左中右和右中左的遍历结果是否相同 */
        std::queue<TreeNode*> queTree;
        TreeNode* leftNode  = NULL;
        TreeNode* rightNode = NULL;

        if (root == NULL) {
            return true;
        }
        queTree.push(root->left);
        queTree.push(root->right);
        while (!queTree.empty()) {
            /* a. 取出两个对称节点 */
            leftNode  = queTree.front(); queTree.pop();
            rightNode = queTree.front(); queTree.pop();
            /* b. 比较两个节点是否相同 */
            if (leftNode == NULL && rightNode == NULL) {
                continue;
            }
            if (leftNode == NULL || rightNode == NULL || \
                leftNode->val != rightNode->val) {
                return false;
            }
            /* c. 依次放入一对对称节点（左左和右右孩子，左右和右左孩子） */
            queTree.push(leftNode->left);
            queTree.push(rightNode->right);
            queTree.push(leftNode->right);
            queTree.push(rightNode->left);
        }
        return true;
    }
};
```

### 04 完全二叉树的节点个数

```c++
/**
 * 注意：
 *     访问全部节点即可（前/后序遍历和层序遍历）
 *     利用完全二叉树的某一深度的子树一定是满二叉树（但我不喜欢递归，太浪费内存空间）
 */
```

### 05 平衡二叉树

```c++
/**
 * 注意：
 *     平衡二叉树：每个节点的左右子树的高度差绝对值不超过1
 *     二叉树节点深度：从根节点到该节点所经过的最短节点数，从上往下访问（前序遍历：中左右）
 *	   二叉树节点高度：从该节点到叶子节点所经过的最短节点数，从下往上访问（后序遍历：左右中）
 *     当前节点的最大深度为其高度
 *     后序遍历（空指针标记）计算当前节点最大深度（Depth++ 和 Depth--）（为什么是后序遍历计算深度呢？为什么前/中序遍历不行呢？？为什么后序遍历可以呢？层序遍历计算深度最简单。（中）节点要最后访问，因为访问（中）节点意味着回溯，所以最好用后序遍历）
 */
class Solution {
public:
    int getDepth(TreeNode* root) {
        /* 当前节点的最大深度（后序遍历--空指针标记） */
        std::stack<TreeNode*> stkTree;
        TreeNode* curNode = NULL;
        int Depth = 0;;
        int ans   = 0;

        if (root != NULL) {
            stkTree.push(root);
        }
        while (!stkTree.empty()) {
            curNode = stkTree.top();
            /* 访问节点 */
            if (curNode != NULL) {
                /* 向下访问 */
                Depth++;
                /* 弹出（中）节点 */
                stkTree.pop();
                stkTree.push(curNode);          /* 中 */
                /* 第二次访问（中）节点，压入空指针标记 */
                stkTree.push(NULL);
                if (curNode->right != NULL) {   /* 右 */
                    stkTree.push(curNode->right);
                }
                if (curNode->left != NULL) {    /* 左 */
                    stkTree.push(curNode->left);
                }
            }  /* 输出节点 */
            else {
                /* 回顾路径 */
                Depth--;
                stkTree.pop();
                curNode = stkTree.top();
                stkTree.pop();
            }
            ans = (ans > Depth) ? ans : Depth;
        }
        return ans;
    }
    bool isBalanced(TreeNode* root) {
        /* 访问所有节点(层序遍历)，判断其左右子树高度差是否不大于1 */
        std::queue<TreeNode*> queTree;
        TreeNode* curNode = NULL;
        int sizeFloor;
        
        if (root != NULL) {
            queTree.push(root);
        }
        while (!queTree.empty()) {
            sizeFloor = queTree.size();
            while (sizeFloor--) {
                curNode = queTree.front(); queTree.pop();
                if (curNode->left != NULL) {
                    queTree.push(curNode->left);
                }
                if (curNode->right != NULL) {
                    queTree.push(curNode->right);
                }
                if (std::abs(getDepth(curNode->left) - getDepth(curNode->right)) > 1) {
                    return false;
                }
            }
        }
        return true;
    }
};
```

### 06 二叉树的所有路径

```c++
/**
 * 注意：
 *     Line-30: pathSave.push(path + "->" + to_string(curNode->right->val));
 */
class Solution {
public:
    vector<string> binaryTreePaths(TreeNode* root) {
        /* 二叉树的所有路径：前序遍历的应用 */
        /* 前序遍历从根节点往下访问，同步记录当前节点到根节点的绝对路径 */
        std::stack<TreeNode*> stkTree;
        std::stack<string> pathSave;
        vector<string> ans;
        TreeNode* curNode = NULL;
        string path = "";

        if (root != NULL) {
            stkTree.push(root);
            pathSave.push(to_string(root->val));
        }
        while (!stkTree.empty()) {
            curNode = stkTree.top();  stkTree.pop();    /* 中 */
            path    = pathSave.top(); pathSave.pop();
            /* 遇到叶子节点，输出已有路径 */
            if (curNode->left == NULL && curNode->right == NULL) {
                ans.push_back(path);
                continue;
            }
            if (curNode->right != NULL) {                /* 右 */
                stkTree.push(curNode->right);
                pathSave.push(path + "->" + to_string(curNode->right->val));
            }
            if (curNode->left != NULL) {                /* 左 */
                stkTree.push(curNode->left);
                pathSave.push(path + "->" + to_string(curNode->left->val));
            }
        }
        return ans;
    }
};
```

### 07 左叶子之和

```c++
/**
 * 注意：
 *     左叶子：要结合其父节点和其是否有左右孩子来判断，if (node->left != NULL && node->left->left == NULL && node->left->right == NULL)
 *     前序遍历：从根节点往下访问
 */
```

### 08 找树左下角的值

```c++
/**
 * 注意：
 *     层序遍历，每次更新为当前层的第一个节点，直到最后一层
 */
```

### 09 从中/后序遍历序列中构造二叉树

```c++
/**
 * 注意：
 *     a. 利用后序遍历数组的最后一个元素（子树的根）切割中序遍历数组，得到中序左数组和中序右数组
 *     b. 根据中序左数组切割后序遍历数组，得到后序左数组和后序右数组
 *     c. 直到中序左/右数组只包含一个元素，即为叶子节点
 *     d. 左数组表示左孩子，右数组表示右孩子。
 *     不喜欢递归，但是只有递归的题解。
 */
```

### 10 合并二叉树

```c++
/**
 * 注意：
 *     层序遍历（逐层访问）
 *     如果两棵树的左/右孩子都不为空，则压入队列继续逐层访问；
 *     如果两棵树中tree1的左/右孩子为空，则将tree2的左/右孩子赋值过去。
 */
```

### 11 二叉搜索树中的搜索

```c++
/**
 * 注意：
 *     二叉搜索树的有序节点确定了搜索的方向，直到叶子节点，不需要回溯。
 */
```

### 12 验证二叉树

```c++
/**
 * 注意：
 *     感觉是前/中/后序遍历，附加一个当前节点判断条件即可
 *     题解怎么跟中序遍历的差距这么大呢
 */
```

### 13 二叉搜索树的最小绝对差

```c++
/**
 * 注意：
 *     二叉搜索树的中序遍历数组是有序数组，保留上一个访问节点preNode，计算当前节点curNode和上一节点的绝对差（curNode->val > preNode->val），并更新最小绝对差
 */
```

### 14 二叉搜索树的众数

```c++
/**
 * 
 */
class Solution {
public:
    vector<int> findMode(TreeNode* root) {
        /* 寻找二叉搜索树中的众数
         * 1. 中序遍历是有序数组，比较前一个和当前节点是否相同，即可记录元素出现频率，不需要小顶堆 + map
         * 2. 众数是出现频率最高的元素，与出现频率最高的前K个元素题型类似，小顶堆 + map
         * 3. 众数不止一个，所以要保存count == countMax的所有元素
         */
        std::stack<TreeNode *> stkTree;
        TreeNode* curNode = NULL;
        TreeNode* preNode = NULL;
        vector<int> ans;
        int count = 1;
        int countMax = 1;

        /* 1. 中序遍历 */
        if (root != NULL) {
            stkTree.push(root);
        }
        while (!stkTree.empty()) {
            curNode = stkTree.top(); 
            stkTree.pop();
            /* 访问节点 */
            if (curNode != NULL) {
                /* a. 入栈：右 */
                if (curNode->right != NULL) {
                    stkTree.push(curNode->right);
                }
                /* b. 中 */
                stkTree.push(curNode);
                stkTree.push(NULL);     /* 空指针标记"重复访问" */
                /* c. 左 */
                if (curNode->left != NULL) {
                    stkTree.push(curNode->left);
                }
            }  /* 处理节点 */
            else {
                curNode = stkTree.top();
                stkTree.pop();
                if (preNode != NULL) {
                    if (preNode->val == curNode->val) {
                        count++;
                    }
                    else {
                        count = 1;
                    }
                }
                if (count == countMax) {
                    ans.push_back(curNode->val);
                } 
                else if (count > countMax) {
                    countMax = count;
                    ans.clear();                 /* 清空结果 */
                    ans.push_back(curNode->val); /* 放入当前可能的众数 */
                }
                preNode = curNode;
            }
        }
        return ans;
    }
};
```

### 15 二叉树的最近公共祖先

```c++
/**
 * 注意：
 *     1. 题解是递归和回溯
 *     2. 二叉树数组是层序遍历，n的左孩子下标为2n+1，右孩子下标2n+2；n的父节点为(n-1)/2，向下取整
 *     3. 保存访问指定节点的路径，然后双指针遍历两条路径，找到最远相同节点
 */
```

### 16 二叉搜索树的最近公共祖先

```c++
/**
 * 注意：
 *     1. 路径固定
 */
```

### 17 二叉搜索树的插入操作

```c++
/**
 * 注意：
 *     1. 所有节点的值都不相同，只是插入叶子节点
 *     2. 大于当前节点：右孩子；小于：左孩子；遇到空指针则插入新叶子节点
 */
```

### 18 二叉搜索树的删除操作

```c++
/**
 *
 */
class Solution {
public:
    TreeNode* deleteCurNode(TreeNode* curNode) {
        TreeNode* lchildNode;
        if (!curNode->left && !curNode->right) {
            return NULL;
        }
        else if (!curNode->left) {
            return curNode->right;
        }
        else if (!curNode->right) {
            return curNode->left;
        }
        else {
            lchildNode = curNode->right;
            while (lchildNode->left) {
                lchildNode = lchildNode->left;
            }
            lchildNode->left = curNode->left;
            return curNode->right;
        }
    }

    TreeNode* deleteNode(TreeNode* root, int key) {
        /* 删除二叉搜索树中的节点
         * 1. 根节点
         *    a. 根节点为空，则返回空
         * 2. 中间节点
         *    a. 叶子节点：直接删除
         *    b. 左孩子为空：右孩子顶替当前节点
         *    c. 右孩子为空：左孩子顶替当前节点
         *    d. 左右孩子均不为空：左孩子成为右孩子的最左孙子，右孩子顶替当前节点
         */
        TreeNode* curNode = root;
        /* 1. 根节点 */
        if (!root) {
            return NULL;
        }
        /* 2. 找到需要删除的节点的父节点 */
        while (curNode) {
            /* a. 根节点 */
            if (curNode->val == key) {
                return deleteCurNode(curNode);
            }
            if (curNode->val > key) {
                /* b. 删除当前节点的左孩子 */
                if (curNode->left && curNode->left->val == key) {
                    curNode->left = deleteCurNode(curNode->left);
                    break;
                }
                curNode = curNode->left;
            } 
            else {
                /* c. 删除当前节点的右孩子 */
                if (curNode->right && curNode->right->val == key) {
                    curNode->right = deleteCurNode(curNode->right);
                    break;
                }
                curNode = curNode->right;
            }
        }
        return root;
    }
};
```

### 19 修剪二叉搜索树

```c++
/**
 * 注意：
 *     0. 难
 *     1. 二叉搜索树的删除操作
 *     2. 当前节点小于区间范围，则删除当前节点及其左子树，只保留右子树
 *     3. 当前节点大于区间范围，则删除当前节点及其右子树，只保留左子树
 *     4. 遍历节点：中序遍历，效率低；
 *     5. （高效处理）先将根节点放到[L, R]区间内（root小于L，则root = root->right；root大于R，则root = root->left），然后分别修剪左子树（curNode->left小于L，则curNode->left = curNode->left->right）和右子树
 */
```

### 20 构造平衡二叉搜索树

```c++
/**
 * 注意：
 *     1. 左闭右闭区间[left, right]，mid = left + (right - left)/2
 *        mid作为当前节点(curNode)，[left, mid-1]作为左子树(curNode->left = ...)，[mid+1, right]作为右子树(curNode->right = ...)
          先创建空节点new TreeNode(0)并放入节点队列，left, right也同步放入left队列和right队列。
          先确定节点指向(curNode->left = TreeNode(0))（先访问节点，并保存访问路径），再沿着访问路径处理节点(curNode->val = nums[mid])
 *	   2. Line-21: TreeNode* root = new TreeNode(0); 
 */
class Solution {
public:
    TreeNode* sortedArrayToBST(vector<int>& nums) {
        std::queue<TreeNode*> queNode;
        std::queue<int> queLeft;
        std::queue<int> queRight;
        TreeNode* curNode;
        TreeNode* root;
        int left = 0;
        int right = nums.size() - 1;
        int mid = left + (right - left)/2;

        root = new TreeNode(0);
        queNode.push(root);
        queLeft.push(left);
        queRight.push(right);
        while (!queNode.empty()) {
            /* 1. 取出创建好的空节点并赋值 */
            curNode = queNode.front();
            queNode.pop();
            left = queLeft.front();
            queLeft.pop();
            right = queRight.front();
            queRight.pop();
            mid = left + (right - left)/2;
            curNode->val = nums[mid];
            /* 2. 构建左子树 */
            if (left <= mid - 1) {
                curNode->left = new TreeNode(0);
                queNode.push(curNode->left);
                queLeft.push(left);
                queRight.push(mid-1);
            }
            if (mid+1 <= right) {
                curNode->right = new TreeNode(0);
                queNode.push(curNode->right);
                queLeft.push(mid+1);
                queRight.push(right);
            }
        }
        return root;
    }
};
```

### 21 转换二叉搜素树为累加树

```c++
/**
 * 注意：
 *     1. 题目要求累加大于等于当前节点的所有值
 *     2. 反中序遍历，访问节点，处理节点（记录当前的累加值）
 */
```

## 回溯算法

回溯法，一般可以解决如下几种问题：

- 组合问题：N个数里面按一定规则找出k个数的集合
- 切割问题：一个字符串按一定规则有几种切割方式
- 子集问题：一个N个数的集合里有多少符合条件的子集
- 排列问题：N个数按一定规则全排列，有几种排列方式
- 棋盘问题：N皇后，解数独等等

### 01 组合问题

```c++
/**
 * 注意：
 *		1. 横向遍历：访问该层所有可能的结果（访问其中一个可能元素，便进入下一层；当回溯后，继续访问另一种可能元素）
 *		2. 纵向遍历：访问新的一层（输入参数：该层所有的可能元素）
 * 		3. 当 k=2 时，快慢指针可以处理；当 k=3 时，快慢指针法难以处理。遍历组合时有时需要弹出第一个元素，有时需要弹出最后一个元素，既que又stk
 *      4. 剪枝优化：优化横向遍历的可能元素，for (int i = startIdx; i <= (endIdx-k+1); i++)
 */
class Solution {
public:
    vector<vector<int>> ans;
    vector<int> path;
    void nextNumber(int startIdx, int endIdx, int k) {
        /* a. 当前深度是否满足k */
        if (k == 0) {
            ans.push_back(path);
            return ;
        }
        /* b. 横向遍历，访问该层所有可能的元素 */
        for (int i = startIdx; i <= (endIdx-k+1); i++) {
            path.push_back(i);
            /* c. 纵向遍历，访问新的一层，深度加一 */
            nextNumber(i+1, endIdx, k-1);
            path.pop_back();
        }
        return ;
    }
    vector<vector<int>> combine(int n, int k) {
        /* 组合问题：回溯算法的应用 */
        nextNumber(1, n, k);

        return ans;
    }
};
```

### 02 电话号码的字符组合

```c++
/**
 * 注意：
 *		1. 字符查找表
 		const string letterMap[10] = {
            "", // 0
            "", // 1
            "abc", // 2
            "def", // 3
            "ghi", // 4
            "jkl", // 5
            "mno", // 6
            "pqrs", // 7
            "tuv", // 8
            "wxyz", // 9
        };
 *		2. 取出数字对应的字符集 string letters = letterMap[digit];
 *		3. 回溯算法：横向遍历 + 纵向遍历
 */
```

### 03 组合总和

```c++
/**
 * 注意：
 *		1. 可重复选取元素的组合问题，新的一层的起始位置等于上一层，否则，会出现重复。
 *		2. 回溯算法：横向遍历 + 纵向遍历
 */
```

### 04 组合总和 II

```c++
/**
 * 注意：
 *		1. 集合中包含重复元素，但是不可重复选取元素的组合问题。a. 利用 map<int, int> 删除重复元素；b. 集合排序后，sort(nums.begin(), nums.end()); 排除重复元素，if (i > startIndex && candidates[i] == candidates[i - 1])
 */
```

### 05 分割回文串

```c++
/**
 * 注意：
 *		1. 回溯算法--分割问题，横向遍历中访问所有可能的分割情况（只切割一个字串，[start, end]）；纵向遍历中访问之前分割后获得的新字符串和新的一层中所有可能的分割情况（[end+1, ...]）
 *		2. 获取string& 变量中的子串，string str = s.substr(startIdx, len);
 *		3. 判断回文串，左右双指针法
 */
```

### 06 复原IP地址

```c++
/**
 * 注意：
 *		1. 回溯算法--分割问题，横向遍历访问所有可能的分割情况（只切割一个子串，[start, end]）；纵向遍历访问之前分割后获得的新字符串和新的一层中所有可能的分割情况（[end+1, ...]）
 *		2. 判断子串是否合法，a. 前导零，if (s[start] == '0' && start != end)；b. 字符串转数字，num = num * 10 + (s[i] - '0');
 */
```

### 07 子集问题

```c++
/**
 * 注意：
 *		1. 两种解法：a. 拆分成从集合中取出长度为 len 的不重复元素组合；b. 在长度为 num.size() 的不重复元素组合问题中保存所有节点，而非叶子节点（叶子是长度固定的组合问题）
 */
class Solution {
public:
    vector<vector<int>> ans;
    vector<int> path;
    void findSubSet(int left, int right, vector<int>& nums, int len) {
        if (len == 0) {
            ans.push_back(path);
            return ;
        }
        for (int i = left; i < right; i++) {
            /* 切割子集 */
            path.push_back(nums[i]);
            findSubSet(i+1, right, nums, len-1);
            path.pop_back();
        }
        return ;
    }
    vector<vector<int>> subsets(vector<int>& nums) {
        /* 回溯算法之遍历所有子集
         * 0. 获取集合 nums 中长度为 len 的所有不重复组合，不同于切割（连续的）
         * 1. 横向遍历，访问当前层（区间[left, right]）所有可能的元素（添加 nums[idx]）
         * 2. 纵向遍历，访问新的一层（区间[idx+1, right]）
         */
        ans.clear();
        path.clear();
        for (int len = 0; len <= nums.size(); len++) {
            findSubSet(0, nums.size(), nums, len);
        }

        return ans;
    }
};
```

### 08 子集问题 II

```c++
/**
 * 注意：
 *		1. 树枝去重：先对 nums 排序，横向遍历时不访问重复元素
 */
class Solution {
public:
    vector<vector<int>> ans;
    vector<int> path;
    void findSubSet(int left, int right, vector<int>& nums) {
        ans.push_back(path);
        for (int i = left; i < right; i++) {
            /* 横向遍历不访问重复的元素 */
            if (i > left && nums[i] == nums[i-1]) {
                continue;
            }
            /* 切割子集 */
            path.push_back(nums[i]);
            findSubSet(i+1, right, nums);
            path.pop_back();
        }
        return ;
    }
    vector<vector<int>> subsetsWithDup(vector<int>& nums) {
        /* 树枝去重：先对 nums 排序，横向遍历时不访问重复元素 */
        ans.clear();
        path.clear();
        sort(nums.begin(), nums.end());
        findSubSet(0, nums.size(), nums);

        return ans;
    }
};
```

### 09 递增子序列

```c++
/**
 * 注意：
 *		1. 子集问题：回溯算法之不重复元素组合，取所有节点
 *		2. 非重复的递增子序列：if ((!path.empty() && nums[i] < path.back()) || used.find(nums[i]) != used.end()) continue;
 *		3. 映射：unordered_set<int> used;
 				used.insert(nums[i]);
 *		4. 哈希表：int used[201] = {0};
				  used[nums[i]+100] = 1;	/* -100 <= nums[i] <= 100 */
 */
```

### 10 全排列

```c++
/**
 * 注意：
 *		1. 前提：数组元素不重复
 *		2. 全排列：横向遍历所有可能的元素（用哈希表 used[num[i]] = true 来标记已用过的元素）；纵向遍历新的一层，直到叶子节点
 */
```

### 11 全排列 II

```c++
/**
 * 注意：
 *		1. vector<>类型变量初始化：vector<bool> used(nums.size(), false);
 *		2. 哈希表下标要做处理：usedNums[nums[i] + -bound] = true;
 *		3. ⭐去重 + 不用上一层的：if ((i > 0 && nums[i] == nums[i-1] && !usedIdxs[i-1]) || usedIdxs[i])
 *		4. 去重 + 不用上一层的：映射表set，if(uset.find(nums[i]) != uset.end() || used[i])
 */
class Solution {
public:
    vector<vector<int>>ans;
    vector<int> path;
    void NextNum(vector<bool> usedIdxs, vector<int> nums) {
        int i = 0;
        /* a. 处理叶子节点 */
        if (path.size() == nums.size()) {
            ans.push_back(path);
            return ;
        }
        /* b. 回溯算法：横向遍历 + 纵向遍历 */
        for (i = 0; i < nums.size(); i++) {
            /* 去重 + 不用上一层的
             * usedIdxs[i-1] = true : 说明nums[i-1]上一层用过了，nums[i]有可能可以用
             * usedIdxs[i-1] = false: 说明本层曾经用过nums[i-1]，不能再用
             */
            if ((i > 0 && nums[i] == nums[i-1] && !usedIdxs[i-1]) || usedIdxs[i]) {
                continue;
            }
            usedIdxs[i] = true;
            path.push_back(nums[i]);
            NextNum(usedIdxs, nums);
            path.pop_back();
            usedIdxs[i] = false;
        }
    }
    vector<vector<int>> permuteUnique(vector<int>& nums) {
        /* 全排列：数组包含重复元素 */
        vector<bool> usedIdxs(10, false);
        sort(nums.begin(), nums.end());
        NextNum(usedIdxs, nums);
        return ans;
    }
};
```

### 12 重新安排行程

```c++
/**
 * 注意：
 *		1. 回溯算法：横向遍历当前起点机场的所有终点机场（按照字典序顺序遍历，并且未访问 ticket.second > 0），纵向遍历下一个起点机场，直到所有航班均被访问
 *		2. ⭐预处理：记录所有起点机场的所有终点机场（按照字典序排序）unordered_map<string, map<string, int>> tickets;
 				map<string, int>：key有序；multimap<string>：key有序
 				pair<const string, int>& ticket : tickets[path[path.size() - 1]]，其中引用&表示可以修改tickets对象中的ticket的值，const string是固定用法，path[path.size() - 1]是取出当前起点机场的字符串
 				vector<vector<string>> nums：输入，遍历nums，记录所有起点机场的所有终点机场。tickets[nums[0]][nums[1]]++;
 				遍历tickets：vector<string>& ticket : tickets
 				如果机票已用过：if (ticket.second <= 0)
 				从路径中删除这张不合理机票：path.pop_back();
 */
class Solution {
public:
    unordered_map<string, map<string, int>> proTickets;
    vector<string> path;
    bool nextTicket(string startAirField, int numTicket) {
        path.push_back(startAirField);
        if (path.size() == numTicket) {
            return true;
        }
        for (pair<const string, int>& ticket : proTickets[startAirField]) {
            if (ticket.second <= 0) {   /* 如果机票已用过，则用下一张 */
                continue;
            }
            ticket.second--;
            if (nextTicket(ticket.first, numTicket)) {
                return true;
            }
            ticket.second++;
            path.pop_back();    /* 从路径中删除这种不合理机票 */
        }
        return false;
    }
    vector<string> findItinerary(vector<vector<string>>& tickets) {
        /* 回溯算法：横向遍历当前起点的所有终点机场（按照字典序排序），纵向遍历下一张机票，直到遍历所有机票
         * 机票数据预处理：unorderedmap<string, map<string, int>> tickets, 根据起点string查询终点string以及该机票是否被使用过int，值value按照字典序排序
         */
        /* 1. 机票数据预处理 */
        for (vector<string>& ticket : tickets) {
            proTickets[ticket[0]][ticket[1]]++;
        }
        nextTicket("JFK", tickets.size()+1);
        return path;
    }
};
```

### 13 N皇后

```c++
/**
 * 注意：
 *		1. 回溯算法：横向遍历当前行所有可能的皇后位置；纵向遍历下一个皇后，直到遍历所有皇后
 *		2. 当前行只影响后续选择，不影响以前的选择
 *		3. 输出结果处理：将数字组合还原成字符棋盘
 */
class Solution {
public:
    vector<vector<string>> ans;
    vector<vector<int>> sumPath;
    vector<int> path;
    vector<vector<int>> posQueen = vector<vector<int>>(10, vector<int>(10, 0));
    void markQueen(int row, int col, int add) {
        /* 当前行只影响后续选择，不影响以前的选择 */
        int i, j;
        /* 当前列 */
        for (i = row+1; i <= 9; i++) {
            posQueen[i][col] += add; 
        }
        /* 左下斜线 */
        for (i = row+1, j = col-1; i <= 9 && 1 <= j; i++, j--) {
            posQueen[i][j] += add;
        }
        /* 右下斜线 */
        for (i = row+1, j = col+1; i <= 9 && j <= 9; i++, j++) {
            posQueen[i][j] += add;
        }
    }
    void nextQueen(int curQueen, int numQueen) {
        /* 回溯算法：横向遍历当前行所有可能的皇后位置；纵向遍历下一个皇后，直到遍历所有皇后 */
        if (path.size() == numQueen) {
            sumPath.push_back(path);
            return ;
        }
        for (int pos = 1; pos <= numQueen; pos++) {
            if (posQueen[curQueen][pos]) {  /* 不能放入皇后 */
                continue;
            }
            path.push_back(pos);
            markQueen(curQueen, pos, 1);
            nextQueen(curQueen+1, numQueen);
            markQueen(curQueen, pos, -1);
            path.pop_back();
        }
        return ;
    }
    vector<vector<string>> solveNQueens(int n) {
        vector<string> curStrs;
        string curStr;

        nextQueen(1, n);
        /* 输出结果处理：将数字组合还原成字符棋盘 */
        for (vector<int>& curPath : sumPath) {
            curStrs.clear();
            for (int curPos : curPath) {
                curStr.clear();
                for (int i = 1; i <= n; i++) {
                    if (i == curPos) {
                        curStr.push_back('Q');
                    }
                    else {
                        curStr.push_back('.');
                    }
                }
                curStrs.push_back(curStr);
            }
            ans.push_back(curStrs);
        }
        return ans;
    }
};
```

### 14 解数独

## 贪心算法

通过局部最优，推出整体最优。

**最好用的策略就是举反例，如果想不到反例，那么就试一试贪心吧**。

贪心算法一般分为如下四步：

- 将问题分解为若干个子问题
- 找出适合的贪心策略
- 求解每一个子问题的最优解
- 将局部最优解堆叠成全局最优解

### 01 摆动数组

```c++
/* 摆动序列：遍历数组，平坡、上坡、下坡则跳过（不摆动则跳过当前节点）
 * 判断峰值：preDiff, curDiff，当出现摆动时，更新preDiff
 */
class Solution {
public:
    int wiggleMaxLength(vector<int>& nums) {
        /* 摆动序列：遍历数组，平坡、上坡、下坡则跳过（不摆动则跳过当前节点）
         * 判断峰值：preDiff, curDiff，当出现摆动时，更新preDiff
         */
        int preDiff;
        int curDiff;
        int ans;

        if (nums.size() <= 1) {
            return 1;
        }

        ans = 1;
        preDiff = 0;
        for (int i = 1; i < nums.size(); i++) {
            curDiff = nums[i] - nums[i-1];
            /* 出现摆动，更新preDiff，记录摆动元素 */
            if ((curDiff > 0 && preDiff <= 0) || (curDiff < 0 && preDiff >= 0)) {
                ans++;
                preDiff = curDiff;
            }
        }
        return ans;
    }
};
```

### 02 最大子数组和

```c++
/**
 * 注意：
 *		1. 最大子数组和：长度为k，滑动窗口，超时
 *		2. 贪心算法：如果当前和为负数，则从下一个数重新计算
 */

class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        /* 最大子数组和：如果当前和为负数，则重新开始计算 */
        int maxSum = 0;
        int curSum = 0;
        maxSum = nums[0];
        for (int i = 0; i < nums.size(); i++) {
            curSum += nums[i];
            maxSum = (maxSum > curSum) ? maxSum : curSum;
            if (curSum < 0) {   /* 重新开始计算 */
                curSum = 0;
            }
        }
        return maxSum;
    }
};

class Solution {
public:
    int maxSum = 0;
    void sumSubArray(vector<int>& nums, vector<int>&preSums, int k) {
        int curSum;
        for (int i = 0; i < nums.size()-k+1; i++) {
            curSum = preSums[i+k] - preSums[i];
            maxSum = (maxSum > curSum) ? maxSum : curSum;
        }
        return ;
    }
    int maxSubArray(vector<int>& nums) {
        /* 最大子数组和：长度为k，滑动窗口 */
        /* 前缀和 */
        int i;
        vector<int> preSums = vector<int>(nums.size()+1, 0);
        for (i = 0; i < nums.size(); i++) {
            preSums[i+1] = preSums[i] + nums[i];
        }
        maxSum = nums[0];
        for (i = 1; i <= nums.size(); i++) {
            sumSubArray(nums, preSums, i);
        }
        return maxSum;
    }
};
```

### 03 买卖股票的最佳时机 II

```c++
/* 买卖股票的最佳时机 II：相邻两天的股票差价，大于0则有利润 */
```

### 04 跳跃游戏

```c++
/* 跳跃游戏：记录当前可以达到的最远距离（最远距离以前的都可以到达，超过最远距离则无法到达） */
```

### 05 跳跃游戏 II

```c++
/* 最小跳跃次数：
 *	1. 记录当前下标的最小跳跃次数
 *	2. 记录当前覆盖的最远距离和下一步覆盖的最远距离，当遇到当前覆盖的最远距离时，步数加1，更新可覆盖的最远距离，如果距离大于目标下标，则已得到最小跳跃次数。
 */
```

### 06 K次取反后最大化的数组和

```c++
/**
 * 注意：
 *		1. 排序sort(nums.begin(), nums.end())，反转最小值，如果最小值为负数，则需要重新排序，时间复杂度高
 *		2. ⭐按照绝对值大小排序sort(nums.begin(), nums.end(), cmp)，遍历数组，遇到负数则反转，如果没有负数，则反转最后一个元素，直到用完K次。
 			static bool cmp(int a, int b) {
 				return abs(a) > abs(b);
 			}
 */
```

### 07 加油站

```c++
/**
 * 注意：
 *		1. 途径加油站要保证始终富裕：当前加油站的供给 - 消耗 = 富裕供给，记录当前总富裕量：sumRich，如果sumRich < 0，则更新起始位置为i+1，表示[0~i]区间不存在起始位置
 *		2. 环形暴力枚举：while
 */
class Solution {
public:
    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {
        /* 加油站：供给 >= 消耗，当前加油站的供给 - 消耗 = 富裕供给，富裕供给越多越好
         * 记录当前总富裕量：sumRich，如果sumRich < 0，则更新起始位置为i+1，表示[0~i]区间不存在起始位置
         */
        vector<int> richNums = vector<int>(gas.size(), 0);
        int curSumRich;
        int startIdx;
        int sumRich;

        sumRich     = 0;
        curSumRich  = 0;
        startIdx    = 0;
        for (int i = 0; i < gas.size(); i++) {
            richNums[i] = gas[i] - cost[i];
            sumRich     += richNums[i];
            curSumRich  += richNums[i];
            /* 更新起始位置，重新记录总富余量 */
            if (curSumRich < 0) {
                curSumRich  = 0;
                startIdx    = i+1;
            }
        }
        if (sumRich < 0) {
            return -1;
        }
        return startIdx;
    }
};
```

### 08 分发糖果

```c++
/**
 * 注意：
 *		1. 思路：先确定一边后，再确定另一边，不能同时考虑两边而顾此失彼。右孩子评分大于左孩子，则右孩子多拿一颗糖果（从前往后遍历，因为child[i+1](左)与child[i+2](右)比较的前提是child[i](左)与child[i+1](右)的比较结果）；左孩子评分大于右孩子，则左孩子取max(left, right+1)（从后往前遍历）。
 *		2. 如果山峰左右不平衡，则左右两次遍历的 +1 的结果也不同，要取最大值。
 *		3. 找到山顶和山谷，但是平坡不好判断，因为平坡除了两端以外，只需要1颗糖果。
 */
class Solution {
public:
    int candy(vector<int>& ratings) {
        /* 分发糖果：
         *  1. 找到山顶和山谷，但是平坡不好判断，因为平坡除了两端以外，只需要1颗糖果
         *  2. a) 右边评分高的孩子比左边的多一颗糖果，从前往后遍历；
         *     b) 左边评分高的孩子比右边的多一颗糖果，从后往前遍历
         */
        vector<int> childCandy = vector<int>(ratings.size(), 1);
        int ans;
        /* a. (从前往后遍历)右边评分高的孩子比左边的多一颗糖果 */
        for (int i = 1; i < ratings.size(); i++) {
            if (ratings[i] > ratings[i-1]) {
                childCandy[i] = childCandy[i-1] + 1;
            }
        }
        /* b. (从后往前遍历)左边评分高的孩子比右边的多一颗糖果 */
        for (int i = ratings.size() - 2; i >= 0; i--) {
            if (ratings[i] > ratings[i+1]) {
                /* 如果山峰左右不平衡，则左右两次遍历的 +1 的结果也不同，要取最大值 */
                childCandy[i] = max(childCandy[i], childCandy[i+1]+1);
                // childCandy[i] = childCandy[i+1]+1;
            }
        }
        /* c. 糖果计数 */
        ans = 0;
        for (int num : childCandy) {
            ans += num;
        }
        return ans;
    }
};
```

### 09 柠檬水找零

```c++
/**
 * 注意：
 *	1. 柠檬水找零：暴力枚举，记录零钱数量，逐个找零
 */
class Solution {
public:
    typedef struct Change {
        int fives;
        int tens;
    } Change;
    bool lemonadeChange(vector<int>& bills) {
        /* 柠檬水找零：暴力枚举，记录零钱数量，逐个找零 */
        Change curChange;
        curChange.fives = 0;
        curChange.tens  = 0;
        for (int bill : bills) {
            switch (bill) {
                case 5:
                    curChange.fives++;
                    break;
                case 10:
                    curChange.tens++;
                    curChange.fives--;
                    if (curChange.fives < 0) {
                        return false;
                    }
                    break;
                case 20:
                    if (curChange.tens > 0) {
                        curChange.tens--;
                        curChange.fives--;
                    }
                    else {
                        curChange.fives = curChange.fives - 3;
                    }
                    if (curChange.fives < 0) {
                        return false;
                    }
                    break;
                default:
                    break;
            }
        }
        return true;
    }
};
```

### 10 根据身高重建队列

```c++
/**
 * 注意：
 *	1. 身高降序排列，保证前面的人一定比后面的高；
 	   static bool compare() {}
 	   sort(people.begin(), people.end(), compare);
 *	2. ⭐按照属性k逐个插入，注意到靠后的矮的人插入不影响前面高的插入结果。
 	   遍历vector<vector<int>>类型：for (const vector<int>& man : people)
 	   在指定位置插入元素：ans.insert(ans.begin() + man[1], man);
 *	3. ⭐链表 list 在指定位置插入元素：
 	   list<vector<int>> que;
 	   std::list<vector<int>>:: iterator it = que.begin();
 	   while (postion--) {
 	   		it++;
 	   }
 	   que.insert(it, man);
 *	4. ⭐vector 的底层实现：vector 的大小有两个维度，size 和 capicity，动态扩容时 vector 申请两倍原数组大小的数组，并将数据全部拷贝到新数组中，但是只对外暴露 size+1 的大小。此时，capicity > size。
 */
class Solution {
public:
    static bool compare(vector<int> a, vector<int> b) {
        if (a[0] != b[0]) {
            return a[0] > b[0];
        }
        else {
            return a[1] < b[1];
        }
    }
    vector<vector<int>> reconstructQueue(vector<vector<int>>& people) {
        /* 根据身高重建队列：
         *   1. 身高降序排列，保证前面的人一定比后面的高；
         *   2. 按照属性k逐个插入，注意到靠后的矮的人插入不影响前面高的插入结果 */
        vector<vector<int>> ans;
        sort(people.begin(), people.end(), compare);
        for (const vector<int>& man : people) {
            ans.insert(ans.begin() + man[1], man);
        }
        return ans;
    }
};
```

### 11 引爆气球

```c++
/* 引爆气球：
 *  1. 按照 x_start 排序，保证后续气球只在前方气球的右边重叠，这样射箭时射到气球的最右边
 *  2. 先射出一箭，引爆气球，然后遍历所有气球，将该箭引爆的所有气球标记，后续不再遍历
 */
class Solution {
public:
    static bool compare(vector<int> a, vector<int> b) {
        if (a[0] != b[0]) {
            return a[0] < b[0];
        }
        else {
            return a[1] < b[1];
        }
    }
    int findMinArrowShots(vector<vector<int>>& points) {
        /* 引爆气球：
         *  1. 按照 x_start 排序，保证后续气球只在前方气球的右边重叠，这样射箭时射到气球的最右边
         *  2. 先射出一箭，引爆气球，然后遍历所有气球，将该箭引爆的所有气球标记，后续不再遍历
         */
        vector<bool> isBoom;
        int bound;
        int ans;

        /* 1. 按照 x_start 排序 */
        sort(points.begin(), points.end(), compare);
        /* 2. 射箭引爆气球 */
        isBoom = vector<bool>(points.size(), false);
        ans    = 0;
        for (int i = 0; i < points.size(); i++) {
            /* 如果已引爆 */
            if (isBoom[i]) {
                continue;
            }
            /* 引爆该气球 */
            isBoom[i] = true;
            bound     = points[i][1];
            for (int j = i; j < points.size(); j++) {
                /* 超出边界 */
                if (points[j][0] > bound) {
                    break;
                }
                /* 更新重叠气球的最小右边界 */
                if (points[j][1] < bound) {
                    bound = points[j][1];
                }
                isBoom[j] = true;
            }
            ans++;
        }
        return ans;
    }
};
```

### 12 无重叠区间

```c++
/* 无重叠区间：
 *  1. 区间 [left, right] 排序: a[0] < b[0]; if (a[0] == b[0]) a[1] > b[1]
 *  2. 遍历，当前区间如果与后续区间重叠，则删除当前区间（left开头最大的区间）
 *	   ❌ 示例：[1, 100], [1, 11], [2, 12], [11, 22] 期望：2，输出：3
 *	3. 遍历，当前区间如果与后续区间重叠，保留 end 最小的区间（它与后续不重叠的可能性更大）
 */
class Solution {
public:
    static bool compare(vector<int> a, vector<int> b) {
        if (a[0] != b[0]) {
            return a[0] < b[0];
        }
        else {
            return a[1] > b[1];
        }
    }
    int eraseOverlapIntervals(vector<vector<int>>& intervals) {
        /* 无重叠区间：
         *  1. 区间 [left, right] 排序: a[0] < b[0]; if (a[0] == b[0]) a[1] > b[1]
         *  2. 遍历，当前区间如果与后续区间重叠，保留 end 最小的区间（它与后续不重叠的可能性更大）
         */
        int curEnd;
        int ans;

        /* 1. 排序 */
        sort(intervals.begin(), intervals.end(), compare);

        /* 2. 遍历：当前区间如果与后续区间重叠，保留 end 最小的区间（它与后续不重叠的可能性更大） */
        ans = 0;
        curEnd = intervals[0][1];
        for (int i = 1; i < intervals.size(); i++) {
            /* 两个区间重叠 */
            if (intervals[i][0] < curEnd) {
                ans++;
                /* 保留 end 最小的区间，即保留最不可能与后续区间重叠的区间 */
                /* 注意后续区间的 start 一定比当前的 start 大 */
                curEnd = (intervals[i][1] > curEnd) ? curEnd : intervals[i][1];
            }
        }
        else { /* 不重叠，则更新 curEnd 为下一个不重叠区间的 end */
            curEnd = intervals[i][1];
        }
        return ans;
    }
};
```

### 13 划分字母区间

```c++
/* 划分字母区间：
 *  1. 遍历，记录所有字母的最远边界，即为分割点
 *	2. 遍历，分割点更新为当前片段内字母的最远边界，得到片段长度列表
 */
class Solution {
public:
    vector<int> partitionLabels(string s) {
        /* 划分字母区间：
         *  1. 遍历，记录所有字母的最远边界，即为分割点
         *	2. 遍历，分割点更新为当前片段内字母的最远边界，得到片段长度列表
         */
        vector<int> charBounds;
        vector<int> ans;
        int maxBound;
        int curBound;
        int startIdx;
        int len;

        /* 1. 遍历，记录所有字母的最远边界 */
        charBounds = vector<int>(26, 0);
        for (int i = 0; i < s.length(); i++) {
            charBounds[s[i] - 'a'] = i;
        }

        /* 2. 遍历，分割点更新为当前片段内字母的最远边界，得到片段长度列表 */
        curBound = 0;
        maxBound = 0;
        startIdx = 0;
        for (int i = 0; i < s.length(); i++) {
            curBound = charBounds[s[i] - 'a'];
            maxBound = (curBound > maxBound) ? curBound : maxBound;
            /* 遇到当前片段内字母的最远边界，分割 */
            if (i == maxBound) {
                len = i - startIdx + 1;
                ans.push_back(len);
                startIdx = i + 1;
                maxBound = 0;
                continue;
            }
        }

        return ans;
    }
};
```

### 14 合并区间

```c++
/* 合并区间：
 *  1. 区间排序[left, right]，left 升序，right 降序
 *  2. 如果区间重合，则更新 bound 为最远 right；如果区间不重合，则记录已合并的不重叠区间
 */
class Solution {
public:
    static bool compare(vector<int> a, vector<int> b) {
        if (a[0] != b[0]) {
            return a[0] < b[0];
        }
        else {
            return a[1] > b[1];
        }
    }
    vector<vector<int>> merge(vector<vector<int>>& intervals) {
        /* 合并区间：
         *  1. 区间排序[left, right]，left 升序，right 降序
         *  2. 如果区间重合，则更新 bound 为最远 right；如果区间不重合，则记录已合并的不重叠区间
         */
        vector<vector<int>> ans;
        vector<int> ansInterv;
        int curLeft;
        int curRight;
        int start;
        int end;

        /* 1. 区间排序[left, right]，left 升序，right 降序 */
        sort(intervals.begin(), intervals.end(), compare);

        /* 2. 遍历所有区间 */
        start = intervals[0][0];
        end   = intervals[0][1];
        curLeft   = start;
        curRight  = end;
        ansInterv = vector<int>(2, 0);
        for (int i = 1; i < intervals.size(); i++) {
            curLeft  = intervals[i][0];
            curRight = intervals[i][1];
            /* a. 如果区间重叠，则更新 end 为最远 right */
            if (curLeft <= end) {
                end = (curRight > end) ? curRight : end;
            }
            /* b. 如果区间不重合，则记录已合并的不重叠区间 */
            else {
                ansInterv[0] = start;
                ansInterv[1] = end;
                ans.push_back(ansInterv);
                start = intervals[i][0];
                end   = intervals[i][1];
            }
        }
        ansInterv[0] = start;
        ansInterv[1] = end;
        ans.push_back(ansInterv);

        return ans;
    }
};
```

### 15 位数单调递增的数字

```c++
/* 数字位数单调递增：
 *	0. 数字转为位数数组：
 		string strNum = to_string(N);
 		vector<int> digits;
        int temp = n;
        while (temp > 0) {	// temp是随着位数数组元素的添加而变化的。
            digits.push_back(temp % 10);
            temp /= 10;
        }
        reverse(digits.begin(), digits.end()); // 翻转数组
 *  1. 倒序遍历，如果不满足单调递增，则前一位 -1，后一位为9（既最大，又容易满足单调递增）
 *  2. 多次遍历，直到所有位数均满足单调递增
 *  3. 示例："1234", "12321", "12323", "100"(❌)
 *	4. 倒序遍历，如果不满足单调递增，则前一位 Arr[i-1] - 1，并标记 i 为 9 的起始位置
 */
```

## 动态规划

### 00 基础

动态规划问题的关键点

1. 确定 dp 数组以及下标的含义
2. 确定递推公式
3. dp 数组如何初始化
4. 确定遍历顺序
5. 举例推导 dp 数组

### 01 爬楼梯

```c++
/* 爬楼梯：
 *  1. dp[i] : 爬上 i 阶楼梯的方法数
 *  2. 递推公式：dp[i] = dp[i-1] + dp[i-2]，爬 i 阶楼梯的方法数是 i-1 阶和 i-2 阶的方法数之和
 *  3. 初始化：dp[1] = 1, dp[2] = 2
 *  4. 遍历顺序：根据依赖关系可得从前往后遍历
 *  5. 举例：dp[3] = 3, dp[4] = 5
 */
class Solution {
public:
    int climbStairs(int n) {
        vector<int> dp = vector<int>(46, 0);
        dp[1] = 1;
        dp[2] = 2;
        for (int i = 3; i <= n; i++) {
            dp[i] = dp[i-1] + dp[i-2];
        }

        return dp[n];
    }
};
```

### 02 爬楼梯的最小花费

```c++
/* 爬楼梯的最小花费
 *  0. 顶层为 cost.size() + 1
 *  1. dp[i] : 爬到第 i 个台阶的最小花费
 *  2. 递推公式：dp[i] = min{dp[i-1] + cost[i-1], dp[i-2] + cost[i-2]}，
    第 i 个台阶是从第 (i-1)/(i-2) 个台阶上来的，并分别经历了 cost[i-1]/cost[i-2]，取其中最小花费
 *  3. 初始化：dp[0] = 0, dp[1] = 0，这两个台阶不需要花费
 *  4. 遍历顺序：根据依赖关系从前往后遍历
 *  5. 动手模拟：[1, 100, 1, 1, 1, 100, 1, 1, 100, 1]
 */
class Solution {
public:
    int minCostClimbingStairs(vector<int>& cost) {
        vector<int> dp = vector<int>(cost.size() + 1, 0);
        dp[0] = 0;
        dp[1] = 0;
        for (int i = 2; i <= cost.size(); i++) {
            dp[i] = min(dp[i-1] + cost[i-1], dp[i-2] + cost[i-2]);
        }
        return dp[cost.size()];
    }
};
```

### 03 不同路径

```c++
 /* 不同路径：
 *  1. dp[i][j] : 到达 (i, j) 的可能路径条数
 *  2. 递推公式：dp[i][j] = dp[i-1][j] + dp[i][j-1]
    (i, j) 要么 (i-1, j) 向右走一步到达，要么 (i, j-1)向下走一步到达
 *  3. 初始化: dp = vector<vector<int>>(m+1, vector<int>(n+1, 0));
             dp[0][1] = 1 : 保证dp[1][1] = 1, dp[1][2] = 1, dp[2][1] = 1
 *  4. 遍历顺序：从前往后
 *  5. 动手模拟：...
 */
class Solution {
public:
    int uniquePaths(int m, int n) {
       vector<vector<int>> dp = vector<vector<int>>(m+1, vector<int>(n+1, 0));
        dp[0][1] = 1;
        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                dp[i][j] = dp[i-1][j] + dp[i][j-1];
            }
        }
        return dp[m][n];
    }
};
```

### 04 不同路径 II

```c++
/**
 *	0. 如果终点或者起点遇到障碍物，则直接返回 0 
 *	0. 如果遇到障碍物，则之后的 (i, 0) 均为 0
 *	0. dp[0][0] = 1;
 *	0. 遍历中，如果 (i, j) 遇到障碍物，则dp[i][j] = 0
 */
class Solution {
public:
    int uniquePathsWithObstacles(vector<vector<int>>& obstacleGrid) {
        /* 不同路径 II:
         *  1. dp
         */
        vector<vector<int>> dp;
        int row;
        int col;

        row = obstacleGrid.size();
        col = obstacleGrid[0].size();
        dp  = vector<vector<int>>(row, vector<int>(col, 0));
        /* 0. 如果终点或者起点遇到障碍物，则直接返回 0 */
        if (obstacleGrid[row-1][col-1] || obstacleGrid[0][0]) {
            return 0;
        }
        /* 0. 如果遇到障碍物，则之后的 (i, 0) 均为 0 */
        for (int i = 0; i <= row-1 && !obstacleGrid[i][0]; i++) {
            dp[i][0] = 1;
        }
        for (int j = 0; j <= col-1 && !obstacleGrid[0][j]; j++) {
            dp[0][j] = 1;
        }
        for (int i = 1; i <= row-1; i++) {
            for (int j = 1; j <= col-1; j++) {
                if (obstacleGrid[i][j]) {
                    continue;
                }
                dp[i][j] = dp[i-1][j] + dp[i][j-1];
            }
        }
        return dp[row-1][col-1];
    }
};
```

### 05 整数拆分

```c++
/**
 * 整数拆分
 *	1. 递推公式：a. 上一次计算的: dp[i]；b. 直接相乘: j*(i-j)；c. 继续拆分(i-j): j*dp[i-j]
 */
class Solution {
public:
    int integerBreak(int n) {
        /* 整数拆分 */
        vector<int> dp;
        dp = vector<int>(60, 0);
        for (int i = 2; i <= n; i++) {
            for (int j = 1; j <= i/2; j++) {
                dp[i] = max(dp[i], max(j*(i-j), j*dp[i-j]));
            }
        }
        return dp[n];
    }
};
```

### 06 不同的二叉搜索树

```c++
/* 二叉搜索树：
 *  1. dp[i] : 根节点为 i 的二叉搜索树种类
 *  2. 递推公式：dp[i] = dp[1] + ... + dp[j] + ... + dp[i]
    表示：根节点分别为 1, ..., i 的二叉搜索树种类
    dp[j] = dp[j-1] + dp[i-j]，表示左子树为 j-1 和 右子树为 i-j 的二叉搜索树种类
 *  3. 。。。
 */
class Solution {
public:
    int numTrees(int n) {
        vector<int> dp = vector<int>(20, 0);
        dp[0] = 1;
        dp[1] = 1;
        dp[2] = 2;
        for (int i = 3; i <= n; i++) {
            for (int j = 1; j <= i; j++) {
                dp[i] = dp[i] + (dp[j-1] * dp[i-j]);
            }
        }
        return dp[n];
    }
};
```

### 07 01背包(携带研究材料)

```c++
/* 卡码网 */
#include <iostream>
#include <vector>

using namespace std;

int main(void) {
    vector<vector<int>> dp;
    vector<int> value;
    vector<int> space;
    int m, n;
    /* 1. 读取输入数据 */
    cin >> m >> n;
    space = vector<int>(m, 0);
    value = vector<int>(m, 0);
    for (int i = 0; i < m; i++) {
        cin >> space[i];
    }
    for (int i = 0; i < m; i++) {
        cin >> value[i];
    }
    /* a. dp[i][j] : 下标为 0~i 的物品，装入空间 j 的最大价值
     * b. 递推公式：
     	dp[i][j] = dp[i-1][j];
     	dp[i][j] = max(dp[i-1][j], dp[i-1][j-space[i]] + value[i])
        不放入/放入第 i 个物品后，空间 j 的最大价值
     * c. 初始化：先放入第 1 个物品，不放入/放入为 0/value[0]，
     * d. 遍历顺序：i: 0~m, j: 0~n
     * e. 动手模拟：...
     */
    dp = vector<vector<int>>(m, vector<int>((n+1), 0));
    for (int j = 1; j <= n; j++) {
        if (j < space[0]) {
            continue;
        }
        dp[0][j] = value[0];
    }
    for (int i = 1; i < m; i++) {
        for (int j = 1; j <= n; j++) {
            if (j < space[i]) {
                dp[i][j] = dp[i-1][j];
                continue;
            }
            dp[i][j] = max(dp[i-1][j], dp[i-1][j-space[i]] + value[i]);
        }
    }
    cout << dp[m-1][n] << endl;

    return 0;
}
```

### 08 分割等和子集

```c++
/* 分割等和子集：01 背包，背包容量 sum/2，物品数字的重量 = 价值，
装满背包时的最大价值 == sum/2，则正确。
 */
class Solution {
public:
    bool canPartition(vector<int>& nums) {
        vector<vector<int>> dp;
        vector<int> costs  = nums;
        vector<int> values = nums;
        int sum = 0;
        int m = nums.size();
        int n = 0;
        for (int i = 0; i < m; i++) {
            sum += costs[i];
        }
        n = sum/2;
        /* 01 背包 */
        dp = vector<vector<int>>(m, vector<int>((n+1), 0));
        for (int j = costs[0]; j <= n; j++) {
            dp[0][j] = values[0];
        }
        for (int i = 1; i < m; i++) {
            for (int j = 1; j <= n; j++) {
                if (j < costs[i]) {
                    dp[i][j] = dp[i-1][j];
                    continue;
                }
                dp[i][j] = max(dp[i-1][j], dp[i-1][j-costs[i]] + values[i]);
            }
        }
        if (dp[m-1][n]*2 == sum) {
            return true;
        }
        return false;
    }
};
```

### 09 最后一块石头的重量 II

```c++
/**
 * 注意：
 *	0. ⭐将一堆石头分成两堆尽可能重量相等的石头，即背包大小为 sum/2 时最多能装多少东西
 */
```

### 10 目标和

```c++
class Solution {
public:
    int findTargetSumWays(vector<int>& nums, int target) {
        /* 目标和：01 背包之装满背包的方法数
         *  0. sum < target || (sum + target) % 2 != 0
         *  1. dp[i][j] : 用下标为 0~i 的物品装满空间为 j 的背包的方法数
         *  2. 递推公式：
                dp[i][j] = dp[i-1][j];
                dp[i][j] = dp[i-1][j] + dp[i-1][j-costs[i]];
                dp[i][j] 包括不放入/放入物品 i 的方法数之和
         *  3. 初始化：dp[i][0] = 1; dp[0][costs[i]] = 1;
                数组中包括 0，则dp[i][0] = (int)pow(2.0, numZero), numZero 为 0~i 中 0 的数量
         *  4. 遍历顺序：
         *  5. 动手模拟：...
         */
    }
};
```

### 11 一和零

```c++
/* 01 背包：一个物体有两个重量，并且价值为 1，方法：滚动数组
 *  1. dp[i][j] : 背包空间为 i 个 0 和 j 个 1 时的最大子集长度
 *  2. 递推公式：dp[i][j] = max(dp[i][j], dp[i-costs0[i]][j-costs1[j]]+1);
                dp[i][j]在被新的物体 i 覆盖之前，为下标区间 0~i-1 的数据
 *  3. 初始化：因为所有物体价值非零，则初始化为全零数组
 *  4. 遍历顺序：倒序遍历，防止 dp[icosts0[i]] 提前被覆盖
 */
class Solution {
public:
    int findMaxForm(vector<string>& strs, int m, int n) {
        vector<vector<int>> dp = vector<vector<int>>((m+1), vector<int>(n+1, 0));
        vector<int> costs0;
        vector<int> costs1;
        int numItem = strs.size();
        costs0 = vector<int>(numItem, 0);
        costs1 = vector<int>(numItem, 0);
        /* 0. 所有物体的重量 */
        for (int k = 0; k < strs.size(); k++) {
            for (char c : strs[k]) {
                if (c-'0') {
                    costs1[k]++;
                }
                else {
                    costs0[k]++;
                }
            }
        }
        for (int k = 0; k < strs.size(); k++) {
            for (int i = m; i >= costs0[k]; i--) {
                for (int j = n; j >= costs1[k]; j--) {
                    dp[i][j] = max(dp[i][j], dp[i-costs0[k]][j-costs1[k]]+1);
                }
            }
        }
        return dp[m][n];
    }
};
```

### 12 完全背包

```c++
/* 卡码网 */
/* 完全背包：
 *  1. dp[i][j] : 空间为 j 时放入下标为 0~i 的物品的最大价值
 *  2. 递推公式：dp[i][j] = max(dp[i-1][j], dp[i][j-costs[i]] + values[i])
        dp[i][j-costs[i]] : 表示可能已经放入过物品 i
 *  3. 初始化：dp[i][0] = 0, dp[0][j > costs[i]] = values[i];
 *  4. 遍历顺序：
 *  5. 动手模拟：
 */
#include <iostream>
#include <vector>

using namespace std;

int main(void) {
    int numItem;
    int numSpace;
    vector<int> costs;
    vector<int> values;
    vector<vector<int>> dp;

    cin >> numItem >> numSpace;
    costs  = vector<int>(numItem, 0);
    values = vector<int>(numItem, 0);
    for (int i = 0; i < numItem; i++) {
        cin >> costs[i];
        cin >> values[i];
    }

    dp = vector<vector<int>>(numItem, vector<int>(numSpace+1, 0));
    for (int j = costs[0]; j <= numSpace; j++) {
        dp[0][j] = dp[0][j-costs[0]] + values[0];
    }
    for (int i = 1; i < numItem; i++) {
        for (int j = 0; j <= numSpace; j++) {
            if (j < costs[i]) {
                dp[i][j] = dp[i-1][j];
                continue;
            }
            dp[i][j] = max(dp[i-1][j], dp[i][j - costs[i]] + values[i]);
        }
    }

    cout << dp[numItem-1][numSpace] << endl;

    return 0;
}
```

### 13 零钱兑换 II

```c++
/* 零钱兑换 II：完全背包之装满背包的方法总数
 *	0. int 类型越界
 *  1. dp[i][j]: 用下标 0~i 的物品装满空间 j 的方法总数
 *  2. 递推公式：dp[i][j] = dp[i-1][j] + dp[i][j-coins[i]];
 *  3. 初始化：dp[i][0] = 1; dp[0][j >= coins[0]] = dp[0][j-coins[0]];
 *  4. 遍历顺序：0~i, 1~j
 */
class Solution {
public:
    int change(int amount, vector<int>& coins) {
        int numItem = coins.size();
        vector<vector<int>> dp = vector<vector<int>>(numItem, vector<int>(amount+1, 0));

        /* a. 初始化 */
        for (int i = 0; i < numItem; i++) {
            dp[i][0] = 1;
        }
        for (int j = coins[0]; j <= amount; j++) {
            dp[0][j] = dp[0][j-coins[0]];
        }
        /* b. 递推 */
        for (int i = 1; i < numItem; i++) {
            for (int j = 1; j <= amount; j++) {
                if (j < coins[i]) {
                    dp[i][j] = dp[i-1][j];
                    continue;
                }
                dp[i][j] = dp[i-1][j] + dp[i][j-coins[i]];
            }
        }
        return dp[numItem-1][amount];
    }
};
```

### 14 排列总数

```c++
/* 排列总和数：
 *	0. int 类型越界
 *  1. dp[j] : 装满空间 j 的背包的排列总数
 *  2. 递推公式：if (j >= costs[i]) dp[j] += dp[j-costs[i]];
 *  3. 初始化：dp[0] = 1, 其余全零
 *  4. 遍历顺序：先 0~j，再 0~i 是排列总数；
                先 0~i，再 0~j 是组合总数
 *  5. 动手模拟：
 */
class Solution {
public:
    int combinationSum4(vector<int>& nums, int target) {
        vector<int> dp = vector<int>(target+1, 0);

        dp[0] = 1;
        for (int j = 1; j <= target; j++) {
            for (int i = 0; i < nums.size(); i++) {
                if (j >= nums[i] && dp[j] < INT_MAX - dp[j - nums[i]]) {
                    dp[j] += dp[j-nums[i]];
                }
            }
        }
        return dp[target];
    }
};
```

### 15 爬楼梯 II

```c++
/* 爬楼梯：完全背包之排列总和
 *  1. dp[j] : 用下标 0~i 的物品填满空间 j 的方法总数
 *  2. 递推公式：dp[j] = dp[j] + dp[j-costs[i]];
 *  3. 初始化：dp[0] = 1, 其余全零
 *  4. 遍历顺序：先 0~j，再 0~i 是所有排列方式
 *  5. 动手模拟：n = 3, m = 2
 */
#include <iostream>
#include <vector>

using namespace std;

int main(void) {
    vector<int> costs;
    vector<int> dp;
    int m, n;

    cin >> n >> m;
    costs = vector<int>(m, 0);
    for (int i = 0; i < m; i++) {
        costs[i] = i+1;
    }

    dp = vector<int>(n+1, 0);
    dp[0] = 1;
    for (int j = 1; j <= n; j++) {
        for (int i = 0; i < m; i++) {
            if (j >= costs[i]) {
                dp[j] = dp[j] + dp[j-costs[i]];
            }
        }
    }

    cout << dp[n] << endl;
}
```

### 16 零钱兑换

```c++
/* 零钱兑换：完全背包之最少硬币个数
 *  1. dp[j] : 用物品 0~i 装满空间 j 的最少硬币个数
 *  2. 递推公式：dp[j] = min(dp[j], dp[j-costs[i]]+1); 
    if (dp[j-costs[i]] == -1) dp[j]不变
    if (dp[j] == -1) dp[j] = dp[j-costs[i]] + 1;
 *  3. 初始化：dp[0] = 0; 其余全 -1
 *  4. 遍历顺序：先 0~i，再 0~j
 *  5. 动手模拟：amount = 11, coins = [1, 2, 5]; 
                amount = 12, coins = [2, 5];
 */
class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {
        vector<int> dp = vector<int>(amount+1, -1);
        dp[0] = 0;
        for (int i = 0; i < coins.size(); i++) {
            for (int j = coins[i]; j <= amount; j++) {
                if (dp[j-coins[i]] != -1) {
                    if (dp[j] == -1) {
                        dp[j] = dp[j-coins[i]]+1;
                        continue;
                    }
                    dp[j] = min(dp[j], dp[j-coins[i]]+1);
                }
            }
        }
        return dp[amount];
    }
};
```

### 17 完全平方数

```c++
/* 完全平方数：
 *	1. dp = vector<int>(n+1, INT_MAX);
 *	2. dp[j] = min(dp[j-i*i]+1, dp[j]);
 */
```

### 18 单词拆分

```c++
/* 单词拆分：完全背包之所有排列情况
 *	0. ⭐查找字典中的单词：
 		a. 将字典中的单词放入集合中 unordered_set wordSet
 		b. 查找 wordSet.find(word) != wordSet.end()
 		c. word = s.substr(i, j-i); 其中 j-i 表示裁剪的字符串长度，而不是字符串末尾
 *  1. dp[j] : 长度为 j 时是否可以由单词组成
 *  2. 递推公式：if (s.substr(j, i)是字典单词 && dp[j]可以由单词组成) dp[i] = true;
 *  3. 初始化：dp[0] = true; 其余均为 false;
 *  4. 遍历顺序：apple'pen'apple不同于 apple'apple'pen，对于元素顺序有要求，是排列情况
 *  5. 动手模拟：
 */
class Solution {
public:
    bool wordBreak(string s, vector<string>& wordDict) {
        unordered_set<string> wordFind = unordered_set<string>(wordDict.begin(), wordDict.end());
        vector<bool> dp = vector<bool>(s.size()+1, false);
        string word;

        dp[0] = true;
        for (int j = 1; j <= s.size(); j++) {
            for (int i = 0; i < j; i++) {
                word = s.substr(i, j-i);
                if (wordFind.find(word) != wordFind.end() && dp[i]) {
                    dp[j] = true;
                }
            }
        }

        return dp[s.size()];
    }
};
```

### 19 打家劫舍

```c++
/* 打家劫舍
 *	1. dp[i] : 表示 0~i 家偷盗的最多钱数
 *	2. 递推公式：第 i 家偷/不偷 dp[i] = max(dp[i-2] + values[i], dp[i-1]);
 *	3. 初始化：递推基础 dp[0] = values[0], dp[1] = values[1];
 *	4. 遍历顺序：
 *	5. 动手模拟
 */
```

### 20 打家劫舍 II

```c++
/* 打家劫舍 II
 *	0. ⭐对于环形数组，考虑区间 0~N-2 和 1~N-1，取最大值即可。
 *	1. 先正常计算 0~N-2，再考虑偷不偷最后一家。其中需要标记 dp[i-1] 和 dp[i-2] 是否包含第一家
 */
```

### 21 打家劫舍 III

```c++
/* 打家劫舍 III
 *	0. 树形dp：后序遍历（递归）+动态规划
 *	1. dp[0] : 表示不偷当前节点的最大金额；dp[1] : 表示偷当前节点的最大金额
 *	2. 递推公式：dp[0] = max(left[0], left[1]) + max(right[0], right[1])
 				不偷：则左右孩子可以偷也可以不偷
 			 	dp[1] = left[0] + right[0] + curNode;
 			 	偷：则左右孩子不能偷
 *	3. 初始化：遇到空节点，返回 {0, 0}
 *	4. 遍历顺序：当前节点依赖于左右孩子，所以采用后序遍历（左右中）
 *	5. 动手模拟：
 */
```

### 22 买卖股票的最佳时机

```c++
/* 买卖股票的最佳时机
 *	1.  dp[i][0] : 不持有股票所得最多现金（卖不卖）
 		dp[i][1] : 持有股票所得最多现金（买不买）
 *	2. 递推公式： dp[i][0] = max(dp[i-1][0], dp[i-1][1] + values[i]); 卖/不卖，0~i 天最高价卖出
 				dp[i][1] = max(dp[i-1][1], -values[i]); 买/不买，0~i 天最低价买入
 				dp[i][1] = max(dp[i-1][1], dp[i][0] - values[i]); 可以不停地买卖
 *	3. 初始化：dp[0][0] = 0, dp[0][1] = -values[0];
 *	4. 遍历顺序：
 *	5. 动手模拟：
 */
```

### 23 买卖股票的最佳时机 III

```c++
/* 买卖股票的最佳时机 III（第二次买入时，不能持有股票）
 *	1.  dp[i][0] : 第一次不持有股票所得最多现金
 		dp[i][1] : 第一次持有股票所得最多现金
 		dp[i][2] : 第二次不持有股票所得最多现金
 		dp[i][3] : 第二次持有股票所得最多现金（依赖于 dp[i-1][0]）
 *	2. 递推公式：
 		dp[i][0] = max(dp[i-1][0], dp[i-1][1] + prices[i]);
 		dp[i][1] = max(dp[i-1][1], -prices[i]);
 		dp[i][2] = max(dp[i-1][2], dp[i-1][3] + prices[i]);
 		dp[i][3] = max(dp[i-1][3], dp[i-1][0] - prices[i]);
 *	3. 初始化: dp[0][0] = 0; dp[0][1] = -prices[0];
 			  dp[0][2] = 0; dp[0][3] = -prices[0];
 *	4. 遍历顺序：
 *	5. 动手模拟：
 */
```

### 24 买卖股票的最佳时机 IV

```c++
/* 买卖股票的最佳时机 IV（ k 次交易，不能同时进行两次交易）
 *	1.	dp[i][0] : 第一次不持有股票所得最多现金
 		dp[i][1] : 第一次持有股票所得最多现金
 		dp[i][2*j]   : 第 j 次不持有股票所得最多现金
 		dp[i][2*j+1] : 第 j 次持有股票所得最多现金
 *	2. 递推公式：
 		dp[i][0] = max(dp[i-1][0], dp[i-1][1] + prices[i]);
 		dp[i][1] = max(dp[i-1][1], -prices[i]);
 		dp[i][2*j]   = max(dp[i-1][2*j], dp[i-1][2*j+1] + prices[i]);
 		dp[i][2*j+1] = max(dp[i-1][2*j+1], dp[i-1][2*j-1] - prices[i]); 依赖于上一次不持有股票时所得最多现金
 */
```

### 25 买卖股票的最佳时机（冷冻期）

```c++
/* 买卖股票的最佳时机（冷冻期）
 *	1.	dp[i][0] : (卖)持续不持有股票
 		dp[i][1] : 今日卖出股票
 		dp[i][2] : 冷冻期
 		dp[i][3] : (买)持有股票时所得最多现金
 *	2. 递推公式：
 		dp[i][0] 包括昨天不持有 dp[i-1][0] 和昨天冷冻期 dp[i-1][2]
        		 = max(dp[i-1][0], dp[i-1][2]);
        dp[i][1] 包括昨天持有股票，今日卖出 dp[i-1][3] + prices[i]
        		 = dp[i-1][3] + prices[i];
        dp[i][2] 包括昨天卖出股票 dp[i-1][1]
        		 = dp[i-1][1];
        dp[i][3] 包括昨天持有股票 dp[i-1][3] 、昨天持续不持有股票 dp[i-1][0] + prices[i] 和昨天冷冻期 dp[i-1][2] + prices[i]
        		 = max(dp[i-1][3], dp[i-1][0] + prices[i], dp[i-1][2] + prices[i]);
 */
```

### 26 买卖股票的最佳时机（含手续费）

```c++
/* 买卖股票的最佳时机（含手续费）
 *  1.  dp[i][0] : 不持有股票时的最大金额
        dp[i][1] : 持有股票时的最大金额
 *  2. 递推公式：
        dp[i][0] = max(dp[i-1][0], dp[i-1][1] + prices[i] - fee);
        dp[i][1] = max(dp[i-1][1], dp[i-1][0] - prices[i]);
 *  3. 初始化：dp[0][0] = 0, dp[0][1] = -prices[0];
 *  4. 遍历顺序：
 */
class Solution {
public:
    int maxProfit(vector<int>& prices, int fee) {
        vector<vector<int>> dp = vector<vector<int>>(prices.size(), vector<int>(2, 0));
        dp[0][0] = 0;
        dp[0][1] = -prices[0];
        for (int i = 1; i < prices.size(); i++) {
            dp[i][0] = max(dp[i-1][0], dp[i-1][1] + prices[i] - fee);
            dp[i][1] = max(dp[i-1][1], dp[i-1][0] - prices[i]);
        }
        return dp[prices.size()-1][0];
    }
};
```

### 27 最长递增子序列

```c++
/* 最长递增子序列：
 *  1. dp[i] : 下标 0~i 中以 nums[i] 结尾的最长递增子序列长度
 *  2. 递推公式：j = 0~i-1, if (nums[i] > nums[j]) dp[i] = max(dp[i], dp[j]+1)
        dp[i] 的最长长度是所有 nums[j] < nums[i] 的 dp[j] 的长度再 +1 的最大值
        (最长连续递增子序列) if (nums[i] > nums[i-1]) dp[i] = max(dp[i], dp[i-1]+1)
 *  3. 初始化：全 1
 *  4. 遍历顺序：依赖关系
 *  5. 动手模拟：
 */
class Solution {
public:
    int lengthOfLIS(vector<int>& nums) {
        vector<int> dp = vector<int>(nums.size(), 1);
        int ans = 1;
        for (int i = 1; i < nums.size(); i++) {
            for (int j = 0; j < i; j++) {
                if (nums[j] < nums[i]) {
                    dp[i] = max(dp[i], dp[j]+1);
                }
            }
            ans = (ans < dp[i]) ? dp[i] : ans;
        }
        return ans;
    }
};
```

### 28 最长重复子序列

```c++
/* 最长重复子序列：
 *	1. dp[i][j] : 以 A[i-1] 结尾和以 B[j-1] 结尾的序列的最长重复子序列
 *	2. 递推公式：if (A[i-1] == B[j-1]) dp[i][j] = dp[i-1][j-1] + 1;
 *	3. 初始化：全 0，即 dp[0][j] = 0, dp[i][0] = 0 他们实际无意义，只是方便后续递推
 *	4. 递推顺序：依赖关系
 *	5. 动手模拟：
 */
```

### 29 最长公共子序列

```c++
/* 最长公共子序列 / 不相交的线：
 *	1. dp[i][j] : 以 A[i-1] 结尾和以 B[j-1] 结尾的序列的最长公共子序列
 *	2. 递推公式：
 		if (A[i-1] == B[j-1]) dp[i][j] = dp[i-1][j-1] + 1;
 		if (A[i-1] != B[j-1]) dp[i][j] = max(dp[i-1][j], dp[i][j-1])
 */
```

### 30 最大连续子数组和

```c++
/* 最大连续子数组和：
 *	1. dp[i] : 下标 0~i 的最大连续子数组和
 *	2. 递推公式：dp[i] = max(dp[i-1]+values[i], values[i]);
 		dp[i] 要么是与上一个累加，要么是自己从新开始
 */
```

### 31 不同的子序列

```c++
/* 不同的子序列（字符串 A 在 B 中的出现次数）：
 *	1. dp[i][j] : 以 A[i-1] 和 B[j-1] 结尾的字符串，A[i-1] 在 B[j-1] 中出现次数
 *	2. 递推公式：
 		if (A[i-1] == B[j-1]) dp[i][j] = dp[i-1][j-1] + dp[i][j-1];
 		用 B[j-1] 来匹配和不用 B[j-1] 来匹配的结果，即 baggg, 最后一位 g 有自己的 1 和前面的 2
 		if (A[i-1] != B[j-1]) dp[i][j] = dp[i][j-1];
 *	3. 初始化：dp[i][0] = 0, dp[0][j] = 1, dp[0][0] = 1
 *	4. 遍历顺序：先 i 后 j
 *	5. 动手模拟：
 */
```

### 32 编辑距离

```c++
/* 编辑距离：
 *	1. dp[i][j] : 以 A[i-1] 和 B[j-1] 结尾的字符串，两者相同的最小操作数
 *	2. 递推公式：
 		if (A[i-1] == B[j-1]) dp[i][j] = dp[i-1][j-1];
 		如果相同，则 A[i-1] 和 B[j-1] 不需要操作
 		else dp[i][j] = min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]) + 1;
 		dp[i-1][j]   : A 删除一位 / B 添加一位
 		dp[i][j-1]   : B 删除一位 / A 添加一位
 		dp[i-1][j-1] : 令 A[i-1] = B[j-1]
 *	3. 初始化：dp[i][0] = i, dp[0][j] = j;
 *	4. 遍历顺序：
 *	5. 动手模拟：
 */
```

### 33 回文子串

```c++
/* 回文子串：从外向内递推
 *	1. dp[i][j] : 以 A[i-1] 开头和以 B[j-1] 结尾的字符串是不是回文串
 *	2. 递推公式：
 		if (A[i-1] != B[j-1]) dp[i][j] = false;
 		else 
 			if (abs(i-j) <= 1) dp[i][j] = true;
 			else dp[i][j] = dp[i-1][j-1];
 *	3. 初始化：全 false
 *	4. 遍历顺序：从下往上，从左往右（从左下向右上遍历）
 		i = N~1, j = i~N
 *	5. 动手模拟：cbabc
 */
```

### 34 最长回文子序列

```c++
/* 最长回文子序列（回文串长度）：
 *	1. dp[i][j] : 以 A[i+1] 开头和以 B[j-1] 结尾的字符串中的最长回文子序列
 *	2. 递推公式：
 		if (A[i+1] == B[j-1])
 			if (abs(j-i) <= 1) dp[i][j] = j-i+1;
 			else dp[i][j] = dp[i+1][j-1] + 2;
 		添加新的回文串元素
 		if (A[i+1] != B[j-1]) dp[i][j] = max(dp[i-1][j], dp[i][j-1])
 		删除 A[i+1]/B[j-1] 来保持回文串
 *	3. 初始化：不需要/全 1
 *	4. 遍历顺序：从下往上，从左往右：i = N~1, j = i~N
 *	5. 动手模拟：cbacbc
 */
```

## Hot 100

### 01 相交链表

```c++
/**
 * 注意：
 *	1. 暴力枚举，时间复杂度 O(m*n)
 *	2. 哈希法：unordered_set<ListNode *> records
 		记录链表 A，再遍历链表 B，查看当前节点是否被记录过
 *	3. 双指针法：
 		ptA 和 ptB 同时遍历链表 A, B，指向 NULL 之后，ptA = headB/ ptB = headA，直到两个指针相等。ptA = a+c+b, ptB = b+c+a 时两者指向相同，此时为相遇节点。
 */
class Solution {
public:
    ListNode *getIntersectionNode(ListNode *headA, ListNode *headB) {
        ListNode *curNodeA = headA;
        ListNode *curNodeB = headB;
        ListNode *meetNode = NULL;

        while (curNodeA != NULL) {
            curNodeB = headB;
            while (curNodeB != NULL) {
                if (curNodeA == curNodeB) {
                    return curNodeA;
                }
                curNodeB = curNodeB->next;
            }
            curNodeA = curNodeA->next;
        }
        return NULL;
    }
};
```

### 02 ⭐二叉树的最近公共祖先

```c++
/* 二叉树的最近公共祖先：
 *  1. 后序遍历（左右中）：先遍历左右子树，再遍历中间节点，如果左右子树有需要的节点，则返回可能的最近公共祖先
 *  2. 回溯/递归：
 *      a. 输入参数和返回值：输入当前节点和要寻找的节点，返回找到 / 没找到 -- 当前节点/左/右子树 / NULL
 *      b. 结束条件：找了需要的节点/空节点
 *      c. 该层处理方式：
            左右子树都为空  ：没找到，返回 NULL
            左右子树都不为空：当前节点为最近公共祖先，返回当前节点
            左子树不为空：左子树为最近公共祖先，返回左孩子
            右子树不为空：右子树为最近公共祖先，返回右孩子
 */
class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
        /* b. 结束条件 */
        if (root == p || root == q || root == NULL) {
            return root;
        }
        /* c. 该层处理方式 */
        TreeNode *leftNode  = lowestCommonAncestor(root->left, p, q);
        TreeNode *rightNode = lowestCommonAncestor(root->right, p, q);
        if (leftNode != NULL && rightNode != NULL) {
            return root;
        }
        else if (leftNode != NULL) {
            return leftNode;
        }
        else if (rightNode != NULL) {
            return rightNode;
        }
        else {
            return NULL;
        }
    }
};
```

### 03 回文链表

```c++
/* 回文链表：
 *	1. 将链表保存到数组中
 *	2. 双指针法判断回文数组
 */
class Solution {
public:
    bool isPalindrome(ListNode* head) {
        ListNode *curNode;
        vector<int> nums;
        int left;
        int right;

        nums.clear();
        curNode = head;
        while (curNode != NULL) {
            nums.push_back(curNode->val);
            curNode = curNode->next;
        }

        /* 双指针法 */
        left = 0;
        right = nums.size() - 1;
        while (right > left) {
            if (nums[left] != nums[right]) {
                return false;
            }
            right --;
            left ++;
        }
        return true;
    }
};
```

### 04 ⭐每日温度

```c++
/* 每日温度：单调栈
 *  1. 寻找数组右边第一个更大的元素位置，保持单调栈栈顶到栈底单调递增（保存下标）
 *  2. 如果当前元素大于栈顶，则弹出栈顶，记录出现了下一个更大的元素位置；最后将当前元素入栈
 */
class Solution {
public:
    vector<int> dailyTemperatures(vector<int>& temperatures) {
        stack<int> recordsStk;
        vector<int> ans = vector<int>(temperatures.size(), 0);
        int idxTop;

        recordsStk.push(0);
        for (int i = 1; i < temperatures.size(); i++) {
            idxTop = recordsStk.top();
            /* a. 如果当前元素大于栈顶，表明出现了更大的元素，记录ans，并弹出栈顶 */
            while (!recordsStk.empty() && temperatures[i] > temperatures[idxTop]) {
                ans[idxTop] = i-idxTop;
                recordsStk.pop();
                if (!recordsStk.empty()) {
                    idxTop = recordsStk.top();
                }
            }
            /* b. 将当前元素下标压入栈内 */
            recordsStk.push(i);
        }

        return ans;
    }
};
```

### 05 翻转二叉树

```c++
/* 翻转二叉树：
 *	1. 递归/回溯
 *		a. 输入参数和返回值：输入当前节点，返回当前节点
 *		b. 结束条件：遇到空节点
 *		c. 该层处理方式：
 			翻转当前节点的左右子树
 			继续遍历当前节点的左右子树
 */
class Solution {
public:
    TreeNode* invertTree(TreeNode* root) {
        TreeNode* tmpNode;
        if (root == NULL) {
            return NULL;
        }
        /* 反转左右子树 */
        tmpNode     = root->left;
        root->left  = root->right;
        root->right = tmpNode;
        /* 继续遍历左右子树 */
        invertTree(root->left);
        invertTree(root->right);

        return root;
    }
};
```

### 06 最大正方形

```c++
/* 最大正方形（动态规划）：
 *  1. dp[i][j] : [i-p,j-p]~[i,j]是否是正方形，以及大小是 p
 *  2. 递推公式：
        if (dp[i-1][j-1] > 0 && dp[i][j] > 0)
            dp[i][j] = min(dp[i-1][j-1], dp[i-1][j], dp[j][i-1]) + 1;
 *  3. 初始化：dp[i][0], dp[0][j] = 1/0
 *  4. 遍历顺序
 *  5. 动手模拟
 */
class Solution {
public:
    int maximalSquare(vector<vector<char>>& matrix) {
        vector<vector<int>> dp;
        int sizeSquare;
        int cols;
        int rows;
        int ans;
        int p;

        rows = matrix.size();
        cols = matrix[0].size();
        dp   = vector<vector<int>>(rows, vector<int>(cols, 0));
        ans  = 0;
        /* dp 数组初始化 */
        for (int i = 0; i < rows; i++) {
            dp[i][0] = (matrix[i][0] == '1') ? 1 : 0;
            ans = max(ans, dp[i][0]);
        }
        for (int j = 0; j < cols; j++) {
            dp[0][j] = (matrix[0][j] == '1') ? 1 : 0;
            ans = max(ans, dp[0][j]);
        }
        /* 遍历 */
        for (int i = 1; i < rows; i++) {
            for (int j = 1; j < cols; j++) {
                dp[i][j] = (matrix[i][j] == '1') ? 1 : 0;
                if (dp[i-1][j-1] > 0 && dp[i][j] > 0) {
                    dp[i][j] = min(dp[i-1][j-1], min(dp[i-1][j], dp[i][j-1])) + 1;
                }
                ans = max(ans, dp[i][j]);
            }
        }
        return ans*ans;
    }
};
```

### 07 ⭐数组中的第K个最大元素

```c++
/**
 * 注意：
 *	1. 大顶堆定义：
 		a. 自定义比较函数 class compare {};
 		b. 声明定义大顶堆 priority_queue<int, vector<int>, compare> maxHeap;
 */
class Solution {
public:
    class compare {
        public:
            bool operator()(const int& a, const int& b) {
                return a < b;
            }
    };
    int findKthLargest(vector<int>& nums, int k) {
        priority_queue<int, vector<int>, compare> maxHeap;

        for (int num : nums) {
            maxHeap.push(num);
        }
        for (int i = 1; i < k; i++) {
            maxHeap.pop();
        }
        return maxHeap.top();
    }
};
```

### 08 ⭐实现前缀树

```c++
class Trie {
    /* 前缀树：
     *  1. 数据结构："apple" -> 'a' -> 'p' -> 'p' -> 'l'(isEnd = false) -> 'e'(isEnd = true)
     *  2. 类成员变量（私有）：
            vector<Trie *> children = vector<Trie *>(26, NULL);
            每个节点都含有 26 个子节点，并判断当前节点是不是结尾 isEnd，是则表明已找到，不是则继续寻找或者返回错误
            bool isEnd;
     *  3. 类成员函数（私有）：
            Trie * searchPrefix(string prefix)
            遍历前缀树，返回找到的节点/NULL
     *  4. 初始化：Trie() : children(26, NULL), isEnd(false) {}
     *  5. 插入前缀：
            void insertPrefix(string prefix)
            将 prefix 逐个字符地插入到前缀树中，并在结尾标记 isEnd = true;
     *  6. 寻找前缀：
            bool search(string prefix)
            找到 prefix 结尾节点后，判断节点是否为空，isEnd == true?
     *  7. 寻找小前缀：
            bool startsWith(string prefix)
            找到 prefix 结尾节点后，判断节点是否为空
     */
private:
    /* 成员变量 */
    vector<Trie *> children;
    bool isEnd;

    Trie *searchPrefix(string prefix) {
        Trie *node = this;
        for (char ch : prefix) {
            if (node->children[ch-'a'] == NULL) {
                return NULL;
            }
            node = node->children[ch-'a'];
        }
        return node;
    }
public:
    /* 成员列表初始化 */
    Trie() : children(26, NULL), isEnd(false) {
        
    }
    
    void insert(string word) {
        Trie *node = this;
        for (char ch : word) {
            if (node->children[ch-'a'] == NULL) {
                node->children[ch-'a'] = new Trie();
            }
            node = node->children[ch-'a'];
        }
        node->isEnd = true;
    }
    
    bool search(string word) {
        Trie *node = searchPrefix(word);
        return node != NULL && node->isEnd;
    }
    
    bool startsWith(string prefix) {
        return searchPrefix(prefix) != NULL;
    }
};
```

### 09 翻转链表

```c++
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        /* 翻转链表：
         *  1.  preNode = head, curNode = head->next, tmpNode
         *  2.  head->next = null;
         *  3.  tmpNode = curNode->next;
                curNode->next = preNode;
                preNode = curNode;
                curNode = tmpNode;
         */
    }
};
```

### 10 打家劫舍

```c++
class Solution {
public:
    int rob(vector<int>& nums) {
        /* 打家劫舍：
         *	0. if (nums.size() == 1) return nums[0];
         *  1. dp[i] : 偷盗 0~i 号房屋所得最多现金
         *  2. 递推公式：
                偷/不偷：dp[i] = max(dp[i-1], dp[i-2] + values[i]);
         *  3. 初始化：dp[0] = values[0], 偷/不偷：dp[1] = max(values[1], values[0]);
         *  4. 遍历顺序：
         *  5. 动手模拟：
         */
        vector<int> dp = vector<int>(nums.size(), 0);
        if (nums.size() < 2) {
            return nums[0];
        }
        /* 初始化 */
        dp[0] = nums[0];
        dp[1] = max(nums[1], nums[0]);
        /* 遍历，递推 */
        for (int i = 2; i < nums.size(); i++) {
            dp[i] = max(dp[i-1], dp[i-2]+nums[i]);
        }
        return dp[nums.size()-1];
    }
};
```

### 11 多数元素

```c++
class Solution {
public:
    int majorityElement(vector<int>& nums) {
        /* 多数元素：
         *  1. 哈希表 map 记录元素和元素出现次数: unordered_map<int, int> records;
         */
        unordered_map<int, int> records;
        int ans;

        if (nums.size() == 1) {
            return nums[0];
        }

        for (int num : nums) {
            if (records.find(num) != records.end()) {
                records[num]++;
                if (records[num] > nums.size()/2) {
                    ans = num;
                }
            }
            else {
                records.insert({num, 1});
            }
        }

        return ans;
    }
};
```

### 12 除自身以外数组的乘积

```c++
/* 除自身以外数组的乘积：
 *  1. 前缀 [0, i-1] 和后缀 [i+1, N-1] 的乘积
 */
class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {
        vector<int> prefix = vector<int>(nums.size()+1, 1);
        vector<int> suffix = vector<int>(nums.size()+1, 1);
        vector<int> ans    = vector<int>(nums.size(), 0);
        /* 前缀 prefix[i] = [0, i-1] */
        prefix[0] = 1;
        for (int i = 1; i < nums.size()+1; i++) {
            prefix[i] = prefix[i-1]*nums[i-1];
        }
        /* 后缀 suffix[i] = [i+1, N-1] */
        suffix[nums.size()] = 1;
        for (int i = nums.size() - 1; i >= 0; i--) {
            suffix[i] = suffix[i+1]*nums[i];
        }
        /* 前缀 prefix[i] = [0, i-1] 和后缀 suffix[i] = [i, N-1] 的乘积 */
        for (int i = 0; i < nums.size(); i++) {
            ans[i] = prefix[i] * suffix[i+1];
        }
        return ans;
    }
};
```

### 13 最小栈

```c++
/* 最小栈：
 *  1. 维护最小栈，记录当前栈顶元素下栈内最小元素；
        入栈时，记录当前栈顶元素下栈内最小元素
        出栈时，弹出
 */
class MinStack {
    private:
    stack<int> stk;
    stack<int> minStk;
public:
    MinStack() {
        
    }
    
    void push(int val) {
        if (stk.empty()) {
            stk.push(val);
            minStk.push(val);
            return ;
        }
        stk.push(val);
        minStk.push(min(minStk.top(), val));
        return ;
    }
    
    void pop() {
        stk.pop();
        minStk.pop();
        return ;
    }
    
    int top() {
        return stk.top();
    }
    
    int getMin() {
        return minStk.top();
    }
};
```

### 14 ⭐乘积最大子数组

```c++
/* 乘积最大子数组：维护两个 dp 数组，dpMax, dpMin
 *  1.  dpMax[i] : 区间 0~i 的最大连续子串的乘积（正）
        dpMin[i] : 区间 0~i 的最小连续子串的乘积（负）
 *  2. 递推公式：
        最大连续子串乘积：考虑续上 i 或者从 i 重新开始
        dpMax[i] = max(dpMax[i-1]*values[i], dpMin[i-1]*values[i], values[i]);
        dpMin[i] = min(dpMax[i-1]*values[i], dpMin[i-1]*values[i], values[i]);
 *  3. 初始化：dpMax[0] = values[0], dpMin[0] = values[0];
 *  4. 遍历顺序：
 *  5. 动手模拟：
 */
class Solution {
public:
    int maxProduct(vector<int>& nums) {
        vector<int> dpMax = vector<int>(nums.size(), 0);
        vector<int> dpMin = vector<int>(nums.size(), 0);
        int ans;

        dpMax[0] = nums[0];
        dpMin[0] = nums[0];
        ans = nums[0];
        for (int i = 1; i < nums.size(); i++) {
            dpMax[i] = max(dpMax[i-1]*nums[i], max(dpMin[i-1]*nums[i], nums[i]));
            dpMin[i] = min(dpMax[i-1]*nums[i], min(dpMin[i-1]*nums[i], nums[i]));
            ans = (ans > dpMax[i]) ? ans : dpMax[i];
        }
        return ans;
    }
};
```

### 15 ⭐排序链表

```c++
/* 排序链表：
 *	0. 虚拟链表头：ListNode* fakeHead = new ListNode();
 *  1. 冒泡排序：第 i 次遍历的长度为 N-1-i，如果 a<b，则两两交换
 *	2. 归并排序：
 		a. 分割，将链表分割成左右区间的子链表：通过快慢指针法，快指针移动 2 格，慢指针移动 1 格，快指针指向链表末尾时 (fast->next == tail)，慢指针为链表中间节点。直到子链表为空 (head == NULL) 或者仅含有一个节点 (head->next == tail)。
 			sortList(head, mid), sortList(mid, tail)
 		b. 合并，将两个有序子链表合并成一个有序链表 (虚拟链表头指向合并后的链表)：比较两个子链表的节点，将相对较小的节点放入合并链表中，直到某个子链表到达末尾。然后将另一个子链表的剩余节点放入合并链表中。
 			merge(sortList(head, mid), sortList(mid, tail));
 		c. 将子链表的尾部 tail 更换为 nullptr
 			if (head->next == tail) {
                head->next = nullptr;
                return head;
            }
 */
class Solution {
public:
    ListNode* sortList(ListNode* head) {
        ListNode* fakeHead = new ListNode();
        ListNode* preNode;
        ListNode* curNode;
        ListNode* tmpNode;
        bool isSwap;
        fakeHead->next = head;
        if (head == NULL || head->next == NULL) {
            return head;
        }
        preNode = fakeHead;
        curNode = fakeHead->next;
        while (1) {
            isSwap = true;
            preNode = fakeHead;
            curNode = fakeHead->next;
            while (curNode != NULL && curNode->next != NULL) {
                tmpNode = curNode->next;
                if (curNode->val > tmpNode->val) {
                    curNode->next = tmpNode->next;
                    preNode->next = tmpNode;
                    tmpNode->next = curNode;

                    preNode = tmpNode;
                    isSwap  = false;
                }
                else {
                    preNode = curNode;
                    curNode = tmpNode;
                }
            }
            if (isSwap) {
                break;
            }
        }
        return fakeHead->next;
    }
};
/***************************************/
class Solution {
public:
    ListNode* sortList(ListNode* head) {
        /* 排序链表之归并排序：
         *	0. (fastNode != tail)，而不是(fastNode != nullptr)
         *  1. 分割：快慢指针法寻找链表的中间节点，并分割为左右区间子链表，直到链表长度为 1（head1->next == tail），令 head1->next = nullptr;
         *  2. 合并排序：将两个子链表按照相对大小关系依次放入合并链表（虚拟链表头）中，直到一个子链表达到尾部，然后将另一个子链表的剩余部分放入合并链表中；
         */
        return mSortList(head);
    }
    ListNode* partitionList(ListNode* head, ListNode* tail) {
        /* a. 结束条件：子链表大小为 1 */
        if (head == NULL) {
            return head;
        }
        if (head->next == tail) {
            head->next = nullptr;
            return head;
        }
        /* b. 该层处理逻辑：找到链表中间节点并返回 */
        ListNode* slowNode = head;
        ListNode* fastNode = head;
        while (fastNode != tail) {
            slowNode = slowNode->next;
            fastNode = fastNode->next;
            if (fastNode != tail) {
                fastNode = fastNode->next;
            }
        }
        /* c. 递归条件：继续分割子链表，直到达到结束条件 */
        return mergeList(partitionList(head, slowNode), partitionList(slowNode, tail));
    }
    ListNode* mergeList(ListNode* head1, ListNode* head2) {
        /* 合并排序链表 */
        ListNode* fakeHead = new ListNode();
        ListNode* curNode = fakeHead;
        ListNode* head;
        while (head1 != nullptr && head2 != nullptr) {
            if (head1->val < head2->val) {
                curNode->next = head1;
                head1 = head1->next;
            }
            else {
                curNode->next = head2;
                head2 = head2->next;
            }
            curNode = curNode->next;
        }
        if (head1 != nullptr) {
            curNode->next = head1;
        } 
        else {
            curNode->next = head2;
        }
        head = fakeHead->next;
        delete fakeHead;
        return head;
    }
    ListNode* mSortList(ListNode* head) {
        return partitionList(head, nullptr);
    }
};
```

### 16 ⭐LRU (最近最少使用) 缓存

```c++
/* LRU 缓存：
 *	0. 维护双向链表 DLinkList 头部 head 和尾部 tail、映射哈希表 map、尺寸 size 和容量 capacity
 		struct DLinkList {
 			int key;
 			int value;
 			DLinkList* prev;
 			DLinkList* next;
 			DLinkList() : key(0), value(0), prev(nullptr), next(nullptr);
 			DLinkList(int _key, int _value) : key(_key), value(_value), prev(nullptr), next(nullptr);
 		};
 		unordered_map<int, DLinkList*> cache;
 		DLinkList* head;
 		DLinkList* tail;
 		int size;
 		int capacity;
 *	1. 更新/添加新的键值对
 		if (cache.count(key))	如果键值对已存在，则更新键值对
 			DLinkList* node = cache[key];
 			node->value = value;
 			removeNode(node);	删除双向链表中的该节点
 			addToHead(node);	将该节点添加到链表头部，表示最近使用过
 		else	添加新的键值对
 			DLinkList* node = new DLinkList(key, value);	生成新节点
 			cache.insert({key, node}); / cache[key] = node;	将键值对添加到 map 中
 			addToHead(node);
 			size ++;
 			if (size > capacity) 
 				DLinkList* removed = removeTail();	删除链表尾部节点，表示删除最近最少使用的节点
 				cache.erase(removed->key);			删除 map 中的指定键值对
 				delete removed;						防止内存泄漏
 				size--;
 *	2. 查找键值对
 */
/**
 * 注意：
 *	1. struct DLinkList 定义在 private 私有成员内
 *	2. 初始化成员列表与函数体 {}; 共同初始化类成员
 *	3. 在双向链表和 map 中都删除尾部节点，否则会导致二者数据不同步
 */
class LRUCache {
private:
    /* 维护：双向链表头部和尾部，哈希表，大小，容量 */
    struct DLinkList {
        int key;
        int value;
        DLinkList* prev;
        DLinkList* next;
        DLinkList() : key(0), value(0), prev(NULL), next(NULL) {};
        DLinkList(int _key, int _value) : key(_key), value(_value), prev(NULL), next(NULL) {};
    };
    unordered_map<int, DLinkList*> cache;
    DLinkList* head;
    DLinkList* tail;
    int size;
    int capacity;
public:
    /* 初始化 LRU Cache */
    LRUCache(int _capacity) : capacity(_capacity), size(0) {
        head = new DLinkList();
        tail = new DLinkList();
        head->next = tail;
        tail->prev = head;
        cache.clear();
    }
    /* 寻找键值对：
     *  1. 如果存在，则返回查询值，并将 node 放到链表头部
     *  2. 如果不存在，则返回 -1
     */
    int get(int key) {
        if (cache.count(key)) {
            DLinkList* node = cache[key];
            deleteNode(node);
            addToHead(node);
            return node->value;
        }
        else {
            return -1;
        }
    }
    /* 更新/插入键值对:
     *  1. 如果键值对存在，则更新键值对
            a. 更新 node 键值对：用 map 找到键对应的节点
            b. 删除当前节点
            c. 将当前节点插入头部
     *  2. 插入键值对
            a. 创建双向链表节点 node
            c. 将键值对插入到 map 中
            b. 将 node 插入链表头部
            c. 如果超出容量，则在双向链表和 map 中删除尾部节点
     */
    void put(int key, int value) {
        /* 1. 更新键值对 */
        if (cache.count(key)) {
            DLinkList* node = cache[key];
            node->value = value;
            deleteNode(node);
            addToHead(node);
        }
        /* 2. 插入键值对 */
        else {
            DLinkList* node = new DLinkList(key, value);
            cache.insert({key, node});
            addToHead(node);
            size++;
            if (size > capacity) {
                int removedKey = deleteTailNode();
                cache.erase(removedKey);
                size--;
            }
        }
    }
    /* 双向链表：将该节点插入头部 */
    void addToHead(DLinkList* node) {
        DLinkList* nextNode = head->next;
        node->prev = head;
        node->next = nextNode;
        head->next = node;
        nextNode->prev = node;

        return ;
    }
    /* 双向链表：删除该节点，但是不要 delete，保留 node 用于放到链表头部 */
    void deleteNode(DLinkList* node) {
        DLinkList* prevNode = node->prev;
        DLinkList* nextNode = node->next;
        prevNode->next = nextNode;
        nextNode->prev = prevNode;

        return ;
    }
    /* 双向链表：删除尾部节点 */
    int deleteTailNode() {
        DLinkList* node = tail->prev;
        DLinkList* prevNode = node->prev;
        int key = node->key;
        prevNode->next = tail;
        tail->prev = prevNode;
        delete node;

        return key;
    }
};
```

### 17 环形链表 II

```c++
/* 环形链表：
 *  1. 快慢指针从头节点出发的相遇节点必定在环中，如果快指针到达 null，则链表无环
 *  2. 两个指针从头节点和相遇节点同时出发，再次相遇，为环的起始节点
 		如果头节点 == 相遇节点，则头节点即为相遇节点
 */
class Solution {
public:
    ListNode *detectCycle(ListNode *head) {
        ListNode* fastNode = head;
        ListNode* slowNode = head;
        ListNode* meetNode = NULL;
        /* 1. 快慢指针从头节点出发后的相遇节点必定在环中 */
        while (fastNode != NULL) {
            slowNode = slowNode->next;
            fastNode = fastNode->next;
            if (fastNode != NULL) {
                fastNode = fastNode->next;
            }
            if (fastNode == slowNode) {
                break;
            }
        }
        meetNode = fastNode;
        /* 如果快指针到达 null，则链表无环 */
        if (meetNode == NULL) {
            return NULL;
        }
        /* 2. 两个指针从头节点和相遇节点同时出发，再次相遇，为环的起始节点 */
        slowNode = meetNode;
        fastNode = head;
        while (1) {
            /* 如果头节点 == 相遇节点，则头节点即为相遇节点 */
            if (slowNode == fastNode) {
                break;
            }
            slowNode = slowNode->next;
            fastNode = fastNode->next;
        }
        return fastNode;
    }
};
```

### 18 环形链表

```c++
class Solution {
public:
    bool hasCycle(ListNode *head) {
        /* 环形链表：
         *  1. 快慢指针从头节点同时出发，相遇时一定在环内
         */
        ListNode* slowNode = head;
        ListNode* fastNode = head;
        while (fastNode != NULL) {
            slowNode = slowNode->next;
            fastNode = fastNode->next;
            if (fastNode != NULL) {
                fastNode = fastNode->next;
            }
            if (fastNode == slowNode) {
                break;
            }
        }
        if (fastNode != NULL) {
            return true;
        }
        return false;
    }
};
```

### 19 单词拆分

```c++
/**
 * 注意：
 *	1. 单词拆分中的状态推导：dp[i] = dp[j] && [j, i]，即动态规划
 *	2. 单词拆分：word = s.substr(j, i-j), 起始位置和字符串长度
 */
class Solution {
public:
    bool wordBreak(string s, vector<string>& wordDict) {
        /* 单词拆分：动态规划
         *  1. dp[i] : 字符串区间 [0, i-1] 能否用单词拼接
         *  2. 递推公式：dp[i] = dp[j] && [j+1, i] 是否在单词表中出现
         *  3. 初始化：dp[0] = true;
         *  4. 遍历顺序：
         *  5. 动手模拟：
         */
        unordered_set<string> records;
        vector<bool> dp = vector<bool>(s.length()+1, false); // length()/size()
        string word;

        records.clear();
        for (string str : wordDict) {
            records.insert(str);
        }

        dp[0] = true;
        for (int i = 1; i <= s.length(); i++) {
            for (int j = 0; j < i; j++) {
                word = s.substr(j, i-j);
                if (!dp[i] && (records.find(word) != records.end())) {
                    dp[i] = dp[j];
                }
            }
        }
        return dp[s.length()];
    }
};
```

### 20 只出现一次的数字

```c++
/**
 *	1. unordered_set<int> records 容器的标准操作
 		添加：records.insert{元素/迭代器范围[first, last)}
 		删除：records.erase{元素/迭代器指向的元素}
 		查找：records.count(), records.find()
 		遍历：unordered_set<int>::iterator it = records.begin(); it != records.end(); ++it
 */
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        /* 只出现一次的数字：第一次出现添加，第二次出现删除 */
        unordered_set<int> records;
        int ans;
        for (int num : nums) {
            /* 第二次出现 */
            if (records.find(num) != records.end()) {
                records.erase(num);
                continue;
            }
            records.insert(num);
        }
        ans = *records.begin();
        return ans;
    }
};
```

### 21 回文子串数量

```c++
class Solution {
public:
    int countSubstrings(string s) {
        /* 回文子串数量：
         *  1. dp[i][j] : 区间 [i, j] 是不是回文串
         *  2. 递推公式：
                if (s[i] ！= s[j]) dp[i][j] = false;
                else {
                    if (abs(j-i) <= 1) dp[i][j] = true;
                    else dp[i][j] = dp[i+1][j-1];
                }
         *  3. 初始化：全 0，dp[i][i] = 1;
         *  4. 遍历顺序：i = N-1 ~ 0, j = i+1 ~ N-1
         *  5/ 动手模拟："aabbcdc", "fdsklf"
         */
        /* ❌ 回文子串数量：
         *  1. dp[i][j] : 区间 [i, j] 中的回文子串数量
         *  2. 递推公式：
                if (s[i] == s[j]) s[i][j] = s[i+1][j] + s[i][j-1] - s[i+1][j-1] + 1;
                else s[i][j] = s[i+1][j] + s[i][j-1] - s[i+1][j-1];
         *  3. 初始化：全 0，dp[i][i] = 1;
         *  4. 遍历顺序：i = N-1 ~ 0, j = i+1 ~ N-1
         *  5/ 动手模拟："aabbcdc"
         */
        vector<vector<bool>> dp = vector<vector<bool>>(s.size(), vector<bool>(s.size(), false));
        int ans;

        for (int i = 0; i <= s.size() - 1; ++i) {
            dp[i][i] = true;
        }

        for (int i = s.size() - 1; i >= 0; --i) {
            for (int j = i + 1; j <= s.size() - 1; ++j) {
                if (s[i] != s[j]) {
                    dp[i][j] = false;
                }
                else {
                    if (abs(j-i) <= 1) {
                        dp[i][j] = true;
                    }
                    else {
                        dp[i][j] = dp[i+1][j-1];
                    }
                }
            }
        }

        ans = 0;
        for (int i = s.size() - 1; i >= 0; --i) {
            for (int j = i; j <= s.size() - 1; ++j) {
                if (dp[i][j]) {
                    ++ans;
                }
            }
        }

        return ans;
    }
};
```

### 22 最长连续序列

```c++
class Solution {
public:
    int longestConsecutive(vector<int>& nums) {
        /* 最长连续序列：
         *  1. 哈希表：依次查找 x, x+1, ..., x+y, 此时连续序列长度为 y+1
         *  2. 只查找连续序列开头的数字，如果 x 存在对应的 x-1，则 x 不是开头，跳过 x
         */
        unordered_set<int> records;
        int curLen;
        int ans;

        /* set 记录数字序列 */
        for (int num : nums) {
            records.insert(num);
        }

        /* 遍历可能的连续序列 */
        ans = 0;
        for (auto iter = records.begin(); iter != records.end(); ++iter) {
            /* 判断当前元素是否为连续序列的起始元素 */
            if (records.count(*iter-1) != 0) {
                continue;
            }
            /* 查找连续序列的长度 */
            curLen = 0;
            while (records.count(*iter+curLen) != 0) {
                ++curLen;
            }
            ans = max(ans, curLen);
        }

        return ans;
    }
};
```

### 23 零钱兑换

```c++
class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {
        /* 最少硬币个数：
         *  1. dp[j] : 用物品 0~i 装满背包 j 的最少硬币个数
         *  2. 递推公式：
                if (dp[j-coins[i]] != -1)   // 可以装满 j 
                    if (dp[j] == -1)        // 第一次装满 j
                        dp[j] = dp[j-coins[i]] + 1;
                    dp[j] = max(dp[j], dp[j-coins[i]] + 1);
         *  3. 初始化：dp[0] = 0, 其余全为 -1
         *  4. 遍历顺序：i = 0~coins.size()-1, j = coins[i]~amount
         *  5. 动手模拟：coins = [2, 5], amount = 12
         */
        vector<int> dp = vector<int>(amount+1, -1);

        dp[0] = 0;
        for (int i = 0; i < coins.size(); ++i) {
            for (int j = coins[i]; j <= amount; ++j) {
                if (dp[j-coins[i]] != -1) {
                    if (dp[j] == -1) {
                        dp[j] = dp[j-coins[i]] + 1;
                    }
                    dp[j] = min(dp[j], dp[j-coins[i]]+1);
                }
            }
        }

        return dp[amount];
    }
};
```

### 24 目标和

```c++
class Solution {
public:
    int findTargetSumWays(vector<int>& nums, int target) {
        /* 目标和：01 背包
         *  1. dp[i][j] : 从物品下标 0~i 装满背包空间 j 的方法数
         *  2. 递推公式：
                用/不用物品 i
                j = nums[i] ~ maxSize, dp[i][j] = dp[i-1][j] + dp[i-1][j-nums[i]]
                j = 1 ~ nums[i], dp[i][j] = dp[i-1][j]
         *  3. 初始化：dp[i][0] = pow(2.0, numZero);
         *  4. 递推顺序：i = 0~N, j = 1~maxSize
         *  5. 动手模拟：[1, 2, 3, 4, 5] -- maxSize = 7
         */
        vector<vector<int>> dp;
        int bagSize = 0;
        int numsSum = 0;
        int zeroNum = 0;

        for (int num : nums) {
            numsSum += num;
        }
        if (((numsSum + target) % 2) == 1 || numsSum < abs(target)) {
            return 0;
        }
        bagSize = (numsSum + target) / 2;

        dp = vector<vector<int>> (nums.size(), vector<int>(bagSize+1, 0));
        if (nums[0] <= bagSize) {
            dp[0][nums[0]] = 1;
        }
        for (int i = 0; i < nums.size(); ++i) {
            if (nums[i] == 0) {
                ++zeroNum;
            }
            dp[i][0] = (int) pow(2.0, zeroNum);
        }
        for (int i = 1; i < nums.size(); ++i) {
            for (int j = 1; j <= bagSize; ++j) {
                if (j < nums[i]) {  // 不可能使用物品 i
                    dp[i][j] = dp[i-1][j];
                }
                else {              // 用/不用物品 i
                    dp[i][j] = dp[i-1][j] + dp[i-1][j-nums[i]];
                }
            }
        }

        return dp[nums.size()-1][bagSize];
    }
};
```



